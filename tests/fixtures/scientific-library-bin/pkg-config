#!/bin/sh
set -eu

[ "$#" -eq 2 ] || exit 2
case $1:$2 in
    --exists:hdf5|--exists:blas) exit 0 ;;
    --modversion:hdf5) printf '%s\n' '1.14.3'; exit 0 ;;
    --modversion:blas) printf '%s\n' '3.11.0'; exit 0 ;;
    --exists:*|--modversion:*) exit 1 ;;
    *) exit 2 ;;
esac
