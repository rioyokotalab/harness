# Shared, non-secret SSH defaults. Node-local host entries stay in
# ~/.ssh/config and take precedence because OpenSSH uses the first value set.
Host github
    HostName github.com
    User git
    ForwardX11 no

Host *
    AddKeysToAgent yes
    ForwardX11 yes
    ForwardX11Trusted yes
    ServerAliveInterval 15
