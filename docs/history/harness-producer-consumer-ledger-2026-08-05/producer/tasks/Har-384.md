task: Har-384
repository: harness
state: ready
priority: 0
created: 2026-08-03
consumer: any
record: pending
authority: owner-authorized-project-root-relocation
---
# Objective

Relocate the canonical Students and Swallow repositories from the legacy
workspace boundary into their exact `projects` destinations, preserving Git
identity, ignored state, linked worktrees, managed-client availability, and
cold-restart recovery.

# Scope and gates

The owner explicitly declared the pre-existing Students destination
disposable. Remove it only through guarded deletion after fresh identity,
ownership, process, and size checks. Preserve both canonical sources until
their replacements pass byte, Git, worktree, ledger, client, and recovery
acceptance; retire each source only after exact zero-reference proof.

Update the closed target maps, both client launch sentinels, trust mapping,
monitor and recovery selectors, focused fixtures, and fleet-managed copies.
Do not read pane, transcript, credential, private-data, or ignored payload
content. Use bridge-first replacement for each assigned managed client and
require the recorded owner-visible acceptance before retiring its old root.

# Acceptance

Both real canonical roots resolve under `projects`; protected Git and ignored
bytes are preserved; every linked worktree resolves; all live and cold-start
references select only the new roots; managed clients and phone control pass;
the complete Harness suite and both repository validators pass; no old-root or
task residue remains. Publish through protected Git and retain a bounded
rollback until final acceptance.
