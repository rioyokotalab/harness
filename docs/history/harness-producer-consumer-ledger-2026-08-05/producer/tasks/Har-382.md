task: Har-382
repository: harness
state: ready
priority: 1
created: 2026-08-03
consumer: any
record: pending
authority: repository-local-archive-owner-migration
---
# Objective

Replace pathname-bound archive ownership with an immutable repository identity
and retire reconstructed archive-anchor worktrees only after every affected
receipt remains independently restorable and auditable.

# Scope and benchmark

Use
`docs/audits/har381-producer-lifecycle-closeout/archive-owner-followup.md` as
the complete decision and evidence route. Preserve all 12 reconstructed roots,
84 existing receipts, source bundles, generations, unrelated worktrees, remote
branches, and dirty state until the new alias contract passes. Do not weaken
the current fail-closed scratch-owner guard or rewrite historical evidence in
place.

Implement and test a versioned immutable owner alias, deterministic migration,
collision/missing-generation rejection, mixed old/new audit behavior, exact
rollback, and post-migration restore. Deletion is a separate final guarded
step only after equivalent audit and restore evidence proves the pathname
anchors redundant.

# Acceptance

All existing archive receipts still audit, representative restores match exact
tips, hostile alias and generation cases fail closed, the complete Harness
suite passes, and only independently proven redundant anchors become eligible
for guarded cleanup. No remote branch, external service, credential, ruleset,
consumer runtime, or unrelated residue is changed.
