# Har-396 portfolio plan

Implement one repository-local task per project in isolated worktrees. In each
repository, replace role-exclusive authority with trust-equivalent capability
and advisory coordination; convert role diff rejection to a visible advisory;
preserve immutable packet, lifecycle, privacy, external-action, and protected
publication gates; and add focused regressions. Personal additionally replaces
"exact confirmation" wording with contextual plain-language approval while
retaining bounded target/action requirements. Website retains its separate
delegated WORKER restrictions because those describe subagent dispatch, not a
producer-versus-consumer permission difference.

Validate and publish each repository independently. Reconcile producer state
inside each accepted task publication when repository invariants permit it;
otherwise use one immediate reconciliation publication. Rollback is the five
recorded protected-main revisions before this task.
