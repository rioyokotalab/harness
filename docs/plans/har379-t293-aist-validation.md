# Har-379 frozen execution plan

## Window

- Start: 2026-08-02 13:47:47 JST
- Material cutoff: 2026-08-02 19:47:47 JST
- Deadline: 2026-08-02 20:47:47 JST
- Evidence cadence: exactly seven consecutive hourly slices

## Sequence

1. Preserve clean protected main and perform all task edits in the isolated
   `codex/har377-t293-aist-cowork` worktree.
2. Ask native Claude Code for one independent critique of this plan, limited
   to repository-scoped file reads and its one named review artifact. Treat
   its output as advice, verify every claim, and do not replay an ambiguous
   invocation. Record the deviation from the original "no tools" shorthand.
3. From Aist, verify local supervisor, unattended-auth, watchdog, and service
   ownership. Through the approved ordinary `login` route, obtain fresh
   Local-vantage probes of `aist` and `aist2` without exposing credentials.
4. If both routes and sibling ownership are exact, use the independent
   ordinary `login` ControlMaster to run repository-native Local snapshots,
   then kick only the primary Aist supervisor once with the exact command and
   acknowledgement contract recorded in the task. Sample the selected
   generation and both Local-vantage routes after every completed snapshot,
   separated by one second, until recovery or the 30-second wall-clock bound.
   Never inject dual loss and never touch another Mac's service.
5. Observe current state through the seven-hour window. Immediately after
   slice 3 closes at 16:47:47 JST, ask Claude to critique only the accumulated
   value-free evidence. Implement no change unless a reproducible defect is
   demonstrated and its authority, rollback, owning tests, and protected
   publication route are all exact.
6. At the material cutoff stop mutations. Run integrated repository and fleet
   checks, ask Claude for a final file-scoped acceptance audit with no target
   tools or mutation authority, and reconcile each frozen requirement against
   authoritative evidence, including the disclosed original wording
   deviation.
7. Publish a coherent protected change only if repository bytes changed. Keep
   exactly seven hourly evidence slices and a minimum 350-word durable final
   summary. Preserve any failed or ambiguous operation with explicit retry
   safety.

## Rollback

The pilot relies on the already accepted launchd supervisor and does not edit
configuration. If the selected route fails while its sibling remains ready,
use only the repository-native supervisor's recorded recovery interface after
classifying the failed acknowledgement. If both routes fail or state becomes
ambiguous, stop; do not improvise. Repository changes remain isolated until
validated and protected.
