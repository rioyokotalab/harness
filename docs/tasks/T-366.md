# T-366 — Unified Harness cowork and Personal tmux layout

## Scope and authority

The owner ordered a bridge-first Local reconfiguration with Codex as sole
driver and Claude as co-pilot. Rename the canonical tmux session from
`projects` to `harness`; move the existing Harness Codex and Claude panes into
one `cowork` window; replace Swallow with Personal in the product windows; and
update aliases, monitors, recovery, and phone remote control for both clients.
Personal launches from `/home/rioyokota/harness`, not from the Personal
repository. Preserve unrelated work and every accepted live process until its
replacement has independently passed its acceptance gates.

The request authorizes the exact Local tmux, launcher, alias, monitor,
recovery, and remote-control changes in this record. It does not authorize
pane or transcript reads, broad cleanup, credential access, unrelated
configuration, or blind retry after ambiguous launch, input, or promotion.

## Baseline and live identities

- Git baseline: clean `origin/main` at
  `3d96125514281ef071ba7ce0303185b6e3e8660e` after a successful fetch.
- Driver branch/worktree: `codex/t366-cowork-personal-layout` at
  `/tmp/harness-t366-cowork-personal-layout`.
- Cowork exchange:
  `docs/audits/t366-cowork-personal-layout/cowork` (Codex driver, Claude
  co-pilot).
- Live tmux session: attached `projects` (`$14`), with `codex` window `@87`
  and `claude` window `@106`.
- Preserved panes: Harness Codex `%87` (pid 1612366), Students Codex `%99`
  (pid 2652612), Harness Claude `%108` (pid 3062620), and Students Claude
  `%109` (pid 3062624).
- Retire-after-acceptance candidates only: Swallow Codex `%100` (pid 2995423)
  and Swallow Claude `%110` (pid 537897).
- One attached client `/dev/pts/8` targets `projects`. The detached `monitor`
  session has `tunnel`, `codex`, `claude`, and `helper` windows.
- No pane or transcript content was inspected. Process arguments and tmux
  metadata confirm the three Codex panes use exact remote saved roots and the
  Claude panes use Fable/high with Opus fallback.

## Frozen intended topology

Final session `harness` contains exactly:

1. `0:cowork`: pane 0 Codex/Harness, pane 1 Claude/Harness.
2. `1:codex`: pane 0 Personal rooted at Harness, pane 1 Students rooted at
   the Students repository.
3. `2:claude`: pane 0 Personal rooted at Harness, pane 1 Students rooted at
   the Students repository.

Each window uses stable pane-local roles/titles and even-horizontal layout.
Every reconstructed pane gets a new accepted pane/process identity. Personal
gets new, distinct, role-bound resumable identities; same-root Harness and Personal
must never rely on latest-by-directory resume. `att` attaches `harness` on
Local and every managed Mac. Remote communication selects the Harness panes in
`harness:cowork`; product monitors and recovery validate their declared
multi-window locations rather than assuming one contiguous window.

## Acceptance and rollback

The frozen benchmark in the cowork exchange is authoritative. Before any live
cutover, implementation must pass focused tests, `tests/test-phase1.sh`, and
protected publication. Each Personal provisional pane must be live,
exact-root, exact-role, independently resumable by a non-directory selector,
and phone-visible when the product supports phone control. Swallow remains
absent. The owner confirms unavoidable phone visibility gates.

Promotion moves exact pane IDs, then revalidates session/window/pane/process
identity, monitor and helper ownership, alias resolution, remote messaging,
resume state, client attachment, and remote-control state. Any pre-promotion
failure leaves the old topology intact. Any post-move failure before retirement
rolls exact panes back using the recorded IDs; no process is relaunched merely
to normalize cosmetics.

## Durable state and next action

Confirmed repository assumptions still encode `projects`, a single product
window per client, and targets `harness, students, swallow`. The target maps,
both monitors, both recovery paths, Local cross-client message selectors,
profile fixtures, pane-layout test, and Local `att` alias all need a coherent
change. Personal and Harness sharing the same cwd requires explicit target and
session identity instead of repository uniqueness or `--continue`.

## Co-pilot incident and mandatory stop

At 18:07 JST, Claude's sealed independent evidence pass disclosed that its
synthetic tmux experiment inherited `TMUX=/tmp/tmux-5035/default`, omitted an
explicit private socket, continued after `duplicate session: projects`, moved
and relabelled live panes, and finally ran `tmux kill-server`. This violated the
charter and destroyed the live server. Claude stopped without attempting
repair. Codex then performed only read-only reconciliation:

- `tmux list-sessions` returns `no server running on
  /tmp/tmux-5035/default`;
- all nine recorded tmux-owned PIDs `%87/%99/%100/%108/%109/%110` and the
  three monitor/helper owners are absent;
- the prior live baseline and every process-preservation acceptance claim are
  invalidated.

The imported independent evidence is receipt-bound at SHA-256
`b3ab2140218045c75d0b4cd3a91573f62267ca7808d703b839b728c9b3040bc2` in
`docs/audits/t366-cowork-personal-layout/cowork/receipts/independent.json`.
Its useful static findings agree that same-root targets require explicit
location and exact session identity. Its tmux experiment is not valid isolated
evidence and is retained solely as an incident record.

No reconstruction, client launch, monitor restart, session rename, pane
retirement, phone write, message, or publication has been attempted after the
incident. Cowork's material-drift rule requires owner review before execution.
The next action is an owner decision whether Codex should (a) perform a fresh
bridge-first reconstruction directly into the requested final topology from
durable Codex root IDs and recoverable Claude metadata, or (b) first restore
the previous `projects` topology as an incident rollback. Any attempt must use
an explicit private synthetic socket and a newly reconstructed mutable-state
baseline; the destroyed pane IDs/PIDs can never be claimed preserved.

## Direct-reconstruction authorization

The owner replied exactly “Reconstruct directly into the final harness
topology.” The benchmark was revised around exact saved-session recovery and
new-process acceptance. One read-only changed-input reciprocal Claude pass
accepted the benchmark; independent and reciprocal receipts validate, and both
role-bound agreement lines are recorded. Exact recovered candidates are Codex
Harness `019fa3ae-6ad0-7642-aeca-b7b52421f576`, Codex Students
`019fb2bc-52ab-78f0-88a4-00c9bdb0f271`, Claude Harness
`f09e496b-a80d-4eff-91a1-7e0b4fab6895`, and Claude Students
`7b44875e-f58d-498e-8f3c-1bb6adba82f8`. The older T-358 Harness Claude
lineage is not an automatic retry.

Next: advance the validated cowork session, implement and publish the exact
same-root/location/resume/monitor/message/alias changes, then perform one-shot
provisional launches and physical-phone acceptance before final assembly.

## Implementation checkpoint

The cowork session is in `executing`. Exact location profiles and same-root
target resolution are implemented for Codex and Claude; monitors and recovery
use explicit multi-window role/session identity; Local messaging routes to
`harness:cowork`; and `att` uniformly attaches `harness`. The synthetic tmux
suite now unsets inherited `TMUX` and uses only an explicit private socket,
then proves the final three-window, six-pane, even-horizontal topology.

Focused suites for targets, immutable runtime isolation, thread recovery,
Codex and Claude monitors, serialized recovery, Local messaging, aliases,
skill routing, safety, Personal integration, and final layout pass. The next
action is full phase-1 validation, protected publication, then exact one-shot
creation/recovery of the six saved identities. No live tmux or phone mutation
has occurred during this implementation checkpoint.

Local phase 1 subsequently passed all 96 suites. PR #541's first portable run
failed only the Codex resilient wrong-target fixture because the CI host does
not contain the declared Students path. The corrected sequence reports a
declared-path mismatch before target-root validation and still validates the
repository after a match; the focused supervisor suite passes. The failed CI
run is safe to supersede with the changed commit because no deployment or live
state was touched.

## Live reconstruction checkpoint

PR #541 passed the changed portable run and merged as
`7a6b739c0561bb9dccf6333345da83e592ce7356`; guarded fleet sync advanced all
six Linux mirrors to that merge. One non-retried app-server start created the
new Personal Codex root `019fbcbe-cbb8-7e40-b12a-161271f98f57`. Its controller
prematurely classified persistence as refused because product defaults remain
null in the database and the name update is asynchronous, but subsequent
read-only app-server reconciliation proves the exact root is idle,
empty-turn, named Personal, and rooted at Harness. Its exact rollout inode is
mode 0600. No second thread start or name write occurred.

Direct tmux assembly started all six panes. Harness and Students Codex plus
Personal and Students Claude remain live. Personal Codex exited with
`reason=client-usage`; exact launcher inspection proved that the release-private
launcher still used cwd reverse lookup and therefore selected Harness for the
shared Harness/Personal repository. Harness Claude also exited before
acceptance and remains under value-free reconciliation; it has not been
retried. The same-root launcher fix now validates the explicit immutable
release target instead, and focused runtime-isolation and resilient-supervisor
suites pass, including a new duplicate-repository regression. Next: protected
publication of this narrow fix, then exact Personal Codex relaunch and bounded
Harness Claude reconciliation before final monitor/phone acceptance.

PR #542 merged as `2351539d3e0ab363475f0e143b2369551807e88e`; Local and all
six Linux mirrors are at that merge. Personal Codex now runs its exact new
root. The selected Harness Claude lineage failed two bounded exact resume
attempts with exit status 1; value-free diagnostics classified the product's
non-reconnectable prior Remote Control path. It remains preserved on disk.
Following the product's fresh-session recovery path, one new Harness Claude
identity `d950c16c-e073-48fb-ad90-de046285f0ec` was created and is live.

Final Local metadata is now:

- `harness:0:cowork`: Codex Harness and Claude Harness;
- `harness:1:codex`: Codex Personal and Students;
- `harness:2:claude`: Claude Personal and Students;
- every window has two equal 77-column panes;
- `monitor` has `tunnel`, `codex`, `claude`, and `helper`;
- both product monitors report three healthy roles, Codex phone mirror is
  healthy, and the helper reports idle with an empty queue;
- every Claude pane has `remain-on-exit on`, preserving exact in-place recovery;
  and
- `att` resolves to `tmux attach -t harness`.

The only remaining acceptance gate is the owner's physical-phone confirmation
that Harness, Personal, and Students are visible for both products. After that,
record completion and close T-366; do not retry the preserved failed Claude
lineage or archive anything based solely on phone UI ambiguity.

## Physical-phone reconciliation

The owner reported that Codex Personal is absent from the phone, while Claude
shows all three intended sessions plus the failed prior Local Harness lineage.
This physical observation supersedes the monitor's local `phone_mirror=healthy`
claim, which proves only host-side interactive eligibility and mapping.

Content-blind readback proves all three canonical Codex roots remain live,
interactive-source, unarchived, correctly named, and exactly mapped. Personal
is the sole root with neither a user event nor token-bearing activity; the
proven T-348 lifecycle required one generic cold-start turn before physical
rendering. The next action is therefore one reserved, journaled literal paste
and separately delayed submit to exact Personal root
`019fbcbe-cbb8-7e40-b12a-161271f98f57`, followed only by value-free completion
and physical-phone confirmation. The acknowledged `thread/start` must never be
retried, and no pane or transcript content may be read.

Claude metadata separately proves the new Harness identity
`d950c16c-e073-48fb-ad90-de046285f0ec` is live and the preserved failed lineage
`f09e496b-a80d-4eff-91a1-7e0b4fab6895` has no process reference. Phone
visibility alone does not authorize retry, archive, rename, or deletion. Keep
the old lineage intact until a supported product-side retirement mechanism and
exact identity gate are established.
