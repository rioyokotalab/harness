# T-359 — Claude pane monitor and recovery parity

## Status

Phase: `executing` (exact owner instruction at ~02:47 during the T-358
overnight run: "Build and deploy it tonight within the remaining window").

Scope, owner-worded: (1) deploy for the `projects:claude` panes the same
blocked-thread restart mechanism the codex window has; (2) add a `claude`
window to the `monitor` session between `codex` and `helper` running that
monitor; (3) update the helper so it serves both codex and claude recovery;
(4) mirror the codex window's top pane-name border titles on the projects
claude window and the monitor session windows; (5) run tests demonstrating
automatic claude recovery.

## Frozen boundaries

- The live codex monitor, its targets module, and codex recovery semantics
  stay behavior-identical; claude support is additive.
- Content-blind: no pane or transcript capture in monitor/helper code paths.
- Recovery relaunches claude with the frozen T-358 flags and `--continue`
  first, plain relaunch second; attempts bounded and serialized through the
  helper; no unbounded restart loops.
- Validation: focused new suite plus the owning codex monitor/helper suites
  and `harness validate` tier for a lifecycle change; live drill on one pane.

## Completion

Shipped and deployed within the owner's window. `harness_claude_targets.py`
plus `profiles/claude-session-targets.tsv` define the closed claude target
map (CLAUDE.md marker instead of codex's `.codex/config.toml`).
`harness-tmux-claude-monitor` mirrors the codex monitor for
`projects:claude`: content-blind health from tmux metadata and `/proc`
identity, pane-order enforcement, owner-focus deferral with a bounded 600s
attached-dead grace, and a 3-per-hour per-target enqueue rate limit.
`harness-claude-pane-recovery` respawns only dead panes in place (no `-k`,
no kill, no send-keys), relaunching with the frozen flags and `--continue`,
falling back to a plain relaunch after a failed generation.
`harness-codex-recovery-helper` gained an additive `--enqueue-claude-blocked`
mode and `claude-pane-blocked` worker dispatch; codex behavior is unchanged
and its suites still pass.

Deployment: `remain-on-exit on` for `projects:claude`; monitor session
reordered to tunnel/codex/claude/helper with the claude monitor running
`--interval 30 --enforce-order --auto-recover`; pane-border titles (top,
`#{pane_index}:#{role}`) mirrored onto `projects:claude` and every monitor
window; `bin/harness` dispatch extended.

Live drill 03:17–03:50: killing the swallow pane's claude (pid 3062635) left
a dead pane; repair correctly deferred while the owner's client was focused
on it, then recovered automatically at 03:50:37 as pid 537897 with the exact
frozen argv plus `--continue`. Helper receipt `completed/worker-success`;
monitor 3/3 healthy. Two defects were found and fixed by the drill: dead
panes report an empty cwd (dead check now precedes cwd checks), and
owner-focus deferral was unbounded (now 600s for dead panes only).

Validation: new `tests/test-tmux-claude-monitor.sh` (source audits, health
matrix, deferral/grace/rate-limit, helper end-to-end, worker respawn and
alive-pane rejection) registered in `focused-suites.tsv`; ShellCheck clean;
`test-tmux-codex-monitor.sh`, `test-codex-targets.sh`, and
`test-codex-recovery-helper.sh` unchanged and passing; phase 1 at 92 passing
suites with only the pre-existing `.nfs`-dirty pair failing.

## Working set and next action

Branch `claude/t358-overnight-duration` (same overnight run publication).
Files: `libexec/harness_claude_targets.py`,
`libexec/harness-tmux-claude-monitor`, `harness-codex-recovery-helper`
(additive client support), `tests/test-tmux-claude-monitor.sh`, T-358/T-359
records. Next action: implement per the architecture maps, deploy the
monitor window, run the synthetic suite and one live kill/recover drill.
