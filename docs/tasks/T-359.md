# T-359 — Claude pane monitor and recovery parity

## Status

Phase: `executing` (exact owner instruction at ~02:47 during the T-358
overnight run: "Build and deploy it tonight within the remaining window").

Scope, owner-worded: (1) deploy for the `projects:claude` panes the same
blocked-thread restart mechanism the codex window has; (2) add a `claude`
window to the `monitor` session between `codex` and `helper` running that
monitor; (3) update the helper so it serves both codex and claude recovery;
(4) mirror the codex window's top pane-name border titles on the projects
claude window and the monitor session windows; (5) run tests demonstrating
automatic claude recovery.

## Frozen boundaries

- The live codex monitor, its targets module, and codex recovery semantics
  stay behavior-identical; claude support is additive.
- Content-blind: no pane or transcript capture in monitor/helper code paths.
- Recovery relaunches claude with the frozen T-358 flags and `--continue`
  first, plain relaunch second; attempts bounded and serialized through the
  helper; no unbounded restart loops.
- Validation: focused new suite plus the owning codex monitor/helper suites
  and `harness validate` tier for a lifecycle change; live drill on one pane.

## Working set and next action

Branch `claude/t358-overnight-duration` (same overnight run publication).
Files: `libexec/harness_claude_targets.py`,
`libexec/harness-tmux-claude-monitor`, `harness-codex-recovery-helper`
(additive client support), `tests/test-tmux-claude-monitor.sh`, T-358/T-359
records. Next action: implement per the architecture maps, deploy the
monitor window, run the synthetic suite and one live kill/recover drill.
