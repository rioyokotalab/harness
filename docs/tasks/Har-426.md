task: Har-426
status: active
updated: 2026-08-08
phase: protected publication
---
# Har-426 — Run bounded Personal work from Riken Harness

## Outcome and scope

Preserve the owner's current Harness phone thread and canonical tmux pane while
allowing it to drive the already-reviewed bounded Personal operation process
from Riken. Do not create, replace, rename, or promote a Personal tmux pane,
saved root, phone thread, monitor, app-server root, or managed target.

The exact Personal root is `/Users/yokotar/projects/personal` on Darwin and
remains `/mnt/nfs-03/safe/Users/rioyokota/projects/personal` on Linux. Personal
stays a non-managed bounded root. Each operation must still run in a fresh
non-persistent Personal-rooted child, load Personal policy, hold the one
cross-client lock, scrub Harness variables, and retain only the value-free
receipt. This task does not authorize reading Gmail, Calendar, local document
content, credentials, or any other Personal source.

## Confirmed baseline

- Harness `main` was clean and matched `origin/main` at
  `e2d0cc5f27ec2484784a319640767383a179953d` after a fresh fetch.
- The current branch is `codex/har426-riken-personal-agent`.
- Riken's exact Personal directory is a current-user-owned mode-0700 real Git
  root with a clean `main` checkout.
- The live Codex sentinel is a symlink to Harness's tracked sentinel, which
  already admits `$HOME/projects/personal` on macOS and labels Personal
  non-managed.
- `harness personal-agent --status` fails before inspecting Personal state
  because its production root, GNU `stat`, runtime directory, and `flock`
  assumptions are Linux-specific.
- T-355 D-010 and D-010a already freeze the desired lifecycle: operate
  Personal from Harness through one bounded non-persistent child and create no
  dedicated Personal tmux or phone identity.

## Frozen implementation

1. Select the exact production Personal root by native platform and reject all
   other production identities.
2. Add narrow portable owner, mode, and link-count probes for Linux and Darwin.
3. Preserve Linux's file-descriptor `flock`; add an owner-only, PID-backed
   directory lock on Darwin with exact stale-lock reconciliation and exact
   unlink/rmdir cleanup.
4. Use the owner-only macOS temporary directory as the ephemeral runtime base;
   keep the private connector profile in its existing user-state location.
5. Extend focused tests for both lock implementations and production-root
   selection without weakening the Personal contract or connector gates.
6. Run focused tests, task-ledger checks, the owning validation tier, and a live
   value-free Riken `--status` probe. Publish only through protected Harness
   `main`.

## Retry safety and next action

## Implementation and validation result

- The launcher now resolves the exact platform-specific Personal root, uses
  native Linux/Darwin metadata probes, retains Linux `flock`, and uses an
  owner-only PID-backed directory lock under macOS's private temporary root.
  Exact stale-lock recovery uses only `unlink` and `rmdir`.
- Focused tests pass for local Codex and Claude children, connector gates,
  value-free receipts, lock exclusion, Darwin lock cleanup, stale-lock
  recovery, and unsafe-lock refusal. Shellcheck, task-ledger routing,
  `git diff --check`, and the refreshed context benchmark pass.
- Live Riken status reports `root=ready`, `lock=free`, `profile=absent`,
  `privacy=blocked`, `connectors=ready`, and `managed_target=false`. The ready
  rows are repository declarations; the absent private Riken profile keeps
  Gmail and Calendar unavailable.
- One non-sensitive, connector-free Codex child loaded only normal Personal
  startup policy and returned
  `PERSONAL_BRIDGE status=ready root=/Users/yokotar/projects/personal
  managed_target=false`. Its value-free receipt is complete/success. It did
  not inspect local documents, credentials, email, calendar, or external
  services and made no file change.
- Native Riken `tests/test-phase1.sh` reached the expected MPI skip but failed
  unrelated Linux-assumption and host-environment suites, including system
  Python 3.9 lacking `zip(strict=...)`/`tomllib`, GNU `stat`, Linux
  `SO_PEERCRED`, and long macOS temporary socket paths. The task-owned suite
  passes natively; protected portable CI remains the full R3 authority.

## Retry safety and next action

No connector, credential, Personal source content, tmux, app-server, or
external write was accessed or changed. Commit the verified Harness tree,
publish one protected task PR, require portable R3 success, merge, and
fast-forward canonical `main`. An ambiguous child or connector result remains
non-retryable until reconciled.
