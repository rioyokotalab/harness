# T-351 — Autonomy and execution efficiency

## Status

Executing through 2026-07-31 09:00 JST. Optimize in the frozen order autonomy,
efficiency, then non-obstructive safety. The selected plan and evidence are:

- `docs/plans/t351-autonomy-efficiency.md`
- `docs/audits/t351-autonomy-efficiency/cowork/`
- `docs/audits/t351-autonomy-efficiency/time-slices.md`
- `docs/audits/t351-autonomy-efficiency/actions-transition.md`
- `docs/audits/t351-autonomy-efficiency/context-benchmark.md`
- `docs/audits/t351-autonomy-efficiency/execution-performance.md`
- `docs/audits/t351-autonomy-efficiency/validation-performance.md`
- `docs/audits/t351-autonomy-efficiency/parallel-isolation.md`

## Current facts

- Codex and Claude accepted benchmark `t351-autonomy-efficiency-v1`.
- Students PRs #490/#491 end at `dafb4a3`; Swallow #136/#137 end at `0e9029b`.
  Owner jobs skip before runner allocation, duplicate `main` push triggers are
  gone, mixed/non-owner checks remain hosted, and weekly/manual full runs
  remain. Harness keeps PR/weekly/manual CI but removes duplicate `main` push.
  No ruleset or approval count changed.
- Root policy is 805 words, the active board 52 lines / 246 words, and the
  17-skill catalog 381 words. Eight frozen scenarios, including the selected
  task record, reduce total context 94.1–97.6% (96.5% median) and non-ledger
  context 46.8% at the median.
  Git/external/Codex/fleet policy and the largest workflows route independently.
- `harness validate` uses fail-closed R0–R3 selection and exact-tree local
  receipts with post-test identity checks and value-free environment binding.
  Every validator and unknown/sensitive path is R3; ordinary selected work
  takes 0.01–1.03 seconds. The 114.77-second immutable baseline fell to
  earlier 72–74-second integrated runs. A matched ShellCheck-overlap candidate
  passed three times at a 65.51-second median versus 76.38 (14.2% faster), and
  longest-first focused admission improved its matched median from 50.16 to
  45.86 seconds (8.6%). The subsequent exact integrated checkpoint passed all
  91 suites in 56.54 seconds; the protected changed-input checkpoint passed
  all 91 in 55.37 seconds.
- Harness, Students, and Swallow now have target-scoped releases, state, locks,
  recovery, and receipts. Closed target/CWD checks reject cross-project
  launch, rollback, helper, and released-runtime confusion. Legacy, stale, and
  incomplete release locks reconcile without cross-target adoption. Concurrent
  probes left other projects' Git, lock, and tmux metadata unchanged.
- Matched retained improvements include tmux monitoring 410.532→42.516 ms,
  fleet-health barriers 4.500→1.925 s, connection snapshots 922→304 ms,
  backup warnings 0.05→0.03 s, and batched immutable-release checks
  14.2–23.5% faster than the first payload implementation.
- No live Codex/tmux/app-server/process lifecycle was changed. Account plan,
  billing cycle, budget, payment, and organization Actions policy remain
  unknown because current read-only credentials lack `admin:org`.
- Local's live checkout has one recovery-held untracked `.nfs` inode; preserve
  it. Independent Students and Swallow work remains unrelated.

## Working set and next action

Working files are routed policy/skills, validation/runtime/monitor scripts,
tests, this record and linked audits, plus task-owned experiment worktrees.

Run one final exact-tree suite, create one protected Harness pull request, prove
its exact merge and absent push-trigger run, perform guarded task cleanup, run
fresh fleet health, and complete the 09:00 handoff.

Ordinary repository Git and the task pull request are authorized. Do not write
hosting settings, rulesets, billing, app-server, tmux, pane, transcript,
scheduler, credential, or live project-agent state.
