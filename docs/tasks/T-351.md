# T-351 — Autonomy and execution efficiency

## Status

Complete upon protected merge of PR #484. Its terminal body is the receipt for
the exact final head, protected run, merge commit/tree identity, absent
post-merge push run, cleanup counts, and deadline fleet snapshot. Durable
implementation and evidence are:

- `docs/plans/t351-autonomy-efficiency.md`
- `docs/audits/t351-autonomy-efficiency/cowork/`
- `docs/audits/t351-autonomy-efficiency/time-slices.md`
- `docs/audits/t351-autonomy-efficiency/actions-transition.md`
- `docs/audits/t351-autonomy-efficiency/context-benchmark.md`
- `docs/audits/t351-autonomy-efficiency/execution-performance.md`
- `docs/audits/t351-autonomy-efficiency/validation-performance.md`
- `docs/audits/t351-autonomy-efficiency/parallel-isolation.md`

## Accepted outcomes

- Codex and Claude accepted benchmark `t351-autonomy-efficiency-v1`.
- The fixed 30-day audit found 1,821 private rounded non-Dependabot
  job-minutes in Students and Swallow. Trigger corrections directly remove
  1,142 (62.7%); ordinary owner PRs allocate zero private runners, while
  mixed/non-owner checks and weekly/manual full opportunities remain hosted.
  Harness removes duplicate push work and routes PRs by impact; weekly/manual
  runs and R3 changes remain full.
- Root policy is 805 words, the completed board is 43 lines / 211 words, and
  the 17-skill catalog is 381 words. Eight frozen context scenarios and every
  phase/gate result are reproduced by the linked benchmark rather than copied
  into this recovery record.
- `harness validate` uses fail-closed R0–R3 selection and exact-tree local
  receipts with post-test identity checks and value-free environment binding.
  Every validator and unknown/sensitive path is R3. Ordinary selected work is
  sub-second to about one second; matched ShellCheck overlap improved 14.2%,
  longest-first admission 8.6%, and repeated integrated checkpoints pass all
  91 suites near 55–57 seconds versus the immutable 114.77-second baseline.
- Harness, Students, and Swallow now have target-scoped releases, state, locks,
  recovery, and receipts. Closed target/CWD checks reject cross-project
  launch, rollback, helper, and released-runtime confusion. Legacy, stale, and
  incomplete release locks reconcile without cross-target adoption. Concurrent
  probes left other projects' Git, lock, and tmux metadata unchanged.
- Matched retained improvements include tmux monitoring 410.532→42.516 ms,
  fleet-health barriers 4.500→1.925 s, connection snapshots 922→304 ms,
  backup warnings 0.05→0.03 s, and batched immutable-release checks
  14.2–23.5% faster than the first payload implementation.

## Residual boundaries

- Local receipts are owner self-attestation from the same trust root, not
  independent CI. Weekly schedules are best-effort opportunities, not a hard
  seven-day completion guarantee.
- No live Codex/tmux/app-server/process lifecycle or project checkout was
  rolled out. Existing supervisors retain their current bytes until a
  separately coordinated natural refresh; no T-352 is inferred.
- Account plan, billing cycle, budget, payment, and organization Actions
  policy remain unknown because read-only credentials lack `admin:org`.
- No hosting settings, rulesets, approval counts, credentials, scheduler, or
  live project-agent state changed. Preserve Local's recovery-held untracked
  `.nfs` inode and unrelated clean T-347/T-350 worktrees.

No T-351 task action remains after PR #484 merges. The active board resumes
with two time-gated tasks and one blocked task.
