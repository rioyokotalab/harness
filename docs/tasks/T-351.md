# T-351 — Autonomy and execution efficiency

## Status

Executing through 2026-07-31 09:00 JST. Optimize in the frozen order autonomy,
efficiency, then non-obstructive safety. The selected plan and evidence are:

- `docs/plans/t351-autonomy-efficiency.md`
- `docs/audits/t351-autonomy-efficiency/cowork/`
- `docs/audits/t351-autonomy-efficiency/time-slices.md`

## Current facts

- Codex and Claude accepted benchmark `t351-autonomy-efficiency-v1`.
- The immutable Harness baseline passed phase one in 114.77 seconds. The
  integrated 91-suite runner repeatedly passes in about 72–74 seconds.
- Root policy is 888 words; the active board is 52 lines / 246 words; the
  selected record is one conditional hop; and completed chronology remains
  byte-exact in tracked history. Frozen scenario reductions are 94.2–98.1%,
  with 97.2% median total and 56.1% median non-ledger reduction.
- `harness validate` selects deterministic R0–R3 gates and exact-tree local
  receipts. Measured documentation, routed-skill, and mapped-terminfo routes
  take 0.13, 0.31, and 1.03 seconds. Three additional ordinary terminfo,
  debugger, and scientific-library fixtures now select their 0.01–0.93-second
  owning suites; unknown and authority/lifecycle paths still select R3.
- Cowork, HPC, PIE, remote communication, fleet hardening, and personal-Mac
  onboarding select only current phase/site/mode/component references, with
  37.6–79.2% largest-route reductions and regression-tested authority gates.
  The always-visible 17-skill catalog is 375 words, down from 819.
- Remote-reply, duration, and Git authority rules are conditionally routed;
  the always-injected command rules are 1,152 bytes, down from 2,915, with a
  regression budget.
- Git, external-operation, managed-Codex, and fleet policy are independently
  routed. Ordinary Git work selects 1,163 policy words and managed-Codex work
  1,111, without loading installer/account or fleet-rollout rules.
- External-user, mirrored-node, and reboot onboarding/recovery now select at
  most 391, 630, and 408 words, reductions of 41.1%, 45.5%, and 31.9%, with
  mandatory unloaded-reference authority and safety gates.
- Fleet health now runs for fleet/runtime work, route failures, rollout, and
  final handoff—not every unrelated turn. Recorded validation is reused when
  bytes, environment contract, and acceptance scope are unchanged; a resume
  alone no longer causes duplicate checks.
- Target-scoped runtime identity isolates Harness, Students, and Swallow
  releases, state, locks, recovery, and validation receipts. Concurrent
  repository-local probes did not change another project's Git, lock, or tmux
  metadata.
- Exact saved-thread launches and rollback now require closed-map target/CWD
  equality; targetless global resume is outside managed supervision. Unknown
  helper targets fail before creating runtime or worker state. Direct and
  released launchers reject ambient or cross-repository target confusion and
  strip Harness control/test variables before native Codex.
- The continuous tmux monitor now takes one process/socket snapshot per cycle:
  matched discovery fell from a 410.532 ms median to 42.516 ms. A four-slot
  work-conserving fleet-health queue reduced a synthetic 4.500-second
  three-batch critical path to a 1.925-second median without changing route,
  maintenance, or output contracts.
- The connection monitor takes its ten-route snapshot through a bounded
  four-slot queue, then classifies and recovers sequentially. Its matched
  synthetic critical path fell from a 922 to 304 ms median (67.0%).
- Interactive backup-warning checks now validate and parse private state once
  before one fresh scheduler query. Five healthy calls improved from a 0.05
  to 0.03-second median and from 35 to 22 observed process launches.
- Immutable Codex releases are keyed by the exact seven-file runtime payload,
  and both object resolution and released-file hashing now use one argv-safe
  Git batch. Against the first payload implementation, cold, warm, and
  unrelated-commit paths improved 14.2%, 23.5%, and 19.5%.
- Students PRs #490/#491 end at `dafb4a3`; Swallow #136/#137 end at `0e9029b`.
  Owner jobs skip before runner allocation, duplicate `main` push triggers are
  gone, mixed/non-owner checks remain hosted, and weekly/manual full runs
  remain. No ruleset or approval count changed.
- Harness retains required pull-request CI plus weekly/manual full validation
  but removes the duplicate `main` push trigger, avoiding 678 observed public
  post-merge runs. The final task PR and merge must prove this transition.
- Account plan, billing cycle, budget, payment, and organization Actions
  policy are unknown because current read-only credentials lack `admin:org`.
- Local's live checkout intentionally has one recovery-held untracked `.nfs`
  inode. Preserve it. Independent Students and Swallow work is unrelated.

## Working set and next action

Working files are routed policy and skills, the validator/tests, target-scoped
runtime, this task record, plan/audit files, and task-owned experiment
worktrees.

Integrate the independently measured shell-warning and immutable-release
slices, then run the complete suite. Continue only benchmarked improvements.
Finally create one protected Harness pull request, reconcile its exact merged
tree, perform guarded cleanup, run fresh fleet health, and complete the 09:00
handoff.

Ordinary repository Git and the task pull request are authorized. Do not write
hosting settings, rulesets, billing, app-server, tmux, pane, transcript,
scheduler, credential, or live project-agent state.
