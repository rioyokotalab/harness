task: Har-390
status: active
updated: 2026-08-04
---
# Har-390 — Cross-client producer/consumer communication

## Current result

The former Codex-only local route now has a client-neutral target map and
commands for send, report, durable report readback, and exact confirmation.
Reports are keyed by logical source, client, and request ID, so Codex and Claude
cannot collide. Existing Codex and Harness-Claude commands remain compatibility
aliases. Remote delivery now declares both sender and destination clients and
supports managed Claude panes under the same helper.

The duplicate `remote-claude-communication` skill is retired; its remote-Mac
delivery contract moved into `remote-agent-communication`. Routed references
keep local checkpoints separate from SSH delivery and acceptance-critical
request/fallback paths. No pane content is read and no process is created.

## Live consumer finding

The first real Per-031 request reached the exact Personal Claude pane. The
owner-provided visible output proved that Claude understood and completed the
diagnosis, but its interactive process inherited Personal's project-local
`dontAsk` mode. Bash, Write, and Edit were denied, so it could neither create
the consumer record nor submit the scripted report. No Personal bytes changed.

The transport now rejects Claude destinations unless their process carries
the explicit managed `--permission-mode bypassPermissions` pair. Managed
recovery carries the same pair, and the Claude monitor treats a live process
without the complete frozen launch contract as unhealthy. This converts the
silent stranded-task failure into a pre-insertion stop.

## Verified implementation and live trial

The unified transport was published at protected merge `c662ccf`; explicit
Claude permission readiness followed at merge `b1c1fee`. All 98 Harness
phase-one suites pass on the latter tree, with only the declared native-MPI
environment skip. Focused transport, monitor, context-budget, and safety-gate
tests pass.

Bridge-first replacements capability-tested Bash before promotion for Harness,
Personal, and Students. The accepted panes are `%21`, `%20`, and `%22`; the
three restricted predecessors exited before their dead provisional windows
were removed. The interval monitor was reloaded from protected main and now
reports all three targets healthy with canonical ordering. Codex inspected no
pane content; the owner supplied the restricted Personal output that diagnosed
the original denial.

Personal Claude completed the client-neutral parser, focused tests, record,
and receipt through pull requests 80 and 81 at protected merges `4c8df0a` and
`3f71213`. The Codex producer reconciled Per-031 complete through pull request
82 at `4d38103`; strict ledger convergence passes and the assignment is idle.
Every report was read from the client-keyed durable route and confirmed once.

Per-032 then exercised a private-value-safe continuation of Per-016. The
producer supplied only redacted role defaults and exception counts; exact
owner labels remained outside repositories, commands, reports, and Claude
context. Personal Claude published the parameterized synthetic calendar
availability skill through pull requests 85 and 86, including the independently
required all-day-role correction and bytecode-clean validation. Protected main
passed 12 focused tests, repository skill validation, and complete Personal
validation. The owner then froze tentative events as busy. Claude recorded the
non-sensitive decision without changing implementation bytes through pull
request 91, complete validation passed independently, and the producer parked
Per-032 at decision 4 through pull request 92. The owner then froze placeholder
events to follow role defaults and exact exceptions without title inference;
Claude recorded that decision through pull request 94 with no implementation
change, and the producer parked Per-032 at decision 5 through pull request 95
at `08d5285`. The owner then froze travel as buffered busy time; Claude
recorded the decision through pull request 97 with no implementation change,
and the producer parked Per-032 at decision 7 through pull request 98 at
`a574b61`. The owner then froze the default slot duration at 60 minutes; Claude
recorded and compacted the active register through pull request 100 with no
implementation change, and the producer parked Per-032 at decision 8 through
pull request 101. The owner then replaced start-grid enumeration with maximal
qualifying free ranges, froze weekday 09:00–20:30 defaults, and specified the
human line format. Claude reworked the engine, skill contract, and focused
tests through pull request 103; independent focused and complete validation
passed. A record-only wording correction followed through pull request 104,
and the producer parked Per-032 at decision 9 through pull request 105 at
`5805237`. The owner then froze a zero-minute buffer; Claude made that the
per-query-overridable default with focused coverage through pull request 107,
and the producer parked Per-032 at decision 10 through pull request 108 at
`c20517a`. The owner then froze a 31-day default horizon and 365-day hard
ceiling. Claude made range end optional relative to an explicit deterministic
anchor and added focused boundary coverage through pull request 110; the
producer parked Per-032 at decision 11 through pull request 111. The owner then
froze Asia/Tokyo as the default with explicit IANA overrides and fail-closed
DST boundaries. Claude implemented the optional default with focused coverage
through pull request 113; all eleven policy decisions are frozen, and the
producer parked Per-032 at the separate live acceptance gate through pull
request 114 at `44d641d`.

Independent review then found that the existing raw Calendar connector could
return source values to the consumer during that acceptance. The producer
dispatched Per-034 as a LIFO private-boundary correction. Personal Claude built
a dedicated read-only availability profile whose agent-facing schema and
responses exclude calendar identities, event values, and private exception
lists; the broker loads those values only from a fixed owner-only non-Git file,
uses minimum provider fields and bounded pagination, and maps failures through
finite value-free reasons. After three evidence-driven corrections, the exact
seven-path implementation passed 16 focused tests and complete Personal
validation and merged through pull request 116 at `98e0010`. Its terminal
record and receipt merged through pull request 117 at `da294e2`; the producer
reconciled Per-034 complete and returned the Claude assignment to idle through
pull request 118 at `380dd63`. Strict ledger convergence now passes. No live
Calendar source, credential, or private configuration was accessed.

A nested pre-existing discovery drift became Per-033. Personal Claude removed
only two retired discovery links and added only the two missing current links
through pull request 88. Independent readback and complete validation passed.
The producer reconciled its terminal receipt and returned the Claude assignment
to idle through pull request 89 at `64634ae`; Per-032 alone remains gated at
its next serial owner decision.

## Next action

Have the owner create the fixed mode-0600 availability configuration through a
local owner-only editor so no exact calendar or exception value enters an agent
command, report, repository, or transcript. Then obtain exact authorization for
one bounded live read-only acceptance. Before source access, verify provider
model-improvement controls are off and prove through metadata-only fresh
inventories that the dedicated read capability is exposed only in Personal.
Use minimum event fields over the authorized interval, persist no source
values, perform no writes, and return only formatted availability plus
value-free evidence. Later Per-017 through Per-021 trials retain their existing
reversible gates. Keep Har-390 active until the six trials complete or reach
their exact owner gates.
