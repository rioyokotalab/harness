task: Har-390
status: active
updated: 2026-08-04
---
# Har-390 — Cross-client producer/consumer communication

## Current result

The former Codex-only local route now has a client-neutral target map and
commands for send, report, durable report readback, and exact confirmation.
Reports are keyed by logical source, client, and request ID, so Codex and Claude
cannot collide. Existing Codex and Harness-Claude commands remain compatibility
aliases. Remote delivery now declares both sender and destination clients and
supports managed Claude panes under the same helper.

The duplicate `remote-claude-communication` skill is retired; its remote-Mac
delivery contract moved into `remote-agent-communication`. Routed references
keep local checkpoints separate from SSH delivery and acceptance-critical
request/fallback paths. No pane content is read and no process is created.

## Evidence and next action

Focused transport, client-neutral Personal-Claude send/report/confirmation,
skill context, and catalog tests pass. The real six-target profile resolves one
metadata- and process-matched pane per client and project without content
inspection. All 98 Harness phase-one suites pass on clean commit `b593a7a`,
with only the declared native-MPI environment skip.

One acceptance request and one permitted status-only follow-up were submitted
to the exact metadata-healthy Personal Claude pane. Neither created its durable
report within the bounded waits. Delivery is therefore ambiguous and neither
prompt may be retried; no pane or transcript was inspected.

Next: publish this validated partial implementation through one protected PR,
then use the merged helper and Per-031 packet for a new task-bound checkpoint.
Keep Har-390 active until a Claude report is read and confirmed; Personal
protocol work remains Per-031.
