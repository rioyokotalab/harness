# T-375 — Compact portfolio task boards

## Status

Complete on the protected-publication candidate. Harness, Personal, Students,
Swallow, and Website were reconciled independently from their fetched
canonical `main` revisions. Completed, canceled, and superseded chronology was
removed from each cold-start board and retained in its task index, linked task
record, full-board archive, or immutable Git anchor.

## Objective

Make every repository's `TODO.md` an active/pending-only resume surface before
the owner changes repository-organization models. Preserve task IDs, durable
evidence, current safety gates, sensitive-data boundaries, live Students and
Swallow sessions, and repository-native publication routes.

## Scope and decisions

- Harness retains T-371 plus pending T-196, T-328, and T-303; the T-354 and
  T-363 decisions remain indexed history.
- Personal turns its two existing external gates into linked pending P-008 and
  P-009 records.
- Students retains only T-052; compatibility and completed-task pointers move
  behind its task index.
- Swallow retains SW-040 and paused SW-034; completed SW-039 stays indexed.
- Website archives the exact pre-compaction board, adds a Harness-style task
  index, and links active T-213 to a focused record.

No runtime, scheduler, provider, connector, deployment, managed session,
ruleset, approval count, credential, or remote branch is changed. The live
Students and Swallow checkouts remain untouched; their changes use isolated
canonical-main reference clones after the worktree helper correctly rejected
non-main live references.

## Acceptance and validation

Each board contains only active or externally pending work plus a compact
history lookup. Every displaced item remains addressable from a tracked index,
record, archive, or immutable commit pointer. Run repository-local validators,
diff checks, index/board contract checks, and protected publication for each
independent repository.

## Next action

None after protected publication and guarded retirement of task-owned `/tmp`
worktrees. Resume only the first executable item shown on each repository's
board.
