# T-367 — Post-reorganization quick fixes

## Scope

The owner requested a short batch of fixes before resuming T-363 and explicitly
deferred tests and commits until the batch is complete. Preserve the final
T-366 topology, saved identities, phone mappings, monitors, attached client,
and unrelated state. Do not inspect pane or transcript content.

## Fix 1 — tmux color environment

Read-only diagnosis proved the reconstructed tmux server and every managed
Codex/Claude process inherited `NO_COLOR=1` with an empty `COLORTERM`. The
attached client is `xterm-256color`; tmux exposes 256 colors but does not
advertise RGB. The terminal is healthy—the inherited environment explicitly
disabled client color.

Authorized correction:

1. Make tmux configuration unset `NO_COLOR`, set `COLORTERM=truecolor`, and
   advertise RGB for `xterm*` clients.
2. Apply the same environment and option to the live server/session.
3. Restart the five panes not containing the active owner turn against their
   exact saved identities, preserving pane IDs and roles.
4. After this Harness Codex turn becomes idle, one private journaled controller
   restarts only its exact pane and saved root. Never retry an acknowledged or
   ambiguous respawn.

Working file: `config/tmux/tmux.conf`. Runtime evidence belongs under
`/run/user/5035/harness-t367-quick-fixes/`. No tests or commit yet. After the
sixth pane is verified, remain ready for the owner's next quick fix.

### Verified live progress

- The tmux server and both managed sessions now remove `NO_COLOR`, export
  `COLORTERM=truecolor`, and advertise RGB support for `xterm*` clients.
- Students Codex (`%3`, PID 748521), Personal Codex (`%10`, PID 749116),
  Students Claude (`%5`, PID 749873), and Personal Claude (`%4`, PID 750943)
  completed the journaled graceful restart.
- Harness Claude exited at the pane boundary but its old PID remained a tmux
  childless zombie. The controller stopped without retrying; the already-dead
  pane was then respawned exactly once as PID 755675, which reaped the zombie.
- All five refreshed roots retain their exact recorded remote session IDs and
  have `COLORTERM=truecolor`, `TERM=tmux-256color`, and no `NO_COLOR`.
- Harness Codex (`%0`) completed its exact post-idle restart as PID 787890.
  Detached controller PID 782487 used digest
  `94bdbfe9460d6dac85b4ee4da96bda0c7ab9eb308c472253f6ff38a582e119fc`;
  `restart-harness-result.json` records `status=verified`. The refreshed root
  has `COLORTERM=truecolor`, `TERM=tmux-256color`, and no `NO_COLOR`.

The controller's original ambiguous result and append-only journal are
preserved. `restart-five-reconcile.json` records the bounded reconciliation;
do not rerun `restart-five.py`.

## Fix 2 — Local Codex-to-Claude round trip

The owner requested one bounded greeting test against the cold-started Claude
process in `harness:cowork.1`. Local Codex submitted request
`t367-claude-hi-20260801-204436` exactly once through the metadata-verified
`send-local-claude` route. Local Claude returned exactly one identified reply
with `status=complete`: greeting received and returned, round-trip test OK.
Neither pane content nor transcript content was inspected.

## Validation and publication

Branch: `codex/t367-post-reorg-quick-fixes`, based on fetched
`origin/main` at `cf7b093`. The validator selects full R3 because the shared
tmux configuration is lifecycle-sensitive. A focused test now starts an
isolated tmux server with inherited monochrome variables and verifies the
canonical color contract.

The first direct focused invocation stopped at the component's required clean
committed-checkout preflight before behavior ran. This is retry-safe. Create a
narrow checkpoint containing only the T-367 files, then rerun the focused suite
and `harness validate --base origin/main` from that clean checkpoint. Close the
ledger only after both pass, fetch again, and publish through protected main.

Checkpoint `5345c18` was clean and the focused tmux suite passed. The first R3
run then reached every suite: all components passed except two deterministic
integration gates. ShellCheck rejected `COLORTERM=` in the new test, and the
context benchmark detected the expected active-board count change. No runtime
behavior failed. The final candidate uses `COLORTERM=''` and removes completed
T-367 from the active board. Removing the completed task restored the frozen
benchmark exactly, so
its evidence artifact required no edit. Rerun full R3 before publication; a
failed or interrupted rerun is safe to repeat because validation has no
external writes.
