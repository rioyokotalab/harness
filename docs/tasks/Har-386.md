# Har-386 — Make Personal managed clients repository-native

## Status

Implementation in progress from protected allocation revision
`3f1e11eec7dcef27e4a9709ea213e1e555a23fd8`. The Personal producer ledger is
healthy and exposes Per-015, but both Local target profiles still map the
Personal role to Harness. The live Personal Codex process therefore has native
CWD `/home/rioyokota/harness`, and the content-blind monitor accepts the drift
as canonical.

## Scope

Map Personal Codex and Claude to the exact Personal repository, update the
owning target/runtime tests, publish the validated bytes, and cold-restart only
those two Personal runtimes. Preserve the Harness and Students processes,
saved thread identities, remote-control mirrors, and unrelated repository
state. Never inspect interactive content or replay a prompt.

## Validation and next action

The focused target, runtime-isolation, Codex monitor/recovery, Claude monitor,
and topology suites pass. The validator-selected R3 complete phase-one suite
passes with only its declared native-MPI environment skip. Claude dead-pane
recovery now also binds the profile-selected repository as its exact respawn
directory.

Next publish these bytes through protected Git, update the Local protected
checkout, snapshot peer identities, rebuild and restart only Personal, and
verify exact process/session repository roots plus Personal producer
selection.

## Protected implementation and Codex checkpoint

PR #614 merged the validated implementation at
`e728f28c63f29b166f81daffb2eef9334bb1a11e`. The Local protected checkout is
clean at that revision. A one-shot app-server transaction acknowledged exactly
one new root, `019fc765-0206-7112-8293-146fb29e8087`, with the exact Personal
repository CWD, frozen granular approval, disabled sandbox, and Sol/high
defaults. Its initial provisional-name persistence check refused because the
database omits native name/model fields; the sent requests were not retried.
Same-connection structural readback subsequently proved the provisional name
and exact CWD.

The distinct bridge runtime `personal-har386-provisional` launched in pane
`%19` with a healthy watcher, reciprocal unchanged app server, and zero
recoveries or rollbacks. Its one cold-start turn completed with assistant and
terminal file-change items, no active item or system error, and reached an
idle durable Personal consumer state. A separate read-before/write/read-after
transaction renamed the old saved root to `personal-har386-retired` and the
accepted root to `personal`, with both CWDs unchanged.

The pane-native cutover is now reversible and accepted: `%19` occupies exact
canonical `harness:codex.0` with the Personal role and exact Personal CWD;
Students pane `%16` is unchanged at `harness:codex.1`; the old pane `%10`
remains live and isolated in window `@16` named `har386-retired`; both attached
clients stayed on unaffected Harness pane `%0`. Harness and Students process
start identities remain unchanged.

Next revalidate and signal only the exact old Codex TUI leaf once, let its
launcher and supervisor unwind, then recover Claude Personal in place using
the protected repository-bound respawn helper. Reload both content-blind
monitors, verify all canonical metadata and native roots, and close Har-386.

## Post-promotion lifecycle reconciliation

The exact old Codex TUI leaf received one `SIGTERM`; its TUI, launcher, and
supervisor all exited, and its saved root remains intact. Claude's prior
Personal identity exited immediately after each of the one manual and one
already-queued exact-resume attempts. It is preserved without further retry.
The supported fresh-session fallback is now stable in exact pane `%4` with
session `d3a229fe-65a1-4fde-bf27-999e1d9c208a`, exact Personal CWD, Personal
role/name, Remote Control, Fable/high, and Opus fallback. Harness and Students
process start identities remain unchanged, and the reloaded Claude monitor is
healthy 3/3.

The reloaded Codex monitor proves all three canonical roots exact but reports
two extra unarchived top-level roots. They are the retired old Personal root
`019fbcbe-cbb8-7e40-b12a-161271f98f57` and the wrong-CWD root
`019fc0c0-7aec-7640-a93d-9946ac8dbeb3` created when the owner first asked the
misrooted consumer to execute its ready packet. Content-blind app-server
readback shows exactly one completed assistant-bearing turn in each, no active
items or system error, and exact Harness CWD. No surviving process references
either root; their rollouts remain preserved.

Next, under the shared message lock, revalidate those exact identities,
rollouts, completed structures, zero process references, canonical roots,
app-server identity, and clean protected Harness checkout. Send exactly one
non-retryable `thread/archive` request for each extra root in the order above,
require same-connection and database readback, then reload the helper receipt
and require Codex monitor health 3/3. Archival is reversible and must not
remove either saved rollout.
