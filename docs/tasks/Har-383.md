# Har-383 — Build the synthetic productive-idle selector

## Status

Complete on 2026-08-03 from protected base
`615f4b0591289cba85ea767d6d3d823d172600c7`. The immutable producer packet is
`docs/producer/tasks/Har-383.md`; the compact contract and its 29 scenario and
26 acceptance rows have been read completely.

## Frozen scope

- Build only a Harness-local synthetic pilot using temporary fixtures.
- Keep the selector separate from `producer-ledger.py` and leave
  `docs/producer/`, live `NIGHTLY.md`, sibling repositories, consumer runtimes,
  schedulers, wait controllers, and rollout allocation unchanged.
- Preserve closed schemas, append-only event recovery, immutable receipts,
  local authority and path boundaries, value-free cross-repository status,
  and no replay after an unresolved selected event.
- Accept only independently valuable frozen cards; never invent work to fill a
  duration window.

## Ordered execution

1. Locate the narrowest existing parser, schema, and fixture-test integration
   points without reading unrelated task records.
2. Implement synthetic catalog, admission, event, receipt, planning, selection,
   start, receive, and recovery behavior with hostile-path rejection.
3. Drive all 29 normative scenarios and 26 acceptance measures from the
   contract tables, including the 60-card latency benchmark.
4. Replay the Har-381 checkpoint evidence and prove one durable wait, no early
   duplicate reads, unchanged always-read bytes, no billable Actions increase,
   and no extra protected transition.
5. Run focused tests, complete Harness validation, publish one protected task
   pull request, and write the terminal consumer receipt.

## Current next action

Run complete R3 on the terminal tree, push the closeout to PR #607, merge only
if its hosted check remains skipped and conflict-free, then reconcile the
producer-owned queue without allocating a rollout.

## Validation

- Focused pilot: pass; all 29 normative scenarios, closed-schema and hostile
  path cases, crash recovery, one-way finalization, immutable receipts, and
  matched Har-381 wait replay pass.
- Matched 60-card selector benchmark: 20 measured runs, p95 2.670 ms against
  the 100 ms limit.
- Producer ledger, consumer writer boundary, task-ledger routing, producer-role,
  validation-router, and focused-runner suites: pass.
- First complete R3: the pilot and 94 other suites passed. The context benchmark
  reported its exact generated diff after this active-board pointer changed;
  tmux-config and terminfo correctly refused the dirty uncommitted checkout.
  Retry is safe after the benchmark refresh and branch checkpoint because no
  target, external, or production state changed.
- Clean committed R3 at `aa8528c`: all 98 focused suites and phase one pass.
- Public audit at `aa8528c`: `value_exposed=false`; no workflow path changed.
- Terminal evidence is indexed under
  `docs/audits/har383-productive-idle-pilot/`.
- PR #607 hosted run `30792138878` completed with `portable-phase1=skipped`, so
  the pilot added zero billable hosted minutes before terminal closeout.

## Retry safety

No target action, production route, sibling state, scheduler, or rollout was
started. The branch and PR writes succeeded; retry only changed closeout bytes
after reconciling PR #607 and the recorded branch head.
