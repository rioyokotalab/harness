task: Har-430
status: active
updated: 2026-08-08
phase: validating
---
# Har-430 — Accelerate macOS test feedback

## Outcome and frozen scope

Make Harness development testing on macOS faster and less load-sensitive
without weakening the complete phase-one publication gate. The owner approved
this sequence: make the two interrupt fixtures deterministic, retain complete
per-suite timing evidence, add cleanup-safe fail-fast execution, use that
evidence to prevent process-heavy suites from contending on Darwin, and add a
short Darwin preflight for development iterations.

The final `tests/test-phase1.sh` pass remains mandatory for protected
publication. The preflight is an earlier feedback layer, not a replacement.
Do not change host configuration, install packages, contact fleet nodes, or
modify the preserved Har-428 phone checkout.

## Baseline evidence

- Riken Har-427: 99 focused suites passed in 390.575 seconds.
- Home Har-428: the focused pool took 944.826 seconds (2.42x Riken); its first
  failure appeared after 16.001 seconds in `test-evaluation.sh`, but phase one
  returned after 969.38 seconds.
- Office Har-428: the focused pool took 1570.962 seconds (4.02x Riken), with
  interrupt-related failures in housekeeping and evaluation.
- `tests/focused-suites.tsv` contains estimates for only 23 of 99 suites.
- The runner admits all suites and waits for all futures after a failure.
- The evaluation self-test arms a 0.2-second alarm before proving its child has
  written `child.pid`; the observed missing-file failure is therefore a
  readiness race under load.

Har-428's Home checkpoint remains preserved at
`ce4e9257cdc322edc25913efdfa6bb4c3c5b6aa5` on its separate branch and primary
worktree.

## Execution plan and acceptance

1. Replace wall-clock readiness assumptions in the evaluation and
   housekeeping interruption fixtures with explicit observable handshakes.
2. Extend focused-runner contracts to retain every suite's duration and
   platform classification in a machine-readable receipt while preserving
   compact human output.
3. Stop new admission after the first failure, terminate already-running suite
   process groups gracefully, wait for their cleanup, and retain an explicit
   keep-going diagnostic mode.
4. Add a Darwin resource class so process-heavy suites have bounded concurrent
   admission while lightweight suites continue in parallel; base membership on
   measured/process-lifecycle behavior rather than host-name assumptions.
5. Add a fast Darwin preflight that covers portability and interrupt contracts,
   and prove it is documented as subordinate to the full final gate.
6. Run owning focused tests while iterating, then the repository-required full
   validation tier on the final integrated tree. Inspect cleanup and timing
   receipts independently.

Success means deterministic interrupt tests, materially earlier failure
return, complete timing evidence, bounded heavy-suite concurrency on Darwin,
a passing preflight, and an unchanged all-suite success contract. Do not claim
a successful-run speedup until matched Mac measurements exist.

## Working state

- Branch: `codex/har430-mac-test-speed`
- Worktree: `/Users/yokotar/harness-worktrees/har430-mac-test-speed`
- Base: protected `origin/main` at `2b4e8ae`
- Phase: `validating`
- Open decisions: none
- Exact next action: inspect the runner and the two interrupt fixtures, add
  focused regression tests for the frozen contracts, then implement step 1.

## Execution checkpoint — deterministic fixtures and runner

- Evaluation now arms its interrupt only after the child has durably written
  `child.pid` and explicitly signaled readiness. Its owning suite passes on
  Darwin in 6.73 seconds.
- Housekeeping now proves directory and administration absence before recording
  each synthetic interruption phase. Its fixture selects recovery receipts by
  exact worktree identity and retires its two long-lived liveness helpers after
  converting those worktrees to deterministic residue. Its owning suite passes
  on Darwin in 71.15 seconds.
- The focused runner now performs dynamic admission, stops admission on the
  first failure, terminates running suite process groups with a cleanup grace
  period, and retains `--keep-going`. Regression fixtures prove a cancelled
  suite's cleanup trap completes and a later suite never starts.
- A versioned JSON timing receipt records platform, job limits, resource class,
  exit state, and duration for every admitted suite. Darwin limits manifest
  `heavy` suites to two by default while light capacity remains available.
- `tests/darwin-preflight.sh` passed all six portability/interruption suites in
  72.209 runner seconds (73.93 wall seconds) on this Mac. The contract and README
  both preserve `tests/test-phase1.sh` as the final publication gate.
- Focused runner regressions, Python compilation, shell syntax, ShellCheck, and
  diff hygiene pass.

Phase remains `executing`. Exact next action: checkpoint these bytes, run the
complete phase-one gate with an external timing receipt, use its 99-suite Mac
measurements to replace missing admission estimates, then validate the final
integrated tree again.

The first complete-gate attempt at `d9b9de8` safely failed because Har-430's
board growth made the generated context benchmark stale. The owning generator
and benchmark check then passed. Fail-fast stopped the focused pool after
24/99 results at 58.842 seconds, gracefully cancelled seven, and left 75
unstarted; the enclosing phase one returned after 151.58 seconds. Retry is safe
after committing the refreshed benchmark. Exact next action: rerun the complete
gate with a fresh timing-receipt path.

The refreshed complete gate at `b30bd48` passed all 99 focused suites in
287.759 seconds and the enclosing phase one in 290.26 seconds; native MPI was
the sole declared skip. Its Darwin receipt contains all 99 results with no
cancellation or omission. The receipt then replaced every missing manifest
estimate with the ceiling of the observed Mac duration while retaining any
higher prior estimate: coverage is now 99/99 instead of 23/99. Sixteen
process-lifecycle suites retain the bounded `heavy` resource class. The
manifest contract and focused runner suite pass after this update.

Phase is now `validating`. Exact next action: commit the measured admission
estimates, run the final complete phase-one gate from that clean checkpoint,
compare its focused-pool duration with the matched 287.759-second Darwin run,
and inspect the final diff and residue before publication.
