# Har-388 — Consumer autonomy and efficiency hardening

## Status

Phase: `active`. The protected packet is at `ac6b3cf`; profile-bound Local
Codex routing and confirmations are at `ff47292` and `47793dd`. This stream owns
generic Harness communication, producer/consumer policy, routed validation,
and efficiency changes. Personal details remain in its private ledger.

## Protected results

- The Local route is profile-bound (`ff47292`), builds confirmations from a
  schema (`47793dd`), preserves validated reports before insertion (`f55a3d9`),
  and preflights target-only thread status (`b4ea821`). Exact duplicates are
  idempotent; changed bytes, ambiguous watchers, and malformed envelopes fail
  closed without pane or transcript reads.
- `7f1239e` protects client-compatible terminal handoffs and final accepted
  confirmations without fabricated continuation IDs. `46fff96` requires the
  producer Queue table to exactly match every nonterminal index disposition.
  Their clean R3 runs passed all 98 suites in 66.564 and 65.739 seconds,
  respectively, with only the declared native-MPI skip.
- Test-only message settling reduced its owning suite from 25.07 to about 3.06
  seconds without changing production delays. Longest-first admission reduced
  complete R3 from 83.76 seconds to a 64.72-second median; seven focused workers
  plus one integration reserve remains the measured schedule. Successful R3
  output is one aggregate line, with verbose timing and failure logs retained.
- Protected routing sends Markdown-only message-policy edits to about 3.4
  seconds of owning checks and declarative ledger edits to about 2.1 seconds of
  owning checks; executable, validator, safety, and unknown changes remain R3.
  Immutable-snapshot caching reduced the housekeeping suite from 35.45 to
  33.02 seconds while retaining mutation invalidation.
- The always-read Harness policy is protected at 760 words, down from 824.
  Linked-worktree publication uses explicit repository identity, and managed
  communication routes retain their frozen context ceilings. Detailed
  revisions, rejected candidates, and complete timings are in the ten hourly
  slices rather than this recovery index.
- Personal Per-023 through Per-030 are complete. Its cold-start policy fell
  from 835 to 676 words and producer entrypoint from 408 to 329 words. Producer
  reconciliation restored the explicit terminal-receipt no-delete invariant,
  passed 120 tests plus complete protected-root validation, and closed the
  terminal checkpoint at `bcf4a6f` with an idle selector and thread.
- A stale but protected-equivalent Swallow checkout was recoverably reconciled
  to `6c6bfed`; its strict ledger converges, selector is idle, and no task was
  replayed or recreated.
- `985cc0f` reduces the active board from 299 to 220 words, this recovery index
  from 904 to 500, and the Local consumer-message route from 525 to 498 words.
  Its exact tree passed all 98 R3 suites in 66.375 seconds.
- `eb7db61` reduces managed-Codex policy from 250 to 222 words and the largest
  message-delivery route from 644 to 589 selected words. `9ef3351` replaces 62
  redundant archive-threshold fixtures with a test-only fail-closed boundary;
  housekeeping's standalone median fell from 33.02 to 27.63 seconds.
- `03cd399` routes the rare installer exception out of ordinary guarded-delete
  context, reducing that route from 572 to 431 words while preserving every
  exception gate in the explicitly selected reference. The three successive
  clean integration runs passed 98 suites in 67.098, 64.230, and 63.894 seconds.
- Guarded archive-first cleanup left Harness and Personal with one clean
  `main` worktree and no non-main local or remote-tracking refs. `3b9d8e0`
  caches immutable stat syntax without caching metadata; its exact R3 passed
  in 62.500 seconds and the post-merge focused profile fell from 56.941 to
  54.955 seconds. `9103479` replaces repeated manifest-reader `awk` launches;
  its exact R3 passed in 58.984 seconds and the next focused profile fell to
  52.239 seconds. `2341171` removes checksum parser processes; its exact R3
  passed in 58.549 seconds after a 51.303-second focused median. Git and
  housekeeping policy compaction is pending complete integration.

## Active gate and durable evidence

Per-016 has a legacy terminal
receipt; Per-017 through Per-021 are gated at distinct owner choices, with
Per-017 through Per-021 preserving reversible partial plans without terminal
receipts. No owner value was inferred and no private source or connector was
accessed. Personal `main` is clean at `bcf4a6f`; its assignment, selector, and
metadata-only thread are idle, with no preserved report or follow-on request.
The frozen plan and hourly evidence are linked under `docs/plans/` and
`docs/audits/har388-consumer-autonomy/`. No pane or transcript content was
inspected, and no Personal payload is retained here.

## Exact next action

Run complete integration for the policy-compaction candidate, publish only if
green, then continue evidence-backed optimization through the material cutoff
and perform durable closeout without opening an owner-gated Personal task.
