# Har-388 — Consumer autonomy and efficiency hardening

## Status

Phase: `active`. The protected producer packet is at `ac6b3cf`, the
profile-bound Local Codex route is at `ff47292`, and its protected confirmation
envelope is at `47793dd`. This stream owns generic Harness communication,
producer/consumer policy, routed skill and validation efficiency changes.
Personal implementation and task details remain in Personal's private ledger
and independent publication stream.

## Protected results

- `ff47292` provides profile-bound Local controller-to-consumer messages;
  `47793dd` adds schema-built confirmations with distinct next report IDs.
  Personal's nine-test consumer protocol is protected at `ca85e2d`, and its
  terminal Per-023 producer reconciliation is at `8a1b608`.
- Test-only message settling reduced the owning suite from 25.07 to about 3.06
  seconds without changing production delays. Longest-first admission then
  reduced complete R3 from 83.76 seconds to a 64.72-second median.
- `2a15011` routes Markdown-only message-policy edits to three owning suites
  (about 3.4 seconds) while executable, client, and schema changes remain R3.
  `01cbc18` makes writer-boundary checks include untracked non-ignored paths.
- `c749a87` reduces a successful complete run from 98 per-suite lines to one
  aggregate line; verbose benchmarks and full failure attribution remain.
  Its clean R3 passed 98 suites in 64.751 seconds.
- Ready for publication: always-read policy is 760 words, down from 824, with
  every routed scenario 64 words smaller. All marker/context tests and clean R3
  pass; the context generator now rejects either stale board-size mention.
- Scheduling alternatives were rejected on evidence: six workers took 71.14
  seconds, eight took 64.57, and serialized gates exceeded 92 seconds. Keep
  seven focused workers with one CPU reserved for overlapping integration.

## Active gate and durable evidence

Personal `next-ready` selects Per-016. Its first synthetic-only plan is staged
as one consumer path and waits for the owner's transparent/free-event choice;
no confirmation may substitute for that preference. The frozen Harness plan is
`docs/plans/har388-consumer-autonomy.md`; detailed checkpoints and rejected
candidates are in `docs/audits/har388-consumer-autonomy/time-slices.md`. No pane
or transcript content has been inspected, and no Personal payload is retained
in Harness.

## Exact next action

Protected-publish the always-read compaction, then resume Personal Per-016 only
after the owner answers its first exact preference. Otherwise continue measured
context and validation-cost reduction without changing the frozen scope.
