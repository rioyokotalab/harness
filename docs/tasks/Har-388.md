# Har-388 — Consumer autonomy and efficiency hardening

## Status

Phase: `active`. The protected producer packet is at `ac6b3cf`. This consumer
stream owns generic Harness communication, producer/consumer policy, routed
skill and validation efficiency changes. Personal implementation and task
details remain in Personal's private ledger and independent publication stream.

## Baseline and LIFO stack

1. Resolved locally: `agent-message` now has profile-bound controller↔consumer
   Local Codex send and structured-report routes. Focused transport, policy,
   skill-routing, shell, ledger, and context checks pass.
2. Personal protected `main` is clean at `a4f144e`, while its selector is idle
   and six producer packets are gated. Their repository-local preconditions
   must be reconciled in Personal rather than guessed or overridden here.
3. Harness cold-start and phase-one execution recently showed avoidable NFS
   latency, generated-bytecode residue, and validation parallelism sensitivity;
   quantify before changing routing or tests.

## Durable evidence

- Frozen plan: `docs/plans/har388-consumer-autonomy.md`.
- Hourly evidence: `docs/audits/har388-consumer-autonomy/time-slices.md`.
- No pane or transcript content has been inspected. Personal source payloads
  and task details are excluded from Harness.
- The first documentation attempt exceeded the 250-word managed-policy and
  400-word delivery budgets. Moving Local Codex details to one routed 143-word
  reference and compacting common text reduced the largest remote-agent route
  from 1,241 to 750 words while retaining all gates.
- The first complete R3 run passed 94 focused suites and failed only because
  description compaction changed the catalog trigger phrase from singular
  `one bounded reply` to plural. Restoring that exact discovery phrase is a
  safe documentation-only retry; transport behavior and all owning tests pass.

## Exact next action

Run complete R3 on a clean checkpoint, protected-publish the route, send one
bounded queue diagnostic to Personal, and use its structured report as the
next LIFO input.
