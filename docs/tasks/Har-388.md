# Har-388 — Consumer autonomy and efficiency hardening

## Status

Phase: `active`. The protected packet is at `ac6b3cf`; profile-bound Local
Codex routing and confirmations are at `ff47292` and `47793dd`. This stream owns
generic Harness communication, producer/consumer policy, routed validation,
and efficiency changes. Personal details remain in its private ledger.

## Protected results

- `ff47292` provides profile-bound Local controller-to-consumer messages;
  `47793dd` adds schema-built confirmations with distinct next report IDs.
  Personal's nine-test consumer protocol is protected at `ca85e2d`, and its
  terminal Per-023 producer reconciliation is at `8a1b608`.
- Test-only message settling reduced the owning suite from 25.07 to about 3.06
  seconds without changing production delays. Longest-first admission then
  reduced complete R3 from 83.76 seconds to a 64.72-second median.
- `2a15011` routes Markdown-only message-policy edits to three owning suites
  (about 3.4 seconds) while executable, client, and schema changes remain R3.
  `01cbc18` makes writer-boundary checks include untracked non-ignored paths.
- `c749a87` reduces a successful complete run from 98 per-suite lines to one
  aggregate line; verbose benchmarks and full failure attribution remain.
  Its clean R3 passed 98 suites in 64.751 seconds.
- `66eadf8` protects the 760-word always-read policy, down from 824 words, and
  fixes the context generator to reject either stale board-size mention.
- `c4c00de` integrates declarative-ledger validation routing, immutable
  housekeeping evidence caching, reversible owner-choice parking, and narrow
  root-policy markers. Its clean complete R3 passed 98 suites in 72.115
  seconds with only the declared native-MPI skip.
- Scheduling alternatives were rejected on evidence: six workers took 71.14
  seconds, eight took 64.57, and serialized gates exceeded 92 seconds. Keep
  seven focused workers with one CPU reserved for overlapping integration.
- Protected ledger routing replaces an accidental complete R3 for ordinary
  producer queue, packet, and receipt edits with two owning suites that pass in
  2.07 seconds. Runtime and validator changes still require R3.
- Protected immutable-snapshot caching retains bundle identity, digest, and
  mutation checks while reducing the housekeeping suite from 35.45 to 33.02
  seconds including its new cache-invalidation regression.
- Personal producer packets at `3e930d2` and `2eee0b2` freeze safe-work-first,
  single-checkpoint parking and a 900-word active-record ceiling. The queue can
  continue those generic protocol tasks after Per-018 without owner input.
- `4090cfb` protects the explicit-repository linked-worktree merge rule and the
  current evidence after a clean 98-suite R3 in 66.993 seconds. The same
  procedure merged Personal PR #55 without a false local-checkout failure.
- `81b892c` compacts the managed-agent communication routes and adds one exact,
  bounded retry for the accepted transient development-evaluation failure. The
  frozen evaluator remains byte-identical; its retest and a final 98-suite R3
  passed, the latter in 67.812 seconds with only the declared native-MPI skip.
- Personal's safe-work-first and active-record compaction tasks are protected
  and reconciled at `56f0a5b` and `8d5f28c`. The two over-budget recovery
  records fell from 970/1,105 words to 441/460 words while preserving their
  distinct terminal and reversible lifecycles.
- Personal's synthetic Day One task protected a zero-I/O linear aggregator at
  `a15166f`; producer reconciliation at `96cddb8` gated only its first owner
  choice and advanced the selector to proportional-validation task Per-027.
- `f55a3d9` preserves each validated Local consumer report in private mode-0600
  state before terminal insertion. Exact duplicates are idempotent, changed
  bytes fail closed, and the controller can recover a missing or partial report
  without another consumer prompt. Its clean R3 passed 98 suites in 63.932
  seconds with only the declared native-MPI skip.
- Personal's proportional validation and exact local-skill discovery are
  protected at `75482f7`; terminal producer reconciliation is at `093f3cb`.
  Its 0.119/2.138/3.544-second route baselines now select checks by changed
  scope while publication still requires one complete validation.
- A measured cold-start gap scheduled a bounded self-describing planner task at
  `5acc3ab`, behind the two existing source-specific tasks. Their source-free
  partial records and queue-local gates are protected through `505ee22`; the
  real asynchronous producer advance reconciled conflict-free.
- `b4ea821` adds a target-only active-turn preflight before Local Codex
  follow-ups. It resolves one live watcher, fails on ambiguity, retains the
  525-word route ceiling, and passed a clean 98-suite R3 in 78.070 seconds.

## Active gate and durable evidence

Personal Per-023 through Per-027 are complete. Per-016 has a legacy terminal
receipt; Per-017 through Per-021 are gated at distinct owner choices, with
Per-017 through Per-021 preserving reversible partial plans without terminal
receipts. Producer `main` is clean at `505ee22`, and the remaining eligible
task has independently passed its bounded descriptor and confirmation-
continuation checkpoint without source or connector access; only terminal
publication and producer reconciliation remain.
The frozen plan and hourly evidence are linked under `docs/plans/` and
`docs/audits/har388-consumer-autonomy/`. No pane or transcript content was
inspected, and no Personal payload is retained here.

## Exact next action

Review and reconcile the self-describing task's terminal publication, then
continue measuring generic communication and validation overhead while
preserving every owner gate. Protected-publish only evidence-backed
improvements and the bounded hourly record.
