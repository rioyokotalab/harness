# Har-388 — Consumer autonomy and efficiency hardening

## Status

Phase: `active`. The protected producer packet is at `ac6b3cf`, the
profile-bound Local Codex route is at `ff47292`, and its protected confirmation
envelope is at `47793dd`. This stream owns generic Harness communication,
producer/consumer policy, routed skill and validation efficiency changes.
Personal implementation and task details remain in Personal's private ledger
and independent publication stream.

## Baseline and LIFO stack

1. Resolved and protected: `agent-message` has profile-bound
   controller↔consumer Local Codex send and structured-report routes. Focused
   transport, policy, skill-routing, shell, ledger, context, and complete R3
   checks pass.
2. Resolved in Personal: the first live report proved producer-side
   over-gating. Personal protected publication released safe PIE/preparation
   phases while retaining exact live-source and external-write gates. The
   first handshake task is advancing one confirmed stage at a time.
3. Active LIFO finding: the changed remote-agent suite spent about 22 seconds
   of its 25.07-second wall time in production-only paste/submit settling.
   A test-only fail-closed zero-settle route preserves production delays and
   passes the same suite in a 3.06-second median candidate.
4. Resolved and integration-validated: the first R3 attempt found that the
   productive-idle fixture emitted Python bytecode into the checkout while
   focused suites ran in parallel. Later clean-tree tmux and terminfo fixtures
   correctly failed. The exact residue was guarded-deleted; the owning wrapper
   now disables bytecode and asserts that none is created. An unchanged-input
   R3 retry passed every registered suite in 83.76 seconds.
5. Resolved and protected: the same R3 output showed stale
   longest-first admission estimates. Refreshing only measured suite weights
   produced three clean complete runs at 64.43, 64.99, and 64.72 seconds
   (64.72-second median), 22.7% below the immediate 83.76-second baseline with
   effectively unchanged user and system CPU totals.
6. Resolved and integration-validated: live checkpoints exposed that a
   confirmation and its next report need distinct request IDs. The generic
   route now constructs a two-line confirmation envelope, permits a next ID
   only for accepted or changes-required decisions, distinguishes decision
   from transport submission, and keeps authority unchanged. Complete R3
   passed on the exact behavior checkpoint in 65.22 seconds.
7. Active Harness efficiency finding: routed message-policy text still selected
   complete R3 despite changing no executable or schema. A split candidate
   selects only the owning transport, context-budget, and safety-gate suites;
   they pass in about 3.4 seconds while scripts, client configuration, and the
   response schema remain R3. The impact-map change itself still requires one
   final complete R3 before protected publication.
8. Resolved Personal handshake: independent review accepted the corrected
   nine-test confirmation parser and exact eight-path boundary. Personal PR
   #43 protected-published it at `ca85e2d`; its terminal receipt was reconciled
   by producer PR #44 at `8a1b608`. Exact-root validation is green, the selector
   now returns Per-016, and its first report is bound to a fresh request ID.

## Durable evidence

- Frozen plan: `docs/plans/har388-consumer-autonomy.md`.
- Hourly evidence: `docs/audits/har388-consumer-autonomy/time-slices.md`.
- No pane or transcript content has been inspected. Personal source payloads
  and task details are excluded from Harness.
- The first documentation attempt exceeded the 250-word managed-policy and
  400-word delivery budgets. Moving Local Codex details to one routed 143-word
  reference and compacting common text reduced the largest remote-agent route
  from 1,241 to 750 words while retaining all gates.
- The first complete R3 run passed 94 focused suites and failed only because
  description compaction changed the catalog trigger phrase from singular
  `one bounded reply` to plural. Restoring that exact discovery phrase is a
  safe documentation-only retry; transport behavior and all owning tests pass.
- The first Personal stage report had the correct schema but its diff check
  inspected zero paths because the new record was untracked. Producer
  confirmation was withheld; the consumer corrected the evidence to one
  staged path and added untracked-path coverage to its next stage.

## Exact next action

Complete and protected-publish the validation-routing checkpoint, then review
Personal's first selector-driven Per-016 report without crossing repository
writer boundaries or answering an unresolved owner preference.
