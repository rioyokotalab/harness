# Har-378 — Repository-local producer/consumer task protocol

## Status

Phase: `interviewing`.

The owner requested a producer/consumer model spanning Harness, Personal,
Students, Swallow, and Website. `harness:cowork.0` Codex is the sole task
producer. Repository-assigned Codex or Claude sessions consume producer tasks,
resolve nested issues immediately in repository-local LIFO order, and never
invent durable goals. The full public-safe architecture and decision register
are in `docs/plans/har378-producer-consumer-ledgers.md`.

## Privacy and authority

This Harness record contains protocol facts only. Never copy task content,
findings, execution evidence, or queue state from a private repository into
Harness or Website. Planning may update only this generic Harness ledger.
Target repositories, tmux/session topology, schedules, and external state stay
unchanged until all material decisions are frozen and the owner says `go`.

## Current evidence

All five remotes were fetched read-only and have no open pull requests.
Harness, Personal, and Website are clean on current `main`. Students and
Swallow remain on the preserved live legacy branches governed by Har-371;
their current `origin/main` boards use `Stu-*` and `Swa-*`. No branch,
worktree, process, task, or queue was changed during discovery.

## Exact next action

Decision D-001 selected automatic execution within packet scope while
preserving every closer authority gate. D-002 selected instruction, validator,
and role-aware command enforcement without a new credential. Number all
remaining interview choices. D-003 selected producer priority followed by the
oldest ready packet, with LIFO limited to nested issues. Ask D-004 from the
plan. After the last decision, set `ready-for-go` and wait for an explicit `go`
before creating any repository-local producer ledger or changing cold-start/
runtime behavior.

Planning validation initially rejected `interviewing` as an index state; that
failure was local and retry-safe with no external mutation. Keep the task
record phase as `interviewing`, use the index's supported `active` state, and
include `TODO.md` among the active index sources. The first retry exposed that
missing board pointer and likewise changed no external state. Rerun the ledger
gate before checkpointing.
