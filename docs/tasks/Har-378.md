# Har-378 — Repository-local producer/consumer task protocol

## Status

Phase: `complete` on the protected-publication candidate.

The owner requested a producer/consumer model spanning Harness, Personal,
Students, Swallow, and Website. `harness:cowork.0` Codex is the sole task
producer. Repository-assigned Codex or Claude sessions consume producer tasks,
resolve nested issues immediately in repository-local LIFO order, and never
invent durable goals. The full public-safe architecture and decision register
are in `docs/plans/har378-producer-consumer-ledgers.md`.

## Privacy and authority

This Harness record contains protocol facts only. Never copy task content,
findings, execution evidence, or queue state from a private repository into
Harness or Website. Planning may update only this generic Harness ledger.
Target repositories, tmux/session topology, schedules, and external state stay
unchanged until all material decisions are frozen and the owner says `go`.

## Current evidence

All five remotes were fetched read-only and have no open pull requests.
Harness, Personal, and Website are clean on current `main`. Students and
Swallow remain on the preserved live legacy branches governed by Har-371;
their current `origin/main` boards use `Stu-*` and `Swa-*`. No branch,
worktree, process, task, or queue was changed during discovery.

## Completed result

The frozen five-decision design is implemented independently in all five
repositories. Each now has a producer-only queue, assignment metadata outside
immutable task packets, consumer-owned execution state, bounded receipts,
owner-started nightly optimization rules, and repository-native enforcement.
Consumers cannot modify producer paths or allocate unproduced task records;
malformed, oversized, cross-root, assignment-coupled, sensitive-field, and
public privacy-canary packets fail closed. A synthetic producer/consumer merge
proves ordinary disjoint changes do not conflict.

Protected publication completed for Personal PR #22 at `1825194`, Students PR
#566 at `2fddc79`, Swallow PR #145 at `ecf69ba`, and Website PR #44 at
`5f295a3`. Website's required offline check passed. Personal's canonical-root
validation passed 24 application tests plus nine producer tests; Swallow's full
static suite and Website's full offline suite passed. Students' unchanged broad
suite had two isolated findings; its updated 82%-reduction cold-start test and
the worktree-mode-dependent recovery test both passed after correction, as did
Ruff, secret, CLI, and all new producer tests.

Harness R3 initially found three exact contract updates: always-read policy
grew past 825 words, the generated context benchmark was stale, and takeover
tests still named the old source of truth. The compacted policy is 813 words;
the refreshed benchmark reports a 97.2% median total reduction and 46.9%
median non-ledger reduction; all three failed suites now pass. The final
complete R3 suite passed all focused suites on the closeout candidate.

Live Students and Swallow checkouts, processes, saved roots, and untracked work
remain untouched behind Har-371's idle/cold boundary. Personal activated by a
clean canonical fast-forward; Website has no dedicated consumer. No deployment,
message, credential, scheduler, compute, or hosted-setting write occurred.

## Exact next action

Publish this branch through protected Git, verify merged main, then
guarded-clean only Har-378 worktrees, temporary reference clones, and merged
local task branches. Preserve the live Students and Swallow checkouts and
continue to leave their runtime activation to Har-371.
