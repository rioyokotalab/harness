task: Har-405
status: complete
updated: 2026-08-07
---
# Har-405 — Make complete gates worktree-compatible

The Stu-080 temporary worktree failed two pre-existing checks because inherited
group-write executable modes differed from canonical-root expectations and a
nested `uv --no-sync` subprocess could not resolve the project package. The
Swa-051 temporary worktree similarly failed executable metadata admission.
Exact canonical-root validation passed.

Students protected PR 625 replaced source-worktree `uv --no-sync` calls with
frozen isolated environments, removed the canonical installed-environment
override from unattended validation, moved source launcher executability to
Git mode 100755, and updated the deterministic figure command and its frozen
boundary evidence. Its final gate passed 1,185 tests and 81 subtests and left
the repository-local `.venv` absent. Protected main is clean and idle at
`8b92c3fcb` with candidate tree `921007dba2`.

Swallow protected PR 173 preserved strict installed-runtime contracts while
testing exact source bytes through bounded private strict-mode projections and
using Git metadata for source executability. It also repaired the stale SW-034
policy assertion discovered LIFO. The complete render-only static gate passed;
protected main is clean and idle at `466804bb8` with candidate tree
`263e492ee9`.

The final generated caches and private test logs were removed. Harness
housekeeping archived and removed both exact isolated worktrees and local task
branches; the merged remote branches are absent. No live runtime, scheduler,
compute, credential, or private source was accessed. Complete.
