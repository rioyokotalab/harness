# T-196 — Backup lifecycle phase 2

## Status

Read-only phase-2 monitoring is active; all eight chains remain 2/8 until
scheduler success, the exact new snapshot, successor continuity, and private
state are jointly verified. Query only these recorded successors:

| Target | Exact successor | Earliest local time |
| --- | --- | --- |
| local | `94950` | 00:30 JST |
| ab | `2064918.pbs1` | 01:00 JST |
| ab2 | `2064919.pbs1` | 01:30 JST |
| ri | `10386` | 02:00 JST |
| rc | `260847` | 02:30 JST |
| t4 | `8270230` | 03:00 JST |
| abq | `176525.qjcm` | 03:30 JST |
| al | `4275926` | 01:00 Europe/Zurich |

Delay is healthy. Preserve pending successors and never seed or replace a
healthy chain. RI's old accounting ID `7242` may be retried read-only; an
unavailable query is unknown and must not touch successor `10386`.

## 2026-08-02 scheduler checkpoint

Exact-ID native readback found terminal success for Local `94950`, AB
`2064918.pbs1`, AB2 `2064919.pbs1`, RI `10386`, RC `260847`, and T4 `8270230`;
all matched their recorded deterministic names and owners. RI required the
declared process-local `SLURM_CONF_SERVER` value from its host profile after a
plain `sacct` query failed DNS discovery; the failed query was treated as
unknown and no job was retried or changed.

ABQ `176525.qjcm` matched owner/name and terminal state `F`, but its documented
summary/history surfaces did not expose an exit status. It remains
terminal-but-unverified; `F` is not inferred to mean success. The official
ABCI-Q status page reported System H in service before contact. AL `4275926`
remains unqueried until 01:00 Europe/Zurich (08:00 JST). No Restic command,
snapshot query, successor query, scheduler write, credential access, or chain
count change occurred.

Acceptance requires eight successful weekly chains, two verified restores per
node, and a current independent generation. No forget, prune, recurring
restore, or replica automation is authorized. Evidence:
`docs/backup-lifecycle-phase2.md`, `docs/home-backup.md`, and
`docs/audits/restic-first-weekly-2026-07-19.md`.
