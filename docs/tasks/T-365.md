# T-365 — Direct Local Codex-to-Claude messages

## Scope

Extend `remote-agent-communication` with one owner-requested, content-blind
route from the existing Local Harness Codex pane to the existing Local Harness
Claude pane. Preserve the merged Claude-to-Codex route and all no-loop,
attribution, size, locking, exact-target, and ambiguous-retry boundaries.

## Frozen decisions

- Add a narrow `send-local-claude --source local` command. Require
  `[Agent: Local Codex]`; do not expose a general cross-client target selector.
- Select exactly one live `projects:claude.0` pane rooted at `~/harness`, with
  pane-local Claude role `harness` and exactly one Claude process on its TTY.
- Pass the message only through a private tmux buffer, settle, and submit a
  separate `C-m`; never read or capture pane content.
- Report only `status=submitted`. Do not add a reliable response route or an
  autonomous conversation loop. Claude may make one separate, owner-expected
  reply through the existing Claude-to-Codex route.
- Keep the remote-Mac Claude helper separate and route Local Claude delivery
  to this skill.

## State

- Baseline: clean `origin/main` at
  `8a65f1632a16c3f6056b2053e0f4277db9793c8a` after PR #537.
- Branch/worktree: `codex/t365-local-codex-to-claude` at
  `/tmp/harness-t365-local-codex-to-claude`.
- Implemented `send-local-claude --source local`, exact Local Claude pane and
  process selection, honest cross-client policy, routed guidance, and
  fail-closed synthetic coverage. The prior Claude-to-Codex SSH sender path now
  also has an explicit `--client claude` integration test.
- No live message has been sent and no pane content has been inspected.

## Validation and handoff

Passed: both skill validators, Python syntax parsing, focused remote-agent
transport tests, skill context/catalog budgets, policy routing/context gates,
projects pane layout, metadata-only live Local-Claude target preflight, and
`harness validate --base origin/main` at R3/phase one. Native MPI was skipped
by its declared environment contract. `git diff --check` passes.

Hosted PR #538 run `30690546196` failed only because adding the active task and
25 routed words changed two exact generated fixtures: task index routing and
the T-351 context table. No transport test failed. Added the active T-365 index
row and refreshed the benchmark's measured counts; both previously failing
focused suites and the transport suite now pass locally. The failed run is
safe to supersede with the changed commit; do not retry that run.

Next: commit, fetch/reconcile, push one protected PR, merge after hosted checks,
run final integrated validation, then plan the guarded fleet rollout. Do not
send a live message merely to test submission.
