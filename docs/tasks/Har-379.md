# Har-379 — Aist-driven T-293 self-healing validation

## Status

Phase: `executing` from 2026-08-02 13:47:47 JST through 20:47:47 JST.
Material changes stop at 19:47:47 JST; the final hour is reserved for
integrated validation, durable evidence, and handoff.

## Objective and authority

Use Aist Codex as the sole target driver and Claude Code 2.1.220 as a
read-only co-pilot during one seven-hour validation window over the already
deployed T-293 connection self-healing system. Preserve T-293's immutable
historical audit: Local was its original driver and Cowork was then out of
scope. This task does not rewrite that history or replay the completed fleet
rollout. It revalidates the requested method against current protected main
and may change only a defect demonstrated during this window.

Authority covers read-only fleet and repository inspection, value-free
supervisor/watchdog status, fresh route probes, one bounded Aist single-route
pilot after its sibling is independently ready, repository implementation and
protected publication for a demonstrated defect, and guarded synchronization
of clean eligible checkouts. It excludes credentials, SSH configuration,
authorization files, package changes, broad process cleanup, simultaneous
route loss, another host's failure drill, and replay of a historical helper or
transaction.

## Frozen benchmark and evidence cadence

Baseline is clean protected Harness `main` at
`bbc2a4b92e580bb20ee44facda5816c3fdc1a7e8`. The authoritative historical
contract is `docs/audits/t293-connection-self-healing-2026-07-22.md`.

Acceptance requires:

1. Aist stays clean/aligned on protected main and no credential bytes are
   inspected.
2. Both Aist aliases report loaded/running, exclusive managed ownership,
   unattended authentication ready, and a healthy watchdog.
3. Fresh Local-vantage probes prove both inbound Aist routes before and after
   one bounded single-route pilot. The sibling must remain ready throughout;
   no dual-loss drill is authorized.
4. The pilot changes the selected supervisor generation, restores its fresh
   route within the recorded bound, and leaves both aliases exclusively
   managed.
5. Every hourly slice records repository identity, value-free connection
   state, unexpected findings, Claude evidence when scheduled, and the next
   safe action. Exactly seven slices cover the seven requested hours.
6. Claude independently critiques the frozen plan before the pilot, reviews
   midpoint evidence, and audits final acceptance. Claude receives no tools,
   credentials, private logs, pane content, or authority to mutate targets.
7. Any code change passes its owning focused tests, deterministic validation
   tier, protected publication, eligible clean-checkout synchronization, and
   post-rollout health. No demonstrated defect means no speculative change.
8. The final durable and owner summaries each contain exactly seven
   evidence-backed hourly slices and at least 350 words.

## Pre-pilot protocol frozen after independent review

Claude review `aist-20260802-01` returned a conditional-go verdict and is
retained at
`docs/audits/har379-t293-aist-validation/claude-plan-review.md`. Before the
single authorized kick, the following definitions resolve its findings:

- Recovery is bounded to 30 wall-clock seconds from dispatch of the local
  supervisor command. A repository-native Local `connection-monitor --once`
  snapshot runs before dispatch and repeatedly after dispatch, with a
  one-second delay after each completed snapshot. Every snapshot evaluates
  both `aist` and `aist2`; any observed sibling failure ends mutation
  immediately. The 30-second bound is deliberately above the historical
  15-second healthy single-route maximum and treats the historical greater-
  than-30-second hang as failure.
- The control channel is Aist's already authenticated ordinary `login`
  ControlMaster to Local. It is a distinct current-user-owned socket and SSH
  process from both launchd-managed reverse-tunnel processes. Local runs fresh
  `ControlMaster=no`, `ControlPath=none` route probes through the repository
  monitor. The primary reverse route is never the control transport.
- The sole mutation command is
  `harness macos-tunnel-supervisor --host aist --kick tunnel`, executed
  locally on Aist. Exit zero plus the exact value-free line
  `MACOS_SSH_SUPERVISOR alias=tunnel action=kick status=running` is an
  acknowledged success. A nonzero exit with captured value-free diagnostics
  is an acknowledged failure. Missing, truncated, or transport-mediated
  output is ambiguous and permanently forbids retry during this window.
- Claude used repository-scoped file reads and wrote only its named review
  artifact. This deviates from the plan's literal "no tools" shorthand while
  preserving its actual security boundary: no target tools, credentials,
  private logs, pane or transcript reads, or target mutation. Final acceptance
  must report this deviation rather than silently treating the wording as met.
- The midpoint review occurs immediately after slice 3 closes at 16:47:47
  JST. Slice 7 is validation and handoff only.

## Stop conditions

Stop target mutation immediately on dirty or diverged protected state,
ambiguous command acknowledgement, unavailable sibling, dual-route loss,
unexpected ownership, failed unattended authentication, credential boundary,
or any unrecorded maintenance condition. Preserve current managed services and
continue only read-only evidence that does not depend on the failed gate.
Never retry an ambiguous pilot or Claude submission. At 19:47:47 JST stop all
material work regardless of progress.

## Completion audit

| Requirement | Current proof | State |
| --- | --- | --- |
| Protected-main alignment and credential boundary | Final sample found clean/aligned `45e1119`; no credential bytes were accessed in the full window | proved |
| Supervisor, ownership, auth, watchdog | All seven samples retained exclusive managed ownership, ready auth, and healthy watchdog results | proved |
| Fresh external route probes and sibling preservation | Local snapshots passed before, during, and after the pilot | proved |
| Generation-changing bounded recovery | Exact acknowledgement, changed generation, three-second recovery inside 30 seconds | proved |
| Seven hourly slices | Exactly seven consecutive boundaries completed | proved |
| Three Claude reviews | Plan, midpoint, and final artifacts retained; every finding independently reconciled | proved |
| Defect/change/publication decision | No self-healing defect; the in-scope execution incident remained bounded; evidence-only publication uses protected review | proved |
| Two seven-slice summaries of at least 350 words | Exactly seven entries; durable summary 611 words and owner summary 605 words | proved |

## Current evidence

- Historical T-293 implementation, pilot, independent four-Mac rollout,
  rollback/reapply, protected CI, and durable handoff are complete at ancestor
  `97162ef3c554a80a29c63a4b83d39d292ad4fb14`.
- Current Aist status reports both aliases loaded/running with
  `managed=1 external=0`, both unattended-auth checks ready, and the watchdog
  installed/loaded with its latest classification healthy.
- At task start the repository was clean/aligned at the frozen baseline. It
  later advanced cleanly to the protected revision recorded below. The managed
  local tmux session is detached; no pane or transcript content was read.
- `codex-claude-cowork` was retired by T-368. This task uses native Claude
  Code as the explicitly selected read-only co-pilot and retains no live
  dependency on the retired skill.
- The 14:01:55–14:02:02 JST pre-pilot gate passed: the current-user ordinary
  `login` master is distinct from the isolated launchd-managed primary; both
  aliases have exclusive managed ownership and ready unattended auth; the
  watchdog's latest classification is healthy; and Local's fresh repository-
  native snapshot reports both inbound routes healthy.
- The sole primary kick at 14:03:56 JST was exactly acknowledged and was not
  retried. The managed primary generation changed; two repository-native
  Local snapshots kept the sibling ready throughout; and both routes were
  healthy within three seconds. Post-pilot ownership remained exclusively
  managed, unattended authentication remained ready, and the watchdog's
  14:04:00 JST run classified the pair healthy. Local-vantage `fleet-health`
  then passed every declared Linux and Mac target. Aist-vantage fleet checks
  failed for non-local routes as expected from that vantage and are not used
  as external route evidence.
- A read-only observer was armed at 14:07:03 JST for the seven exact slice
  boundaries. It records protected-main and task-worktree identity, local
  supervisor/auth/watchdog state, and repository-native Local-vantage route
  snapshots. It has no recovery flag and cannot mutate target services.
- Protected main advanced cleanly to `572a62ae76f7ce1e63e48143dbdffcd0cdb5f5ec`
  and independently completed Har-377. This isolated active task was therefore
  renumbered to the next free ID, Har-378, before rebase or publication. The
  old branch name remains temporarily frozen because the already-running
  read-only observer validates it; it will be renamed only after the observer
  completes. No live target state changed during reconciliation.
- The task tree was then squash-reconciled onto protected
  `572a62ae76f7ce1e63e48143dbdffcd0cdb5f5ec` as clean checkpoint `d35bf9d`.
  The pre-reconciliation ref remains preserved; no force update or target
  mutation occurred.
- Native `claude --version` independently reported Claude Code 2.1.220 at
  14:21:59 JST. The primary and isolated task worktrees were both clean.
- Slice 1 closed healthy at 14:47:55 JST: both worktrees were clean at their
  recorded revisions, both aliases retained exclusive managed ownership and
  ready auth, the watchdog was healthy, and fresh Local-vantage probes passed
  every declared route pair.
- After a fresh authenticated fetch confirmed unchanged protected main and an
  absent destination branch, clean slice-1 checkpoint `477514e` was published
  to remote branch `codex/har378-t293-aist-cowork`. No pull request, merge, or
  rollout has occurred.
- Slice 2 closed healthy at 15:47:55 JST: protected and task repositories were
  clean at their recorded revisions, both aliases retained exclusive managed
  ownership and ready auth, the watchdog was healthy without recovery, and
  fresh Local-vantage probes passed every declared route pair.
- Protected `708334a` introduced ready producer packet Har-379 for this exact
  owner-authorized validation. Aist claimed only that assigned ID and changed
  no producer-owned path. The stale remote Har-378 branch remains preserved as
  required until the observer completes.
- The slice-2 checkpoint push fetched protected `2568e1f`, but a missing
  fail-fast guard let execution continue after the expected-old-main comparison
  failed. The only external change was advancing the already-created stale
  Har-378 branch to `bf5236a`; no PR, merge, rollout, or target mutation ran.
  That branch is preserved and will not be reused for final publication.
- Producer request `har378-collision-aist-20260802-01` was submitted once at
  15:50:41 JST and its private input was exact-unlinked; it was not retried.
  Protected `708334a` then supplied the authoritative Har-379 packet. Aist
  fast-forwarded clean main, read the packet, and squash-reconciled only its
  consumer-owned six paths as clean checkpoint `00b9f42`.
- The pre-reconciliation task-ledger test failed because the old branch still
  carried the retired consumer-owned next-ID line while the new producer
  expected Har-380. On the protected producer base, producer validation,
  consumer-diff validation, task-ledger routing, agent-policy routing, and
  diff checks all pass. No unchanged retry of the failed old-base test ran.
- A fail-fast authenticated fetch then confirmed unchanged protected
  `708334a` and an absent Har-379 remote branch. Clean checkpoint `a1e5301` was
  published to `codex/har379-t293-aist-cowork`; the stale Har-378 branch remains
  preserved and no protected merge or rollout has occurred.
- The Local controller's one required response for
  `har378-collision-aist-20260802-01` reported `status=complete`, assigned
  Har-379, and confirmed the stale Har-378 checkpoint remains preserved. No
  reply loop was created.
- Aist protected main then advanced cleanly through the canonical Mac
  agent-message fixes to `69ba0a6`. The isolated task branch merged that
  protected history without conflict as `bc71c4d`; producer validation,
  consumer-diff enforcement, and task-ledger routing still pass. No target
  service or observation cadence changed.
- Slice 3 closed healthy at 16:47:54 JST: clean protected and task revisions,
  exclusive managed ownership, ready auth, a healthy no-recovery watchdog
  result, and fresh Local-vantage success for every declared route pair.
- Claude midpoint request `aist-20260802-midpoint-01` was submitted exactly
  once at 16:49 JST through the installed content-blind transport. Its private
  input was exact-unlinked and the named artifact appeared at 16:53 JST, so
  no retry occurred. The retained review returned `CONTINUE-WITH-CONDITIONS`:
  it accepted the pilot and producer reconciliation, found no T-293
  self-healing defect, and required deliberate final treatment of the
  baseline wording, file-tooling deviation, narrative evidence provenance,
  observer dependency, stale-ref authority, and the slice-2 push incident.
- The driver independently classified that push lapse as a nested, in-scope
  task-execution incident rather than a repository-native or out-of-scope
  product defect. The missing guard existed only in one ad hoc shell command;
  no reusable Harness byte can reproduce or repair it. Its bounded external
  effect is disclosed, the affected stale ref is preserved, and every later
  publication command uses `set -euo pipefail` plus an explicit fetched-base
  comparison before any push. The producer writer contract therefore selects
  LIFO handling inside Har-379, not a blocked receipt: Har-379 remains able to
  complete, and no new goal or speculative implementation is authorized.
  Deleting the stale remote or local preservation refs remains a separate
  post-observer external-write authority and is only a handoff item.
- Slice 4 closed healthy at 17:47:54 JST: clean protected and task revisions,
  exclusive managed ownership, ready auth, a healthy no-recovery watchdog
  result, and fresh Local-vantage success for every declared route pair.
- Slice 5 closed healthy at 18:48:16 JST: clean protected and task revisions,
  exclusive managed ownership, ready auth, a healthy no-recovery watchdog
  result, and fresh Local-vantage success for every declared route pair.
- Protected main advanced to `45e1119` for completed Har-380. The clean primary
  worktree fast-forwarded, and the isolated branch preserved both Har-379 and
  Har-380 task-index rows while merging that history. Producer validation,
  consumer-boundary enforcement, task-ledger routing, policy routing, and diff
  checks passed before the correct Har-379 remote branch advanced; the stale
  Har-378 branch remained untouched.
- Slice 6 closed healthy at 19:47:54 JST: clean protected and task revisions,
  exclusive managed ownership, ready auth, a healthy no-recovery watchdog
  result, and fresh Local-vantage success for every declared route pair. The
  material cutoff is now in force, so target and implementation mutation are
  permanently closed for this window.
- Claude final-review request `aist-20260802-final-01` was sent exactly once
  after the material cutoff through the installed content-blind transport.
  The transport returned no value-free acknowledgement, so the submission was
  classified ambiguous and permanently not retried. Its private input was
  exact-unlinked. The named artifact subsequently appeared as a regular,
  current-user-owned, single-link file and was read completely and reconciled
  independently, which proves review completion without treating transport
  submission as completion. The retained review returned
  `ACCEPT-PENDING-CLOSURE`: requirements auditable through slice 6 are met,
  while slice 7, both seven-entry summaries, integrated validation, and
  protected publication correctly remain open. Its findings preserve the
  value-free narrative provenance, restate the protected-main and literal
  "no tools" wording deviations, distinguish the healthy T-293 system from
  the bounded task-execution incident, and require every stale ref to remain
  untouched through observer completion.
- Slice 7 closed healthy at 20:48:01 JST. The exact 20:47:47 observer sample
  found clean protected main at `45e1119` and the clean task checkpoint at
  `5d2f8f0`; both aliases remained loaded/running with
  `managed=1 external=0`, both unattended-auth checks were ready, and the
  watchdog's 20:47:28 run was healthy with no recovery attempt. Fresh
  Local-vantage probes found every declared pair healthy. The observer then
  exited cleanly, completing exactly seven consecutive slices without any
  post-cutoff target or implementation mutation.
- With the observer dependency released, the active local branch was renamed
  to `codex/har379-t293-aist-cowork`. The stale remote
  `codex/har378-t293-aist-cowork` and both local preservation refs remain
  intact. Their deletion is not required for correctness and remains a
  separate external-write authority; no cleanup is part of this closeout.
- The first requested `/tmp` worktree path and the logical macOS `TMPDIR` path
  both failed closed before Git mutation because they were outside the
  resolved scratch boundary. The physical `TMPDIR` retry created the isolated
  task worktree safely.

## Owner summary

1. **Hour 1.** Aist began from clean protected and isolated task revisions.
   Both reverse routes were managed, running, authentication-ready, and healthy
   from Local, with the watchdog healthy as well. The one authorized pilot
   restarted only the primary managed generation; it returned the exact
   acknowledgement, was never retried, and restored full dual-route health in
   three seconds while the sibling stayed available. The initial task-number
   collision was resolved only in repository bookkeeping, with no tunnel or
   other target change. Claude's pre-pilot critique was retained and every
   safety condition was checked independently before the kick.

2. **Hour 2.** Both Aist routes and all other declared route pairs remained
   healthy, with exclusive managed ownership, ready unattended authentication,
   and no watchdog recovery. During repository checkpointing, protected main
   advanced unexpectedly and an ad hoc push command lacked fail-fast behavior.
   Its only effect was advancing the already-stale Har-378 branch; it created
   no PR or merge and touched no target. We preserved that ref, disclosed the
   incident, and used explicit fail-fast base comparisons for every later
   publication. It is an execution incident, not evidence of a T-293 product
   defect.

3. **Hour 3.** The producer assigned the collision-free Har-379 packet, and
   the task reconciled only consumer-owned files onto clean protected main.
   Both Aist routes stayed healthy and every Local-vantage fleet probe passed.
   Claude's one-shot midpoint review accepted the pilot and found no
   self-healing failure. It asked for clearer treatment of narrative evidence,
   protected-main advances, the original "no tools" wording, observer and ref
   dependencies, and the push incident. Each point was independently checked
   and recorded; none justified a speculative code or runtime change.

4. **Hour 4.** The observer again found clean repository state, both services
   loaded and running with `managed=1 external=0`, unattended auth ready, the
   watchdog healthy with no attempts, and all declared routes healthy from
   Local. There were no unexpected findings. Runtime evidence is deliberately
   the retained timestamped value-free narrative rather than copied raw logs.
   Claude had repository-scoped file tooling for its named artifacts, which is
   the disclosed deviation from literal "no tools"; it never had target,
   credential, pane, transcript, or private-log access.

5. **Hour 5.** Both repositories remained clean while both reverse routes,
   their auth checks, the watchdog, and the entire Local-vantage fleet snapshot
   remained healthy. No T-293 defect emerged and the observer performed no
   recovery. Protected main was allowed to advance through normal merged work:
   the frozen starting hash is the clean baseline for comparison, not a ban on
   later protected commits. The stale Har-378 remote and two local preservation
   refs remained intact because deleting them is a separate authority boundary,
   not part of this validation window.

6. **Hour 6.** The task cleanly reconciled protected Har-380 and preserved both
   ledger rows. Both routes remained exclusively managed and auth-ready, the
   watchdog remained healthy without attempts, and every declared route pair
   passed. At 19:47:47 the material cutoff permanently ended target and
   implementation changes. The final Claude submission returned no value-free
   acknowledgement, so it was not retried. Its named artifact later appeared,
   was verified and read in full, and returned `ACCEPT-PENDING-CLOSURE`, leaving
   only the exact final sample, summaries, validation, and publication open.

7. **Hour 7.** At 20:47:47 protected main was clean at `45e1119`, the task
   checkpoint was clean at `5d2f8f0`, both Aist services were still
   loaded/running with exclusive managed ownership, both auth checks were
   ready, and the latest watchdog run was healthy with no attempt. Fresh
   Local-vantage probes at 20:48:01 found Aist and every other declared pair
   healthy. No credential bytes were accessed anywhere in the seven hours; no
   T-293 self-healing defect was demonstrated. The next action is final
   repository validation and protected evidence publication, preserving all
   stale refs for a separately authorized cleanup decision.

## Handoff

- Verified result and identifiers: the authorized window ran from 13:47:47 to
  20:47:47 JST with exactly seven completed slices. The one acknowledged
  primary kick recovered dual-route health in three seconds. The final sample
  found protected `45e1119`, task `5d2f8f0`, both aliases exclusively managed
  and auth-ready, the watchdog healthy without attempts, and every declared
  Local-vantage route pair ready. No credential bytes were accessed and no
  T-293 self-healing defect was demonstrated.
- Failures and retry safety: the slice-2 checkpoint incident advanced only the
  stale Har-378 remote to `bf5236a`; it was never reused for final publication
  and remains preserved. The final Claude transport returned no
  acknowledgement and was permanently not retried; the later named artifact
  proved review completion. Two rejected scratch paths failed before Git
  mutation. No failed or ambiguous target operation remains retryable.
- Modified files: `TODO.md`, `docs/tasks/index.tsv`, this task record, the
  frozen plan, the seven-slice ledger, three Claude review artifacts, and the
  Har-379 consumer receipt. No implementation, SSH configuration, credential,
  launchd, watchdog, or target file changed.
- Validation: both summaries contain exactly seven entries (611 durable words
  and 605 owner words); producer validation, consumer diff enforcement,
  task-ledger routing, agent-policy routing, `git diff --check`, and the
  deterministic Harness R0 suite pass on the final candidate.
- Remaining check and next action: protected hosted review/CI and merge are the
  only remaining publication gate. After merge, the producer may close its
  ready packet. Cleanup of the stale Har-378 remote and local preservation refs
  requires separate external-write authority; a reusable publication-guard
  audit, if desired, must be assigned by the producer as later work.

## Next action

Run the final integrated validation and publish this evidence-only closeout
through protected review. Preserve every stale ref; perform no target or
implementation mutation. After merge, the producer may mark its ready packet
complete and may separately decide whether to authorize stale-ref cleanup or a
follow-up audit of reusable publication guards.
