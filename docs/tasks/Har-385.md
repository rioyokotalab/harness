# Har-385 — Retire all non-main portfolio branches

## Status

In progress under the owner's explicit declaration that all five ledgers are
complete and every remaining non-`main` branch is obsolete.

## Starting inventory

- Local non-main branches: Harness 14, Personal 3, Students 1, Swallow 1,
  Website 3. The existing guarded branch transaction archived and removed all
  14 eligible Harness branches before this implementation began.
- Hosted non-main branches after authoritative readback: Harness 49, Personal
  0, Students 0, Swallow 109, Website 8.
- Personal and Website retain dirty linked worktrees; Students retains one
  clean historical controller worktree; Swallow retains one unpublished local
  archive branch.

## Frozen safety contract

Archive exact branch and dirty-worktree state before deletion. Hosted deletion
must bind canonical repository and remote identity, protected `main`, every
branch name and OID, a fresh mode-0600 plan, and a passing private archive
audit. Apply must abort before mutation on any drift and use one atomic set of
expected-OID leases. Protected `main`, credentials, private payload content,
panes, transcripts, deployments, workflows, rulesets, and unrelated state are
out of scope.

Dirty worktrees will be preserved without payload inspection in local archive
commits and bundles, switched to clean temporary branches at protected main,
and retired through the ordinary guarded worktree route. Har-385's allocation
and implementation branches are retired last after protected publication.

## Validation

The focused housekeeping suite passes, including synthetic exact-OID drift
with zero partial remote deletion, archive recovery, local retire-all, and
tracking-only cleanup. Housekeeping owner-alias, producer-ledger,
consumer-writer-boundary, policy-routing, and `git diff --check` pass. Complete
`tests/test-phase1.sh` passes with only its declared native-MPI environment
skip. Protected publication, five-repository archive audits, and final
zero-non-main readback remain.
