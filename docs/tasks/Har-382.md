# Har-382 — Migrate archive ownership to immutable repository aliases

## Status

Active on 2026-08-03 from protected base
`7c7628413d861f564003306818abc1056bf63d0b`. The immutable producer packet is
`docs/producer/tasks/Har-382.md`; the complete decision route is
`docs/audits/har381-producer-lifecycle-closeout/archive-owner-followup.md`.

## Frozen scope and safety

- Preserve all 12 reconstructed legacy roots, 28 bound archive-v2 receipts,
  their source bundles, generations, compactions, unrelated worktrees, remote
  branches, and dirty state until every migration acceptance check passes.
- Add private immutable alias receipts; never rewrite historical source
  receipts or weaken the scratch-owner, compaction, or two-generation gates.
- Bind aliases only through one exact absolute local-origin hop to one of the
  four declared durable repositories, then to its normalized credential-free
  terminal remote and protected-main snapshot.
- Reject relative or symlinked origins, arbitrary chains, identity inference,
  changed receipt/bundle bytes, missing tips, remote drift, protected-main
  rewind/divergence, duplicate aliases, and unsafe metadata.
- Any root deletion is a separate final guarded step after alias, report,
  generation, compaction, audit, and independent-restore evidence passes.

## Ordered execution

1. Reconcile the exact live archive inventory and one-hop durable-owner map.
2. Implement a versioned owner-alias schema, atomic publication, strict parser,
   and one owner resolver used before legacy-root canonicalization.
3. Route archive audit, archive reporting, generation creation, and compaction
   audit through that resolver only when the legacy root is absent.
4. Add hostile identity/schema/interruption tests plus mixed old/new,
   fast-forward, rewind/divergence, generation, compaction, and restore tests.
5. Publish and audit one alias for every affected receipt; temporarily hide
   each legacy root and prove reports, generation, and independent restore.
6. Guard-delete only independently redundant roots, rerun all audits and the
   complete suite, publish through protected Git, and reconcile the terminal
   receipt.

## Current next action

Implement the versioned alias schema and resolver with the exact relocation
bridge authorized below. Keep alias publication separate from parser and
resolver tests, then migrate the bounded 28-receipt set only after hostile
fixtures pass.

## Changed-input inventory result

The bounded set still matches exactly 12 legacy owner paths, 28 archive-v2
receipts, 28 unique items, 28 present source bundles, and 377,250,219 bundle
bytes. Only eight legacy roots now exist:

- Personal and Website roots remain present with their exact durable
  `projects` origins.
- Five Students roots remain present but their absolute local origins point to
  the Har-384-retired `codex-workspaces/students` owner.
- One Swallow root remains present but points to the retired
  `codex-workspaces/swallow` owner.
- Four Swallow roots bound by nine receipts are already absent.

Thus only two of twelve mappings currently satisfy the packet's exact live
one-hop rule. Reconstructing four roots, rewriting six local origin URLs, or
accepting Har-384 relocation evidence as a replacement identity proof would
all change the frozen input contract. No implementation, receipt, alias,
bundle, generation, compaction, root, Git configuration, or deletion write was
performed. The same archive report is not retryable until the producer/owner
chooses a fail-closed migration rule for this changed state.

## Owner decision — 2026-08-03

The owner selected the recommended narrow relocation-evidence bridge. Har-382
may treat the protected Har-384 durable record and merge revisions as an
immutable, single-generation mapping from exactly the retired Students and
Swallow `codex-workspaces` paths to their `projects` successors.

- Do not reconstruct the four absent roots or rewrite any historical receipt,
  bundle, generation, compaction, or surviving root's Git origin.
- A live root whose exact absolute origin is the recorded retired predecessor
  is admissible only when the relocation evidence proves the exact successor,
  normalized credential-free terminal remote, protected-main ancestry, and
  receipt/bundle identity.
- An already-absent root additionally requires its unchanged Har-381 one-hop
  mapping evidence; absence alone never grants identity.
- Permit no arbitrary alias chain, inferred repository identity, second
  relocation generation, mixed successor, or broader predecessor class.

## Implementation checkpoint

The first implementation slice adds a private `owner-aliases` namespace,
versioned exact-field JSON receipts, deterministic source-receipt filenames,
single-link mode-0600 atomic publication, and one resolver that retains the
legacy route while its root exists. Each alias binds the source receipt and
bundle digests, exact tips, legacy repository/common-directory identity,
durable owner inode, normalized credential-free terminal remote, protected
main snapshot, immutable protected mapping evidence, and an independent bare
restore proof.

Archive audit, reporting, generation parsing/creation, and compaction now use
the resolver before dereferencing a missing recorded path. Existing generation
and compaction repository rows resolve by immutable legacy identity, while new
generations group by the durable owner. Protected-main fast-forwards remain
valid only while the recorded snapshot is an ancestor; rewind, divergence,
remote drift, source changes, mixed aliases, and unsafe artifacts fail closed.

Focused validation passed:

- `tests/test-housekeeping-owner-alias.sh`
- `tests/test-housekeeping.sh`
- `python3 tools/producer-ledger.py validate`
- `python3 tools/producer-ledger.py check-consumer-diff --base origin/main`
- Python syntax compilation and `git diff --check`

No live alias or archive state has been written. Next, finish independent code
review and complete risk-tier validation, publish the implementation and exact
12-root mapping through protected Git, then use only that protected revision
for the private 28-receipt migration.

## First protected migration attempt

PR #595 merged the implementation at protected revision
`90343fbf1667c5ac55112d408124d75c1af0ed33`. A fresh preflight again matched
12 roots, 28 receipts, 28 bundles, and zero aliases. The first alias command
then failed before publication because it compared the Personal reconstructed
root's current directory inode with the historical receipt inode.

That check is invalid for the explicitly reconstructed Har-381 roots: their
path, Git common-directory path, origin mapping, receipt bytes, and bundle are
preserved, but reconstruction necessarily creates a new directory inode. No
alias exists and no archive/root state changed, so the failed input must not be
replayed until a protected correction replaces the live-inode comparison with
the receipt-bound exact Git common-directory and origin checks. Add a
new-inode reconstruction fixture, rerun complete validation, and publish the
correction before retrying the migration.

## Post-publication changed-input reconciliation

PR #596 merged the reconstruction correction at protected revision
`5fe8112effcf4eebf1dad033ff1a3da50ef0c2aa`. The changed-input migration then
published all 28 bounded aliases; publication ran longer than the first tool
yield but remained one live controller process, so no duplicate command was
started. Read-only polling observed 9, 18, 26, then 28 exact aliases before
that process exited.

The first full archive report then failed on a different, newer class: Har-384
created 3 Students and 10 Swallow receipts directly against the old canonical
roots after Har-382's 28-receipt inventory, then retired those roots. Eleven
source bundles remain present; two are validly compacted under two verified
generations. This is within the owner-authorized exact old-to-new Har-384
relocation bridge, but it requires two additional mapping rows and alias
creation from generation recovery when a source bundle was already compacted.

Preserve the 28 published aliases. Add exact protected tests for alias
publication from two-generation compacted recovery, publish the two canonical
predecessor rows, then create aliases only for the 13 newly discovered
receipts. Do not retry any of the 28 completed publications.

PR #597 merged those rows and the compacted-source path at protected revision
`bfc0b89ce48c1c49ddbc5a0d31ba1a4323e4db0e`. The changed-input publisher
correctly skipped all 28 prior aliases, published one new Students alias, then
failed before the next alias because that source bundle was absent without a
compaction receipt. Preserve the resulting 29 valid aliases and do not replay
their publications.

Read-only reconciliation found two such historical T-369 handoff receipts,
one Students and one Swallow. Both immutable source receipts are present and
each exact digest/tip is covered by three independently restored generation
receipts, but neither has a compaction receipt; the other eleven changed-input
source bundles are present. Add owner-alias v2 recovery metadata that requires
at least two digest-bound covering generations and a fresh independent exact-
tip restore. Continue parsing immutable v1 aliases, and route audit through v2
generation recovery only when both source bundle and compaction are absent.
Hostile tests must prove missing/changed/insufficient generation rejection and
must retain the two-generation rule.

## Protected-publication correction

The generation-recovery bytes passed the focused suites and complete local
validation, but commit `ca8139e457587acfe41575ad889c8c38b6056d44` was
accidentally committed and pushed while the checkout was on `main`. GitHub
accepted it through the owner bypass while reporting the PR and required-check
violations. No private alias or archive mutation used that revision.

PR #598 merged an exact revert at protected revision
`2d5114844c1421cfbeaf20481dd501216d18a003`. The validated change is now
reapplied byte-for-byte on a feature branch, with this incident record added,
and must merge through a clean protected PR before the remaining twelve alias
publications resume.

PR #599 restored the validated bytes through the protected path at
`805f25fbec1559ecb3c74fcaaaa21a537c748f75`. A present-bundle-only pass then
published the ten remaining source-bundle aliases without replaying any prior
publication, bringing the valid total to 39. The two generation-recovery
aliases failed before publication because the mixed-alias guard treated
different protected evidence commit snapshots as different owner mappings.

The mapping file path, digest, evidence repository identity/remote, exact
legacy origin, owner identity/remote, and method are identical; only the
protected ancestor commit containing those same bytes differs across batches.
That is a valid immutable evidence fast-forward, not mixed ownership. Exclude
the commit snapshot from the repository-binding equality while continuing to
validate each alias's own commit ancestry and exact evidence bytes. Add a
mixed-old/new protected-snapshot fixture before retrying only the two absent-
bundle receipts.

PR #600 merged that correction at protected revision
`0eb4931f8e9fd59aff2cf567636bdf9ec9be77e8`. The two generation-recovery
publications still failed before publication because private source receipts
are supplied through the lexical state path while generation rows resolve them
through the canonical NFS state path. The immutable files are identical, but
the resolver keyed aliases by the lexical spelling.

Preserve all 39 aliases. Canonicalize only the in-memory source lookup key and
compare the alias's immutable lexical source path by exact canonical file
identity; retain the lexical path and filename hash in every receipt. Add a
parent-symlink lexical/canonical state fixture, then retry only the two missing
generation-recovery aliases after protected publication.

PR #601 merged canonical lookup at protected revision
`0d121ccbef408b0d68e44ac417e7812bea63b885`. The two publications still
failed before publication while parsing the oldest covering generation. Four
already-absent reconstructed Swallow roots have original archive-receipt inode
IDs in their aliases but newer immutable generation rows correctly record the
different inode IDs assigned by Har-381 reconstruction.

This is the same bounded reconstruction identity transition, now inside an
immutable generation. Permit a generation row's recorded filesystem ID to
resolve by exact legacy path only when that generation explicitly names an
aliased source receipt for the path and all such aliases agree on the complete
owner/evidence binding. Keep exact-ID matching everywhere else. Add a fixture
that creates a generation from a reconstructed inode, removes the root, and
then audits and compacts it through the immutable alias.
