# T-364 — Safe repository cleanup and durable recovery evidence

## Status

Phase: `validating`. Owner `go` was received on 2026-08-01. No live cleanup,
restoration, remote write, worktree deletion, or branch deletion has been
performed by T-364. Stage 0 started at `2026-08-01T15:04:56+0900` from clean
branch `codex/t364-housekeeping-cleanup-safety` fast-forwarded to merged plan
commit `cdc1bc8bac49dc026038adb08fb507b580f3329a`.

## Desired outcome

Make routine Harness cleanup prove that every deleted branch or worktree is
both eligible and durably recoverable, while permanently keeping remote-ref
deletion outside autonomous housekeeping. Preserve the current clean outcome;
do not restore obsolete branches or worktrees merely to repeat their removal.

## Durable routing

- Exhaustive plan: `docs/plans/t364-housekeeping-cleanup-safety.md`.
- Reproducible audit: `docs/audits/t364-housekeeping-cleanup/audit.md`.
- Owning policy: `docs/agent-policy/housekeeping-and-promotion.md`.
- Related lifecycle decision, not owned here: `docs/tasks/T-363.md`.

## Frozen decisions

1. Do not restore the removed worktrees or branch names: no intended tracked
   content is absent from `main`.
2. Protect all 21 recorded cleanup tips before future garbage collection with
   local archive refs and a verified durable bundle under user state.
3. Keep remote-branch deletion report-only in Harness. A future remote delete
   remains a separately authorized external operation, never a T-362 apply
   routine.
4. Require exact pull-request head/tip matching or ancestry to current
   `origin/main`; a branch name with an older merged PR is insufficient.
5. Worktree deletion requires zero tracked, untracked, ignored, nested-repo,
   submodule, lock, and live-process residue. Any nonzero result is report-only.
6. Route worktree-directory and Git-administration-tree removal through
   separate guarded-delete manifests with ordered recovery checkpoints; never
   use force removal.
7. Store receipts and bundles durably, mode 0600 below a mode-0700 state root;
   `$XDG_RUNTIME_DIR` may hold only a pointer or transient status.

The decision register is complete. No owner interview is needed.

## Execution checkpoint

The go authorizes implementation, tests, protected publication, fleet
synchronization, and creation of non-destructive archive refs plus a verified
bundle for the already-recorded tips. It does not authorize deleting any
current local branch, worktree, remote ref, receipt, bundle, archive ref, user
data, or external state. Any future cleanup apply must pass its own frozen
candidate and authority gates.

Stage 0 is complete. All 21 exact commit objects were revalidated and retained
under
`refs/harness-housekeeping/archive/t364-20260801T060854Z/`. The verified
mode-0600 bundle and receipt are respectively
`/mnt/nfs-03/safe/Users/rioyokota/home/.local/state/harness/housekeeping/t364-20260801T060854Z.bundle`
and the sibling `.receipt`. Independent readback proved 21 refs, 21 receipt
items, 21 bundle heads, a matching SHA-256 digest, modes 0600, link counts one,
and `git bundle verify` success.

Stages 1–4 are implemented in `libexec/harness_housekeeping.py`, with
`libexec/harness-housekeeping` as its no-bytecode CLI wrapper. Branch plans now
use fresh GitHub state plus an `origin/main` readback, freeze exact PR heads,
and apply only after full-set revalidation, archive publication, and one atomic
expected-old ref transaction. Remote inventory has no apply route. Worktree
plans require zero tracked, untracked, ignored, nested, submodule, lock, and
process/open-file findings; eligible directory and admin records receive
separate guarded-delete manifests and a durable between-stage checkpoint.

Focused validation passes `tests/test-housekeeping.sh`,
`tests/test-guarded-delete.sh`, and `git diff --check`. Synthetic evidence
covers branch reuse, squash heads, API failure, ambiguous PRs, candidate drift,
archive modes/link counts/classification, bundle-only survival after immediate
GC, hard-link and symlink rejection, report-only remotes, every planned
worktree residue class, guarded deletion ordering, and interruption after the
directory stage. Live plan/audit mode reports the 21-tip archive passing,
local branch candidates 0, one report-only remote branch, worktree candidates
0, scratch 0, launchers 0, and no live mutation. Arg0 remains independent:
live 35, eligible 72, young 7, unexpected 0, removed 0.

One generated `libexec/__pycache__` directory was removed through guarded
manifest `/tmp/harness-t364-pycache.manifest`; protected anchors were unchanged.
The wrapper now disables bytecode generation and the focused suite proves the
directory is not recreated.

Next action: complete independent source/diff review, run the selected risk
tier and final phase one, then publish one protected PR. No live cleanup apply
is part of validation or rollout.

The first R3 run reached the full suite and failed only at four integration
gates: the edited housekeeping policy exceeded its 275-word routed budget,
the context benchmark still contained pre-edit expected values, ShellCheck
required quoting `main^{tree}`, and two clean-checkout suites rejected the
intentionally uncommitted tree. The first three are safe local fixes; the last
two must be rerun from a committed tree. No cleanup apply or external write
occurred in this failed run.

The policy is now 272 words, benchmark values match the 65-line/305-word
board, and ShellCheck passes. Focused revalidation also passes policy routing,
context routing, task-ledger routing, housekeeping, guarded-delete, Python AST,
and diff checks. Working files are `TODO.md`, the routed housekeeping policy,
this task record, the T-351 benchmark table, the housekeeping wrapper and new
core module, and `tests/test-housekeeping.sh`. Next action: commit this exact
tree, rerun R3 from the clean commit so clean-checkout suites are meaningful,
then fetch/reconcile and publish.

Stage 0 attempt 1 stopped before temporary files or refs were created because
the lexical state path `/home/rioyokota/.local/state` resolves through the
owner-managed `.local` symlink to its durable NFS location. Readback proved
zero archive refs and an empty new housekeeping state directory, so retry was
safe. The successful retry preserved both lexical and canonical paths in the
receipt and operated on the canonical, current-user-owned mode-0700 directory.
