task: Har-404
status: complete
updated: 2026-08-06
---
# Har-404 — Investigate the Students dependency alert

GitHub reported one existing high-severity Dependabot alert while Stu-080 was
published. Identify the affected package and reachable usage through the
repository's authorized security interface, choose the smallest compatible
upgrade or mitigation, run the complete Students gate, and publish through its
protected flow. Do not expose private alert payloads in Harness.

The metadata-only inventory found two high-severity alert records representing
one advisory twice: `cryptography` in the direct manifest and frozen lockfile,
both GHSA-g6cj-pr64-35w5. The repository resolved 49.0.0; GitHub's reviewed
advisory and the upstream release notes identify 50.0.0 as the first patched
release. Reachability inspection found only AES-GCM and HKDF use, not the
affected PKCS#7 decrypt functions, but the package remains a direct runtime
dependency.

Students packet PR 623 merged first, then protected PR 624 upgraded only the
direct requirement and lock entry to 50.0.0. After synchronizing the frozen
environment, the complete gate passed with 1,185 tests and 81 subtests. The
terminal producer/consumer ledger converges, the selector is idle, protected
main is clean at `08eb60c4c`, and runtime readback reports 50.0.0.

One immediate post-merge GitHub inventory still showed both alert records open,
consistent with asynchronous dependency-graph processing; it was not retried
or treated as failed remediation. Complete: the affected protected bytes and
runtime are patched without unrelated dependency churn.
