# Har-381 — Producer lifecycle convergence and portfolio closeout

## Status

Phase: `executing` from 2026-08-03 01:00 through 09:00 JST. Material changes
stop at 08:00; the final hour is validation, publication, cleanup verification,
and handoff only.

## Objective and authority

Execute the protected Har-381 packet published by PR #582 at `976b56a`. The
owner's `go` authorizes the frozen repository work and ordinary protected Git
publication. It does not authorize credentials, hosting administration,
rulesets, approval-count changes, packages, deployments, fleet-service
mutation, consumer-runtime mutation, remote-branch deletion, or gated-task
targets.

## Confirmed baseline

- Protected Harness `main` is clean at `976b56a`.
- Har-379 has a merged complete execution record and complete receipt, while
  its immutable packet, producer queue, and producer index still say `ready`.
  The current validator reports pass, demonstrating the missing convergence
  invariant.
- PR #582 changed the stale Aist assignment from `active` to `idle`; the
  assignment table now has no active consumer. Fresh Codex and Claude monitor
  status is healthy. No pane or transcript content was read.
- The published Har-381 packet is 2,269 bytes and the producer entrypoint is
  1,366 bytes, both below their 4,096-byte limits.
- The existing protocol requires producer and consumer writer surfaces to be
  published separately. The minimum protocol-required transitions supersede
  the proposal's looser one-PR preference; combining the surfaces would fail
  the mandatory writer-boundary check.

## Frozen benchmark and acceptance

Measure always-read bytes, selected-task bytes, routed-policy bytes, action
count, wall time, and validation cost independently per repository. Compact
only when capability checks remain equivalent and at least one measured cost
decreases without another material regression.

Acceptance requires deterministic rejection of receipt/disposition and active
assignment contradictions; immutable published packet bytes during producer
reconciliation; no duplicate task selection after a complete receipt; exact
independent status for all five protected repositories; public-content checks
for Harness and Website only; guarded cleanup of eligible local task residue;
and exactly eight evidence-backed hourly slices in both summaries, each at
least 400 words.

## Stop conditions

Stop a repository stream on active or unknown consumer ownership, dirty or
diverged protected state, overlapping work, ambiguous publication, private
payload exposure, missing authority, or failed cleanup identity. Continue
unaffected repositories independently. Never retry an ambiguous external
write and never weaken a validator merely to pass current state.

## Working set

- `tools/producer-ledger.py`
- `tests/test_producer_ledger.py`
- consumer board, record, plan, audit, and final receipt for Har-381
- later producer-only queue/index/assignment reconciliation

## First unverified action

Run the complete Harness lifecycle validation tier and the public-content
audit, then publish the consumer-owned implementation through protected Git.
The focused suite now has 17 passing cases. `next-ready` excludes terminal
receipts, strict convergence rejects unreconciled Har-379, terminal
dispositions preserve packet bytes, terminal rows require matching receipts,
and active assignments require compatible executable work. The legacy
Har-378 completion receipt is backfilled without changing its scope.

Portfolio discovery found the same schema in all protected sibling branches,
with no open pull requests. Students and Personal assignment metadata says
`active`; this conflicts with the frozen nightly idle schedule, and Personal
has no executable packet. Students and Swallow primary checkouts remain on
older task branches and must not be changed. Any eligible sibling work uses an
isolated worktree from the freshly fetched protected revision.

The first full R3 run failed on three bounded integration findings. The active
task index omitted its required `TODO.md` pointer, the generated context
benchmark reflected pre-task byte counts, and the tmux/terminfo suites require
a clean committed checkout. The pointer is fixed, the deterministic benchmark
is refreshed, and both focused checks now pass. The clean-checkout suites are
safe to rerun once this checkpoint is committed; no target or runtime state
changed. One test-created Python cache directory was removed through guarded
deletion after a one-target manifest validated 2 entries and 27,055 bytes;
protected anchors were unchanged and the private manifest was exact-unlinked.

After commit `4200bec`, the complete deterministic R3 phase-one suite passed
on the clean candidate, including producer role, task routing, context budgets,
public-repository audit, Actions sustainability, housekeeping, managed-agent,
recovery, fleet, and safety suites. Native MPI was correctly skipped outside a
declared MPI environment. The implementation bytes and environment contract
are unchanged after this value-free ledger checkpoint, so only documentation,
ledger, diff, and consumer-boundary checks require repetition before push.

## Next action

Finish classifying pre-existing Personal, Students, Swallow, and Website
temporary residue without deleting ambiguous state. Recheck Website's exact
fixed-socket gate only after other safe work completes; do not substitute
authentication or replay its failed push preflight. At the 02:00 boundary,
close slice 1 and begin the next evidence slice without inventing activity.

## Protected portfolio results

- Harness lifecycle implementation merged through PR #583 and Har-379 producer
  reconciliation through PR #584. Protected `8068159` passes strict
  convergence and still selects Har-381 while this nightly remains active.
- Students producer, implementation, and reconciliation PRs #571–#573 merged
  to protected `80a0297`. Strict convergence passes and `next-ready` selects
  Stu-052. The live older primary task branch was never switched.
- Swallow producer, implementation, and reconciliation PRs #150–#152 merged
  to protected `0b692de`. Strict convergence passes and `next-ready` selects
  Swa-040. No scheduler, provider, compute, or live primary state changed.
- Personal producer, implementation, and reconciliation PRs #27–#29 merged to
  protected `6abd50a`. Strict convergence and complete Personal validation
  pass; `next-ready` reports idle and no sensitive payload was accessed.
- Website producer commit `94d0eb2` remains only in its isolated local
  worktree. The repository-mandated fixed SSH socket is absent, so its push
  preflight failed before transport. No branch, PR, deployment, or hosting
  write occurred. Retry is safe only after that exact gate changes.

All four completed implementations share validator digest
`df40b9466822b81c2e37669eee60d4534546c8920226b6eaa93236249521d290`.
Students passed 713 tests and 38 subtests using its existing canonical no-sync
environment; Swallow passed login-safe static validation; Personal passed 24
tests plus complete validation; Harness passed the full R3 phase-one suite.
The before/after producer-packet blob identities in
`docs/audits/har381-producer-lifecycle-closeout/packet-immutability.tsv` are
equal for all four reconciliation transitions, independently demonstrating
that mutable disposition closeout did not rewrite immutable packet bytes.

The first Harness producer checkpoint used a plain branch push before the
selected execution reference's explicit-refspec requirement had been read. It
published only the intended absent branch, and every subsequent push used an
explicit refspec after fresh base comparison. No retry or corrective external
write was needed.

## Benchmark and cleanup checkpoint

The first cross-repository measurement is tracked in
`docs/audits/har381-producer-lifecycle-closeout/benchmark.tsv`. Always-read
bytes are root `AGENTS.md` plus `PRODUCER.md`; routed bytes are the board,
selected immutable packet, matching execution record, and Website's required
session record where applicable. The strict validator measurement is a fresh
local wall-clock sample in milliseconds. Personal is idle, so its routed count
is only its active board. Website still has the pre-Har-381 validator and is
reported as `pass-legacy`, not falsely labeled converged.

Repository housekeeping archived and transactionally removed the three merged
Harness implementation worktrees. Students and Swallow test-generated ignored
caches were deleted only through fresh guarded manifests, after which all six
merged sibling worktrees were archived and removed through repository-native
housekeeping. The durable Students and Swallow producer-main worktrees were
retained and fast-forwarded to their final protected revisions; their older
active primary task branches were not switched.

The same closeout route removed one older Students worktree, four older
Swallow worktrees, four older Personal reference worktrees, and four older
Website reference worktrees after their generated caches were guarded-removed
where needed. Four old ancestor-only Harness local branches were also archived
and deleted through the branch-housekeeping transaction. Remote branches
remain report-only by policy.

An independent receipt audit found that guarded-removing the resulting clean
reference clones was locally safe but violated a separate durable invariant:
archive receipts bind to the exact Git common-directory pathname used for
closeout. Their private bundles survived, but formal audit and generation
creation require that repository root to exist. The six roots removed in this
run and six previously missing historical roots were therefore reconstructed
at their exact paths from clean canonical local repositories. All 83 archive
receipts then passed canonical-path audit. New generation
`generation-20260802t170508z-2905616-3f0b18` independently restored 121 heads
for 17 repository identities into bundles totaling 205,638,273 bytes. The
archive report now has zero uncovered receipts, zero incomplete applies, no
generation trigger, and no deletion candidate.

To prevent recurrence, worktree housekeeping now rejects both plan and apply
when the repository owning the archive is itself inside a scratch boundary;
the caller must use the durable canonical repository and place only the task
worktree in scratch. The focused housekeeping suite proves the new fail-closed
gate and all existing transactional behavior. Existing reconstructed archive
anchors remain required because their immutable historical receipts predate
this guard; deleting them again would knowingly break auditability.

Clean checkpoint `21c61c3` passed complete R3 validation: all 96 phase-one
suites passed in 77.85 seconds, including the 30.62-second focused
housekeeping suite; native MPI was correctly skipped outside a declared MPI
environment. Self-attested receipt identity
`e9bf3f920679fb174bd0a7033a458d0e00a4613e1f1f89c22af269c19ad6e396`
binds protected base `8068159`, exact tree `6fc6580`, six-worker environment
contract, and the changed safety runtime and test. This is same-trust local
evidence, not independent hosted CI.

The merged Personal producer worktree was archived and removed. An accidental
dirty Personal implementation worktree created outside Personal's exact
canonical-root execution policy remains preserved: four of its changed files
match protected main exactly, while its board, task record, and index differ
because the canonical protected stream subsequently completed reconciliation.
No destructive normalization was attempted. Website's unpublished Web-219
worktree likewise remains preserved behind its fixed-socket publication gate.

Website's repository-native fixture audit passed in 0.38 seconds. Its
value-free all-reachable-ref audit examined 460 commits, 8,530 objects, and
4,745 blobs in 23.65 seconds. It reported no private-key, known-token,
high-entropy credential, or suspicious-path class. Generic assignment, path,
absolute-path, and large-blob classes remain historical review signals; no
matched value was emitted or copied into Harness.

The remaining matching `/tmp` directories are classified in
`docs/audits/har381-producer-lifecycle-closeout/residue.tsv`, not silently
ignored. Process metadata proved that one connection-monitor root is live and
the other was a stale 10-file root; the latter was guarded-removed. The
Har-381 portfolio worktree is active; five Personal/Website worktrees contain dirty
pre-existing changes; two clean Students/Swallow producer-main worktrees are
durable because their primary checkouts remain on preserved active branches;
three Swallow evaluation trees are owned by another user; twelve reconstructed
reference roots retain pathname identity required by 28 archive receipts; and
Web-219 is the preserved fixed-socket-blocked candidate. None is eligible for
this task's guarded deletion without changing ownership, discarding dirty
bytes, breaking archive audit, or removing retained operating state. All five
repositories currently have zero open pull requests. Harness, Personal, and
Website canonical roots are clean;
Students' preserved primary task branch is clean; Swallow retains its one
known unrelated dirty entry.
