# Har-381 — Producer lifecycle convergence and portfolio closeout

## Status

Phase: `executing` for the owner-started 2026-08-03 01:00–09:00 JST producer
nightly. Material changes stop at 08:00; the final hour is integrated
validation, protected publication, cleanup verification, and handoff.

## Objective, authority, and stop conditions

Execute the immutable Har-381 packet published by Harness PR #582 at
`976b56a`. Repair the demonstrated lifecycle contradiction in which Har-379
had a complete execution record and receipt while its producer packet/index
remained ready and its assignment remained active. Apply the same defect fix
independently only where current repository bytes prove it is needed.

The owner's `go` authorizes the frozen repository work and ordinary protected
Git publication. It does not authorize credentials, hosting administration,
rulesets or approval-count changes, packages, deployments, fleet-service or
consumer-runtime mutation, gated task targets, remote-branch deletion, or
private payload transfer into Harness. Stop one repository stream on unknown
ownership, dirty/diverged protected state, ambiguous publication, failed
identity, or missing authority; continue unaffected repositories. Never replay
an ambiguous external write or weaken a validator to pass existing state.

Acceptance requires deterministic rejection of lifecycle contradictions,
immutable producer packet bytes during reconciliation, independent status for
all five repositories, public-content checks for Harness and Website only,
guarded cleanup of eligible residue, and exactly eight evidence-backed hourly
slices. The durable and owner summaries must each exceed 400 words.

## Next action

Continue hourly metadata, fixed-socket, archive, and hosting readback without
touching consumers or replaying Website's stopped preflight. Preserve all
classified dirty, other-owner, archive-anchor, live-monitor, and
consumer-primary state. Before the 08:00 material cutoff, finish the closing
evidence and prepare the consumer receipt/publication. In the final hour,
publish and verify the consumer diff, remove only its merged worktree through
the guarded archive route, then publish a separate producer reconciliation
that closes Har-381 and queues the bounded archive-owner migration and
Harness-only productive-idle pilot follow-ups.

## Cold-resume route

This record is sufficient to execute the first unverified action. Do not
preload the audit directory or earlier hourly slices. At a slice boundary,
locate and read only the current slice in `time-slices.md`; consult one other
named artifact only when revalidating its cited fact. The evidence map is a
lookup index, not a mandatory context bundle.

## Verified portfolio result

- Harness PR #583 implemented strict lifecycle convergence and PR #584
  reconciled Har-379. Protected `8068159` passes strict validation and selects
  Har-381 while this run is active.
- Students PRs #571–#573 converged protected `80a0297`; strict validation
  passes and `next-ready` selects Stu-052. Its older primary task checkout was
  never switched.
- Swallow PRs #150–#152 converged protected `0b692de`; strict validation
  passes and `next-ready` selects Swa-040. Its primary task checkout and one
  unrelated dirty entry remain preserved.
- Personal PRs #27–#29 converged protected `6abd50a`; strict and complete
  validation pass and `next-ready` reports idle. No sensitive payload was
  accessed.
- Website producer commit `94d0eb2` remains clean and local in
  `/tmp/website-web219-lifecycle`. Its required
  `/home/rioyokota/.ssh/agent.sock` is absent, so the mandatory pull preflight
  stopped before transport. No branch, PR, deploy, hosting write, or
  authentication substitution occurred. Retry is safe only after that exact
  gate changes.

All four published implementations have validator digest
`df40b9466822b81c2e37669eee60d4534546c8920226b6eaa93236249521d290`.
Their packet blob IDs remain equal before and after reconciliation, as recorded
in `packet-immutability.tsv`. Students passed 713 tests and 38 subtests in its
canonical no-sync environment; Swallow passed login-safe static validation;
Personal passed 24 tests plus complete validation. All twelve portfolio PRs
are merged, none has a pending or failed hosted check, and all five
repositories have zero open PRs. Explicit author-and-sender job gates skipped
hosted execution for the owner-authored PRs;
Personal has no hosted check configured. Native validation is the correctness
evidence, while weekly/manual and non-owner paths retain hosted backstops.

## Validation and efficiency evidence

Exact code head `f3d57c7` passed all 96 Harness R3 phase-one suites in 78.04
seconds; native MPI was correctly skipped outside a declared MPI environment.
Same-trust receipt identity
`fd359775423b7831a4fbd47455e6212014ac6332b00a03d161b82a21fc8ce89f`
binds tree `5d697cf`, the protected base, validator contract, and six-worker
environment. Later changes are value-free documentation only and correctly
route to R0; the first such route passed diff and task-ledger checks in under
0.08 seconds at tree `0f2d086`. This reuses exact R3 evidence rather than
repeating unrelated suites.

`benchmark.tsv`, `context-shape.tsv`, `selector-benchmark.tsv`, and
`publication-timing.tsv` cover the frozen benchmark dimensions. Always-read
bytes are Harness 7,663, Students 8,714, Swallow 8,473, Personal 4,289, and
Website 8,255. No repository has an exact nonblank line shared by its
always-read `AGENTS.md` and `PRODUCER.md`, so there is no demonstrated exact
deduplication to remove. Ten-run strict-validator medians are 40.709–47.284 ms
and converged selector medians are 43.921–47.412 ms; Website's 43.201 ms legacy
validator lacks `next-ready` pending Web-219. Harness's three required
writer-separated PRs span 978 seconds; the parallel Students, Swallow, and
Personal streams each span 793 seconds. Defined action groups are 8, 6, 6, 6,
and 4 respectively; incidental shell subcommands are not treated as a portable
benchmark.

The 20 selected policy/skill resources total 41,403 bytes; 6,073 bytes are
exact repeated lines, mainly across five self-contained nightly protocols.
Centralizing them would weaken repository-independent cold recovery for a
producer-night-only saving, so `routing-footprint.tsv` records no change.

The active Harness routed context grew from 12,301 to 27,867 bytes as this
duration-required record accumulated evidence. After linked chronology and
tables were durably checkpointed, total routed context fell to 17,576 bytes, a
10,291-byte (36.929%) reduction. Owner-directed productive-idle analysis later
expanded only this record; `active-context.tsv` carries the exact current bytes
and words without making this paragraph self-referential. Every resume-critical
result, failure, blocker, receipt, next action, and evidence pointer remains.
Other repository contexts remain byte-identical to baseline.

## Housekeeping and archive integrity

Archive-first housekeeping removed three merged Harness worktrees, six current
Students/Swallow task worktrees and generated caches, thirteen older merged
sibling worktrees, four older Harness branches, two Personal branches, and one
stale connection-monitor root. Every recursive or multi-path deletion used a
fresh guarded manifest. Remote branches remained report-only.

An independent audit then found that deleting clean reference clones broke a
separate invariant: immutable archive receipts bind the exact Git
common-directory pathname. Source bundles survived, but formal audit could not
resolve the removed owners. The six newly removed roots and six older missing
roots were reconstructed at their exact paths. All 84 receipts now audit; all
12 roots are owner-correct, clean, sparse, and on main, totaling 490,203,636
bytes. New generation `generation-20260802t170508z-2905616-3f0b18`
independently restored 121 heads for 17 repository identities into 205,638,273
bytes of bundles.

The current archive report has 84 receipts, 113 items, 106 unique tips, zero
incomplete applies, one uncovered 133,401-byte receipt below the next
generation threshold, and no deletion candidate. The 12 reconstructed roots
bind 28 one-tip archive-v2 receipts; all 28 source bundles remain present and
total 377,250,219 bytes. New housekeeping plan/apply now rejects a repository
whose archive owner is under configured scratch, `/tmp`, or `/var/tmp`; focused
tests and a production-mode probe prove fail-closed behavior. Existing anchors
must remain until the immutable-alias migration specified in
`archive-owner-followup.md` is implemented and independently tested.

The remaining `/tmp` set is fully classified in `residue.tsv`: the live
monitor root, this worktree, 12 archive anchors, three dirty Personal
worktrees, two dirty older Website worktrees, clean durable Students/Swallow
producer-main worktrees, three other-owner Swallow evaluation trees, and clean
blocked Web-219. Count-only readback found 15 tracked and four untracked entries
across the five dirty Personal/Website worktrees; no payload name or content
entered Harness. No retained row is currently eligible for deletion.

## Public, hosting, and ruleset readback

The Website protected tree passed its complete credential-free offline suite
in 8.10 seconds and its all-ref public audit in 23.65 seconds, with no browser
installation, live check, or deployment. Harness protected baseline audit
covered 1,615 commits, 11,476 objects, and 4,993 blobs in 31.56 seconds. Its
known-token/private-key/assignment shapes are confined to hostile synthetic
fixtures. Candidate `3c779cc` covers 1,625 commits, 11,573 objects, and 5,034
blobs with every finding count unchanged: zero high-entropy credential, zero
suspicious path, and no exposed matched value. One final integrated audit
remains after closeout bytes are committed.

Paginated hosted branch counts are Harness 32, Students 2, Swallow 105,
Personal 0, and Website 8; all hosted non-main commits are at most 30 days old.
Students and Swallow deliberately fetch only main, while the other three use
all-head refspecs. Local tracking mismatches therefore do not prove staleness,
and no fetch, prune, refspec, or remote branch was changed.

Read-only ruleset checks show all five rulesets active, each with one bypass
actor and zero required approvals. Code-owner and last-push flags are false,
including Students; this differs from old T-311 intent but is outside Har-381's
explicit ruleset exclusion. No ruleset write occurred. Metadata-only Codex and
Claude monitor status is healthy for all three slots each, Codex phone
mirroring is healthy, and no pane or transcript content was inspected.

After the primary nightly work drained early, the owner redirected the
remaining analysis to future queue exhaustion. `productive-idle-contract.md`
now gives the compact execution contract for a bounded producer reserve
catalog, deterministic fair selector, durable pre-action selection, exact
idempotent receipts, one structural discovery allowance, honest coverage
forecasting, and durable event-driven wait. Twenty-six threat scenarios and
23 acceptance measures plus a matched Har-381 replay define a Harness-only
pilot; the longer design is lookup-only rationale. No scheduler, rollout, or
consumer-runtime change was implemented in this task.

## Failures and retry safety

- The first R3 attempt exposed a missing task-index pointer, stale generated
  context counts, and clean-checkout-only suites. The pointer/counts were
  corrected and the clean committed tree passed; no runtime target changed.
- The initial reference-clone deletion passed its own guarded gate but broke
  archive-owner audit. Exact roots were reconstructed and all receipts
  re-audited; the new scratch-owner guard prevents recurrence.
- One early branch push used a plain push before the explicit-refspec rule was
  read. It created only the intended absent branch. Every later push uses a
  freshly resolved explicit refspec.
- One explicit push named a manually copied non-commit object ID and was
  rejected before remote update. Fresh `git rev-parse HEAD` produced the valid
  commit; the subsequent fast-forward push succeeded without force.
- One strict-validator read addressed the intentionally old Students/Swallow
  primary checkouts, where the tool is absent. Only those reads were retried
  from declared producer-main worktrees and passed; no checkout was switched.
- A combined timing sampler reached Website's known-missing `next-ready`
  command and stopped without mutation. Successful samples were retained; only
  the missing Website validator sample was completed separately.
- Website's socket preflight is a known, value-free blocker. It has never been
  retried with changed authentication or alternate transport.

## Evidence map

Hourly chronology is authoritative in
`docs/audits/har381-producer-lifecycle-closeout/time-slices.md`. The same
directory contains `acceptance.tsv`, `active-context.tsv`, `benchmark.tsv`, `context-shape.tsv`,
`packet-immutability.tsv`, `portfolio-status.tsv`, `public-audit.tsv`,
`publication-checks.tsv`, `publication-timing.tsv`, `remote-branches.tsv`,
`residue.tsv`, `routing-footprint.tsv`, `ruleset-readback.tsv`, `selector-benchmark.tsv`,
`validation-cost.tsv`, `archive-owner-followup.md`, `productive-idle-design.md`,
`productive-idle-contract.md`, `productive-idle-scenarios.tsv`, and
`productive-idle-acceptance.tsv`. These value-free artifacts preserve bulky
reproducible evidence without making unrelated future tasks read this
chronology.
