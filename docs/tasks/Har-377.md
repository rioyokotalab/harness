# Har-377 — Admit Website in fleet agent sentinels

## Status

Complete upon protected publication and verified fleet rollout.

## Scope and decision

Add Website to the canonical user-level Codex and Claude launch sentinels.
Admit the physical Linux root
`/mnt/nfs-03/safe/Users/rioyokota/projects/website` and the macOS root
`$HOME/projects/website`. Website remains an independent, non-managed bounded
root: this change does not add a tmux pane, saved thread, phone root, or
managed-target lifecycle.

The owner authorized the exact sentinel and fleet-profile update. Preserve all
other user configuration and every repository's local policy.

## Validation and rollout

Run the focused agent-configuration suite and complete Harness phase one.
Publish through protected `main`, use guarded `harness fleet-sync` for clean
managed checkouts, and verify both sentinel links expose Website on every
reachable node without inspecting credentials or agent transcripts.

The first focused run failed before external mutation because its pre-existing
Personal assertion spanned the newly pluralized sentence; update it to the
joint bounded-root classification and rerun. An attempted
`tests/test-task-ledger.sh` command was absent and had no side effect; use the
repository's declared `tests/test-task-ledger-routing.sh` gate instead.

The first phase-one run found one further stale bounded-root assertion in
`tests/test-claude-takeover.sh`; update it to the joint classification. Its
tmux-config and terminfo failures were expected clean-checkout gates on the
uncommitted task tree, not behavioral failures. Commit the coherent focused-
validated change, rerun those owning suites from clean bytes, then rerun phase
one from the same clean commit.

Focused agent-config, Claude-takeover, task-ledger, tmux-config, and terminfo
checks pass. Phase one passes from the clean task commit, with only the declared
native-MPI environment skip. The remaining action is protected publication,
guarded fleet sync, per-node sentinel readback, context refresh on advanced
Macs, and exact worktree closeout.
