---
task: Har-394
status: active
updated: 2026-08-05
---
# Har-394 — Recover the Personal Codex pane

## Current diagnosis

The canonical Personal TUI rejects turns against archived wrong-CWD thread
`019fca58-03d3-7392-a788-628bed221c3f`. Zero-write app-server readback shows
that root under the Harness cwd; it must remain archived. Canonical pane `%19`
in window `@1` resumes clean Personal root
`019fc765-0206-7112-8293-146fb29e8087`, named `personal`, but its in-TUI target
has drifted. This selects bridge-first replacement. No prompt or transcript
content was read, reconstructed, or replayed.

Frozen canonical identities are supervisor `67919/149176315`, TUI
`68361/149176502`, watcher `2503998/150148010`, pane `%19`/`@1.0`, and shared
app server `2852569/83381863` with mode-0600 socket inode `197:24090962`.
Harness `%0`, Students, both Claude panes, monitors, attached client, and
recovery helper are outside mutation scope. Personal Git is clean on protected
main; its ledger passes.

## Preserved rejected bridges

R1 root `019fceb9-4557-7522-a197-4733147cf515`, window `@20`/pane `%23`, and
supervisor/watcher/TUI `414161/161476103`, `414479/161476148`, and
`414753/161476260` are idle and preserved. Its sole turn completed without
error but had zero tool items, so it was never promoted.

R2 root `019fcec5-16cd-7ad3-94ee-9a4900144dae`, window `@21`/pane `%24`, and
supervisor/watcher/TUI `504634/161553763`, `504878/161553802`, and
`504995/161553914` are also idle and preserved. Its changed-input instruction
explicitly requested tools, but the full one-turn structural view again had
one user item, three assistant items, zero active items, no system error, and
zero tool items. Its journals are mode 0600 and retry-forbidden. Neither bridge
received a second turn; no root was promoted, renamed, archived, or retired,
and no old process was signaled.

Both failed cold starts were sent through the inter-agent transport and began
with `[Agent: Local Codex]` but lacked the exceptional
`LOCAL_REPLY_REQUIRED` contract. Personal policy reserves cross-agent work for
task-specific requests and otherwise runs repository-asynchronously. The
bridge skill instead calls for a generic cold-start turn merely protected by
the shared agent-message lock. Treating the controller cold start as a
non-binding agent message is the leading explanation for the structurally
tool-free completions.

## Authorized R3 route

The owner authorized all actions needed to fix the pane. Create one distinct
R3 root, journal, name, window, pane, and runtime while preserving canonical,
R1, and R2 chains. Under the same mode-0600 injection lock, send one plain
controller cold-start user turn without an agent prefix or reply contract. It
must forbid replay, explicitly require terminal tools to read complete Personal
instructions and ledgers, run selector/Git/validation checks, make no project
or live-source writes, and remain idle. Every acknowledged request is
non-retryable.

Accept only one completed assistant-bearing turn with at least one terminal
tool item, zero active items or system error, exact Personal cwd, and unchanged
root/runtime/process/socket/peer identities. Then perform the recorded
pane-native bridge promotion, publish the reversible cutover checkpoint, and
only afterward signal the revalidated old TUI leaf once with SIGTERM. Never
restart the app server, replay a prompt, signal a group, use SIGKILL, or delete
or archive any saved root.

## Next action

Mode-0600 R3 controller `/tmp/har394-appserver-controller-r3.py` has SHA-256
`44b659c0b721d669d65290eeb372a43d5fbcbcabeb97ae979872a53d56377c11`.
It digest-binds the unchanged base controller and overrides only journal
`/tmp/har394-appserver-create-r3.json` and name
`personal-har394-recovery3`. It created root
`019fcecf-2628-7cd1-acf0-af629980ec2e` once at exact Personal cwd; mode-0600
journal `/tmp/har394-appserver-create-r3.json` is retry-forbidden. The rollout
device/inode `197/243102739` was revalidated through a no-follow descriptor and
tightened from 0664 to 0600; recovery is `idle/ready`. Launch only its distinct
window-5 bridge. It is now detached at `@22`/`%25`; supervisor, watcher, and
TUI are `585107/161615666`, `585358/161615704`, and `585489/161615816`.
Mode-0600 plain sender `/tmp/har394-submit-coldstart-r3.py` has SHA-256
`dbe64ea9fcd2e9d204c11de13e0f615a89d0e579acd64393a18b03d39ce8621d`.
Its lock, exact detached-pane, process, no-agent-envelope, and absent-journal
checks pass with zero writes. Submit it once.
