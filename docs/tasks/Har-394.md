task: Har-394
status: active
updated: 2026-08-05
---
# Har-394 — Recover the Personal Codex pane

## Diagnosed state

The TUI rejects turns against archived wrong-CWD thread
`019fca58-03d3-7392-a788-628bed221c3f`; zero-write app-server readback corrected
the earlier rollout-only absence hypothesis. No prompt was read or replayed.
Canonical Personal pane `%19` resumes clean Personal root
`019fc765-0206-7112-8293-146fb29e8087`, named `personal`. Its TUI target drift
selects bridge-first replacement; unarchiving the Harness-cwd root is invalid.

Frozen old identities are supervisor `67919/149176315`, TUI
`68361/149176502`, watcher `2503998/150148010`, pane `%19`/`@1.0`, and shared
app server `2852569/83381863`. Harness `%0`, Students, Claude, monitors, and the
recovery helper are out of mutation scope.

Personal Git is clean `main` equal to `origin/main`; its producer ledger passes
and selects idle. Harness protected packet PR #677 allocated this task. No
app-server, root-name, provisional-pane, input, promotion, archive, or signal
write has been sent, so the first bridge transaction remains safe to start.

## Pre-write protocol preflight

Schema bundle `/tmp/har394-schema.Rmcgyk` records protocol 0.145.0. The
mode-0600 controller
`/tmp/har394-appserver-controller.py` has SHA-256
`14a122f38dfefdadb61c6cc434dfa57e2a24a93f2fdfd830962d8e5630c3742c`.
Its corrected same-connection preflight passed with `writes=0`; SQLite agrees
on both roots' archive flags, cwd, source, and rollout paths.

## Fresh-root result

The digest-bound controller sent one initialized `thread/start` and one
provisional `thread/name/set`; both were acknowledged and are non-retryable.
New root `019fceb9-4557-7522-a197-4733147cf515` has exact Personal cwd,
interactive source, Sol/high defaults, frozen granular approval, disabled
sandbox, zero turns, idle status, and name `personal-har394-recovery`.
Mode-0600 journal `/tmp/har394-appserver-create.json` records the response and
retry boundary. The created rollout was exact device/inode `197/243090882`,
current-user owned, regular, single-link, and mode 0664. A no-follow descriptor
transaction revalidated those fields and changed only that inode to mode 0600;
recovery check then passed `idle/ready`. The old Personal TUI, root, pane,
app server, attached client, and all peers remain unchanged.

Provisional window `@20` index 3 and pane `%23` launched runtime
`personal-har394-recovery` at attempt zero. Its supervisor/TUI/watcher are
`414161`, `414753`, and `414479`; watcher state is `watching/thread-idle` with
zero recovery/rollback counters, exact Personal cwd, and a reciprocal peer to
the unchanged app server. The pane is detached and has no canonical role.
Mode-0600 sender `/tmp/har394-submit-coldstart.py` has SHA-256
`acadbbca6e3b9a2cdb40f619b71753e6d15ede0ee63a19fc6b51bf855bb11243`;
its exact target/lock/transport check passed with `writes=0`. An earlier sender
invocation failed before creating its journal or inserting input because its
local pane model expected an unavailable window field; that code was corrected
and no submission was made.

The corrected sender then submitted exactly one generic cold-start turn under
the shared lock. Mode-0600 journal `/tmp/har394-coldstart-submit.json` records
`submitted` and makes replay forbidden. The turn returned to idle and completed
without system error. Content-blind structural readback found one completed
turn, one user item, three assistant items, zero active items, but no tool
items. `itemsView=full`, so pagination did not omit them. The required terminal
tool evidence for repository/Git/ledger inspection is absent; the provisional
root is rejected and must not be promoted. No second turn, root, launch,
archive, retirement, or signal was attempted. The old Personal pane and every
unaffected identity remain unchanged.

## Frozen execution and acceptance

Create one fresh root with exact Personal cwd, granular approval, disabled
sandbox, and default model configuration. Read it with turns excluded, assign
one unique provisional name, tighten only an exact current-user single-link
0664 rollout to 0600 if necessary, and launch one distinct watched provisional
pane/runtime. Submit one generic cold-start turn that reads repository truth,
forbids replay, and remains idle. Every acknowledged request is non-retryable.

Accept an assistant-bearing completed turn, zero active items, no system error,
exact socket/process/root identities, and unchanged peers. Then move only the
frozen accepted pane into `harness:codex.0`, preserve the old root under a
blocked label, publish the reversible cutover checkpoint, and signal only the
revalidated old TUI leaf once with SIGTERM. Never restart the app server,
archive/delete a saved root, inspect/replay content, signal a group, or use
SIGKILL.

## Next action

The owner authorized one changed-input R2 fresh root with an explicit
tool-evidence cold start. Preserve the canonical and rejected R1 chains; never
send either another turn or replay either prompt. R2 uses distinct identities
and one cold-start turn. Promote only after full bridge acceptance and peer
preservation; otherwise preserve R2 and stop without old-leaf signaling.

The distinct mode-0600 R2 wrapper
`/tmp/har394-appserver-controller-r2.py` has SHA-256
`695bdc039da04d4eab920adf732f18ad0c5d6aba5e329440ca93b2c076768d5f`.
It binds the unchanged R1 controller and overrides only the R2 journal and
name `personal-har394-recovery2`. Both new journals and the name are absent;
window 4 is free, Personal Git is clean, and R1 remains at `@20`/`%23`. A fresh
zero-write preflight reads both preserved roots unchanged. Next is the sole R2
root/name transaction.

R2 root `019fcec5-16cd-7ad3-94ee-9a4900144dae` was created once at exact
Personal cwd and named `personal-har394-recovery2`; journal
`/tmp/har394-appserver-create-r2.json` is mode 0600 and retry-forbidden. Its
rollout device/inode `197/243095608` was revalidated through a no-follow
descriptor and tightened from 0664 to 0600. Recovery check is `idle/ready`.
The preserved roots, R1 runtime, peers, and repositories remain unchanged.

R2 launched detached at window `@21` index 4, pane `%24`. Supervisor, watcher,
and TUI identities are `504634/161553763`, `504878/161553802`, and
`504995/161553914`; recovery is `idle/ready` and the shared socket remains
mode 0600 inode `197:24090962`. The mode-0600 single-use sender
`/tmp/har394-submit-coldstart-r2.py` has SHA-256
`864464ea39d5735d9a840d73cce1484ed7314506a7bf11a5c0e839f01595a7d0`;
its exact detached-pane check passes with zero writes. Next is its sole
explicit-tool-evidence cold start.

The one-shot R2 journal is mode 0600 and retry-forbidden. R2 returned idle with
one completed turn, one user item, three assistant items, no active item or
system error, `itemsView=full`, and zero tool items. It therefore fails the
explicit terminal-tool-evidence gate and is rejected. No promotion, rename,
archive, retirement, or old-leaf signal occurred; all three chains remain
preserved. A newly frozen owner route is required.
