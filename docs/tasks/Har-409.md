task: Har-409
status: complete
updated: 2026-08-07
---
# Har-409 — Reconcile the failed Riken routes

The personal Riken Mac has no registered external maintenance/status source;
the fleet inventory records that field as not applicable, so no maintenance
absence was inferred. Fresh independent status discovery found both `riken`
routes ready and the managed checkout synchronized on clean `main`. The
connection monitor then reported `riken/riken2` healthy with both aliases ready
and `action=none`; no recovery mutation was required.

Riken value-free validation passed unattended authentication for both tunnel
aliases, found both managed launch agents loaded and running, and reported the
watchdog's latest outcome healthy. `macos-doctor` completed with zero failures
and zero warnings. The reboot status helper confirmed remote control and tmux
ready. Arg0 housekeeping separately reported one eligible item; it was not
applied because that cleanup is outside route recovery.

Final `harness fleet-health` reported Local, all Linux entries, and all four Mac
route pairs passing, including Riken at 2/2. No failed or unknown node remains.
Complete without configuration, process, repository, or remote-control writes.
