task: Har-414
status: active
updated: 2026-08-07
---
# Har-414 — Contain shared legacy Slack surfaces

Fresh Personal Codex and Claude inventories expose the isolated six-tool broker,
but both also advertise their broad global Slack integrations, including search
and write tools. These integrations are shared across repository sessions and
are therefore not uniquely superseded by Personal; Har-403 correctly preserved
them instead of performing a global revocation.

Har-403 has now accepted Personal, Swallow, and the credential-free Students
path. Official client documentation and installed-client metadata establish
the smallest reversible containment controls:

- Codex: disable exact app `asdk_app_69a1d78e929881919bba0dbda1f6436d` with
  project-scoped `apps.<id>.enabled=false`; preserve the installed plugin and
  unrelated apps.
- Claude: deny only `{ "serverName": "claude.ai Slack" }`; do not disable all
  Claude.ai connectors.

Harness is the canary. Its canonical Codex and Claude policies now include the
two exact controls, and the policy validator requires them. Focused agent
configuration and Claude takeover tests pass. A fresh Claude inventory changed
from one Slack match to zero while other connector policy stayed untouched.
Codex accepted the strict config and still lists the installed Slack plugin as
expected; installed-package presence is not tool enablement.

Rollback is the exact removal of one Codex dotted key and one Claude deny
entry. Account-level connector disconnect remains separate and must occur only
after this canary is published and freshly read back. Final readback must show
the hosted Slack integration absent in both clients without exposing account
or workspace values.

PR 734 merged the canary at `f60d331`; protected readback is clean. The user
Codex and Claude policies then received only the same exact deny entries.
Claude's pre-existing settings collision was preserved by a narrow merge rather
than canonical-file replacement. Fresh discovery from another admitted root
shows zero Claude Slack connectors, and ordinary Codex configuration loads the
new app setting. Codex strict mode still rejects an unrelated pre-existing key
and does not identify the Slack app key as the cause.

The owner reported both hosted disconnect actions complete. Codex's
force-refreshed installed-connector snapshot showed the target absent. Claude's
first readback showed the connector entry but did not retain enough health
metadata to distinguish registration from authentication. On the owner's
changed-input direction, one metadata-only recheck classified the entry as
authentication-required rather than connected, confirming the hosted
connection is disconnected. The temporary Claude inventory exception was
removed immediately after each check and the exact local deny was verified
restored. The generated Codex protocol schema was removed through a passing
guarded-delete manifest; no connector tool or Slack content was accessed.

Complete. The exact Harness and user-local client controls remain reversible;
the Slack plugin, broker services, internal Slack applications, Students
runtime, and unrelated connectors remain preserved.
