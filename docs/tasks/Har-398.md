task: Har-398
status: complete
updated: 2026-08-05
---
# Har-398 — Refresh portfolio READMEs

Update the READMEs in Harness, Personal, Students, Swallow, and Website from
each repository's own protected state, instructions, ledger, and validated
capabilities. Preserve repository independence and private-data boundaries.

Personal has pre-existing consumer-owned work in its primary checkout; do not
touch, stash, or include it. Use isolated repository branches and publish each
README independently. Documentation must describe the trust-equivalent
producer/consumer coordination used by sibling projects, the Harness-only
nightly executor, current task namespaces, and project-specific current state
without turning README prose into disposition authority.

## Acceptance

- Every README matches its repository structure and current validated scope.
- Volatile queue details are dated or routed to the authoritative selector.
- No private values, credentials, live writes, deployments, compute, or source
  reads occur.
- Each repository passes its own documentation, ledger, and publication gates.
- Protected readback matches each reviewed candidate and temporary branches and
  worktrees are removed without disturbing active Personal work.

## Outcome

All five READMEs were refreshed and published independently through protected
pull requests:

- Students PR 619 merged at `f63dfa0a`; its complete gate passed 1,182 tests
  and 81 subtests.
- Swallow PR 169 merged at `f93cdda6`; its complete render-only static gate
  passed.
- Website PR 61 merged at `8c768737`; its complete local security gate passed,
  and one hosted focus-test flake passed on the sole evidence-backed rerun.
- Personal PR 153 merged at `b89fa15f`; the complete validator body passed 130
  tests in an independent checkout with the unchanged exact-root guard restored
  before publication. The canonical checkout was then fast-forwarded while its
  unrelated Per-017 and Per-018 changes remained untouched.
- Harness documents its direct Har/Nit model and sole nightly-executor role in
  this task's final protected candidate.

Protected trees matched every reviewed sibling candidate. Generated test
caches and disposable task worktrees or clones were removed through guarded
cleanup, and merged remote task branches were absent or deleted. No private
values, live sources, deployments, compute jobs, or non-Git external writes
were used.

Final Harness integration also corrected the focused ledger assertion to match
the already-ready `Nit-001` nightly queue instead of incorrectly expecting the
nightly selector to be idle.

## Next action

None for Har-398.
