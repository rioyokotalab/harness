# T-353 — Graceful Claude Fable-to-Opus fallback

## Status

Phase: `validating`; implementation and clean committed R3 validation pass.
Final integrated validation, protected publication, fleet synchronization, and
safe Mac context refreshes remain.

## Frozen outcome

- Claude remains Fable/high by default.
- Opus/high is the sole ordered native fallback whenever Claude runs as the
  cowork driver or Codex invokes it as co-pilot.
- Claude Code performs the switch inside the original turn. Harness never
  launches a second client or replays a prompt to simulate fallback.
- Shared session or weekly usage limits, rate limits, authentication, billing,
  request-size, and transport failures remain failures. Preserve their native
  evidence and reset information rather than claiming that Opus can bypass
  them.

## Confirmed baseline

- The task branch `codex/t353-claude-fallback` uses durable T-352 checkpoint
  `b549e5c5dc8558738946bfb7539953d7c960ed4c` as its baseline so the pending
  Riken context-refresh record remains preserved.
- Claude Code `2.1.220` exposes `--model fable`, `--effort high`, and
  `--fallback-model opus`; its local help limits the CLI fallback flag to
  print mode.
- Anthropic's current settings contract accepts an ordered `fallbackModel`
  array for persistent sessions. Its model contract limits native fallback to
  overloaded, unavailable, and other eligible non-retryable server errors for
  one turn.
- Anthropic's current usage contract says subscription session and weekly
  limits are shared across models. No documented Fable-specific usage limit
  can be bypassed by selecting Opus.
- Both tracked Harness Claude settings mirrors previously selected Fable/high
  without a fallback chain. The cowork native templates likewise omitted one.
- The primary checkout's three unrelated live-held `.nfs` files and T-352's
  existing worktree remain outside this task.

## Decisions

- D-001: add `fallbackModel: ["opus"]` to both tracked project-setting mirrors
  and require the exact chain in the project validator.
- D-002: every reviewed Codex-to-Claude print command names
  `--model fable --effort high --fallback-model opus`; the persistent project
  profile supplies the same primary/fallback pair when Claude is driver.
- D-003: the task-bound handoff packet's existing `model: fable` remains the
  requested primary model, so no incompatible schema change is needed.
- D-004: a native fallback notice or emitted resolved-model field is evidence
  of a switch. Silence is not; record uncertainty rather than infer Opus use.
- D-005: do not expand root always-read policy. The focused cowork skill,
  launch reference, recovery reference, project settings, and public client
  documentation are the smallest applicable surfaces.

## Working set

- `.claude/settings.json`
- `config/agent-clients/claude.json`
- `libexec/harness-agent-config`
- `shared/skills/codex-claude-cowork/`
- `docs/agent-client-config.md`
- owning focused tests
- `TODO.md`, `docs/tasks/index.tsv`, and this record

## Validation

- The `skill-creator` structural validator passes.
- `claude --settings .claude/settings.json doctor` accepts the candidate
  settings under installed Claude Code `2.1.220`.
- Agent-config, agent-config-fleet, Claude takeover, cowork-skill,
  task-ledger routing, agent-policy routing, skill context budget/gates,
  skill-catalog budget, external-user onboarding, and validation-router focused
  tests pass.
- The routed cowork context remains 1,011 words, 82% below its legacy
  aggregate. Root policy and catalog budgets are unchanged.
- `git diff --check` passes.
- `harness validate --base origin/main --plan` selects R3
  `tests/test-phase1.sh`, in part because project settings and lifecycle policy
  changed.
- The first clean R3 run at `b6581d7` passed every listed suite except the
  mandatory generated context benchmark; native MPI remained the declared
  environment-specific skip. T-353 added 39 words to the routed cowork entry,
  111 to its native-client reference, 66 to recovery, and 41 to the active
  board. The stale T-352-era table therefore disagreed with deterministic
  current counts. No external action occurred and retry is safe.
- The regenerated table retains every threshold: median total reduction is
  96.4%, median non-ledger reduction is 47.2%, and the largest duration Cowork
  route remains 93.8% below its immutable baseline.
- The clean R3 retry passed every phase-one suite at `82e5615`; native MPI
  remained the declared environment-specific skip. Its exact-tree receipt is
  `/run/user/5035/harness-validation/86ef9e7c9addb71e80dd/4ed2860b37b1acc8acd01ddb8b5f3252805318a839bf3e362029aa3435590d4f--ffb54b5df2d7c17d9ff1344155d35f261efbb47b010d6895f2ddaad49401f7da.json`.

Pending:

- final integrated R3 validation after this ledger checkpoint;
- protected publication, guarded fleet synchronization, safe Mac context
  refreshes, and final fleet health.

## Next action

Create a coherent checkpoint, run clean committed R3 validation, correct only
demonstrated failures, then publish through protected `main`. After merge,
guarded-sync only clean managed Harness checkouts and submit one context
refresh to each safe detached Mac session; retain any proven-absent or
ambiguous delivery for a later safe opportunity.
