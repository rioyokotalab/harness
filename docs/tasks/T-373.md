# T-373 — Bound housekeeping archive growth

## Status

Active in isolated worktree `codex/t373-second-generation` after the byte
trigger was crossed by required exact closeouts. The report-only classifier
merged through PR #557 at
`e344915c1f2552d2d5c3d826d24ed58a6ffb826d` and the generation contract
merged through PR #558 at `17ec140cc935f114ada21acd65646378dd6ee507`.
The completed T-373 worktrees, Website T-212/T-214 handoffs, and T-369
continuations were independently guarded-archived and removed. The current
housekeeping state contains 847,444,982 bytes. All 27 archive receipts audit
successfully and all sixteen
worktree-apply records are complete, so this is bounded growth work rather than
incident recovery. No archive or receipt is presently classified disposable.
Twenty Harness-history bundles account for 263,059,543 bytes; size similarity
suggests repeated full-history packing, but is not evidence of safe redundancy.
The 27 receipts contain 52 item refs and 45 unique tips. Twenty-seven item refs
are ancestors of their current `origin/main`; 25 are not, including unpublished
handoffs and squash-merged task heads. Ancestry alone is therefore unsafe.
Twenty-seven item refs name 21 merged pull requests; after deriving each repository
slug from its own remote rather than assuming one organization, every archived
head has the exact tree of its recorded merge commit. This proves content
equivalence, not yet that receipts or commit-history evidence are disposable.
A non-retained benchmark bundled protected `main` plus all 39 Harness archive
refs into one verified 12,988,438-byte bundle, versus 184,262,769 bytes across
the 16 then-current Harness-history bundles: 171,274,331 bytes / 92.9% smaller. The
benchmark was guarded-deleted with protected anchors unchanged. This proves
consolidation potential, not a retention candidate set.

The report-only `archives` routine now audits and maps every receipt/item
without creating another plan. Its first live result is 20/45/39 receipts,
items, and unique tips; 7 archive-only items; 20 exact PR-tree matches; 29
ledger-referenced and 16 unreferenced item rows; zero unknowns; and zero
candidates. Evidence and the frozen phase-one retention decision are in
`docs/audits/t373-archive-retention-inventory.md`.

The published implementation freezes `harness-housekeeping-generation-v1` as
an audit-only contract. It binds every source receipt digest, owning repository
identity, protected `origin/main`, retained exact head, bundle digest, and a
deterministic head-set digest produced only after an independent bare-repository
fetch reproduces every head. The focused fixture performs that restore before
publishing its synthetic receipt. Immediately before live creation, inventory
reported zero generations and `generation_trigger=bytes`: 623,078,256 bytes,
25 receipts, and an oldest receipt below one day. The trigger authorized the
first creation, not bypassing its restore proof or deleting prior evidence.

A same-state follow-up batches exact ledger patterns without reading or
emitting source context and snapshots each repository's refs once. The live
report preserved all counts and reduced wall time from 24.32 to 18.50 seconds
(23.9%).

The first-generation creator requires a freshly measured trigger, audits every
source receipt, groups every exact tip by owning repository, includes current
protected main, and imports a bundle-only tip through its verified source
bundle when the object is absent locally. It creates a standalone bundle per
repository, fetches every generation head into an independent bare repository,
checks exact tips plus full object integrity, and guarded-removes the drill
tree. Immediately before publication it rechecks every source receipt digest
and protected-main tip. A failure removes newly created generation refs,
temporary/final bundles, and an unpublished receipt; no deletion of source
evidence exists. The focused fixture proves a triggered generation with a
bundle-only orphan, four exact heads, independent restore, report re-audit, and
no-trigger rejection. PR #560 published it at
`e752cdae070e597824e0eef2aff104e68813d753`.

The first live generation
`generation-20260801t222637z-3834971-8937fd` then succeeded: five owning
repositories, 48 exact heads, 98,998,863 bundle bytes, independent restore and
full object integrity for every repository, and one immutable 25-source
receipt. Report re-audit passes with state at 722,092,740 bytes and one
generation. That re-audit exposed a value-free classification defect:
generation refs masked seven archive-only tips by counting as ordinary refs.
The follow-up excludes both housekeeping namespaces from ordinary refs and
rejects an immediate duplicate generation when the latest generation already
covers identical source receipts and protected-main tips; a 30-day generation
age or changed source/main evidence permits the next generation.
The first live unchanged-input retry was rejected with that exact reason before
creating a ref, bundle, or receipt.

After the two T-373 closeout receipts changed the covered source set, a second
generation `generation-20260801t223710z-210357-fd5426` succeeded with five
repositories, 50 exact heads, 98,982,843 bundle bytes, and independent
restore/object checks. Fresh report re-audits both generations, all 27 source
receipts, 52 item rows, seven archive-only tips, and zero incomplete apply
transactions. State is 847,444,982 bytes. The first 25 original bundles occupy
484,447,933 bytes and are each covered by both generations. Their immutable
receipts still carry branch, classification, PR, and interrupted-closeout
metadata, so the compaction design retains every source receipt and changes
only how its exact tips are recovered.

The candidate implementation adds an immutable
`harness-housekeeping-compaction-v1` receipt and two explicit actions. Planning
accepts exactly one source receipt, audits its bundle, requires at least two
digest-bound generation receipts that each cover every source tip, and binds
the source repository, bundle digest, byte count, device/inode identity, and
exact tips. Applying re-audits the complete contract and performs one exact
non-recursive directory-relative unlink; it cannot recurse or select another
path. A compacted source audit then requires the original receipt plus both
generation receipts and uses their independently restored bundles as recovery
sources. Future generations can likewise import compacted tips from those
bundles. The focused fixture proves rejection with only one covering
generation, successful plan/apply after two, retained source metadata,
post-compaction audit/inventory, duplicate rejection, and a subsequent
generation sourced from compacted evidence. No live bundle has been removed.

## Objective

Prevent verified task-worktree archives from growing without bound while
preserving exact rollback, interrupted-closeout recovery, unpublished work,
and repository independence. Optimize future storage and audit cost, not a
one-time deletion count.

## Acceptance

- A read-only inventory maps every bundle, receipt, plan, manifest, apply
  state, exact tip, owning repository, ledger reference, age, reachability, and
  sole-copy status without inspecting Personal payloads.
- Retention is fail-closed: active/unpublished tips, incomplete transactions,
  referenced evidence, unknown ownership, and the newest independent recovery
  generation are never candidates.
- Any compaction preserves exact refs in two independently verified
  generations, proves both restores before cleanup, and uses an immutable
  digest- and inode-bound single-file receipt with post-delete archive and
  repository audits.
- Policy distinguishes redundant merged-task rollback from disaster-recovery
  generations and sets a measured size/age trigger; it does not use age or
  ancestry alone.
- Focused archive/housekeeping tests and clean Harness phase one pass. No
  remote branch, managed session, ruleset, approval count, credential, backup
  scheduler, or sibling repository is changed.

## Exact next action

Review and publish the compaction implementation, then run complete Harness
validation on the exact published tree. Only after publication, plan one live
source receipt, inspect the value-free receipt identity, apply that one exact
bundle, and independently re-audit the source, both generations, state
inventory, and owning repository before considering any additional bundle.
