task: Har-420
status: complete
updated: 2026-08-07
---
# Har-420 — Keep exact Slack targets inside the broker

The first Swallow live-acceptance preparation exposed that an exact-resource
profile still advertised a required `resource` argument. Calling the tool would
therefore require an agent or transcript to learn and relay the owner-only
channel identifier even though the broker already holds the binding. No Slack
content request was made.

For a validated single-resource exact profile, remove the bound resource from
channel, thread, and linked-object tool schemas. Reject caller-supplied target
arguments and inject the bound value only inside the service after request
validation and before provider dispatch. Keep workspace-visible profiles
unchanged: callers still provide their permitted resource. Preserve exact
authorization, provider link proof, untrusted-result marking, and write
absence.

Focused tests prove zero-target exact schemas, internal provider injection,
caller target rejection, and unchanged workspace-visible behavior. The
complete Slack suite and repository-selected integrated gate pass. No target,
credential, or Slack content enters repository bytes, output, or client
arguments. Har-420 is complete with the protected candidate.
