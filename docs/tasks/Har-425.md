task: Har-425
status: active
updated: 2026-08-08
phase: blocked-on-owner-choice
---
# Har-425 — Align the Riken phone Codex with the owner session

## Outcome and scope

Replace only Riken's canonical managed Harness Codex TUI so that the phone
surface resumes the exact owner-requested thread
`019fc0c7-f3e7-7e90-9d64-fceee8dfe161`. Preserve the existing saved root,
old TUI chain, app server, Claude window, attached tmux client, unrelated
sessions, credentials, and repository work until bridge acceptance. Do not
read pane or transcript content, replay a prompt, restart the shared app
server, archive or delete a root, or signal a process group.

## Pre-write checkpoint

- Riken logical host and exact Git root are `/Users/yokotar/harness`; canonical
  main was clean at `5403637fab61d5ac100a88cd2cad242643cdda8f`, 0/0 with
  `origin/main` after a fresh fetch.
- The requested thread's stored CWD is the exact Harness root. Its app-server
  metadata is active, source `cli`, unnamed, and has an existing rollout. No
  transcript or pane content was inspected.
- `codex-thread-recovery --plan` reports the requested root ready, with prompt
  replay disabled. `codex-resilient --plan --remote-session` accepts the exact
  target/thread pairing.
- The existing canonical runtime is `harness-codex-resume`, supervisor PID
  21070, TUI PID 21285, tmux session `harness`, window `@0`, pane `%0`; the
  Claude peer is window `@1`, pane `%1`. One attached client remains on the
  existing topology. The shared app server remains PID 27895 under daemon PID
  60462.
- The existing Harness TUI predates the requested thread and was launched with
  repository-relative `--last`, so exact-thread replacement is required. The
  requested root is distinct from the old TUI's prior root and is already
  assistant-bearing through this owner session; no replacement root may be
  invented because the owner explicitly requested this exact thread.

## Frozen transaction

1. Revalidate the requested root, canonical pane/process identities, app
   server, Git state, and unaffected Claude peer.
2. Launch one distinct provisional detached tmux window and runtime from the
   exact Harness root using `--target harness --remote-session` with the
   requested ID. Never reuse the canonical window or runtime.
3. Require watcher readiness, exact remote-session process identity, live TUI
   socket, unchanged app server, and no recovery rollback or retry.
4. Promote by tmux rename/move only after acceptance. Preserve the old saved
   root and rename the old window to a unique blocked label. Do not move or
   relaunch the provisional pane.
5. Publish the cutover checkpoint, then send one `SIGTERM` only to the
   revalidated old leaf TUI. Let its launcher and supervisor unwind. Never send
   a second signal or signal a zombie.
6. Run native doctor, focused recovery/resilience checks, Git checks, and
   canonical managed-session acceptance. Obtain the owner's physical-phone
   confirmation before declaring completion.

## Execution result

The exact-thread watcher became ready with `reason=thread-idle`, zero
recoveries, and zero rolled-back turns. Provisional window `@2`, pane `%2`,
runtime `har425-current-bridge`, and supervisor PID 27155 were created once.
The TUI never remained live: the supervisor recorded five transient exits and
entered bounded backoff. No promotion, root rename, app-server write, prompt
submission, or old-leaf signal occurred.

The provisional window was removed after exact identity revalidation. One
`SIGTERM` was sent to its surviving supervisor; after its in-flight backoff
sleep unwound, the watcher stopped with `reason=operator-stop` and the runtime
became owner-absent. The attached client was restored from the unchanged
Claude pane to canonical Codex pane `%0`. Canonical Codex `@0`/`%0`, Claude
`@1`/`%1`, the old phone TUI, the desired saved root, and the shared app server
remain preserved. No pane or transcript content was read.

Facts do not establish why the exact remote TUI exited. Concurrent ownership
by the active owner client is a hypothesis, not a verified cause. Retrying the
acknowledged bridge from this transaction is forbidden.

## Rollback and next action

Before promotion, remove only the unaccepted provisional window after exact
identity validation. After promotion, reverse only the window names while the
old leaf remains live. Ambiguous launch, promotion, signal, or app-server
acknowledgement is never retried. Exact next action: the owner chooses whether
to close the current client and authorize a new bridge transaction, or retain
the current phone agent. A new attempt requires a new frozen launch identity;
it must not reuse or replay this failed transaction.
