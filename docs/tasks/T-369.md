# T-369 — Evidence-first portfolio reset and guarded cleanup

## Status

Active. Owner-authorized on 2026-08-01 for Harness, Students, Swallow,
Website, and Personal through 2026-08-02 12:00 Asia/Tokyo. Protected reset,
worktree recovery, all five portfolio publications, capability-lifecycle
follow-ups, complete temporary-state audit, recovery drills, owner-local CI
routing, parallel-safe exact closeout, two restore-proven archive generations,
dual-covered compaction, and delta-based generation triggers are implemented.
Thirteen hourly slices are complete; slice 14 and final handoff remain.

## Frozen objective

Judge whether each repository is pursuing the highest-value work supported by
its purpose and current evidence. Replace completed, duplicate, superseded,
non-actionable, or low-opportunity-cost board entries with smaller actionable
queues. Remove only temporary files, worktrees, and branches independently
proven reconstructable and safe under each repository's own policy.

This is a portfolio reset, not an instruction to erase durable history,
credentials, private source data, active work, live runtime state, or evidence
needed to reconstruct a decision. GitHub backup is supporting evidence, not a
substitute for verifying that local or unpublished work is recoverable.

## Scope and independence

- Coordinating repository: `/home/rioyokota/harness`.
- Independent targets: Students, Swallow, Website, and Personal at their
  canonical repository roots discovered from disk.
- Each target keeps its closest instructions, ledger, branch, worktree,
  validation, publication route, locks, and authority boundaries.
- Website remains independent of Harness. Personal source contents that may
  contain email, calendar, budget, credentials, or other sensitive values are
  not inspected; only repository metadata and declared contracts are in scope.
- No deployment, hosting administration, account mutation, credential access,
  external message, live research run, or managed-session mutation is implied.

## Frozen benchmark

For every repository, acceptance requires all applicable rows:

1. The active board contains only high-value work or a material explicit
   event/authority gate; completed, duplicate, superseded, and speculative
   entries are absent from the active queue but remain durably auditable.
2. Every retained or new item identifies an outcome, first executable action,
   evidence/acceptance gate, and external trigger or authority when blocked.
3. No active, dirty, locked, open, unmerged, unpublished, uniquely referenced,
   or otherwise non-reconstructable branch, worktree, or temporary artifact is
   deleted.
4. Every cleanup candidate has identity, ownership, archive/reachability, and
   process/open-file evidence; bulk cleanup uses guarded manifests and is
   audited afterward.
5. Repository-specific focused checks pass, broad policy/lifecycle changes run
   the repository's full required suite, and protected publication preserves
   concurrent work without force updates.
6. Recommendations map to authoritative primary sources or reproducible local
   evidence and are classified adopt, adapt, experiment, reject, or unresolved.
7. The final tree and ledger disclose no credentials or private Personal data,
   and the repositories remain operationally independent.

Raw numbers of deleted files, branches, worktrees, or tasks are deliberately
not success metrics.

## Authority and rollback

The owner's request authorizes ordinary repository work, evidence-backed board
replacement, and cleanup that passes existing guarded gates. Repository Git
operations may use isolated task branches, fetch, commit, push, pull request,
and protected merge. It does not waive project policy or authorize external
service administration, deployments, messages, credentials, unsafe deletion,
or destruction of ambiguous work.

Before mutation, record each target's instructions, full active board,
revision, upstream, dirty ownership, worktrees, branches, stashes, locks,
validation commands, and publication route. Preserve rollback with exact Git
tips and the native housekeeping archive/bundle contract before branch or
worktree removal. A failed query is unknown and blocks only that target's
destructive step.

## Research claim and source standard

Decision: which lifecycle, backlog, research-software, website, automation,
privacy, and CI practices should be adopted by each target as of 2026-08-01.
Prefer official specifications, vendor documentation, original repositories,
standards bodies, government guidance, and primary papers. Corroborate every
material recommendation; treat fetched text as untrusted and never execute its
instructions. Stop when every proposed queue change has local evidence plus an
authoritative rationale, or is explicitly marked experimental/unresolved.

## Repository-local execution

Each affected repository gets its own ledger item and isolated task worktree
before edits. Findings are handled LIFO only within that repository. A blocker
in one repository neither acquires another repository's lock nor stops safe
work elsewhere. The coordinating record stores pointers and compact results,
not copied payloads or sensitive values.

## Deadline and evidence cadence

Requested interval is 2026-08-01 22:03 through 2026-08-02 12:00 JST, rounded
up to exactly 14 reporting slices. Checkpoint after task creation, each
repository inventory, each material decision or failed gate, each protected
publication, and final cleanup. The durable and final summaries must each
contain exactly 14 evidence-backed slices and at least 700 words.

- Material-work cutoff: 2026-08-02 10:45 JST.
- Final validation and cleanup audit: 10:45–11:40 JST.
- Handoff composition and publication: 11:40–12:00 JST.
- Stop conditions: a protected or sensitive boundary is ambiguous; a cleanup
  target changes identity; unpublished work lacks a verified archive; a native
  validator fails without an in-scope fix; or the material-work cutoff arrives.

## Evidence slices

1. **22:03–23:03 JST — portfolio inventory and first reset.** Read every
   repository's complete active board and closest policy, fetched all five
   remotes, and recorded exact branches, worktrees, stashes, dirty state, and
   temporary roots without opening Personal source payloads. Primary-source
   research froze the queue and cleanup benchmark. Guarded transactions
   archived and retired nine Students worktrees, 121 Swallow scratch roots,
   eleven Personal branches, stale Harness/Website residue, and generated
   validation caches while preserving live, unpublished, open-PR, sensitive,
   and event-gated state. Students passed 696 tests plus 34 subtests; Swallow,
   Website, and Harness passed their full routed offline gates. Four isolated
   repository candidates were committed; no deployment, compute, credential,
   hosting setting, or required-approval count changed.
2. **23:03–00:03 JST — lifecycle enforcement and independent recovery review.**
   Harness T-370 merged through PR #551 after clean local and protected phase
   one, then opened and guarded-retired real Personal task worktrees. Personal
   PR #18 added a value-free connector/revocation inventory and 22 synthetic
   recovery tests; a current-source freshness pass found that the pure partial
   grant classifier was not wired to desktop consent, so PR #19 atomically
   retained only granted scopes and passed 24 canonical tests. Neither task
   accessed a credential, connector, consent, or provider API. Harness PR #552
   recorded T-371's safe idle/cold-restart gate and current NIST, Google,
   GitHub, Slack, and W3C findings. A fresh inventory found 98 Swallow remote
   branches but policy kept every remote ref report-only; the live SW034 local
   tip remains ahead of its same-named remote and was preserved. Independent
   code review then disproved T-370's implied interruption-completion claim:
   checkpoints existed but no later invocation could consume them. T-372 now
   performs exact receipt/archive/admin/branch recovery, rejects changed tips
   and symlink receipts, preserves unstarted bulk candidates, passes focused
   tests, and passed clean local Harness R3 phase one.
3. **00:03–01:03 JST — recovery publication, complete temporary-state audit,
   and leaked-client fix.** Harness PR #553 merged exact T-372 recovery bytes;
   a real completed receipt then returned the expected idempotent no-op.
   Website's validated worker tip and the unpublished Students/Swallow tips
   were independently preserved in verified private bundles, while fresh
   ruleset readback kept all five repositories at zero required approvals.
   All five object databases and worktree administrations passed integrity
   checks; all 17 native archive receipts re-audited cleanly. A full owner
   `/tmp` inventory expanded the initial project-name sweep from 20 files to
   613 entries. Guarded transactions removed 599 stale entries—501.3 MB in
   the main transaction plus two dead SSH-agent directories—while privately
   archiving useful research/test bytes and deliberately not copying browser
   or opaque temporary state. Fourteen justified runtime/handoff roots and
   three other-user research roots remain. An open-unlinked audit then found
   and terminated 18 exact orphaned synthetic resilient-client launchers. The
   background remote launcher now replaces its wrapper with `exec`; two
   focused runs pass with zero residual PIDs. Dirty-tree R3 passed the changed
   suite and reached only the two expected clean-checkout test gates.
4. **01:03–02:03 JST — protected consolidation and residual-state
   closeout.** Harness PR #554 merged exact head
   `0a14bacf880146e9685a21859e8293d2768f30a3` as protected squash
   `00443d4c2ef10311cf34c15ca2ae8dca3c6ad444`; its hosted phase one and
   clean local R3 both passed. The completed task worktree was then retired
   through a guarded receipt, its recovery command returned an idempotent
   no-op, and T-370 opened the present continuation from the exact protected
   main tip. A metadata-only residual sweep archived and removed T-303's
   completed 166.9 MB NFS observation tree from `/var/tmp`, removed five
   unopened July OpenMPI segments from `/dev/shm`, and unlinked one stale
   Students temporary object after object-database integrity passed. All 18
   native archive receipts and all five repositories' full object databases
   re-audited cleanly. The owner temporary boundaries remained 14/0/0 across
   `/tmp`, `/var/tmp`, and `/dev/shm`, with no resilient-client fixture process
   surviving. Fresh ruleset readback kept every repository active at zero
   required approvals with its owner bypass, and workflow review found no
   nightly or push automation reintroduced. Remote branches remain report-only:
   Harness currently has main plus six merged-task refs, Students has four
   refs with Dependabot PR #560 as its only open pull request, Swallow has 98
   refs and no pull request, and Website and Personal each have only main.
5. **02:03–03:03 JST — steady-state portfolio observation.** Recorded slice
   4 locally and passed its routed clean-tree R0 ledger check without spending
   another hosted Actions run. Bounded checks throughout the hour found no new
   cleanup candidate, stale worktree administration, synthetic resilient-client
   leak, queue trigger, or canonical checkout movement. Harness, Students,
   Website, Personal, and both isolated candidates remained clean; Swallow
   retained only its known uninspected local settings file. Owner temporary
   roots held at 14/0/0 across `/tmp`, `/var/tmp`, and `/dev/shm`. Fresh remote
   readback preserved branch counts at 7/4/98/1/1 and open pull-request counts
   at 0/1/0/0/0, with Students Dependabot PR #560 still the sole open request.
   All five protected rulesets remained active with one owner bypass and zero
   required approvals. No evidence changed the compact queue judgments, so no
   project mutation, publication, deletion, or external write was justified.
6. **03:03–04:03 JST — interval-stability confirmation.** Committed slice 5
   locally and passed both dirty-tree and clean-tree routed R0 checks. Two
   interval-separated portfolio probes found canonical tips unchanged, all
   worktree prune dry-runs empty, and the continuation plus Website candidate
   clean; Swallow's single preserved settings file remained the only known
   canonical dirt. Owner temporary roots again measured 14/0/0, and process
   review distinguished the expected live Harness resilient launcher from the
   eliminated synthetic fixtures. Hour-boundary remote reads again returned
   branch counts 7/4/98/1/1, open pull-request counts 0/1/0/0/0, and five
   active protected rulesets with one owner bypass and zero approvals. This
   second stable interval confirms that no queue, cleanup, recovery, CI, or
   publication action became newly justified; unchanged validation and source
   evidence were reused.
7. **04:03–05:03 JST — execution-strategy correction and newly eligible
   backup evidence.** Stable local probes again found no repository or
   temporary-state change, but the owner correctly challenged the low-value
   observation loop. Re-reading the compact queue exposed a missed executable
   gate: T-196's recorded 2026-08-02 backup successors were now due. Exact
   native history reads proved Local `94950`, AB `2064918.pbs1`, AB2
   `2064919.pbs1`, RC `260847`, and T4 `8270230` terminal-success with their
   expected owner and deterministic name. RI's first `sacct` attempt failed
   Slurm DNS discovery and remained unknown at the boundary. No job was
   submitted, retried, canceled, or changed. This finding ended repetitive
   minute-level status reporting and redirected the remaining material window
   toward eligible backup acceptance, a missing ABCI-Q skill route, archive
   restoration drills, retained-branch classification, and isolation tests.
8. **05:03–06:03 JST — recovery proof, latency removal, and parallel-safe
   closeout.** RI's changed-input native accounting retry proved successor
   `10386` successful. A new routed ABCI-Q reference added maintenance-first
   PBS handling; after the official status page reported System H available,
   remote JSON filtering proved `176525.qjcm` had the expected owner/name and
   `Exit_status=0` without exporting its environment. Seven successors are now
   scheduler-successful; AL remains correctly time-gated to 08:00 JST, and no
   scheduler, Restic, credential, or chain state was changed. Independent bare
   restores matched the exact Students, Swallow, and Website bundle tips and
   passed full object checks. A fresh mirror classified all 106 non-main remote
   refs—24 merged and 82 divergent/unique—without deletion, then was guarded-
   removed. PR #555 merged owner-local Harness CI routing as `c587cb4`; its
   required job skipped with zero runtime, eliminating a measured 131-second
   median owner wait while preserving non-owner and weekly/manual gates. Live
   isolation found seven distinct worktrees, no path collision, alternate,
   lock, or stale administration. A broad closeout plan exposed, but did not
   apply, a parallel-tree selection hazard; exact `--path` selection is now
   receipt-bound and a fixture proves the other eligible tree survives. All 19
   archive receipts and eight completed worktree transactions audit cleanly.
   Deterministic context refresh and actionable ledger errors replace repeated
   manual failure diagnosis; final R3 passes. T-373 records 409 MB of healthy
   archive state and a non-retained verified 40-ref bundle benchmark that is
   92.9% smaller, with no real archive deleted.
9. **06:03–07:03 JST — archive contract, final portfolio publication, and
   latency follow-up.** The owner correctly challenged the preceding
   observation-heavy cadence; execution shifted to finished outcomes. PR #556
   first published exact-path worktree closeout, deterministic context refresh,
   and actionable ledger failures. PR #557 then merged a report-only archive
   inventory that audits every receipt, item, bundle, plan, manifest, apply
   state, evidence payload, repository owner, exact PR tree, and ledger
   reference without creating a plan or exposing Personal values. A synthetic
   consolidation benchmark proved 92.9% size reduction but no real archive was
   deleted. PR #558 froze an immutable generation receipt and independent bare
   restore proof while keeping production generation creation/deletion absent;
   complete 100-suite R3 validation passed for both changes, and their owner
   hosted jobs skipped with zero runtime. Both completed T-373 worktrees were
   independently archived and guarded-retired. Exact fixed-string batching and
   one ref snapshot per repository preserved all report counts while reducing
   same-state wall time from 24.32 to 18.50 seconds (23.9%). Website's final
   local T-212 handoff then passed the complete offline suite and required
   5m26s hosted gate, merged through PR #38 as `3573e75`, and was exact-
   archived and guarded-retired without deployment. All five portfolio queues
   are therefore protected-main publications. Current archive state is
   529,056,189 bytes, 23 valid receipts, 48 items/41 unique tips, 12 complete
   worktree transactions, zero generations, and 7,814,723 bytes below the
   512 MiB trigger. All five object databases and worktree administrations
   pass, temporary boundaries are 13/0/0, and no ruleset, approval count,
   managed session, credential, scheduler, or site byte changed. The 5m26s
   Website wait exposed a final efficiency opportunity: local commit `6636494`
   now keeps the required offline gate for every PR but skips locked-browser
   setup only for an explicit, NUL-safe, rename-safe, fail-closed
   ledger/documentation-only path set. Nine scope tests, workflow security, and
   the complete offline suite pass; protected publication is next.
10. **07:03–08:03 JST — scoped CI, restore generations, and verified archive
    compaction.** Website PR #39 (`5849ffaa`) made locked-browser setup
    conditional on an explicit, NUL-safe, rename-safe, fail-closed
    documentation/ledger path classifier while preserving the required policy
    suite on every pull request. PR #40 (`e22c8dc`) supplied protected hosted
    proof: the workflow-changing run passed in 5m02s, while two documentation-
    only runs passed the same required gate in 23s and 25s with browser work
    skipped, a 92.4% reduction and no deployment. Harness PR #559 (`39937c4`)
    published slice 9 and the archive trigger; PR #560 (`e752cda`) published a
    first-generation creator that audits every source, includes protected main,
    independently restores each repository, checks exact heads and full object
    integrity, and rolls back unpublished artifacts on failure. Two live
    generations now cover 25 then-current receipts across five repositories:
    `generation-20260801t222637z-3834971-8937fd` has 48 heads and 98,998,863
    bundle bytes; `generation-20260801t223710z-210357-fd5426` has 50 heads and
    98,982,843 bytes. PR #561 (`bd0f700`) preserved seven archive-only tips by
    excluding generation refs from normal refs and rejected unchanged-input
    duplicate generation creation. PR #562 (`7ab4bb6`) then published an
    immutable exact-source compaction contract: two digest-bound covering
    generations, source and repository identity, exact tip set, bundle digest,
    bytes, and device/inode are revalidated before one directory-relative
    non-recursive unlink; original metadata receipts remain and later audits
    and generations recover through the verified generation bundles. The
    focused end-to-end fixture and three complete 100-suite R3 runs passed;
    owner-hosted Harness jobs skipped with zero runtime. The first live source
    was independently audited after apply, both generations remained valid,
    `git fsck --full --no-dangling` passed, and inventory preserved 27 receipts,
    52 items, 45 unique tips, seven archive-only tips, and zero unknowns.
    Fifteen exact dual-covered source bundles totaling 197,146,306 bytes were
    subsequently compacted with no receipt or tip removed. The AL gate opened
    at 08:00 JST; exact job `4275926` matched recorded name `hal2608020100` and
    owner but remained `RUNNING`, so the chain stayed 2/8 with no retry,
    successor query, scheduler write, snapshot access, or private-state change.
11. **08:03–09:03 JST — compaction completion, protected closeouts, and
    residue correction.** The remaining ten dual-covered source bundles
    completed the immutable compaction gate, bringing the exact total to 25
    bundles and 484,447,933 retired bytes. Every original receipt and tip
    remains recoverable from both independently restored generations; the
    post-compaction baseline fell from 847,444,982 to 363,028,727 bytes. PR
    #564 (`0ece66b`) closed T-373 after those proofs. PR #565 (`7e67b6a`)
    then bound four exact pre-native portfolio bundles and Swallow's separate
    untracked-evidence payload to this ledger without opening Personal or
    Swallow payload values. PR #566 (`a21d87c`) fixed the lifetime receipt
    trigger: current generation need is now measured only from exact source
    name/digest growth absent from the newest valid generation. The latest
    full report passes with 33 receipts, 58 items, 51 unique tips, eight
    archive-only tips, 25 compactions, two valid generations, zero unknown or
    incomplete applies, and no candidate. Six uncovered receipts total
    136,792,663 source-bundle bytes; the delta trigger remains off and total
    archive state is 499,855,448 bytes, so no third generation was created.
    Students PR #563 (`d7c2b000`) reconciled the current Codex 0.146.0 pin
    with original Dependabot authorship after 696 tests plus 34 subtests; the
    superseded PR #560 was closed and the live Students checkout was not
    moved. Website PR #41 (`bc25516`) corrected its protected board after a
    21-second required documentation-only gate, with no browser run or
    deployment, then its exact worktree was archived as
    `worktree-archive-20260801t235044z-2211107-d1bd3f` and guarded-retired.
    A five-repository readback found every object database sound and every
    open-PR list empty; only the intentionally preserved live Students and
    Swallow branches differ from protected main. All five rulesets remain
    active with zero required approvals and one owner bypass, and workflow
    readback retains only weekly/manual full backstops plus scoped PR gates.
    Dedicated Codex helper housekeeping guarded-removed 307 completed entries
    while preserving 19 live locks; a second plan found zero eligible or
    unexpected entries. A separate 9,254-entry, 76,890,655-byte completed
    pytest scratch root had no owner process; its two current-user-owned
    mode-000 fixture directories were made readable, then the exact root was
    manifest-bound, guarded-deleted, and verified absent. Fourteen justified
    runtime, cache, lock, socket, SSH, monitor, and active-task entries remain
    under `/tmp`; `/var/tmp` and `/dev/shm` have none. T-374's stale
    post-completion instruction was corrected to its actual protected merge
    and archive-first closeout. No AL retry, scheduler write, deployment,
    managed-session mutation, credential access, ruleset write, approval-count
    change, generation creation, or remote-branch deletion occurred.
12. **09:03–10:03 JST — independent recovery coverage and final audit
    reconciliation.** PR #567 published slice 11 and T-374's corrected
    closeout as protected `f429761`; exact PR identity matched and the required
    owner job skipped with zero runtime. All six source bundles absent from the
    newest generation—three Harness closeouts plus Students T-053, Harness
    T-374, and Website T-212 board closeout—were then verified, independently
    mirrored into bare repositories, resolved to their recorded exact tips,
    and passed full object integrity. The two disposable restore roots totaled
    139,027,991 bytes and were separately manifest-bound, guarded-deleted, and
    verified absent. All five retained evidence payloads passed gzip stream
    integrity and reproduced the exact SHA-256 values already tracked in the
    audit without listing or opening payload contents. Handoff-time web
    resolution reached fourteen of fifteen sampled primary sources directly;
    ACM's canonical artifact-policy URL returned bot-protection HTTP 403, while
    the current official SIGSIM PADS 2026 page resolved and independently
    exposed the same functional-artifact criteria. The comprehensive audit now
    has one comparable five-repository disposition table, distinguishes three
    stale checkpoint statements from current state, records the protected
    dependency/CI/archive outcomes, and retains source links without executing
    fetched instructions. A 09:30 mutable sample found all PR lists empty,
    Harness/Website/Personal clean on `main`, Students clean on its preserved
    live branch, Swallow unchanged with only its known uninspected settings
    file, and Codex helper state at 19 live with zero eligible or unexpected.
    At the hour boundary, four newly completed helper roots became eligible;
    the dedicated workflow quarantined and guarded-removed exactly those four
    entries, preserving all 19 live roots. The first parallel NFS boundary
    read stalled and its yielded outputs were unavailable; those exact commands
    were not replayed. Different sequential Git plumbing then proved the same
    repository dirt/branch state, and direct GitHub API reads returned zero
    open PRs for all five repositories. The archive report's unchanged bytes
    were reused because no archive source, receipt, generation, or compaction
    changed. No AL query, scheduler write, deployment, managed-session
    mutation, payload read, credential access, ruleset write, approval-count
    change, generation creation, or remote-branch deletion occurred.
13. **10:03–11:03 JST — material cutoff and final integration validation.**
    PR #568 published slice 12 and the reconciled portfolio audit as protected
    `dfa97eb`; exact identity matched and the required owner job skipped with
    zero runtime. Material work stopped at 10:45. The complete protected-tree
    phase-one runner then passed all 95 focused suites plus its aggregate
    guarded-delete, static, routing, safety, and ShellCheck gates; native MPI
    emitted only its declared skip outside a configured MPI environment. The
    five repositories were checked sequentially after a parallel NFS probe had
    proven counterproductive: `git fsck --full --no-dangling` and worktree
    prune dry-runs passed everywhere, with no stale administration. Harness,
    Website, and Personal are clean on their protected tips; Students is clean
    on its preserved live `2f877189...` branch; Swallow remains on preserved
    `ec809eaf...` with only its known uninspected settings file. Fresh GitHub
    API readback found zero open PRs and confirmed active rulesets
    19127355/19716717/19734040/19127356/20104679, each with zero required
    approvals and one owner bypass. Final protected tips and TODO SHA-256
    fingerprints are Harness `dfa97eb`/`3b269e0f...`, Students
    `d7c2b000`/`5cea02fc...`, Swallow `042ab01f`/`951348aa...`, Website
    `bc25516`/`ee69b9a6...`, and Personal `aec19c6`/`c5b21c57...`.
    The archive report passes with 33 receipts, 58 items, 51 unique tips,
    eight archive-only tips, 25 compactions, 484,447,933 retired bytes, two
    valid generations, six uncovered receipts/136,792,663 source-bundle bytes,
    no candidate/unknown/incomplete apply, trigger off, and 499,865,781 total
    state bytes. Validation produced six completed Codex helper roots; the
    dedicated workflow guarded-removed exactly those six and a fresh plan
    returned 19 live, zero eligible, and zero unexpected. Thirteen justified
    runtime/cache/lock/socket entries remain under `/tmp`; current-user
    `/var/tmp` and `/dev/shm` remain empty. Harness's ignored 358-byte
    `node-backups/` is seven declared node symlinks and was preserved. No
    post-cutoff feature or optimization change, AL query, scheduler write,
    deployment, managed-session mutation, payload read, credential access,
    ruleset write, approval-count change, generation creation, remote-branch
    deletion, or sibling-repository write occurred.
14. Pending.

## Immediate next action

Compose slice 14 and the complete handoff without changing validated behavior.
Publish the final documentation-only tree, verify protected readback, then
archive-first retire the exact T-369 worktree and report its resulting receipt
and archive-trigger state. Do not retry AL `4275926`, move the live Students or
Swallow sessions, create a generation while the delta trigger is off, or
delete a remote branch.
