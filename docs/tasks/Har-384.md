# Har-384 — Relocate Students and Swallow roots under `projects`

## Status

Active on 2026-08-03. The producer packet is published on protected `main` by
PR #591 at `457b8783ce1d9636b20d3049296fa159abf08353`.

## Frozen paths and authority

| Project | Accepted source | Accepted destination |
| --- | --- | --- |
| Students | `/mnt/nfs-03/safe/Users/rioyokota/codex-workspaces/students` | `/mnt/nfs-03/safe/Users/rioyokota/projects/students` |
| Swallow | `/mnt/nfs-03/safe/Users/rioyokota/codex-workspaces/swallow` | `/mnt/nfs-03/safe/Users/rioyokota/projects/swallow` |

The owner explicitly authorized deletion of the pre-existing Students
destination and requested both canonical projects under `projects`. This
covers the narrow destination retirement, copies and final source retirement,
required owner-global launch-sentinel and trust-path edits, and managed-client
cutover. It does not authorize reading private payloads, panes, transcripts,
credentials, unrelated repositories, or external services.

## Reconciled starting state

- Harness protected `main` was clean and equal to `origin/main` before task
  allocation.
- Both canonical project roots are clean protected `main` checkouts.
- Swallow has one registered worktree. Students has its canonical worktree and
  the locked Har-371 preservation worktree at
  `/mnt/nfs-03/safe/Users/rioyokota/projects/students-controller/har371-t043-preserved`.
- The pre-existing Students destination is a separate stale checkout, 86
  commits behind with no reported ahead commit and untracked/ignored state.
  Its historical unique-state concern is superseded by the owner's exact
  disposal decision; content was not inspected.
- The Swallow destination is absent.
- Local managed Students Codex and Claude panes are alive and rooted at the
  source. Swallow has no assigned managed pane. Pane and transcript content
  was not read.
- Live Harness target profiles and both generated launch sentinels still name
  the Students source; both sentinels and Codex trust mapping also name the
  Swallow source.

## Ordered execution and rollback

1. Guard-delete only the disposable stale Students destination after a fresh
   manifest proves exact identity, ownership, size, and target.
2. Publish Harness target/sentinel/test changes while retaining both source
   roots and old owner-global entries for bridge rollback.
3. Copy each canonical repository into its destination, compare complete tree
   metadata/content without reading payloads, repair linked worktree metadata,
   and validate Git and repository ledgers.
4. Add new owner-global sentinel/trust entries, fleet-sync the published
   control plane, and bridge-first replace assigned Students clients. Physical
   phone visibility is the acceptance gate before old-client retirement.
5. Prove zero old-root references, remove old entries, guard-delete only the
   accepted old roots, validate all five surfaces, and publish closeout.

Until step 4 acceptance, rollback is selection of the retained old roots and
reversion of the Harness control-plane change. A failed copy, worktree repair,
launch, visibility acknowledgement, or zero-reference check stops without
retiring a source.

## Next action

The guarded plan recorded one exact target with 5,689 entries and 99,666,781
bytes. Its apply revalidated the manifest and prepared owner directories. An
early readback observed partial progress, but a subsequent fresh-plan attempt
failed closed because the target had become absent; final metadata readback
confirmed removal. The canonical Students source was untouched. The completed
mode-0600 manifest is retained privately at
`/mnt/nfs-03/safe/Users/rioyokota/projects/.har384-stale-students-delete.completed.manifest`
until closeout.

Both canonical trees were copied with `cp -a --reflink=auto` while their
sources remained available. The copies completed after bounded NFS RPC waits;
private mode-0600 diff logs reported zero lines and zero bytes for both full
tree comparisons and were exact-unlinked. Students measured 92,962,187 bytes
at both roots before worktree repair; Swallow measured 12,979,411 bytes.

The locked Har-371 Students worktree was repaired against the new Git common
directory and now resolves that exact destination. Both destination roots are
clean `main` checkouts equal to their configured origins.

Harness live target profiles and owning fixtures now select the new Students
root. Both generated client sentinels admit the new Students and Swallow roots
while retaining the old roots explicitly as Har-384 bridge-only rollback
entries. New Codex trust entries are present locally while old trust entries
remain for the same bridge interval.

Focused target, Codex monitor, Claude monitor, recovery-helper, and takeover
suites passed. `harness validate --phase 1` was a no-write invalid CLI attempt;
the repository-native `tests/test-phase1.sh` correction passed, with only its
declared native-MPI environment skip.

Next, publish the transitional control plane through protected Git, fleet-sync
the merged revision, then create and accept fresh destination-root Students
clients before any old client or source retirement. Historical plans and
records retain their original paths.

## Transitional rollout and Codex bridge

PR #592 merged the transitional control plane at
`0108220e376ea8e335cf5a77d6e6e6f47e1530b3`. Guarded fleet sync advanced six
Linux checkouts and all four managed Macs to that exact revision. A required
one-shot Mac Codex context-refresh delivery was attempted on each Mac after
pre-write metadata validation; the aggregate local wrapper returned success
without the required `status=submitted` evidence. Treat all four deliveries as
ambiguous and never retry them. The private value-free input remains at
`/tmp/har384-mac-context-refresh.txt` pending closeout.

One digest-bound Codex controller passed no-write preflight and sent exactly
one destination-root `thread/start`. The app server acknowledged root
`019fc598-a21d-7651-b03f-278ec56b449c` with resolved Sol/high, granular
approval, disabled sandbox, interactive source, and zero turns. One
provisional name request was sent; readback proves exact persisted name
`students-har384`. Neither write may be retried.

The controller then refused because the database leaves model and effort null,
the already accepted product behavior for a newly created root. Its rollout is
current-user-owned, single-link, canonical, and mode `0664`. The first
provisional launch failed closed before a pane survived because the watcher
requires mode `0600`; supervisor status is stopped with reason
`thread-recovery-blocked`. No prompt or turn exists and the old Students pane
remains authoritative.

Next, apply the previously proven descriptor-identity transaction to tighten
only that exact rollout from `0664` to `0600`, then perform one changed-input
provisional TUI launch. Do not repeat the thread start or name request.

The descriptor transaction passed no-write preflight, opened the exact
current-user-owned single-link rollout without following links, revalidated
device/inode/mode through the descriptor, and changed only `0664` to `0600`.
Recovery check then reported the new thread idle and ready.

Changed-input provisional Codex pane `%16` is live at the destination with
thread `019fc598-a21d-7651-b03f-278ec56b449c`, attempt zero, a watching
recovery peer, and no rollback. Exactly one generic cold-start reconstruction
instruction was submitted under the shared lock; it forbids prior-prompt
replay and all writes, and value-free recovery readback returned idle.

Fresh provisional Claude pane `%17` is live at the destination with exact
session `02586099-b2ea-4563-896c-a129f6ae290b`, Fable/high and Opus fallback,
remote-control name `students-har384`, and a current-user mode-0600 session
record. Exactly one equivalent generic cold-start instruction was submitted
under the shared lock. Neither submission may be repeated.

Both old Students panes and both old roots remain live. The current gate is
the owner's physical-phone confirmation that `students-har384` appears in
both Codex and Claude. Before that confirmation, do not promote panes, rename
or archive roots, signal old processes, remove bridge entries, or retire
sources.

## Physical-phone result

The owner confirmed `students-har384` is visible in Claude but absent in
Codex. Claude therefore passes its physical gate; its old pane remains
preserved until joint promotion.

Content-blind Codex readback proves the new root is unarchived, idle,
interactive-source, cwd-matched, and still attached to provisional pane `%16`,
but has `tokens_used=0`, `has_user_event=0`, no first-user-message marker, and
no preview marker. The old Students root remains token-bearing and unchanged.
This proves the first reserved tmux submission did not become a Codex user
event even though the local insertion path reported submitted. Never repeat
that exact input.

The bridge contract still requires one completed assistant-bearing cold-start
turn. Revalidate the exact idle pane/root/watcher and the same zero-event
state, clear only the provisional TUI's unaccepted input line, then reserve and
submit one changed-input generic cold-start instruction. Do not create another
root, rename either root, or replay the first input.

That one changed-input submission was attempted under the shared lock and its
private journal reported `submitted`, but fresh database readback again proved
`tokens_used=0`, `has_user_event=0`, no first-user-message marker, and no
preview marker. The outer wrapper could report success without propagating the
locked inner command's failure, so the journal is not acceptance evidence.
Never replay either attempted input.

The provisional pane remains alive, input-enabled, outside copy mode, rooted
at the destination, and attached to a healthy foreground supervisor, watcher,
and Codex process group. The shared message lock is available. Diagnose the
content-blind delivery path before any further write; if and only if another
input is proven absent, use a checkpointed one-shot path whose outer status is
bound to the actual submission and whose acceptance is a completed
assistant-bearing turn.

The failure was confined to TUI keystroke delivery. A generated protocol
schema proved that the existing root accepts native `turn/start`; a
digest-bound controller then passed an exact zero-write preflight and sent one
new, non-replayed visibility-initialization turn under the shared lock. The
server acknowledged turn `019fc5cb-3e63-7e12-9724-b1bdbd6872ea`. The
controller's immediate readback raced turn persistence and failed closed as
`turn-readback-absent`; the acknowledged request was never retried.

Subsequent read-only reconciliation proves that exact turn completed, the
thread returned idle without `systemError`, and its item metadata contains one
user message and three assistant messages with zero active items. Database
metadata now has nonempty first-message and preview markers and positive token
usage. The Codex bridge therefore passes its content-blind cold-start-turn
contract. The remaining gate is only the owner's physical confirmation that
Codex remote control now shows `students-har384`; do not promote or retire any
old client before that confirmation.

The owner then confirmed `students-har384` visible in Codex. Both Codex and
Claude physical-phone gates now pass. Freeze the exact old and accepted pane,
thread/session, process, cwd, and rollout identities; next promote names and
panes while retaining the old clients in unique rollback windows. Do not stop
an old leaf or retire a source until the promoted topology has its own
published reversible checkpoint.

## Reversible client promotion

A digest-bound Codex name controller matched both idle roots, renamed the old
root to `students-legacy-har384`, then renamed the accepted destination root to
canonical `students`; each one-shot write received exact same-connection
readback. The old pane `%14` and accepted pane `%16` retain their original
process and root identities.

The old Codex pane was moved to window `har384-legacy-codex`, and the old
Claude pane to `har384-legacy-claude`. Accepted panes `%16` and `%17` were
joined without relaunch into canonical `harness:codex.1` and
`harness:claude.1`. Both canonical windows now have equal-width horizontal
personal and Students panes. Old pane role markers were removed so monitors
cannot select rollback clients; their processes and source-root cwds remain
live and unchanged.

One `/rename` command was submitted without pane inspection to each Claude
rollback/accepted pane, legacy first and canonical second. Both Claude
processes remained live; treat the display-name writes as submitted rather
than independently accepted until later metadata or physical readback proves
them.

Fresh one-shot Codex and Claude monitors classify all three repository panes
healthy and correctly ordered. Claude is fully healthy. Codex remains degraded
only because the continuously running recovery helper still has the old
profile loaded and reports stale Students native-cwd/tmux mapping. Publish this
reversible promotion checkpoint, then restart only the exact helper and monitor
processes so they load the already published destination-root profile; do not
signal either old project client yet.

After the checkpoint was pushed, the exact old Codex TUI and old Claude owner
process were each revalidated by PID, parent, start tick, argv, cwd, pane, and
rollback window, then received one `SIGTERM` each. No process-group, repeated,
or forced signal was sent. The old Codex TUI, watcher, supervisor, pane, and
window exited. The old Claude process is a tmux-parented zombie; its exact dead
rollback window was removed, but the zombie remains unreaped. Treat it as
exited but not absent, never signal it, and retain the parent-owned reaping
boundary in closeout evidence.

The continuously running Codex monitor, Claude monitor, and recovery helper
were restarted in their exact monitor panes to load the published profile. The
single-pane helper window exited during its first respawn race after the old
lock holder stopped, then was recreated at canonical `monitor:3` only after
the lock was available. Fresh one-shot checks now report all three Codex and
all three Claude panes healthy, correctly ordered, and Codex phone mirroring
healthy. Accepted Students panes remain `%16` and `%17` with their original
destination-root process identities and equal-width canonical layouts.

The two bridge-only source-root admissions were removed from both tracked
launch sentinels, and only the matching two old trust sections were removed
from Local Codex configuration. Destination-root admissions and trust remain.
Agent-configuration and Claude-takeover focused tests pass. Next publish these
final control-plane bytes through protected Git, fleet-sync the merged
revision, then prove no mutable source-root references remain before guarded
source retirement.

Final integrated `tests/test-phase1.sh` passed on these bytes; its native MPI
smoke emitted only the declared environment skip. The branch is ready for one
protected PR.
