task: Har-424
status: active
updated: 2026-08-08
phase: executing
---
# Har-424 — Establish the Local-outage failover environment

## Outcome and scope

Establish a reversible temporary environment in which t4 replaces Local as the
SSH access hub and the Riken Mac can develop all five projects. Preserve
repository independence, credentials, existing Local tunnels, protected
publication, unrelated configuration, and dirty or divergent state.

The operating window is Har-328's primary 2026-08-08 through 2026-08-09 outage.
Material setup stops before 09:00 JST on August 8. This does not authorize
unattended monitoring, shutdown, reboot, scheduler work, deployment, credential
movement, or hosting/account administration.

## Confirmed facts

- Riken host `riken2` is clean at Harness `main` `47b294441b24`, 0/0.
- t4 is reachable from Riken, is clean at the same Harness revision, and
  receives a safe forwarded agent socket. No credential was inspected or moved.
- TSUBAME publishes two DNS-round-robin login nodes with pin-able `login1` and
  `login2` FQDNs. Its current rules prohibit login-node programs lasting over
  ten minutes and server/auto-restart programs; compute jobs are capped at 24
  hours. Sources: <https://www.t4.cii.isct.ac.jp/docs/all/handbook.en/start/>
  and <https://www.t4.cii.isct.ac.jp/en/resource-limit>.
- t4-vantage fleet health fails: aliases inherit Local-context short Linux
  names and localhost Mac endpoints. Local, AB, and Riken probes failed.
- Riken's existing tunnel pair and watchdog are healthy in the Local topology.
- Riken has only Harness; its four conventional project paths are absent.
- Riken reaches hosted tips Harness `47b294441b24`, Personal `a649035fa37f`,
  Students `fcf9917423d5`, Swallow `3ffcbf8ad620`, and Website `8c7687376468`.
- Sentinels admit Website on macOS but not Mac-local Personal, Students, or
  Swallow. Managed targets name Linux NFS paths and remain unchanged.
- Local returned `Network is unreachable`; its five checkout states are
  unknown. Hosted protected tips are the only usable baselines.
- Worktree gate: `/Users/yokotar/harness-worktrees/...` was refused outside the
  declared scratch boundary before Git mutation. Retry used physical `TMPDIR`
  and created branch `codex/har424-t4-failover` at the protected baseline.
  Ledger transfer stash `f8a729a` applied there; canonical main remains clean.

## Assumptions and decision register

- D-001 recommended default: t4 hub access uses the incoming Riken-forwarded
  current-user agent only. Do not copy, load, enumerate, or persist keys on t4.
- D-002 recommended default: add a separate, transaction-backed t4 hub SSH
  layer and separate Mac-to-t4 reverse-tunnel identities. Do not replace the
  existing Local mappings; rollback removes only the failover layer.
- D-003 selected by owner: t4 must cover Aist, Home, Office, and Riken. The
  owner can use remote control for bounded actions on every Mac.
- D-004 recommended default: clone the four missing projects into
  `$HOME/projects`, admit those exact Git roots in both sentinels, and leave
  Personal and Website non-managed. Do not change Linux canonical roots or
  promote Mac-local roots into the managed saved-session lifecycle.
- D-005 recommended default: each new checkout starts clean on its hosted
  default protected branch. If its repository-native policy or ledger requires
  another starting branch/worktree, stop that repository stream and follow its
  own record.

- D-006 selected by owner go: treat t4 as an on-demand bastion. Pin each session
  to `login1` or `login2`, start a bounded Mac reverse tunnel by remote control,
  validate and use it, then stop it. Never install a persistent tunnel,
  watchdog, server, or auto-restart loop on shared t4 login nodes. A continuous
  hub would instead need paid scheduler allocations renewed at the 24-hour
  limit or a non-HPC host.

Open material decisions: none. Owner `go` authorized D-001 through D-006.

## Planned execution

1. Freeze D-001 through D-006 and the exact failover acceptance scope.
2. Implement and test guarded plan/apply/status/rollback, pinned t4 connections,
   forwarded-agent enforcement, Local preservation, and value-free failures.
3. Run focused and complete validation; publish through protected main and
   guarded-sync clean checkouts.
4. Apply Riken's bounded tunnel, run hub-aware fleet health, and preserve its
   rollback receipt.
5. Clone each missing Riken repository. Read its instructions and active ledger
   before changes; record branch, head, upstream, cleanliness, and tip agreement.
6. Apply the protected sentinel update on Riken, validate exact-root admission
   with new nonpersistent client invocations, and leave managed target profiles
   unchanged unless a separately frozen repository-native plan requires them.
7. Hand off activation, rollback, unreachable nodes, retry safety, and
   restoration. Har-328 remains the sole Local recovery authority.

## Safety gates, validation, and rollback

- Unsafe SSH identity, changed preimage, missing agent, ambiguous route, dirty
  checkout, divergence, unreviewed policy, or failed hosted check stops its stream.
- No private SSH, credential, pane, transcript, or sibling data enters Harness.
- No existing Local tunnel is stopped, signaled, renamed, or overwritten.
- Hub acceptance requires t4-origin value-free route results, Riken reverse
  reachability, exact clean repository tips, sentinel doctor success, and no
  residual transfer/staging artifacts.
- Rollback removes only verified failover state, restores sentinel links through
  agent-config, and preserves project checkouts absent guarded cleanup authority.

## Checkpoint cadence and next action

Implementation checkpoint: commit `f5170cb` contains the bounded runtime,
hub-aware health, sentinels, docs, and tests. Focused t4-hub, fleet-health, and
agent-config pass; Riken plan and pinned `login1` passed without mutation.
Complete R3 on Riken failed safely: one task-owned context benchmark is stale;
the remainder are platform-contract failures (Python 3.9 lacks strict zip,
tomllib, and SO_PEERCRED; Linux fixtures invoke incompatible Darwin tools or
exceed Darwin socket paths). Retry is safe on Linux.

Checkpoint each implementation gate, publication, host apply, repository clone,
and final result. No unattended probing is authorized; triggered checks record
timestamped route evidence. Har-328 owns restoration after power returns.

Exact next action: refresh the benchmark, commit, push explicitly, then run R3
from an isolated t4 Linux worktree. Retain stash `f8a729a` until validation.
