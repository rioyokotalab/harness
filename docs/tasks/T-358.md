# T-358 — Overnight duration run: claude tmux window and multi-repo development

## Status

Phase: `executing` (owner go granted in the originating 2026-07-31 ~23:21
request; hard stop 2026-08-01 05:30 local).

Owner request: (1) add a `claude` window to the tmux `projects` session
mirroring the `codex` window's three equal horizontal panes and launch
directories; (2) run those panes on Fable with graceful native fallback to
Opus at the weekly limit and an unattended switch back to Fable after the
~04:21 reset; (3) survey and independently advance TODO-driven development in
harness, students, swallow, website, and personal under owner priority
autonomy > efficiency > safety, batching commits and minimizing PR count for
private repositories; (4) work without owner input until 05:30.

## Frozen boundaries

- Cowork session: `docs/audits/t358-overnight-duration/cowork` (schema 3,
  driver claude, co-pilot codex). Charter, plan, and benchmark there are the
  frozen scope; its Boundaries section lists non-goals verbatim.
- Existing `codex` window and `monitor` session are untouched. Managed pane
  and transcript content is never inspected.
- Repository always-enforce rules, guarded deletion, credential and
  publication boundaries are not relaxed by the owner priority ordering.
- Time-gated or blocked board tasks (T-196, T-328, T-303, T-354 blocked leg)
  are not started early.

## Working set and next action

- Harness branch `claude/t358-overnight-duration` from `524b3e7`.
- Cowork exchange under `docs/audits/t358-overnight-duration/cowork/`;
  sandboxes and seals under the session scratchpad (disposable).
- Other targets: `codex-workspaces/{students,swallow}`,
  `projects/{website,personal}` under their own instructions.

Execution evidence lives in the cowork `execution.md` and `validation.md`;
this record carries the durable summary below.

## Durable duration summary (seven slices)

**23:21–00:21.** Cold start read `AGENTS.md`, the board, and the duration,
managed-codex, and repository-git policy rows; selected the duration +
long-running-ledger skills. Created T-358, bumped the board to T-359, opened
`claude/t358-overnight-duration` from `524b3e7`, and initialized a schema-3
cowork session (driver claude, co-pilot codex). Froze charter, plan, and
benchmark, then ran the driver's disposable experiment in scratch session
`t358x`: three-pane even-horizontal at 152x40 reproduces 50x40 exactly,
claude 2.1.220 accepts the frozen flags interactively, and `/model fable`
executes at an idle prompt. Iteration 1 came from a real refusal: the first
staged codex pass was rejected by the owner launch sentinel because the
scratchpad sandbox was not an admitted root; a fresh in-repo gitignored
sandbox was staged rather than relaundering the old one. Codex's independent
pass rejected benchmark revision 1 (C3 conflated keystroke delivery with a
verified model switch; index-based targeting could inject into a replacement
pane) and proposed twelve changes, all applied. The reciprocal pass accepted
the amended benchmark with four clarifications — one-shot delivery only, a
conjunctive identity guard, `/proc` argv as C1 evidence, readiness folded
into C3 — and both role-bound agreement lines were recorded. Target
execution then created window `claude` (`@106`) with panes %108/%109/%110
launched via `exec claude --model fable --effort high --fallback-model opus`;
iteration 2 fixed detached-creation defaults (80x24 geometry, mis-ordered
`-d` splits) with `swap-pane`, `resize-window`, and an idempotent
`even-horizontal`. The codex window fingerprint was byte-identical before
and after. The switch-back timer was armed at 23:44:27 (pid 3069131, fire
epoch 1785525900) with pane IDs, paths, PIDs, start times, and argv digests
embedded. The five-repo survey found harness fully blocked or time-gated,
swallow and personal owner-blocked, website idle, and students holding a
prepared T-043 handoff. Website T-211 ran its offline suite, metrics
validation, and hook doctor green, and PR #37 was opened. The harness
validation plan escalated to tier R3, and phase 1 exposed two genuine
fixture drifts caused by the new board entry (ledger index closure and the
context benchmark's pinned table); both were repaired and committed.

**00:21–01:21.** The 00:45 checkpoint confirmed the students pane had
completed its entire authorized scope: independent verification of the
pinned Slack CLI and API contracts, a staged codex critique imported under
receipts `749a4e47…` and `d2e9a0fb…`, benchmark revision 1 rejected by the
co-pilot over exactly-once semantics, revision 2 frozen with
operation-specific terminal cases, and an exact owner-go phrase written into
`docs/tasks/T-043.md` at 00:16. Nothing was implemented, nothing pushed, and
the uncommitted worktree plus both cowork directories were preserved. With
that, no owner-executable work remained in any of the five repositories, so
the run moved to a monitoring cadence and checkpointed the branch.

**01:21–02:21.** A no-change slice. The 01:45 sweep verified the switch-back
timer alive, all three claude panes running with idle titles, the codex
panes untouched, students dirty-by-design at three preserved paths, and
harness clean apart from the immovable `.nfs` residue. Website PR #37's
`Offline checks` passed and the PR was squash-merged as `736e14a`.

**02:21–03:21.** The owner exited the session at 02:32, which killed the
in-session wake alarms while leaving every durable artifact and the detached
timer intact; the alarms were re-armed on resume. The owner then asked
whether the codex blocked-thread restart mechanism covered claude, and the
answer was evidenced as no: the monitor session's
`harness-tmux-codex-monitor` binds to `projects:codex` by `@harness_target`
and had zero claude awareness. On the owner's explicit order to build and
deploy it within the window, T-359 was opened and implemented — a closed
claude target map, a content-blind monitor, a respawn-only recovery worker,
and additive `claude-pane-blocked` support in the shared helper — with the
new focused suite and the three owning codex suites all passing.

**03:21–04:21.** Deployment and the live drill. The monitor session was
rebuilt as tunnel/codex/claude/helper with pane-border titles on every
window and on `projects:claude`. Killing the swallow pane's claude at 03:17
produced two real findings: dead panes report an empty working directory, so
the dead check now precedes the cwd check everywhere; and repair correctly
deferred while the owner's client sat focused on the dead pane, which
demonstrated the guard but would have deferred forever, so a bounded 600s
attached-dead grace was added. Recovery then fired automatically at
03:50:37, respawning pane %110 in place with the frozen flags plus
`--continue` (pid 537897), helper receipt `completed/worker-success`, and
the monitor back to 3/3 healthy. The superseded timer was replaced by v2
(pid 543772) carrying the post-drill identities.

**04:21–05:21.** The switch-back fired at exactly 04:25:00. Pane %110
delivered its single `/model fable`; %108 and %109 were skipped fail-closed
because their idle titles are task-titled rather than the launch-time
`✳ Claude Code`, a readiness-surface finding. Under the frozen clarification
that a conclusively skipped pane may take one first attempt, %109 passed
full identity and `✳ `-prefix idleness checks and received its delivery at
04:27:08; %108 needed none, being this Fable driver session itself. The
owner then asked about phone access; Claude Remote Control was confirmed as
the native feature and enabled on the panes. Extending it to the four Macs
stopped at genuine owner gates.

**05:21–05:30.** Final validation window: acceptance gates C1–C7 recorded in
`validation.md`, the integrated tree re-run at 92 passing suites with only
the known dirty-checkout pair failing, and the single coherent harness pull
request opened for T-358 plus T-359.

## Outcome, risks, and next action

Outcome: the claude window mirrors codex exactly, model failover is
configured with a proven one-shot restore path, four repositories were
surveyed with no available owner-free work and website's audit merged, and
claude pane recovery parity shipped and was proven by drill.

Residual risks are listed in `validation.md`; the load-bearing ones are the
prefix-sensitive readiness gate still embedded in the timer script, unproven
weekly-limit fallback semantics, and the two locally unrunnable suites tied
to the `.nfs` handles.

## Post-merge fleet rollout (blocked)

PR #512 merged as `da1c992cc53e1dcabb76e2123e9cfbe8bb456a1f` after protected
`portable-phase1` passed. Guarded `harness fleet-sync --plan` from
`524b3e78…` to that merge resolves cleanly for `ab`, `ab2`, `ri`, `al`,
`rc`, and `t4`. `--apply` refuses with "apply requires a clean committed
harness checkout": the only dirt is the three untracked `libexec/.nfs*`
silly-rename files whose open handles belong to the live codex pane
processes (pids 1612366 / 2652612 / 2995423). They are not safely
removable while those panes run and clear on their own once the panes
restart. Rerun the same plan/apply pair afterwards; nothing else blocks the
rollout, and the four Macs are outside it this run.

Correction and resolution (05:09–05:12): the "restart the codex panes"
recommendation above was wrong twice over. `fuser` identifies the holders as
four running `/bin/sh` scripts — the three `harness-codex-resilient --run`
supervisors and `harness-connection-monitor`, the last of which lives in the
monitor session's tunnel window rather than any codex pane, so a pane
restart would have left one file and still failed the gate. The holders are
also the Codex crash-recovery machinery itself, which should not be bounced
for a cleanliness check. The residue recurs on every merge that rewrites
`libexec` scripts, because `sh` holds a script open for its whole run and
NFS silly-renames the replaced inode. Resolved with an owner-approved
`libexec/.nfs*` rule in `.gitignore`; the working tree now reads clean,
`test-tmux-config.sh` and `test-terminfo.sh` pass, and phase 1 is 94/94 with
zero failures. Fleet-sync apply is unblocked.

Closed 2026-08-01 08:45. Published as PR #512 (`da1c992`), #513 (`feeab31`),
and #514 (`bb7adec`). Guarded fleet sync advanced all six Linux nodes to
`bb7adec`; all four Macs report `status=ready` and `harness fleet-health`
passes for every node. Claude Remote Control is active on the three Linux
panes and on aist, office, and riken (T-360 corrected the readiness check
that had masked aist).

No Harness item remains open. The planning work this run performed inside
the Students repository is owned entirely by that repository's own ledger and
is tracked there under T-043; it has since advanced past the go request
recorded here at 00:16 and must not be mirrored, scheduled, or re-summarized
from Harness. Read Students' `TODO.md` and `docs/tasks/T-043.md` for its
current phase.
