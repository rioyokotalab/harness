task: Har-406
status: complete
updated: 2026-08-07
---
# Har-406 — Make the Swallow complete gate self-describing

Swallow protected PR 174 makes `bash tests/test-static.sh` default an absent or
empty mode to login-safe `render-only`, preserves explicit `live`, and rejects
other values before unrelated checks with the exact direct command. Two
invalid-value mutations and the direct complete gate passed from an isolated
NFS worktree without an environment prefix.

Protected main is clean and idle at `3ffcbf8ad` with candidate tree
`4a3d1e1693`. Generated caches and the private test log were removed; Harness
housekeeping archived and removed the worktree and local branch, and the merged
remote branch is absent. Scheduler, scientific, explicit-live, compute,
credential, and external-source behavior was not exercised or changed.
Complete.
