task: Har-406
status: pending
updated: 2026-08-06
---
# Har-406 — Make the Swallow complete gate self-describing

The Swallow static gate failed from the canonical root until
`SW031_NVIDIA_RUNTIME_PLAN_MODE=render-only` was supplied. The documented
login-safe path should select its non-compute mode itself or fail with an exact
actionable command before running unrelated checks.

Next: define the smallest deterministic default and focused mutation tests
without changing live scheduler behavior.
