task: Har-423
status: active
updated: 2026-08-07
---
# Har-423 — Reconcile five repositories across the fleet

Inventory Harness, Personal, Students, Swallow, and Website on their canonical
Local roots and every declared managed Linux and Mac checkout. Preserve dirty,
divergent, active, locked, or unknown state. Classify branches, worktrees,
ignored/runtime residue, pull requests, and protected revisions independently
per repository; apply only fresh guarded housekeeping plans whose exact targets
remain eligible.

After protected Local state is reconciled, use guarded fleet synchronization to
advance only clean managed checkouts. Re-run fleet health and verify each
reachable checkout is clean, on its declared protected branch, and at the
current repository revision. Do not copy private repository content into
Harness or expose pane/transcript data.

Finally, prepare the existing Students Claude consumer from the exact Students
root: validate its declared route, client, pane, process, remote-control, and
cold-start state without replacing or interrupting a healthy process. Send at
most one bounded value-free readiness prompt only if the route resolves
unambiguously, and require no task execution.

## Exact next action

The five clean protected baselines were Harness `6eb29f2c`, Personal
`993de91d`, Students `8b92c3fc`, Swallow `3ffcbf8a`, and Website `8c768737`.
Every origin matched. Fleet health passed Local, seven remote Linux nodes, and
four Macs. Scratch, launcher, open-pull-request, and primary-worktree checks
found no unknown or active cleanup target.

Guarded housekeeping archived and removed one 14.7 MB Harness worktree, 31
Harness local branches, six Personal local branches, two Students local
branches, 21 Harness hosted branches, and two Swallow hosted branches. The
archive receipts are rooted in the owner-only housekeeping state; all applies
reported verified success. Swallow and Website had no extra local worktree or
branch. No project source or private value crossed repository boundaries.

The first Harness branch apply failed before deletion because independently
named branches sharing one commit were rejected as duplicate archive tips.
`harness_housekeeping.py` now permits duplicate commit tips while continuing
to require unique branch names and archive refs. The focused housekeeping suite
passes with a regression that archives and audits both names. Final complete
validation and protected publication remain required because this changes a
safety-sensitive cleanup runtime.

After merge, fast-forward only clean declared Harness mirrors from the frozen
old revision to the protected merge revision, re-run fleet health, verify the
known Swallow site checkouts against protected main, and prepare the existing
Students Claude route without assigning work.
