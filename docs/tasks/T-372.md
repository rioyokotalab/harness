# T-372 — Complete interrupted worktree teardown

## Status

Complete. PR #553 merged exact head
`65a677b7ec1b94d31e0e5b69918827f81eba2ce6` as
`4996e198366992a79568237368f925388f3f1306` on 2026-08-02.

## Problem

T-370 records exact `directory-deleted` and `admin-deleted` checkpoints and
tests that the expected residue remains after a synthetic interruption. It
does not provide a later invocation that consumes those checkpoints and
finishes the archive audit, administration cleanup, expected-old branch
deletion, and final state transition. A normal re-plan rejects a missing
worktree path, while the original guarded manifest also expires. Durable
recording alone therefore does not satisfy idempotent recovery.

## Frozen implementation

Add one explicit recovery mode that accepts exactly one private worktree-apply
state receipt. Re-open its immutable plan without reusing an expired deletion
token; resolve the recorded candidate by exact path, branch, and tip; verify
repository identity, current merged-PR classification, archive receipt, path
absence, administration identity/backlink or absence, and expected-old local
branch. If administration remains, create and consume a fresh single-target
guarded manifest. Then delete only the exact branch ref and advance the same
state receipt through `admin-deleted`, `branch-deleted`, and `complete`.

Completed recovery is a value-free idempotent no-op. Reject an unsafe state
file, changed repository, changed branch tip, existing worktree directory,
unknown archive, wrong admin path/owner/backlink, or non-merged classification.
Do not add a daemon, scan arbitrary state automatically, mutate remote refs, or
weaken ordinary closeout gates.

## Acceptance

- The directory-stage interruption test invokes recovery and proves archive,
  administration record, local branch, and worktree row are all resolved.
- The admin-stage interruption test invokes recovery and proves exact-old
  branch deletion and final completion.
- A second recovery invocation is a no-op; changed-tip and unsafe-receipt
  fixtures fail closed.
- Focused housekeeping and routing tests pass, followed by clean Harness phase
  one and protected publication.
- No managed session, phone mapping, repository approval count, remote branch,
  or unrelated state changes.

## Exact next action

None. Use the explicit recovery mode only for a recorded interrupted phase;
ordinary completed tasks continue through the normal guarded closeout.

## Implementation checkpoint

The explicit recovery mode now accepts one current-user mode-0600 state
receipt under the canonical housekeeping state directory. It validates the
immutable plan's recorded SHA-256 and repository identity without weakening the ordinary
15-minute plan gate, rechecks exact merge classification and archive bytes,
rejects an existing task directory or changed branch, validates a remaining
Git administration record and backlink, creates a fresh one-target guarded
manifest, and expected-old deletes only the recorded local branch.

The focused suite now completes both directory-stage and admin-stage
interruptions from a later invocation, proves a second call is a no-op,
rejects a changed branch before deleting administration, rejects a symlinked
receipt or changed plan digest, and proves that a two-candidate interrupted closeout recovers only
the started candidate while leaving the other for a fresh normal plan.

Clean Harness R3 phase one passed on commit `6b300b8`, including the expanded
24-second housekeeping suite and every routed integration suite. Native MPI
was correctly skipped outside a declared MPI environment. No managed runtime,
remote ref, or external project state was used by the recovery fixtures.

The final exact head `65a677b7` passed a second clean local R3 phase one and
the protected `portable-phase1` check. PR #553 merged without bypass. The
ordinary closeout then archive-verified and retired the exact task worktree and
local branch, recording the plan SHA-256 in its completed state receipt. A
real invocation of `--recover-worktree` against that completed receipt returned
the expected idempotent no-op with zero remaining candidates.
