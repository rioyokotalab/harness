# T-356 — Personal-only email/calendar connector boundary

## Status

Phase: `complete`.

Personal task `P-002` owns the domain decision register and exhaustive
connector/privacy plan. This Harness task owns only the bounded launcher,
private runtime-profile contract, synthetic tests, and cross-root validation
needed to implement that plan.

## Reconstructed baseline

- Harness `main` and `origin/main` matched
  `ecf34d2441ed2e224db758851a44b0c85d0bcc27` before branch creation.
- Planning branch: `codex/t356-personal-connectors`.
- Planning worktree: `/tmp/harness-t356-personal-connectors`.
- The primary checkout remains on `main` and retains three unrelated live
  untracked `libexec/.nfs*` inodes.
- Personal `P-002` is on `codex/p002-connector-isolation` at the canonical
  mode-0700 root.
- `harness personal-agent --status` reports connector readiness as partial.
  Combined Gmail-plus-Calendar authorization is present behind the validated
  private-file boundary. The Codex and Claude email rows are ready; Calendar
  remains blocked pending one bounded acceptance read.
- The launcher already provides exact Personal cwd, one cross-client lock,
  non-persistence, Codex Sol/high, Claude Fable/high with Opus fallback,
  strict empty Claude MCP, and value-free receipts.
- Fresh Codex inventories in Harness, Students, Swallow, and Personal expose
  the same enabled Slack and GitHub plugins from shared user state. Available
  Gmail and Google Calendar plugins are not installed.
- Claude has no installed plugin. Its documented remote connectors become
  account-wide after connection.
- The frozen consumer account classes are unchanged. On 2026-07-31 the owner
  attested that both model-improvement controls are off.
- No connector, OAuth grant, live source, credential, pane, or transcript was
  inspected or changed.

## Authority and boundaries

The owner's 2026-07-31 request authorized planning and safe synthetic
implementation after the P-002 architecture was frozen. The owner later
authorized the exact Gmail and Calendar read-only consent attempts and bounded
acceptance reads recorded below. Those authorities are consumed. Future reads
follow Personal policy; no source-system write authority is implied.

Preserve T-355 D-002 through D-007, D-010, D-013, D-015, D-017, and D-019.
Keep Personal absent from managed targets and all tmux, phone, monitor,
app-server, recovery, and saved-root state. Never inspect credential contents.

## Durable routing

- Personal task: `docs/tasks/P-002.md` in the canonical Personal repository.
- Exhaustive Personal plan:
  `docs/plans/p002-connector-isolation-privacy.md` there.
- Harness integration plan:
  `docs/plans/t356-personal-connector-isolation.md`.

## Decision audit and next action

P-002 decisions P2-D001 through P2-D005 are frozen with no gap or
contradiction. Gmail read-only preceded Calendar read-only, and both are now
ready in Personal. No T-356 action remains.

The Personal connector now passes eleven synthetic tests. The Harness launcher
passes focused tests for profile bootstrap, two privacy attestations,
blocked/ready rows, exact domain advertisement, both clients, one lock,
environment scrubbing, and value-free receipts. Native validation proved the
Codex config override and a strict Claude Personal launch that exposed only
the repository-native server and failed closed while the profile was absent.

Ordinary native Claude inventories on 2026-07-31 labeled account-level Gmail,
Google Calendar, and Google Drive MCP endpoints `Connected`. The owner then
verified that the connector UI showed none authenticated. Claude Code's MCP
documentation treats server connection status and OAuth authentication as
separate states, so the CLI label did not prove Google-account authorization.
Strict empty-manifest launches still exclude all account-level endpoints. The
disconnected-state requirement is satisfied; no logout was needed or
performed.

At `2026-07-31T09:29:10Z`, the owner attested that ChatGPT's “Improve the model
for everyone” and Claude's “Help Improve Claude” controls are both off. The
reviewed launcher recorded only the two provider/off rows and now reports
`profile=ready privacy=ready connectors=blocked`.

The owner authorized one Gmail read-only consent using the exact private client
path. At `2026-07-31T09:50:52Z`, the helper installed that client into the
private profile, then Local's native browser opener proved that no browser is
installed. No consent page opened. The waiting callback was terminated; an
authoritative local readback proved `token=absent`, and every connector remains
blocked. The attempt is not retryable unchanged.

The owner selected `riken` as the interactively accessible browser host. The
implementation now binds one fixed Local callback port, creates and cancels an
exact reverse forward through Riken's existing managed SSH master, and sends
the ephemeral authorization URL to macOS `osascript` only through encrypted
standard input. Personal validates the helper boundary and never places the URL
in subprocess arguments or output.

At `2026-07-31T10:02:45Z`, a synthetic callback traversed Riken to Local,
`osascript` passed a no-op browser capability check, the forward was cancelled,
and a post-check proved the remote port closed. No Google page opened and no
credential or source content crossed the tunnel. The next action requires one
exact changed-input authorization to retry Gmail read-only consent with
`--browser-host riken`.

The owner authorized exactly one changed-input retry through Riken. At
`2026-07-31T10:25:30Z`, it completed with `domain=email` and `scope_count=1`.
Metadata-only readback proved the token is a current-user-owned mode-0600
regular file with one link; its contents were never inspected. The operation
lock is free, the Riken callback port is closed, and every connector remains
blocked. The next action is owner selection and exact authorization of one
non-sensitive Gmail read; do not access a live source before that gate.

At `2026-07-31T10:44:48Z`, fresh value-free inventory proved the
repository-native connector absent from Harness, Students, and Swallow and
effective only in Personal for Codex and Claude. The owner authorized one
exact-subject Gmail search with `max_results=1`, metadata/snippet only. Exactly
one result matched the owner-provided harmless acceptance values; no thread or
body read occurred. The result remained in process memory and was neither
printed nor persisted. Personal records only value-free client/domain
acceptance receipts. Both email rows are ready; Calendar and every write remain
blocked. The next action requires exact owner authorization for Calendar
read-only consent through the validated Riken bridge; the installed-app flow
will request the complete Gmail-plus-Calendar read-only scope set.

The owner then authorized exactly one Calendar consent through the validated
Riken bridge, acknowledging the installed-app scope replacement. At
`2026-07-31T10:52:58Z`, it completed with `domain=calendar` and
`scope_count=2`, proving the exact Gmail-plus-Calendar read-only set. A
metadata-only post-check proved the refreshed token is a current-user-owned
mode-0600 regular file with one link; its contents were never inspected. The
operation lock is free, the Riken callback port is closed, and Calendar remains
blocked. The next action is owner selection and exact authorization of one
non-sensitive Calendar read; do not repeat the Gmail read.

Personal PR #10 merged exact-name Calendar selection as
`4732a0b21a492758c076065c54bb49f31c4ee129`. Thirteen synthetic tests prove
that the selector requires an exact display name, resolves one calendar with
bounded pagination, and rejects absent or ambiguous matches without weakening
the interval or result limits.

At `2026-07-31T11:10:41Z`, fresh Codex and Claude inventories again proved the
connector absent from Harness, Students, and Swallow and effective only in
Personal. The owner selected one exact-name public calendar and a one-hour
future Asia/Tokyo interval. Exactly one bounded read was attempted. It returned
a value-free unknown result before any event payload was released, and was not
retried. The token metadata boundary remains valid, no selected name or event
content was persisted, and both Calendar rows remain blocked. The attempt is
non-retryable unchanged. The next action is owner confirmation that the exact
display name exists in the consented account plus one changed-input retry
authorization that preserves the connector's value-free failure reason; do
not repeat the Gmail read.

The owner confirmed the exact display name and authorized one changed-input
retry with value-free error propagation. At `2026-07-31T11:18:04Z`, exactly
one bounded read was attempted for the same selected one-hour interval. It
failed with `google-api-failed` before any event payload was released and was
not retried. An unauthenticated request to the Calendar endpoint returned the
expected HTTP 401, proving transport reachability; the local connector status
still proves exactly two read-only scopes and no writes. The token metadata
boundary remains valid and no selected name or event content was persisted.
The leading explanation is that Google Calendar API is not enabled for the
OAuth client's Cloud project, but provider error-body details remain
intentionally uninspected. Calendar readiness stays blocked. The next action is
owner verification or enablement of that API, then one changed-input retry
authorization; do not repeat the Gmail read.

The owner then verified that Google Calendar API was enabled and authorized
one changed-input retry with the same bounded interval and result limit. At
`2026-07-31T11:30:38Z`, exactly one Calendar tool call succeeded with a
zero-result value-free acceptance outcome. No event payload was released or
persisted, the credential boundary remained intact, and no retry occurred.
Personal now records the shared repository-native Calendar read capability as
ready for Codex and Claude; all Calendar writes remain disabled.

## Publication and owner gate

- Personal PR #13 merged the final acceptance state as
  `ab5a0fadb74e0c96e1c25b2451f6749f3fb3fefe`.
- Personal PR #1 merged as
  `303e041bbe327873ad561287641d6bc30871c192`.
- Harness PR #498 merged as
  `07aeb2fa25a0a498b9f379e96fe3a532bd496df0`; its sole protected
  `portable-phase1` check passed.
- Guarded fleet sync advanced clean `ab`, `ab2`, `ri`, `al`, `rc`, and `t4`
  checkouts from `551357e75f7f47a96a028908ae0a516ff3249b71` to the Harness
  merge commit. No Mac checkout or running session was in that plan.
- The reviewed launcher created the fixed private profile and now reports
  `profile=ready privacy=blocked connectors=blocked`.
- No provider logout, privacy attestation, OAuth grant, credential inspection,
  live source read, or source write occurred.

The owner selected the disconnected provider-connector state and reports that
no provider account-wide Google connector is authenticated. No logout was
needed or performed. Both provider privacy readbacks are recorded value-free.
The repository-native Gmail read-only consent completed through the Riken
bridge with one scope; the private token boundary and exact tunnel cleanup pass.
Its one bounded acceptance read passed and both Personal email rows are ready.
Calendar consent has the exact two read-only scopes, and its one bounded
acceptance read passed after the API was enabled. Both Personal Calendar rows
are ready; all writes remain blocked.
