task: Har-431
status: active
updated: 2026-08-08
phase: implementation measured; final clean validation pending
---
# Har-431 — Reduce Riken test latency

## Outcome and frozen scope

Reduce the complete Harness test latency on the managed `riken` Mac before
resuming Har-428. Preserve all 99 focused suites, integration checks, guarded
cleanup, failure attribution, final full-gate coverage, and the Har-428 phone
checkpoint. Do not contact another Mac, change host configuration, install
packages, weaken assertions, skip suites, or treat cached reuse as a fresh
full-gate timing.

Har-423 is retired from the active queue by explicit owner instruction. Its
record and completed evidence remain indexed; its outstanding fleet rollout
and Students consumer work are cancelled.

## Baselines and comparison boundary

- Historical protected Local profiles on earlier 98-suite trees completed the
  focused pool in 51.076–58.613 seconds. Local is Ubuntu x86_64 and is currently
  unavailable, so those results are a target range, not a matched baseline.
- Har-430's final exact Riken tree passed 99 suites in 283.305 seconds. Its
  six-suite Darwin development preflight passed in 72.209 seconds.
- The exact current Riken host is Darwin arm64 with ten visible CPUs. Har-430
  defaults to eight focused workers, reserves one CPU for overlapping phase-one
  integration, and admits at most two process-heavy suites concurrently.

“Close to Local” is an aspirational full-gate target near 60–75 seconds. A
claim requires a matched Riken before/after pair on identical coverage. If the
measured critical path proves longer than that range, report the lower bound
and preserve correctness rather than hiding work.

## Execution and acceptance

1. Capture an exact protected-main Riken timing receipt with all suite states,
   resource classes, and durations; separately record total phase-one time.
2. Run a bounded matched concurrency sweep over worker count and Darwin-heavy
   admission. Stop configurations that reproduce interrupt or cleanup failure.
3. Use receipts to identify the critical path and distinguish contention from
   intrinsic suite cost. Change only proven runner or fixture hotspots, with
   owning regressions and explicit cleanup checks.
4. Reduce the ordinary development preflight without removing its portability
   and interruption contracts. The complete phase-one suite remains the final
   publication gate.
5. Validate the final exact tree through repository-native R3, record matched
   timing and residual risk, and publish through one protected task PR.

Success is the lowest stable Riken time supported by matched evidence with
99/99 coverage, no failed/cancelled/unstarted suite, only the declared native
MPI skip, clean fixture cleanup, and no Har-428 or fleet mutation.

## Working state

- Branch: `codex/har431-riken-test-speed`
- Worktree: `/Users/yokotar/harness-worktrees/har431-riken-test-speed`
- Base: protected `origin/main` at `41c75b2`
- Phase: `implementation measured; final clean validation pending`
- Open decisions: none; coverage and safety are frozen
- Exact next action: commit the measured implementation, run the complete clean
  phase-one gate with automatic four-heavy admission and a fresh receipt, then
  compare it with the exact 296.280-second current-tree baseline.

## Riken measurements and implementation

The exact Har-431 checkpoint at `965f9ef` passed all 99 focused suites with
eight workers and the prior two-heavy limit in 296.280 seconds; complete phase
one took 299.59 seconds and native MPI was the sole declared skip. Direct
matched sweeps retained 99/99 coverage at 219.488 seconds with three heavy
workers and 204.473 seconds with four. Four-way admission is 91.807 seconds
(31.0%) faster than the exact two-heavy baseline, although individual heavy
suites lengthen under contention. Automatic Darwin admission therefore retains
a light-work lane on smaller machines and scales only to a four-heavy ceiling.

The complete native gate cannot truthfully approach Local's historical
51–59-second range through scheduling alone. Standalone Riken measurements for
SSH sync, SSH supervisor, plan/doctor, Homebrew, and config sync were 126.97,
135.53, 95.57, 105.63, and 87.05 seconds respectively. The longest single
suite is already more than twice the Local target before any other suite runs.
A Linux-container prototype was rejected as the final route: Docker exposes
only four CPUs, its virtual disk cannot hold the storage fixtures without host
scratch, and its Linux environment does not exercise the Darwin behavior that
Har-427 made explicit. No host configuration, fleet node, or Har-428 state was
changed.

The former preflight spent nearly all of its time running the entire routine
housekeeping suite even though its stated contract is interruption lifecycle
feedback. A new narrow fixture proves both durable recovery checkpoints—after
worktree-directory deletion and after Git administration deletion—while the
unchanged full housekeeping suite remains in the complete gate. On the exact
current tree, the original six-suite preflight passed in 72.419 seconds and the
revised six-suite preflight passed in 15.704 seconds (17.15 wall), a matched
56.715-second or 78.3% reduction. Its dedicated fixture passes natively in
10.520 seconds, and focused-runner regressions pass.
