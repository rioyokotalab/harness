task: Har-416
status: active
updated: 2026-08-07
---
# Har-416 — Make remote-agent helper resolution self-contained

Har-403's bounded Claude review exposed two documentation defects in the
remote-agent communication skill. Its examples treated `scripts/agent-message`
as relative to the caller's repository instead of the selected skill root, and
its unconditional compatibility precheck named Harness even though that check
supports consumer targets rather than the Harness cross-client route.

Make helper resolution explicitly relative to the selected `SKILL.md`. Require
the compatibility precheck only for consumer targets and document that the
Harness cross-client send performs its own endpoint validation. Run the skill's
focused contract tests and close this task with Har-403's publication.
