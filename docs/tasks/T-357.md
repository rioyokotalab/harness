# T-357 — Personal owned-calendar write controller

## Status

Phase: `complete`.

Personal P-003 owns the exhaustive plan, domain decisions, event target, and
source-system result. T-357 owns only the validated Riken OAuth bridge mode,
locked explicit `calendar-write` launcher boundary, synthetic Harness tests,
protected publication, and Linux checkout synchronization.

## Frozen boundaries

- Retain only Gmail read-only, Calendar read-only, and
  `calendar.events.owned`; shared-calendar writes remain excluded.
- Keep ordinary Personal MCP sessions read-only. No create or delete tool may
  appear in normal Codex or Claude inventories.
- The write controller is a native Personal command available only through an
  explicit locked `calendar-write` operation.
- Keep Personal absent from managed targets, tmux, phone, app-server, monitor,
  recovery, and saved-root state.
- Do not inspect credential content or persist calendar source values.
- Do not change OAuth or Calendar state before both repositories are published
  and the isolation and safety suites pass.

## Working set and next action

- Harness: `libexec/harness-personal-agent`,
  `tests/test-personal-agent.sh`, `TODO.md`, and this record.
- Personal: the P-003 branch and record; implementation remains owned there.

At `2026-07-31T13:18:36Z`, the owner gave go for the frozen P-003 plan. No live
state changed. The next action is to implement and validate the Personal
controller, then extend Harness with the exact OAuth and operation domains.
Any ambiguous consent or Calendar write is non-retryable until authoritative
metadata-only readback resolves it.

## Implementation checkpoint

At `2026-07-31T13:30:31Z`, the locked `calendar-write` operation, retained-scope
authorization domain, value-free receipt routing, readiness/privacy gates, and
environment isolation passed `tests/test-personal-agent.sh`. Task-ledger and
context-routing suites also pass after routing this active record. Personal's
19 synthetic tests and complete validator pass independently. The first full
Harness suite had only dirty-check-dependent tmux/terminfo failures plus the
now-reconciled ledger/context fixtures; owning and unrelated focused suites
otherwise passed. No OAuth or Calendar state changed. Next: commit and publish
Personal, commit and publish Harness through the protected workflow, rerun the
full Harness suite from a clean commit, then synchronize managed checkouts.

## Completion

Harness PR #510 merged as `075ee96929464bc88a52e5499421ff6ebbe06cc4`
after the protected portable check and complete local phase-1 suite passed.
Guarded fleet sync advanced `ab`, `ab2`, `ri`, `al`, `rc`, and `t4` from
`da233fd9d55007e3f96c9363191c38c817edfc56` to that merge with clean exact
readback. Personal PR #14 published the controller independently.

The initial Riken consent was cancelled before completion because the owner
could not access that browser; metadata reconciliation proved the write scope
absent. One changed-input Office consent then completed with exactly three
approved scopes. At `2026-07-31T13:42:41Z`, the locked native operation
returned value-free success after exactly one delete submission and
authoritative deleted/absent readback. The operation lock is free, normal MCP
inventories remain read-only, Personal remains absent from managed targets,
and T-357 is complete.
