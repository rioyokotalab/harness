task: Har-418
status: complete
updated: 2026-08-07
---
# Har-418 — Bind the exact Swallow Slack resource privately

The first successful Swallow enrollment exposed that the installed exact-policy
profile still contained its public fail-closed `deployment-unbound` sentinel.
The service exited with `provider-resource-unbound`; credentials, source data,
and writes were untouched. No protected owner-only path existed to replace the
sentinel with the exact channel identifier.

Add one root-only `bind-swallow-resource` operation. It accepts a bounded Slack
channel identifier through a terminal with echo proven off, validates the
root-owned profile and its exact unbound state, writes canonical profile bytes
atomically at mode 0600, restarts the profile-local service, and reports no
identifier. Reject malformed, already-bound, symlinked, permissive, or changed
profiles. If service acceptance fails, restore the exact prior bytes and mode.

Focused tests prove private exact binding, atomic mode, stable restart, invalid
and repeated binding rejection, and rollback. The complete Slack suite and
repository-selected integrated gate pass. No credential, target, or Slack
content enters the repository, command line, environment, or output. Har-418
is complete with the protected implementation candidate.
