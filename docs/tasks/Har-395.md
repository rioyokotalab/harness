task: Har-395
status: active
updated: 2026-08-05
---
# Har-395 — Retire rejected Personal recovery controls

## Frozen targets

The owner separately authorized cleanup after Har-394 completed. The accepted
Personal root `019fcecf-2628-7cd1-acf0-af629980ec2e` remains canonical in pane
`%25` and is outside mutation scope. The only live rejected bridges are R1
root `019fceb9-4557-7522-a197-4733147cf515`, window `@20`, pane `%23`, TUI
`414753/161476260`; and R2 root
`019fcec5-16cd-7ad3-94ee-9a4900144dae`, window `@21`, pane `%24`, TUI
`504995/161553914`. Their exact supervisors and watchers remain those recorded
in Har-394.

The recoverable archive set is exactly the retired pre-recovery root
`019fc765-0206-7112-8293-146fb29e8087` plus R1 and R2. The already archived
wrong-cwd root is excluded. No rollout or archived-session file may be deleted.

## Execution

1. Revalidate roots, names, cwd, pane/window identities, process starts and
   parentage, idle state, clean repositories, shared app server, and canonical
   accepted root immediately before mutation.
2. Journal and send one SIGTERM to each exact rejected TUI leaf. Do not retry
   or escalate; classify natural watcher, supervisor, and window teardown.
3. Through one mode-0600 controller and the existing app-server socket,
   pre-read and archive each exact retired root once, then read back archived
   state. Keep the accepted root protected by an explicit negative assertion.
4. Refresh the managed mirror and require exactly Harness, Personal, and
   Students Codex phone roots, canonical Personal pane/name/cwd, and unchanged
   Claude and monitor sessions.
5. Run focused lifecycle checks and complete Harness validation, publish the
   terminal evidence, reconcile producer state, and remove only exact temporary
   controller and journal artifacts through the guarded cleanup workflow.

## Next action

Publish this checkpoint before changing live state, then execute step 1.
