task: Har-395
status: complete
updated: 2026-08-05
---
# Har-395 — Retire rejected Personal recovery controls

## Frozen targets

The owner separately authorized cleanup after Har-394 completed. The accepted
Personal root `019fcecf-2628-7cd1-acf0-af629980ec2e` remains canonical in pane
`%25` and is outside mutation scope. The only live rejected bridges are R1
root `019fceb9-4557-7522-a197-4733147cf515`, window `@20`, pane `%23`, TUI
`414753/161476260`; and R2 root
`019fcec5-16cd-7ad3-94ee-9a4900144dae`, window `@21`, pane `%24`, TUI
`504995/161553914`. Their exact supervisors and watchers remain those recorded
in Har-394.

The recoverable archive set is exactly the retired pre-recovery root
`019fc765-0206-7112-8293-146fb29e8087` plus R1 and R2. The already archived
wrong-cwd root is excluded. No rollout or archived-session file may be deleted.

## Execution

1. Revalidate roots, names, cwd, pane/window identities, process starts and
   parentage, idle state, clean repositories, shared app server, and canonical
   accepted root immediately before mutation.
2. Journal and send one SIGTERM to each exact rejected TUI leaf. Do not retry
   or escalate; classify natural watcher, supervisor, and window teardown.
3. Through one mode-0600 controller and the existing app-server socket,
   pre-read and archive each exact retired root once, then read back archived
   state. Keep the accepted root protected by an explicit negative assertion.
4. Refresh the managed mirror and require exactly Harness, Personal, and
   Students Codex phone roots, canonical Personal pane/name/cwd, and unchanged
   Claude and monitor sessions.
5. Run focused lifecycle checks and complete Harness validation, publish the
   terminal evidence, reconcile producer state, and remove only exact temporary
   controller and journal artifacts through the guarded cleanup workflow.

## Next action

Checkpoint PR #680 merged before live mutation. Mode-0700 leaf controller
`/tmp/har395-retire-rejected.py` had SHA-256
`ea1f7769ebe2572504a3c625b3e991ef28dac453cf219846855d4580a25967fe`.
Its mode-0600 retry-forbidden journal records exactly two acknowledged SIGTERM
calls and zero remaining supervisors. Both rejected process chains exited and
windows `@20` and `@21` closed naturally; no retry or escalation occurred.

Mode-0700 archive controller `/tmp/har395-archive-roots.py` had SHA-256
`bdcd590164c1b84e44455899495d88e2349ee101b2646d079edac9ea396b6513`.
One app-server connection acknowledged all three exact archive requests. Its
mode-0600 retry-forbidden journal is complete. Same-connection readback kept
the accepted root active, idle, named `personal`, and in the exact Personal
cwd; all three targets are `notLoaded` under recoverable archived-session
paths with preserved file identities. The previously archived wrong-cwd root
remains archived and untouched.

Tmux now has only the canonical cowork, Codex, and Claude windows. Harness,
Personal, Students, both Claude panes, all monitor panes, the attached Harness
client, and the shared app-server identity are unchanged. The recovery-helper
receipt and tmux monitor report `healthy=3 phone_mirror=healthy`, with exactly
three active top-level roots and every canonical role check true. Personal and
Harness Git are clean. Focused thread-recovery, recovery-helper, and tmux
monitor suites pass. Next: run complete phase one, add the terminal receipt,
publish, reconcile producer state, and guarded-clean the exact temporary
artifacts.

Complete phase one passed all 98 suites in 58.694 seconds; native MPI was the
declared environment-only skip. The cleanup is terminal pending protected
publication and producer reconciliation.
