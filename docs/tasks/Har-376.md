# Har-376 — Adopt repository-specific task namespaces

## Status

Complete upon protected publication of the coordinated five-repository
migration and the Harness launcher update.

## Result

Harness active records and future allocation use `Har-NNN`; Personal uses
`Per-NNN`, Students uses `Stu-NNN`, Swallow uses `Swa-NNN`, and Website uses
`Web-NNN`. Historical identifiers and archives remain byte-preserved. The
shared `co` alias is retired, native `codex` remains unaliased, and remote
launches invoke the resilient Harness supervisor directly.

## Validation and handoff

Run the owning ledger, shell, launcher, and complete Harness validation gates;
publish through protected main; then propagate the exact merged control-plane
revision and clear idle managed client contexts without reading panes.
