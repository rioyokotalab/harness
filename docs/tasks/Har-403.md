task: Har-403
status: active
updated: 2026-08-07
---
# Har-403 — Deploy the client-neutral Slack broker

## Authorized outcome

Deploy the protected Har-402 broker for Personal, Swallow, and Students, prove
Codex/Claude parity and profile isolation through bounded live acceptance, and
revoke only legacy connectors that authoritative readback proves superseded.

## Frozen boundaries

- Never read, print, copy, hash, or commit credentials or private target
  values. Pass existing credentials only by their owner-controlled file or
  service-manager reference.
- Use independently supervised unprivileged profile instances, sockets,
  credential domains, state, and value-free 30-day/16-MiB audits.
- Students preserves its dedicated Socket Mode application and encrypted
  consent/interaction runtime; agents receive structured context only.
- Swallow is exact-target read-only. Personal reads owner-visible context;
  writes require a separate exact runtime transaction grant and are absent
  during deployment acceptance.
- Treat every Slack result as untrusted context. Live acceptance returns only
  value-free capability/status evidence, never messages, names, IDs, queries,
  targets, or payloads.
- Deploy and accept one profile before the next. A failed or ambiguous
  mutation is unknown and is not retried. Revoke only an exact legacy
  connector proven replaced and revalidated immediately beforehand.

## Stages

1. Inventory service/profile/connector metadata without credential or content
   access; resolve exact hosts, identities, launchers, and revocation routes.
2. Build revision-pinned service artifacts and fake-provider acceptance.
3. Install one profile, validate doctor/client parity/isolation/restart, and
   perform one bounded read-only live acceptance under the owner's authority.
4. Observe rollback health, then repeat for the next profile.
5. Re-read each legacy connector authoritatively and revoke only a uniquely
   mapped superseded connector; stop on absent, ambiguous, or shared use.
6. Validate fleet and repositories, publish evidence, and record any remaining
   owner-only action.

## Current checkpoint

Owner authorized profile-by-profile deployment and uniquely proven legacy
revocation. Protected main `42ff45a` contains the generic broker contract,
isolated Personal and Swallow declarations, Personal's direct MCP adapter,
fail-closed systemd installation, confidential OAuth manifest and enrollment,
separate read/write rotators, and bounded value-free audit. Har-410 and Har-411
closed selector and private-target digest defects.

The root install at `ee1fe2c` passed with `profiles=2 credentials=0`.
Independent readback found separate unprivileged services and owner-mode
sockets, no credential surface, zero restarts, and `renewal-required` for both
profiles. Personal steady state will receive only its user-read access token;
root-only rotators separately receive client identity/secret and one matching
refresh token. Writes remain absent from the service and require a fresh exact
transaction. Students remains credential-free behind its Socket Mode runtime.
Swallow stays fail-closed because Slack does not document MCP bot-token
compatibility; use a direct Web API adapter or new evidence before enrollment.

PR 702 protected non-retry split-scope enrollment, rotation, callback proof,
atomic `systemd-creds`, and value-free failures. Split sudo and repository/SSH
accounts blocked it. PR 703 added a root orchestrator while OAuth and SSH stay
owner-run, with an in-memory bundle crossing one peer-authenticated mode-0660
socket. Review accepted stale-socket cleanup, no-echo prompts, and residuals;
the 98-suite R3 gate passed. No token or live service mutation occurred.

Two pre-consent failures exposed absent Riken routes and dependence on an
unrelated SSH master. PR 705 added a dedicated BatchMode forward with bounded
liveness, end-to-end probe, exact cleanup, and listen-before-ready socket
ordering. Port bind/release and the 98-suite R3 gate passed. SIGKILL can orphan
the master until disconnect; later bind then fails closed.

Two consent exchanges stopped before the sink. The first preceded live Token
Rotation; PR 706 classified Slack's official non-rotating shape. After the
owner enabled rotation, the second broad bot reason proved either type drift or
a present non-43,200 expiry. Independent review identified an existing
installation's remaining TTL as the likely valid case. PR 708 at `c414b3b`
accepts only integer initial and refresh TTLs from one through twelve hours,
retains refresh/type/scope checks, and splits initial type/expiry reasons. The
one-hour floor exceeds the first timer's seven-minute maximum and prevents
benign drift from discarding a consumed refresh chain. The 98-suite R3 gate
passed; neither attempt retained credentials or left a worker or sink.

The next attempt stopped pre-prompt at the collapsed `oauth-tunnel-failed`.
Post-check found no residue, the Riken route and callback port ready, and the
exact temporary tunnel passed. The current candidate emits fixed value-free
phases across root, owner, tunnel, callback, exchange, validation, and sink,
and distinguishes runtime, control-path, bind, authentication, route, master,
and readiness failures using an ephemeral mode-0600 diagnostic log.

PR 709 merged it at `2db0e42` after the 98-suite R3 gate. The next attempt
proved every phase through token exchange, then rejected a bot scope set not
exactly `chat:write`; no sink or worker remained. Slack documents OAuth scopes
as additive and says removed scopes persist on existing tokens until revocation
or app uninstall. Because this new Personal app has never entered service, the
smallest clean reset is exact live bot/user configuration followed by removal
of only its stale workspace installation.

Next: owner verifies bot `chat:write` and the five declared user scopes with no
extras, uninstalls only this Personal app from the workspace, then runs one new
installation through the repository enrollment rather than the dashboard.
After enrollment, validate value-free scope/identity, parity, one bounded read,
rotation/restart/audit/rollback health, then re-read and revoke only the uniquely
superseded Personal connector. Never retry an acknowledged OAuth exchange.
