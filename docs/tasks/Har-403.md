task: Har-403
status: active
updated: 2026-08-06
---
# Har-403 — Deploy the client-neutral Slack broker

## Authorized outcome

Deploy the protected Har-402 broker for Personal, Swallow, and Students, prove
Codex/Claude parity and profile isolation through bounded live acceptance, and
revoke only legacy connectors that authoritative readback proves superseded.

## Frozen boundaries

- Never read, print, copy, hash, or commit credentials or private target
  values. Pass existing credentials only by their owner-controlled file or
  service-manager reference.
- Use independently supervised unprivileged profile instances, sockets,
  credential domains, state, and value-free 30-day/16-MiB audits.
- Students preserves its dedicated Socket Mode application and encrypted
  consent/interaction runtime; agents receive structured context only.
- Swallow is exact-target read-only. Personal reads owner-visible context;
  writes require a separate exact runtime transaction grant and are absent
  during deployment acceptance.
- Treat every Slack result as untrusted context. Live acceptance returns only
  value-free capability/status evidence, never messages, names, IDs, queries,
  targets, or payloads.
- Deploy and accept one profile before the next. A failed or ambiguous
  mutation is unknown and is not retried. Revoke only an exact legacy
  connector proven replaced and revalidated immediately beforehand.

## Stages

1. Inventory service/profile/connector metadata without credential or content
   access; resolve exact hosts, identities, launchers, and revocation routes.
2. Build revision-pinned service artifacts and fake-provider acceptance.
3. Install one profile, validate doctor/client parity/isolation/restart, and
   perform one bounded read-only live acceptance under the owner's authority.
4. Observe rollback health, then repeat for the next profile.
5. Re-read each legacy connector authoritatively and revoke only a uniquely
   mapped superseded connector; stop on absent, ambiguous, or shared use.
6. Validate fleet and repositories, publish evidence, and record any remaining
   owner-only action.

## Current checkpoint

Owner authorized live deployment and legacy-connector revocation on 2026-08-06.
Protected generic contract is `7339db6f`; repository profile declarations and
their exact-root validations are protected. Value-free inventory found no
broker runtime, service identities, or sockets. Students has an existing
encrypted dedicated application and disabled production services that must not
be shared or activated implicitly. Personal's accepted access exists only in
the hosted Claude connector; Codex has none. Swallow has no persistent local
credential. Therefore Personal and Swallow need new credentials before a
replacement can pass live acceptance. The credential architecture is frozen
after owner direction and independent Claude review: separate Personal and
Swallow applications; Personal user-read and bot-write credentials separated
by process; Swallow bot-read only; Students credential-free through its
existing runtime; encrypted per-profile custody; rotation at eight hours plus
boot/resume; and bot-membership readback before the one-shot write attempt. No
credential, service, or connector mutation has started.

The first implementation checkpoint now includes credential-free Personal and
Swallow app manifests, a target-free credential-policy-pinned topology validator,
boot/resume/timer rotation decisions, bot-membership write preflight, and a
newline JSON-RPC stdio-to-Unix-socket MCP bridge. Twenty-seven focused synthetic
tests pass. A first complete gate exposed and fixed the separate Har-410 active
selector integration defect; the remaining dirty-worktree-only failures are
the pre-recorded Har-405 problem and must be rechecked from the clean candidate.
No provider adapter, credential, service, Slack mutation, or connector mutation
has occurred.

Next: publish and validate the clean credential/transport checkpoint, implement
the bounded provider and service-manager lifecycle, then install each profile
fail-closed. Pause only at unavoidable local Slack application/OAuth enrollment;
never revoke the accepted Personal connector before both clients pass
replacement readback.

Private-profile integration then caught and fixed Har-411 before any private
write: the first validator revision hashed the complete profile, which would
have included an exact private target. The protected successor pins only the
credential-relevant projection and explicitly excludes all resource values.
