task: Har-403
status: complete
updated: 2026-08-07
---
# Har-403 — Deploy the client-neutral Slack broker

## Authorized outcome

Deploy the protected Har-402 broker for Personal, Swallow, and Students, prove
Codex/Claude parity and profile isolation through bounded live acceptance, and
revoke only legacy connectors that authoritative readback proves superseded.

## Frozen boundaries

- Never read, print, copy, hash, or commit credentials or private target
  values. Pass existing credentials only by their owner-controlled file or
  service-manager reference.
- Use independently supervised unprivileged profile instances, sockets,
  credential domains, state, and value-free 30-day/16-MiB audits.
- Students preserves its dedicated Socket Mode application and encrypted
  consent/interaction runtime; agents receive structured context only.
- Swallow is exact-target read-only. Personal reads owner-visible context;
  writes require a separate exact runtime transaction grant and are absent
  during deployment acceptance.
- Treat every Slack result as untrusted context. Live acceptance returns only
  value-free capability/status evidence, never messages, names, IDs, queries,
  targets, or payloads.
- Deploy and accept one profile before the next. A failed or ambiguous
  mutation is unknown and is not retried. Revoke only an exact legacy
  connector proven replaced and revalidated immediately beforehand.

## Stages

1. Inventory service/profile/connector metadata without credential or content
   access; resolve exact hosts, identities, launchers, and revocation routes.
2. Build revision-pinned service artifacts and fake-provider acceptance.
3. Install one profile, validate doctor/client parity/isolation/restart, and
   perform one bounded read-only live acceptance under the owner's authority.
4. Observe rollback health, then repeat for the next profile.
5. Re-read each legacy connector authoritatively and revoke only a uniquely
   mapped superseded connector; stop on absent, ambiguous, or shared use.
6. Validate fleet and repositories, publish evidence, and record any remaining
   owner-only action.

## Current checkpoint

Personal acceptance is complete. Its isolated user-read service, transient
bot-write transaction, encrypted rotation, Codex/Claude repository-local
registrations, bounded read, audit, and restart acceptance passed. Shared broad
Codex and Claude Slack integrations are not uniquely revocable; Har-414 tracks
their containment. Students remains credential-free behind its dedicated
Socket Mode Sensei runtime.

PR 728 (`44615bc`) protected Swallow's Web API-only exact-resource adapter,
structured link proof, pinned bounded downloads, bot-only OAuth, four-field
encrypted custody, one rotator, recoverable quarantine, and isolated units.
Har-415 bounds provider envelopes and Har-416 fixes communication helper
routing. PR 730 (`8861c54`) made OAuth scope drift self-diagnosing. It proved a
stale installed `chat:write` grant; the owner removed only that installation
and a fresh consent installed an exact-scope encrypted Swallow bundle.

The first startup then exposed the intentionally unbound exact-resource
sentinel and a 172-millisecond installer readiness race; no Slack content or
write was accessed. Har-418 adds hidden, root-owned, atomic exact-resource
binding with rollback. Har-419 requires Personal and Swallow services to remain
active across a one-second synchronous startup window. PR 731 (`4c8e7ee`)
merged both fixes. Hidden binding, service/socket/timer health, zero restarts,
and exact effective scopes now pass.

Live-acceptance preparation then found that the exact-resource client schema
still required the private bound identifier. No content request occurred.
Har-420 removes that argument for exact profiles and injects it only inside the
broker, while workspace-visible Personal behavior remains unchanged. Focused
Slack and final integrated gates passed; PR 732 (`603db9e`) merged and the live
service refresh completed.

The resulting single live read reached Slack but exceeded the 64-KiB broker
envelope. It was discarded with only a stable failure and was not retried.
Har-421 now returns the largest exactly encoded whole-message prefix with
`has_more=true`, or fails value-free if one item alone cannot fit. PR 733
(`6425c9d`) merged that behavior and refreshed the live service.

One separately authorized changed-input read then passed with nine bounded
whole messages and `truncated=yes`; raw content stayed in process memory and
was discarded. The exact target remained broker-internal, the shared schema
reported five tools for both clients, and service restarts remained zero.
Personal, Swallow, and the credential-free Students path are therefore
accepted. Har-414 subsequently published exact reversible client containment,
applied the same narrow user-local controls, and completed authoritative
metadata-only readback: the Codex hosted target is absent and the Claude entry
is authentication-required rather than connected. No internal Slack
application, broker service, plugin, credential domain, Students runtime, or
unrelated connector was removed.

Complete. All three profile paths pass their bounded acceptance and the shared
legacy hosted connections are disconnected while repository-local containment
remains active and reversible.
