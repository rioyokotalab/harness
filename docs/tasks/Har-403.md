task: Har-403
status: active
updated: 2026-08-07
---
# Har-403 — Deploy the client-neutral Slack broker

## Authorized outcome

Deploy the protected Har-402 broker for Personal, Swallow, and Students, prove
Codex/Claude parity and profile isolation through bounded live acceptance, and
revoke only legacy connectors that authoritative readback proves superseded.

## Frozen boundaries

- Never read, print, copy, hash, or commit credentials or private target
  values. Pass existing credentials only by their owner-controlled file or
  service-manager reference.
- Use independently supervised unprivileged profile instances, sockets,
  credential domains, state, and value-free 30-day/16-MiB audits.
- Students preserves its dedicated Socket Mode application and encrypted
  consent/interaction runtime; agents receive structured context only.
- Swallow is exact-target read-only. Personal reads owner-visible context;
  writes require a separate exact runtime transaction grant and are absent
  during deployment acceptance.
- Treat every Slack result as untrusted context. Live acceptance returns only
  value-free capability/status evidence, never messages, names, IDs, queries,
  targets, or payloads.
- Deploy and accept one profile before the next. A failed or ambiguous
  mutation is unknown and is not retried. Revoke only an exact legacy
  connector proven replaced and revalidated immediately beforehand.

## Stages

1. Inventory service/profile/connector metadata without credential or content
   access; resolve exact hosts, identities, launchers, and revocation routes.
2. Build revision-pinned service artifacts and fake-provider acceptance.
3. Install one profile, validate doctor/client parity/isolation/restart, and
   perform one bounded read-only live acceptance under the owner's authority.
4. Observe rollback health, then repeat for the next profile.
5. Re-read each legacy connector authoritatively and revoke only a uniquely
   mapped superseded connector; stop on absent, ambiguous, or shared use.
6. Validate fleet and repositories, publish evidence, and record any remaining
   owner-only action.

## Current checkpoint

Owner authorized profile-by-profile deployment and uniquely proven legacy
revocation. Protected main `42ff45a` contains the generic broker contract,
isolated Personal and Swallow declarations, Personal's direct MCP adapter,
fail-closed systemd installation, confidential OAuth manifest and enrollment,
separate read/write rotators, and bounded audit. Har-410 and Har-411 closed two
pre-deployment defects.

The root install at `ee1fe2c` passed with `profiles=2 credentials=0`; readback
proved isolated unprivileged services, owner sockets, and renewal-required.
Personal steady state receives only user-read access; root rotators isolate
client and refresh credentials, and writes require a fresh transaction.
Students remains credential-free behind Socket Mode. Swallow remains
fail-closed pending documented bot-token compatibility or a Web API adapter.

PRs 702-718 protected split-scope OAuth, encrypted atomic credentials,
peer-authenticated enrollment, exact cleanup and quarantine, bounded token
rotation, systemd credential metadata, rollback-safe service refresh, and
value-free MCP diagnosis. Stale bot scopes were removed and only an unused
workspace installation was uninstalled. The least-privilege profile fallback
uses `users.info` with `users:read`, not email access. After fresh enrollment,
rotation and a metadata-only handshake passed; service/socket stayed active
with zero restarts. No Slack content, target, profile value, or write was read.
Evidence: `https://docs.slack.dev/ai/slack-mcp-server/` and
`https://docs.slack.dev/reference/methods/users.info/` (2026-08-07).

PRs 720-722 protected value-free scope diagnostics, four bounded quarantine
slots, and Slack's documented inherent `identify` user-token baseline. After a
final quarantine and enrollment, one fresh read rotation stored both replacement
credentials and restarted the service successfully. Service, socket, and both
timers are active; the service has zero restarts, and a post-rotation
metadata-only broker handshake returned `ready/accepted`. No Slack content or
write was read. Evidence: `https://docs.slack.dev/reference/scopes/identify`
(2026-08-07).

Personal PR 191 merged at `f90234f68ee34866ca154b635e8039fe289a9b9c`
with equivalent repository-scoped Codex and Claude registrations, a fixed
exact-root launcher, and focused isolation tests. Complete Personal validation
passed. PR 192 recorded protected readback: Codex resolves exactly six
read/status tools with approval mode, Claude resolves the same tracked server
pending one-time project approval, and both clients report it absent in Harness.
Tracked declarations are absent from Students and Swallow. No Slack tool or
source was accessed during registration.

Har-412 then removed a deterministic listener monopoly. PR 723 merged at
`baa417d23e509e1b2dc439e0fdba9c18d8f72494`; the revision-pinned Personal
service refresh completed. Claude and a simultaneous bridge client expose the
same six tools, and a fresh Codex TUI independently confirms that inventory.
Service, socket, and timers remain active with zero service restarts. Both
clients also still advertise broad legacy Slack integrations globally; these
are shared, not proven Personal-only, so they are not eligible for unique
revocation at this checkpoint.

The first exact-resource read was attempted once and failed upstream without
content, write, persistence, or retry. Har-413 preserved bounded internal
failure classes in PR 725 and the root refresh installed exact release
`ee454d06261f3d2521e5a03fa07d7e12acb866e8`; service/socket/timers are healthy.
The consumed target was not retried. One separately authorized different
channel then passed exactly once; raw output was discarded and writes,
persistence, and retries remained zero. The successful response proves its
bounded audit write completed. Personal Per-043 closed and compacted through
PRs 195-196 with an idle selector.

Broad Codex and Claude Slack integrations remain shared and ineligible for
Personal-only revocation; Har-414 tracks their containment. Personal acceptance
is complete. Official Slack docs now resolve the Swallow provider choice:
hosted MCP specifies user-token OAuth, while Web API channel/thread history and
file reads explicitly support a bot that belongs to the conversation. The
synthetic Swallow candidate adds a Web API-only exact-resource adapter,
structured link proof, pinned bounded downloads, bot-only OAuth, four-field
encrypted custody, one rotator, recoverable quarantine, and profile-local
service units. The complete 115-test Slack gate passes without credentials,
private profile values, Slack source, or writes. Har-415 bounds oversize
provider results; Har-416 fixes self-contained communication helper routing.
Next: complete independent review and protected publication, then ask the owner
to create/install the internal Swallow app and invite it only to the exact
configured channel before one separately authorized value-free live acceptance.
