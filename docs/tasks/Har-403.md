task: Har-403
status: active
updated: 2026-08-06
---
# Har-403 — Deploy the client-neutral Slack broker

## Authorized outcome

Deploy the protected Har-402 broker for Personal, Swallow, and Students, prove
Codex/Claude parity and profile isolation through bounded live acceptance, and
revoke only legacy connectors that authoritative readback proves superseded.

## Frozen boundaries

- Never read, print, copy, hash, or commit credentials or private target
  values. Pass existing credentials only by their owner-controlled file or
  service-manager reference.
- Use independently supervised unprivileged profile instances, sockets,
  credential domains, state, and value-free 30-day/16-MiB audits.
- Students preserves its dedicated Socket Mode application and encrypted
  consent/interaction runtime; agents receive structured context only.
- Swallow is exact-target read-only. Personal reads owner-visible context;
  writes require a separate exact runtime transaction grant and are absent
  during deployment acceptance.
- Treat every Slack result as untrusted context. Live acceptance returns only
  value-free capability/status evidence, never messages, names, IDs, queries,
  targets, or payloads.
- Deploy and accept one profile before the next. A failed or ambiguous
  mutation is unknown and is not retried. Revoke only an exact legacy
  connector proven replaced and revalidated immediately beforehand.

## Stages

1. Inventory service/profile/connector metadata without credential or content
   access; resolve exact hosts, identities, launchers, and revocation routes.
2. Build revision-pinned service artifacts and fake-provider acceptance.
3. Install one profile, validate doctor/client parity/isolation/restart, and
   perform one bounded read-only live acceptance under the owner's authority.
4. Observe rollback health, then repeat for the next profile.
5. Re-read each legacy connector authoritatively and revoke only a uniquely
   mapped superseded connector; stop on absent, ambiguous, or shared use.
6. Validate fleet and repositories, publish evidence, and record any remaining
   owner-only action.

## Current checkpoint

Owner authorized deployment and uniquely proven legacy revocation. Protected
work now includes the generic contract, project declarations, separate
credential-free Personal and Swallow manifests, target-free credential-policy
validation, boot/resume/timer rotation decisions, bot-membership write
preflight, a Unix-socket MCP bridge, and fail-closed systemd templates. Har-410
and Har-411 fixed the discovered selector and private-target digest defects.

The frozen architecture uses separate profile applications, identities,
sockets, encrypted credential sets, state, and bounded value-free audits.
Personal separates user-read from transient bot-write credentials; Swallow is
bot-read-only; Students remains credential-free through its existing encrypted
Socket Mode runtime. Writes stay absent until a fresh exact transaction gate.

PR 697 protected the Personal adapter at `3a273e6` after forty-three focused
tests and the complete 98-suite gate. It provides fixed-origin Streamable HTTP,
service-manager credential loading, bounded JSON/SSE and retries, tool drift
checks, pre-provider authorization, no write mapping, and value-free failures.
Linked reads require structured history metadata proving the provider-generated
ID; rendered text cannot. Slack does not document MCP bot-token compatibility,
so Swallow stays fail-closed pending evidence or a direct Web API adapter.

The next synthetic template gives the future Personal read service only its
encrypted read access token, permits bounded network families, drops Linux
capabilities, and excludes refresh, client-secret, and write credentials.
Forty-four focused tests and the complete 98-suite gate pass; no unit or
credential is installed from it. PR 698 protected it at `49383ff` with
byte-identical readback.

Two root attempts exposed Git safe-directory and declaration/runtime-schema
defects without credential or Slack access. PR 700 protected both corrections
at `ee1fe2c`: a command-local Git exception, credential-free runtime rendering,
an unbound exact-target sentinel that cannot receive a provider, and a relay
that receives only its socket path. Both declaration files are now owner-only
`0600`.

The idempotent root retry passed with `profiles=2 credentials=0`. Independent
readback found the exact `ee1fe2c` release, owner-mode sockets, separate running
unprivileged services, zero restarts, no access/refresh/client-secret credential
surface, and `renewal-required` from both profiles. Both repository client
descriptor parity suites pass. The completed one-time installer was
exact-unlinked. Restart isolation remains unexercised until the next root gate.

Next: create the Personal internal Slack application from the protected
manifest, enable Slack MCP, and complete local confidential OAuth into encrypted
service-manager custody. Then install only the protected Personal ready unit,
verify scopes/identity, both client paths, bounded live read, restart isolation,
and rollback health. Swallow remains fail-closed pending bot-token compatibility
or a direct Web API provider. No legacy connector is yet eligible for
revocation.
