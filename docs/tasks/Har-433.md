task: Har-433
status: active
updated: 2026-08-08
phase: bridge root created; provisional TUI blocked by codex 0.147.0 panic
---
# Har-433 — Rebind the Riken phone and tmux Codex surfaces

## Outcome and authority

Make exactly Riken's Harness phone and tmux surfaces share one app-server-backed
Codex root. Preserve the existing `harness` tmux layout, saved roots, shared
app server, Claude peer, credentials, and unrelated sessions. Office and Home
remain untouched: no connection, message, pane inspection, input, signal, or
checkout action is authorized there.

The owner authorized the Riken repair after Har-429's strict-separation
fallback. This is a new bridge-first transaction, not a retry of either
Har-425's provisional launch or Har-429's failed controller deliveries. Do not
restart the shared app server, replay prompts, rewrite transcripts, delete or
archive roots, signal a process group, or rebuild tmux geometry. The owner
explicitly waived validation and test runs because this operation changes no
repository behavior; retain only this compact operational ledger.

## Frozen preflight

- Repository control truth is protected `origin/main`
  `9a7674bda6b14ba6559576a35635b68bcc6f7a31`; the preserved phone checkpoint
  remains `ce4e9257cdc322edc25913efdfa6bb4c3c5b6aa5` in the primary checkout.
- Canonical tmux session `harness` has old Codex window `@0`, index `0`, name
  `codex`, pane `%0`, pane parent PID `21070`, and repository
  `/Users/yokotar/harness`. The unaffected Claude peer is window `@1`, index
  `1`, name `claude`, pane `%1`, PID `21096`, in the same repository.
- The old supervisor PID `21070` started 2026-08-01 12:55:30 local time and
  owns standalone Codex TUI PID `21285`, started 12:55:32. Its exact selector
  is `--name harness-codex-resume --target harness --last`; fresh status is
  `running`, attempt zero, reason `active`. It is not a remote-backed peer.
- The unchanged shared Codex app-server daemon PID `60462` started 2026-07-31
  12:55:07 and owns remote-control child PID `27895`, started 2026-08-07
  11:25:10. Exact-thread check for Har-429 root
  `019fc0c7-f3e7-7e90-9d64-fceee8dfe161` reached the app server and reported
  `active`; matching IDs alone remain insufficient ownership evidence.
- The fresh fleet-health query from this unsupported failover topology could
  not reach Office or Home. That is route-unavailable evidence only. Neither
  Mac was otherwise queried or changed.

## Transaction and retry boundary

Create one new app-server root with exact Harness CWD and frozen sandbox and
approval policy, then read it back on the same connection and give it a unique
provisional name. A sent `thread/start` is retry-forbidden. Launch one distinct
provisional tmux window/runtime using `--remote-session NEW_ID`; preserve the
old window and process chain. Send one cold-start ledger instruction, require
an assistant-bearing idle completion plus watcher/socket/process acceptance,
then stop for the owner's physical-phone visibility confirmation.

Only after that confirmation may the accepted live pane and root be promoted
by rename/move without relaunch. Publish a reversible identity checkpoint,
revalidate it, and send one `SIGTERM` only to the frozen old leaf TUI. Never
signal a zombie or retry an ambiguous request, launch, promotion, or signal.
If any identity or acknowledgement is ambiguous, retain both chains and stop.

## Controller checkpoint — 2026-08-08 22:24–22:41 local

Bounded controller transaction `har433-riken-bridge-20260808-a` (Riken only;
Office and Home untouched, unqueried).

Preflight revalidation: every frozen identity matched exactly — window `@0`
pane `%0` supervisor PID 21070 (start 2026-08-01 12:55:30) with TUI PID 21285
(12:55:32), Claude peer `@1`/`%1` PID 21096, app-server daemon PID 60462
(2026-07-31 12:55:07) with remote-control child PID 27895 (2026-08-07
11:25:10, socket `~/.codex/app-server-control/app-server-control.sock`),
canonical runtime `harness-codex-resume` running attempt 0 reason active.
Zero drift at start and at final re-verification.

App-server root transaction (one WebSocket connection each, reusing the
`harness-codex-thread-recovery` client):

- First initialized `thread/start` (cwd `/Users/yokotar/harness`, sandbox
  `danger-full-access`, frozen granular approval policy) was deterministically
  rejected: code `-32600` `askForApproval.granular requires experimentalApi
  capability`. No thread was created; read-only `thread/list` reconciliation
  showed only the preserved root `019fc0c7-f3e7-7e90-9d64-fceee8dfe161`
  (`riken`).
- Corrected transaction on a new connection declaring
  `capabilities.experimentalApi=true`: `thread/start` acknowledged exactly
  once. New root `019fe195-cef0-7fb1-9236-a7db95e00814`, cwd
  `/Users/yokotar/harness`, sandbox `dangerFullAccess`, granular policy
  accepted byte-for-byte; `thread/read includeTurns=false` and
  `thread/name/set` → `har433-riken-bridge-provisional-a` read back on the
  same connection. Retry boundary: both sent `thread/start` requests are
  spent; never resend either.
- Rollout `sessions/2026/08/08/rollout-2026-08-08T22-35-19-019fe195-….jsonl`:
  single link, owner-owned; installed 0.147.0 created mode 0644 (skill text
  names 0664) — tightened to 0600 via one no-follow descriptor transaction.

Provisional bridge launch and failure:

- Created one distinct window `har433-bridge` (`@3`, index 2, pane `%3`) and
  launched `harness codex-resilient --run --name har433-riken-bridge
  --target harness --remote-session 019fe195-cef0-7fb1-9236-a7db95e00814`
  (supervisor PID 37292, start 22:36:25). Watcher (PID 37571, start 22:36:29)
  reached `watching`/`thread-idle` with recoveries=0 rolled_back=0 on exactly
  the new thread.
- The remote TUI (`codex resume --remote unix:// NEW_ID`) panicked on every
  attempt: `reader source not set`
  (`crossterm .../src/event/read.rs:39`), supervisor backoff
  `transient-exit` attempts 1–4; the supervisor then exhausted its budget and
  self-unwound (final states: resilient `stale`/`owner-absent`, watcher
  `stopped`/`operator-stop`), and tmux closed window `@3` with it. No
  cold-start instruction was ever sent; the new thread holds zero turns.
- Hypothesis (labeled): codex standalone `current` flipped to
  `0.147.0-aarch64-apple-darwin` at 22:06 today (0.146.1 remains installed);
  the harness remote-launch pattern is byte-identical across staged releases
  `16b2c2d`/`2d254d5`, so this is a 0.147.0 TUI regression under the
  supervised background `</dev/tty` launch, not a harness change.

Both chains preserved: canonical window/pane/process/start identities and the
Claude peer re-verified unchanged after the failure; shared app server
untouched; old root `019fc0c7…` untouched; new root preserved idle under its
provisional name. Promotion was not reached, the old leaf was never signaled,
and the physical-phone confirmation gate was not reached. No validation or
test suite was run (owner waiver). No reply was injected into Codex; this
ledger is the report channel.

## Next action

Owner decision required: the bridge cannot reach acceptance while the
0.147.0 TUI panics under the supervised remote launch. Options include
authorizing a pinned 0.146.1 launcher for the bridge TUI, deferring until a
fixed codex release, or authorizing a supervised foreground-launch change
(repository behavior change; would need owning validation). The next
controller must not resend the spent `thread/start` requests; it may reuse
root `019fe195-cef0-7fb1-9236-a7db95e00814` (`har433-riken-bridge-
provisional-a`), which is idle with zero turns, and must re-run full identity
preflight before any write.
