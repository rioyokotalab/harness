# T-374 — Baseline archive-generation triggers on covered sources

## Status

Complete. PR #566 merged the exact candidate as
`a21d87cec65258d40499a3a9437a7966311680a9` on 2026-08-02, and its isolated
worktree was archive-first retired under transaction
`worktree-archive-20260801t234438z-2173914-9dc3d1`. T-373 cleanup
and later exact worktree closeouts raised the durable receipt count from 27 to
31. The live report then emitted `generation_trigger=receipts` even though the
latest restore-proven generation covered 27 receipts and only four new receipts
existed. The lifetime-count trigger could never turn off after crossing 30;
unchanged creation was rejected later by duplicate suppression, but reports
would permanently request an action that could not succeed.

The exact candidate reports `generation_trigger=no`, delta basis, four
uncovered receipts, and 42,812,925 uncovered bundle bytes on the live
31-receipt state. Focused housekeeping and final complete 100-suite R3 pass;
the only first-run failure was the deterministic context benchmark made stale
by T-373's already-merged board shrink, and its regenerated artifact passes its
own contract test.

## Objective

Measure generation need against growth not covered by the newest valid
generation while preserving bootstrap behavior, immutable receipt audits,
independent restores, and every fail-closed compaction gate.

## Acceptance

- With no generation, byte/receipt/age triggers retain their whole-state
  bootstrap semantics.
- With a valid latest generation, receipt count and age use only source
  receipts absent from its exact name/digest set; byte growth uses unique,
  present bundles named by those uncovered receipts.
- Report output states `bootstrap|delta`, uncovered receipt count, and measured
  trigger bytes without exposing source values.
- More than 30 uncovered receipts triggers a new generation; immediately after
  its verified publication, unchanged state reports no trigger even when
  lifetime receipts exceed 30 and unrelated evidence remains large.
- The end-to-end fixture still proves two-generation compaction and creation of
  a later generation that recovers a compacted source.
- Focused housekeeping tests and complete Harness R3 pass. No live generation,
  archive, repository, session, ruleset, approval count, credential, scheduler,
  deployment, or remote branch is changed.

## Next action

None. Keep normal delta-trigger monitoring; no live generation creation is
warranted while the report remains below its uncovered-growth thresholds.
