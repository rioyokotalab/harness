task: Har-428
status: active
updated: 2026-08-08
phase: independent keep-going Mac repair
---
# Har-428 — Roll out Har-427 to Office and Home

## Outcome and frozen scope

Propagate the protected Har-427 merge `27091cc76f403070f98020aa866ecf3d43466e00`
to the managed Harness checkouts on exactly `office` and `home`. Each Mac acts
independently through its existing local Codex session and network access. This
Riken session coordinates the durable result but does not connect to either
Mac, use fleet-sync, inspect panes, alter credentials, or change tmux state.

Har-427 made the complete suite truthful on Darwin while preserving Linux
contracts. Its authoritative Riken gate passed tier R3 with all 99 focused
suites in 390.575 seconds plus the enclosing phase-one integration. The native
MPI smoke is an expected skip outside a declared MPI environment.

## Per-node procedure

For its own node only, the consumer must:

1. Read root `AGENTS.md`, `TODO.md`, and this record; confirm the repository
   root is exactly `$HOME/harness` and the logical host is the assigned Mac.
2. Inspect branch, upstream, worktree, HEAD, and recent commits. Continue only
   from clean `main`; never stash, reset, discard, or overwrite unrelated work.
3. Fetch `origin`, prove the Har-427 merge is an ancestor of `origin/main`, and
   fast-forward local `main` with `git pull --ff-only`. A dirty tree,
   non-`main` branch, divergence, missing merge, or transport failure is a
   blocked result, not permission to repair broader state.
4. Prove `HEAD == origin/main` and Har-427 is an ancestor of `HEAD`, then run
   `tests/test-phase1.sh`. Do not install packages or change host configuration
   to force a pass.
5. Report the node name, before and after OIDs, ancestry/readback checks,
   complete-suite result and duration, explicit skips, final branch/upstream,
   and final worktree status. On failure, include the first exact error and
   whether guarded fixture cleanup succeeded.

Consumers do not edit this ledger, create branches, commit, push, open PRs,
delete anything, or act on the other Mac. The coordinator records their
reports later. Rollback, if a real regression is found, is a new protected
Harness change; never locally rewind managed `main`.

## Checkpoints

- `office`: advanced cleanly from `825916c` to protected `971f7af`; Har-427
  ancestry and `HEAD == origin/main` passed. Its 99 focused suites completed in
  1570.962 seconds, but housekeeping reported that the admin-stage interrupt
  retained its worktree and evaluation later lacked `child.pid`. Guarded
  cleanup and guarded-delete passed; native MPI skipped; final `main` remained
  clean. No repair or rerun occurred.
- `home`: advanced cleanly from `1a22b51` to protected `971f7af`; Har-427
  ancestry and `HEAD == origin/main` passed. Its 99 focused suites completed in
  944.826 seconds, but evaluation lacked its interrupt fixture `child.pid`.
  Guarded cleanup and guarded-delete passed; native MPI skipped; final `main`
  remained clean. No repair or rerun occurred.

The fresh Riken `harness fleet-health` check could not reach either Mac (or
most Linux routes) from this unsupported topology; that is unavailable route
evidence, not a host failure. The owner separately confirmed remote-control
access to Office and Home, which is sufficient to start their local agents.

## Completion gate

Har-428 completes only after both node reports prove clean protected-main
readback containing `27091cc`, a passing complete phase-one suite with only
declared skips, and a clean final worktree. Until then, either node may progress
without waiting for the other.

## Authorized retry after Har-432

The owner resumed Har-428 after protected merge `9a7674b`. Current `main`
contains both exact failure repairs from `559f632`: housekeeping now selects
the interrupt receipt by its worktree, and evaluation arms its interrupt only
after the child flushes `child.pid`. Har-432 separates discovery, single-owner
repair, and final validation.

Each consumer may now fast-forward its own clean `main`, prove `559f632` and
`9a7674b` are ancestors, and run affected discovery against its recorded base
`971f7af`. Discovery is keep-going: freeze its complete failure inventory
before repair. Create a separate local worktree and node-named branch for
repairs; keep live `main` clean. Fix one frozen issue at a time and run only its
owning suite. Do not rerun discovery after every fix.

Both consumers are authorized to continue until every frozen owner passes and
the final gate passes. If a final run exposes a new issue, classify it as a
selector/discovery defect, repair only its owner, run one corrective
keep-going affected discovery, and try the final gate again. Never blindly
repeat an unchanged failure.

Consumers may edit and commit bounded fixes locally, but must not push, open or
merge a PR, edit this ledger, contact the other Mac, or alter host
configuration. If the same suite fails on both Macs, Office owns the candidate
implementation; Home independently diagnoses and preserves evidence, then
validates the integrated fix. Each reports frozen issues, local commits,
individual owner results, discovery/final durations and skips, and final
branch/worktree state so the coordinator can integrate one protected task PR.
