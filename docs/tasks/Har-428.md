task: Har-428
status: complete
updated: 2026-08-09
phase: complete; both Macs clean on protected main
---
# Har-428 — Roll out Har-427 to Office and Home

## Outcome and frozen scope

Propagate the protected Har-427 merge `27091cc76f403070f98020aa866ecf3d43466e00`
to the managed Harness checkouts on exactly `office` and `home`. Each Mac acts
independently through its existing local Codex session and network access. This
Riken session coordinates the durable result but does not connect to either
Mac, use fleet-sync, inspect panes, alter credentials, or change tmux state.

## Checkpoints

- `office`: after its earlier `971f7af` failures, advanced live `main` cleanly
  to `9a7674b` with required `27091cc`, `559f632`, and `9a7674b` ancestry.
  Discovery against `971f7af` ran 60 suites in 607.132 seconds: 58 passed and
  two failed. `test-personal-agent.sh` reported
  `FAIL: concurrent operation executed` because its five-second lock owner
  could expire under parallel Darwin load. `test-onboard-project-repository.sh`
  raised `FileNotFoundError: maintenance.lock` because synthetic repositories
  permitted background Git maintenance during Darwin cleanup.

  Local commit `1893fbb` disables automatic maintenance only in synthetic
  repositories (`test_producer_ledger.py`; 67 tests passed in 21.097 seconds).
  Commit `a4620f0` uses the live test-shell PID for the Personal lock fixture
  (`test-personal-agent.sh`; owner suite passed). Corrective discovery passed
  35/35 in 160.421 seconds. The final gate passed 104/104 focused suites in
  1396.164 seconds and phase one in 1401.903305 seconds, with no skips. Live
  `main` remained clean at `9a7674b`; the clean isolated candidate ended at
  `a4620f0e14954780f713d998a8ee7aa809557ed2`, two local commits ahead, with
  no upstream, push, PR, merge, ledger edit, or host change.

  After PR #759 merged, Office fast-forwarded live `main` from `9a7674b` to
  `14309ce`; `HEAD == origin/main` and ancestry for `96a17a6` and `a5d5c92`
  passed. The live checkout is clean. Its clean candidate worktree remains at
  `a4620f0`, now tracking its remote branch; no tests or prohibited changes ran.
- `home`: advanced cleanly from `971f7af` to protected `9a7674b`; `HEAD ==
  origin/main` and required `27091cc`, `559f632`, and `9a7674b` ancestry passed.
  The earlier evaluation race was already fixed by `559f632` using child-to-
  parent readiness after writing, flushing, and syncing `child.pid`.
  Discovery against `971f7af` passed all 60 suites in 426.488 seconds,
  including `test-evaluation.sh` in 56.484 seconds, with empty failure and
  not-run sets and no skips. The final gate passed all 104 focused suites in
  991.504 seconds and phase one in 994.563592 seconds, with no skips. Home made
  no changes or repair worktree and ended on clean synchronized `main` with
  only `/Users/yokotar/harness` present.

  After PR #759 merged, Home fast-forwarded live `main` from `9a7674b` to
  `14309ce`; exact `HEAD == origin/main` and ancestry for `96a17a6` and
  `a5d5c92` passed. The sole worktree remains clean on `main`; no test, repair,
  ledger, other-Mac, or host-configuration action ran.

The fresh Riken `harness fleet-health` check could not reach either Mac (or
most Linux routes) from this unsupported topology; that is unavailable route
evidence, not a host failure. The owner separately confirmed remote-control
access to Office and Home, which is sufficient to start their local agents.

## Completion

Both node reports prove clean protected-main readback at `14309ce`, the required
ancestry, passing complete Mac gates with no skips, and clean live worktrees.
Their post-merge readbacks reused unchanged validation evidence as designed.

## Integration

The owner authorized Office to publish only its clean candidate branch. Remote
readback matched `a4620f0`; the coordinator fetched and reviewed both exact
commits, then cherry-picked them unchanged as `96a17a6` and `a5d5c92` onto the
Har-428 integration branch. Live Mac checkouts remain untouched.

Both imported owning suites and the ledger/context checks passed on Riken. The
first final gate stopped safely after 31/104 suites in 132.271 seconds because
`test-tmux-config.sh` requires a clean committed checkout; seven jobs were
cancelled and 73 not run. This is a retry-safe precondition failure: commit the
pending ledger bytes, run only that owner, then run the final gate once.
After committing, `test-tmux-config.sh` passed in 7.7 seconds and the clean
final gate passed all 104 focused suites in 224.438 seconds plus phase-one
integration, with no reported skip or failure.

Protected CI `portable-phase1` passed on exact head `37f484c`. PR #759 merged
cleanly as `14309ce1653b9e5d1185fb641dc01d32f1393aa8`; protected `origin/main`
readback matches that merge. No task branch or worktree was deleted.

## Closeout

Har-428 is complete. Preserve Office's clean repair worktree and published
candidate branch until a separately authorized housekeeping task; Home has no
repair residue. No further validation, Mac action, or publication is required.
