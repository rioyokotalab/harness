# T-368 — Retire the Codex–Claude Cowork skill

## Owner request and boundary

Completely remove `codex-claude-cowork` as an installed, discoverable, or
required workflow, and stop requiring nightly or other duration-specified jobs
to use it. This investigation makes no deletion or behavioral change and waits
for owner go before implementation.

“Complete removal” is interpreted as zero live implementation, discovery,
policy, validator, or active-task dependency. Retain immutable completed-task,
audit, plan, presentation-timeline, and historical Cowork evidence because it
records what actually happened and is not an installed capability. Preserve
the unrelated `harness:cowork` tmux window name and Local Codex↔Claude message
transport.

Baseline: clean `main` and fetched `origin/main` at merge commit `e602c5b`.
Working branch: `codex/t368-remove-cowork-skill`.

## Verified dependency inventory

### Harness

Harness currently exposes 18 shared skills. The retirement leaves 17.

Delete 13 tracked live paths:

- the ten files under `shared/skills/codex-claude-cowork/`;
- `.agents/skills/codex-claude-cowork` and
  `.claude/skills/codex-claude-cowork`;
- `tests/test-codex-claude-cowork-skill.sh`.

Reconcile current policy, documentation, and validation surfaces:

- `docs/agent-policy/duration.md` must select only
  `long-running-task-ledger`, while retaining objective/benchmark, cadence,
  cutoff, stop-condition, and proportional-summary requirements;
- remove the duration composition rule from
  `shared/skills/fleet-repository-hardening/SKILL.md` without weakening its
  independent ledger, authority, LIFO, cutoff, or closeout gates;
- update `README.md` and `docs/agent-client-config.md` so they describe
  optional task-directed cross-client messaging rather than Cowork;
- remove Cowork-only rows and assertions from `tests/focused-suites.tsv`,
  `tests/validation-impact.tsv`, `tests/test-agent-policy-routing.sh`,
  `tests/test-skill-catalog-budget.sh`, `tests/test-skill-context-budgets.sh`,
  `tests/test-skill-context-gates.sh`, and
  `tests/test-validation-router.sh`;
- convert the live T-351 context scenario and benchmark from duration-Cowork
  to duration-ledger in `context-scenarios.tsv`,
  `context-benchmark.md`, and `tests/test-context-routing-benchmark.sh`, then
  regenerate derived counts from exact final bytes;
- replace the one current T-363 statement that assumes the skill still exists.

Dynamic discovery tests such as Claude takeover need no hard-coded edit, but
must prove the final 17-skill tree and both discovery sets agree.

### Students

Students contains its own frozen Cowork implementation plus two relative
discovery links. Remove that ten-file tree and both links. Update:

- root `AGENTS.md` from 17 to 16 frozen Harness skills;
- `tests/test_agent_configuration.py` expected skill set;
- the local frozen `fleet-repository-hardening/SKILL.md` duration rule;
- T-050 and its board row, which currently require and await a Cowork session.

T-050 should retain its synthetic benchmark and secrecy contract but use one
target driver and separately isolated fresh Codex/Claude subjects when a
comparison is explicitly requested; no co-pilot or Cowork gate remains.

The live Students checkout is clean but on
`claude/t043-bg003-replacement-app`, not `main`. Implementation must use an
isolated worktree, publish through Students' protected workflow, and reconcile
the active branch only through its owning session rather than changing files
under the running panes.

### Swallow

Swallow has two tracked absolute discovery links to the Harness skill but no
local copy or live policy requirement. Remove both links and remove the skill
from `tests/test-static.sh`, changing its frozen Harness subset from 17 to 16.
Retain completed `docs/cowork/` evidence. The live checkout is on
`sw034-continuation-20260731` with unrelated untracked
`.claude/settings.local.json`; use an isolated worktree and preserve it.

### Personal

Personal has two tracked absolute discovery links plus a root `AGENTS.md`
requirement that collaboration, comparison, handoff, and duration use Cowork.
Remove the links and replace that requirement with non-mandatory,
task-authorized cross-client work; duration keeps a durable ledger but does not
require a second client. `bin/validate` dynamically mirrors every canonical
Harness skill and therefore needs no code edit, but it can pass this candidate
only after canonical Harness has removed the skill. Personal is clean on
`main`, has no dedicated managed session, and keeps `.github/workflows/`
absent.

No matching global skill links exist under the checked user-level Codex,
Agent, or Claude skill roots.

## Clean implementation and publication order

1. Fetch each repository and create isolated task worktrees. Never modify the
   active Students or Swallow checkout.
2. Prepare and validate Students and Swallow first; publish their independent
   protected pull requests so no link will dangle when Harness retires the
   canonical skill.
3. Prepare the Harness and Personal candidates. Use `guarded-bulk-delete` with
   exact immutable manifests for each multi-path/tree removal; preserve all
   historical evidence.
4. Run Harness focused routing, catalog, duration-policy, hardening, context,
   takeover/discovery, deletion-safety, and ledger checks, then full R3 on the
   final integrated tree. Publish Harness through protected `main`.
5. Harness removal temporarily makes only Personal's two links dangling.
   Immediately remove them in Personal's exact root, run `./bin/validate`
   against the now-17-skill canonical Harness, and publish its private
   no-Actions workflow.
6. Reconcile running clients without reading panes: integrate each repository's
   accepted main into any still-live owner branch through that branch's owning
   agent, then gracefully restart/resume managed Codex and Claude clients so
   their cold-start skill catalogs no longer retain the removed skill. Never
   relaunch an ambiguous root.

## Acceptance

- No live Cowork skill tree, discovery link, executable, focused suite,
  validation rule, policy requirement, or active-task dependency remains in
  any of the four repositories.
- Nightly and duration work selects `long-running-task-ledger` without
  requiring Claude, Codex, a co-pilot, or a Cowork benchmark agreement.
- Harness exposes 17 shared skills; Students and Swallow expose their intended
  16-skill Harness snapshot; Personal dynamically validates 17 canonical
  links.
- Historical audit/task/presentation evidence and tmux `cowork` topology remain
  intact and are not mistaken for a live skill dependency.
- All four repositories are clean on accepted revisions, their native
  validators pass, protected publication succeeds where configured, and fresh
  managed client catalogs omit `codex-claude-cowork`.

## Validation and authority

Investigation used only Git/path metadata, exact policy references, and
read-only repository state. No pane or transcript content was inspected. No
file has been deleted and no consumer repository has been changed.

The removal itself is authorized by a future explicit owner `go`. Ordinary Git
publication is standing-authorized. Guarded deletion is an autonomous safety
gate, not an approval gate. Any active-branch integration or managed-client
restart must use the owning lifecycle/communication workflow and stop on
ambiguity.
