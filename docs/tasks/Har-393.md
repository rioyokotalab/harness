task: Har-393
status: active
updated: 2026-08-04
---
# Har-393 — local agent communication reliability

## Reconstructed failures

- During Stu-061, local delivery returned `status=submitted` while a long
  pasted prompt remained in the Students composer until the producer pressed
  the named Enter key. The transport currently sends `C-m` after paste.
- The Students repository-local communication skill predates the unified
  local-agent routes. Its helper could not submit the required report; the
  owner-managed Harness helper succeeded.

Both are apparatus failures, not Sensei-gent findings. No prompt was replayed.

## Frozen implementation

1. Use the named Enter key for the separate content-blind submit operation and
   add a maximum-shape synthetic paste regression. Preserve private buffers,
   locking, attribution, request IDs, and the no-pane-read boundary.
2. Add a deterministic compatibility check for a target repository's declared
   communication discovery link before a reply-required local request is sent.
   Fail closed on stale or unsafe copies; do not silently rewrite messages.
3. Keep repository-local skills self-contained. Record any required Students
   synchronization in its own producer ledger after Harness publication.
4. Run the owning skill validator, communication suite, complete Harness phase
   one, protected publication, and one bounded live no-reply submission test.

Rollback is the protected pre-task revision. No live source, credential,
student content, or autonomous communication loop is authorized.

## LIFO findings and implementation

- [x] D-003: the context-budget gate had already regressed on both the remote
  communication entry and unsafe-tail diagnosis route. Compact wording now
  preserves every frozen safety marker at 245/250 and 409 words respectively.
- [x] D-002: repository-frozen copies had no compatibility signal. Protocol
  marker version 1 and a value-free check now detect the declared Codex or
  Claude discovery copy without executing it. Personal passes for both clients;
  Students is correctly classified stale pending its own repository task.
- [x] D-001: `C-m` did not reliably submit a long pasted composer payload. The
  helper now sends named `Enter`; a 4096-byte synthetic regression proves the
  separate key operation while preserving content-blind private-buffer use.

Focused communication, skill-budget, skill-gate, syntax, and both skill
validators pass. The proportional validator selects R3/phase one for final
integration.

## Overnight coordination

The evidence cadence, cutoff, and seven-slice summary contract are recorded in
`docs/audits/har393-student-mentoring-overnight/session.md`. After Har-393 is
terminal, allocate the complete balanced Students portfolio before processing
its first task.
