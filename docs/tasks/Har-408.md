task: Har-408
status: complete
updated: 2026-08-07
---
# Har-408 — Make merged-branch cleanup idempotent

Remote retirement now reconciles the live inventory against the immutable
plan by exact branch name and OID. A planned hosted or tracking ref may be
absent at apply time, and a hosted ref may have become its unchanged local
tracking-only projection. New names, changed hosted tips, newly appeared
tracking refs, changed tracking tips, origin drift, and protected-main drift
still fail before archive or deletion.

The apply phase archives all planned tips, fetches and deletes only hosted refs
still present, removes only exact-old tracking refs, and reports the actual
hosted deletion count. The synthetic bare-origin regression deletes every
planned hosted ref before apply and proves a successful zero-hosted-delete
closeout; the existing new-ref race continues to fail without partially
deleting peers. Focused housekeeping tests and the final phase-one gate pass;
the latter covered 98 suites in 63.918 seconds. Complete.
