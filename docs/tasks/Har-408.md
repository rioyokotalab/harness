task: Har-408
status: pending
updated: 2026-08-06
---
# Har-408 — Make merged-branch cleanup idempotent

Final Personal cleanup attempted to delete four PR branches that GitHub had
already removed. Local branches were removed, but the batched remote deletion
returned failure until a fetch-prune reconciled absence. Replace this with an
existence-aware, exact-target cleanup sequence whose already-absent result is
success and whose ambiguous result remains fail-closed.

Next: add a synthetic remote-ref race regression after Har-403.
