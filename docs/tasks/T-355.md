# T-355 — Private Personal operations project

## Status

Phase: `complete`.

The owner requested a long Plan–Interview–Execute session for an independent
Personal project rooted at
`/mnt/nfs-03/safe/Users/rioyokota/projects/personal`. It is intended for
sensitive email, calendar, and research-budget work, must retain Harness
skills and commands across Harness cold starts, and must not interfere with
Harness, Students, or Swallow. The owner later froze that Personal will be
developed and operated from Harness without a dedicated tmux or remote-control
session.

The owner said `go` on 2026-07-31, authorizing execution of the frozen design
in `docs/plans/t355-personal-project.md`. The execution preflight found no
target directory or GitHub repository. No collaborator, connector, Codex root,
tmux pane, or phone-visible thread has been created or changed.

## Reconstructed baseline

- Planning branch: `codex/t355-personal-project`.
- Planning worktree: `/tmp/harness-t355-personal-project`.
- Branch base: pushed T-354 checkpoint
  `b36364af8ac0bde9e4e5097dfca47bb975f35482`; this preserves the still-blocked
  root-relocation handoff.
- The primary Harness checkout was on clean tracked `main` at `a24177e` with
  three unrelated untracked `libexec/.nfs*` inodes. Preserve them.
- The requested Personal path and the older `codex-workspaces/personal` path
  were absent at the 2026-07-31 read-only check. The `projects` parent is a
  current-user-owned mode-0700 NFS directory; the interactive umask was
  `0002`, so creation must explicitly use private permissions.
- `git@github.com:rioyokotalab/personal.git` did not exist at the read-only
  check.
- GitHub reported the `rioyokotalab` organization on a Team plan with base
  repository permission `write`, 67 members, one owner, and 53 outside
  collaborators. Base permission applies to every organization member and a
  lower per-repository role cannot override it. Therefore a private repository
  in that organization cannot currently meet the owner's requirement that
  group members have no access.
- Students and Swallow are private repositories with no direct collaborators
  or teams, but their organization membership still inherits the same base
  permission. Their required pull-request approval count is zero.
- Harness currently models an exact three-target lifecycle:
  `harness`, `students`, and `swallow`. D-010 freezes that Personal is not a
  fourth target, so the target map, sentinels, topology, monitor, helper,
  recovery, remote-agent, phone roots, and app-server roots remain unchanged.
- A project-local `.codex/config.toml` can scope project settings and MCP
  declarations, but installed plugins and connector authorization involve
  user-level state. A Harness-rooted Codex process does not apply Personal's
  project config merely because its tool command addresses the Personal path,
  so D-010a must reconcile the no-session choice with D-004.
- Any bounded Personal execution process will run under the same Unix identity
  selected in D-002. Mode 0700 and separate Git roots isolate group users and
  normal workflows, not another process running as the same user.

## Authority and immutable boundaries

The current authority is execution of D-001 through D-019 under the owner's
2026-07-31 `go`. The external-write, identity, sensitive-data, disclosure, and
non-retry gates in this record remain authoritative. Do not:

- change GitHub organization settings or send the deferred assistant
  invitation;
- authenticate, enable, disable, or exercise a connector before its isolation,
  provider-privacy, and domain gates pass;
- alter Harness's live tmux, monitor, helper, saved-root, app-server, or phone
  state; or
- read panes, transcripts, credentials, email, calendars, or budget data.

GitHub ownership/access architecture is the first blocking decision. Later
execution must preserve approval count zero unless the owner explicitly
chooses otherwise, use no GitHub Actions by default, and never store
credentials or plaintext sensitive records in Git.

## Frozen interview decisions

### D-001 — Repository ownership and group exclusion

Frozen on 2026-07-31 from the owner's exact answer `1`:

- create a private personal-account repository at `rioyokota/personal`;
- prioritize group exclusion over the original `rioyokotalab` ownership
  preference; and
- leave `rioyokotalab` base permissions and existing repositories unchanged.

This changes later assistant-access design: organization roles such as
`Triage` are unavailable on the selected ownership surface, so D-008 must use
the least capability supported by a personal-account repository or select a
metadata-safe collaboration method that requires no repository access.

### D-002 — Security isolation from other same-user agents

Frozen on 2026-07-31 from the owner's exact answer `2`:

- run Personal under the existing `rioyokota` Unix identity;
- use project policy, effective configuration, and cross-root tests as the
  operational boundary; and
- do not claim technical confidentiality from Harness, Students, or Swallow
  processes running as that same user.

The design must therefore keep plaintext sensitive payload out of local
project state, Git history, and logs, while keeping connector credentials out
of all project files. Personal connector capabilities should not be advertised
or enabled in fresh non-Personal roots, but that readback is an operational
guardrail rather than an OS-enforced boundary.

### D-003 — Sensitive-data residency

Frozen on 2026-07-31 from the owner's exact answer `1`:

- keep email, calendar, and research-budget content in their authoritative
  source systems;
- persist only opaque identifiers, status, dates, policy, and redacted
  outcomes needed for durable task recovery; and
- create no local sensitive-data vault or source-content backup.

Message bodies, attachments, attendee-sensitive notes, calendar descriptions,
budget values, exports, generated sensitive drafts, and live-data test
fixtures must not be persisted in the project, Git history, caches, logs, or
validation output. D-015 separately permits only minimum task-scoped sensitive
results in the owner-facing Harness conversation.

### D-004 — Connector and authentication boundary

Frozen on 2026-07-31 from the owner's exact answer `1`:

- use Personal's project-scoped effective configuration first;
- enable only required read connectors initially;
- compare fresh effective tool inventories for Personal, Harness, Students,
  and Swallow before accessing live data; and
- fail closed and return with a dedicated-profile/runtime proposal if the
  selected connectors cannot be limited to Personal's advertised and enabled
  configuration.

Do not enable a connector globally for Personal, silently fall back, or commit
connector state or tokens. Cross-root readback validates an operational
configuration boundary only; it does not supersede D-002.

### D-005 — Email authority

Frozen on 2026-07-31 from the owner's exact answer `1`:

- autonomously search, read, summarize, triage, classify, extract actions, and
  prepare reply or forward text in the active conversation; and
- require one exact owner confirmation for every enumerated batch of sends,
  forwards, Gmail-draft writes, archives, deletions or trash actions, labels
  or moves, unsubscribes, and message-state changes.

A batch confirmation must identify the mailbox, messages or threads,
recipients where applicable, and exact actions. Do not retry an ambiguous
write without state readback, and do not persist fetched bodies or generated
sensitive drafts under D-003.

### D-006 — Calendar authority

Frozen on 2026-07-31 from option `2`, proactive trigger option `2`, and limit
package option `1`:

- autonomous bounded reads, availability/conflict analysis, meeting
  preparation, room search, and exact change drafting are selected;
- qualifying attendee-free transparent holds and personal reminders may be
  written or removed without a second confirmation under a D-006a rule; and
- attendee, resource, busy-time, recurring-event, RSVP, notification,
  cancellation, and non-qualifying deletion changes require exact owner
  confirmation.

D-006a permits proactive qualifying writes. D-006b limits them to at most
three active Personal-created holds, each within 14 days, no longer than two
hours, transparent/private with a generic title, and free of description,
location, conference, guests, resources, recurrence, and notifications.
Remove an unaccepted hold within 48 hours. Reminder changes are not proactive;
they require an explicit owner-requested calendar task but no second
confirmation when all other D-006 constraints hold. Never retry an ambiguous
calendar write without exact state readback.

### D-007 — Research-budget authority

Frozen on 2026-07-31 from the owner's exact answer `1`:

- autonomously read bounded source data, reconcile, detect anomalies, model
  scenarios, forecast, and draft reports in the active conversation;
- require exact confirmation for each enumerated non-binding source edit or
  report submission; and
- never execute approvals, orders, purchases, payments, transfers,
  reimbursements, bank/payee/tax/account changes, attestations, signatures,
  term acceptance, or another financial commitment.

Never retry an ambiguous write or submission without authoritative state
readback. Do not persist budget values, sensitive reports or exports, or
live-data fixtures under D-003.

### D-008 — Administrative-assistant access

Frozen on 2026-07-31 from the owner's exact answer `1`. Read-only evidence
confirmed that a private repository owned by `rioyokota` can grant a
collaborator only write access; personal-account repositories do not offer
read-only or `Triage` roles, and later removal cannot retract a local clone.
After the assistant creates an account, the owner must supply the exact
username and confirm two-factor authentication before one non-retryable
invitation. Protect `main`; grant no connector, credential, `rioyokotalab`, or
Personal runtime access.

### D-009 — Collaboration content

Frozen by D-003 and D-008: GitHub may contain only metadata-safe tasks,
policy/code changes, checklists, opaque identifiers, and redacted decisions.
Never paste, upload, or commit source email/calendar content, attendee details,
attachments, budget values, sensitive reports/exports, credentials, or
connector data. The future assistant invitation is deferred on the missing
account, exact username, and two-factor-authentication confirmation.

### D-010 — No dedicated Personal session

Frozen on 2026-07-31 in the owner's words: “In the same way that
`~/projects/website` does not have a dedicated session in tmux, personal does
not need one. We can continue its development from harness.”

Create no Personal tmux pane/window/session, saved root, phone thread, monitor
or helper target, recovery mapping, managed launch-sentinel target,
remote-agent selector, or app-server root. Keep the existing
Harness/Students/Swallow three-target lifecycle unchanged and operate the
separate Personal repository from Harness. D-018 is consequently frozen as not
applicable. If D-010a selects a bounded process, it may add one exact
non-persistent Personal path admission to the launch sentinels without adding
a managed target.

D-010 conflicts with D-004 if sensitive operations execute directly in the
Harness-rooted process, because Personal project configuration would not
apply. D-010a must select a bounded Personal-rooted process driven by Harness
or explicitly supersede D-004 with Harness-scoped connectors.

### D-010a — Bounded sensitive-operation process

Frozen on 2026-07-31 from the owner's exact answer `1`:

- Harness launches one bounded, non-persistent native agent process with exact
  Personal cwd and task identity for each sensitive operation; D-013 expands
  the eligible client from Codex to Codex or Claude;
- an exact cross-client current-user lock permits at most one such process at
  a time;
- the process reads Personal's repository policy, ledger, skills, and
  effective connector configuration, writes only a metadata-safe receipt, and
  exits; and
- it receives no tmux, saved-root, phone, monitor/helper, recovery, app-server,
  or durable-conversation identity.

Add only an exact non-managed launch-sentinel path admission. Never retry an
ambiguous child launch or connector write without exact process and source
state reconciliation. D-015 permits only minimum task-scoped sensitive output
in the owner-facing Harness conversation.

### D-011 — Cold-start durable authority

Frozen on 2026-07-31 from the owner's exact answer `1`:

- Harness owns integration, exact repository identity, bounded launcher/lock,
  connector-boundary validation, and cross-repository blockers;
- Personal owns operational tasks, metadata-safe source references, domain
  outcomes, and its first next action; and
- the ledgers cross-link exact records without duplicating task narratives.

After a cold restart, Harness reads its own routed T-355 state and then the
selected Personal ledger record. Each bounded child reads Personal repository
state plus the exact current Harness request, never prior chat or child
conversation state. No Personal TUI, saved-root, phone, tmux, app-server, or
host-reboot restoration is in scope.

### D-012 — Live Harness skills and commands

Frozen on 2026-07-31 from the owner's exact answer `1`:

- expose every canonical Harness shared skill through tracked discovery links
  that Personal workflows treat as read-only;
- expose only catalog descriptions at cold start and read full skill
  instructions only when normal trigger rules match;
- track a thin compatibility manifest, not copied shared-skill text or command
  implementations; and
- invoke the canonical installed `harness` command, failing closed when its
  path or interface is incompatible.

Personal intentionally depends on the canonical Harness checkout and receives
validated skill updates automatically. Its own workflows must not modify
linked Harness sources.

### D-013 — Codex/Claude parity

Frozen on 2026-07-31 from the owner's exact answer `2`:

- either native client may drive one bounded Personal operation after
  independent root, policy, skill, connector, permission, retention, and exit
  validation;
- one cross-client lock and one named driver govern each operation;
- matching Codex and Claude project discovery imports one shared policy;
- Claude uses Fable/high, with Opus/high only as its native availability
  fallback; and
- explicit collaboration/comparison/handoff and all duration work use the
  benchmark-first `codex-claude-cowork` protocol.

Cowork planning and evidence exchange exclude credentials, private values, and
live source content. Only the named driver may perform the bounded sensitive
step, under D-003 through D-007. Neither client may rely on prior conversation
or product memory, and connector parity must be proven independently.

### D-014 — Git governance and fallback

Official GitHub documentation verified on 2026-07-31 that rulesets and
protected branches for a private personal-account repository require GitHub
Pro or a higher applicable plan. The authenticated user response did not
expose the current plan, so eligibility is unknown.

The fixed package is protected `main`, pull requests with exactly zero required
approvals, linear history, blocked deletion and force-push, owner bypass, no
required status checks, no Actions/workflow files, and credential-free local
validation.

Frozen from the owner's exact answer `1`: create and publish the repository
owner-only. If private protection is unavailable, keep it owner-only and defer
only the assistant invitation until the owner enables an eligible plan. Never
change billing or allow an unprotected collaborator. Protection, the exact
username, and two-factor-authentication confirmation are all prerequisites to
the one invitation.

### D-015 — Retention, logs, and backup

Official product documentation was revalidated on 2026-07-31. Codex local
history can be disabled with `[history] persistence = "none"`. A bounded
Claude `-p` process can disable session persistence and prompt-history writes,
and project auto-memory plus nonessential traffic can be disabled. These
client controls do not change provider retention.

ChatGPT keeps a conversation until the owner deletes it and then schedules
deletion within 30 days, subject to documented exceptions. Anthropic
documents consumer Claude Code retention of 30 days with model improvement
off and five years with it on, standard commercial retention of 30 days, and
qualified-account zero-data-retention options. The effective account types
and privacy settings are unknown and must be verified before live sensitive
work.

Frozen on 2026-07-31 from the owner's exact answer `1`: permit only the
minimum task-scoped sensitive result in this Harness conversation after
verifying that model-improvement controls are off for both providers. A
Claude-driven result may traverse both Anthropic and OpenAI. Never relay raw
source dumps, attachments, credentials, or unnecessarily broad content.

Bounded children must disable local history, persistence, resume, auto-memory,
nonessential traffic, feedback, plaintext logs, retained temporary output,
local sensitive caches, and source-content backup. Git and normal backup may
contain only policy, code, and metadata-safe records.

### D-016 — Budget sources and classification

Frozen on 2026-07-31 from the owner's exact answer `1`: keep the initial
Personal scaffold source-neutral and connect no research-budget source. Add
only a metadata-safe source-register schema and policy; add no budget
connector, import path, live file, account identity, fixture, or sample value.

Each future budget source requires a separate source-specific PIE decision
covering its non-secret identity and type, system of record, owner,
sensitivity classes, retention, interface, delegated authority, and
reconciliation rule. D-007 authority remains dormant until that decision and
D-004 isolation pass. Initial acceptance must prove no budget source or tool
is advertised or enabled.

### D-017 — External research and disclosure

Frozen on 2026-07-31 from the owner's exact answer `1`: allow sanitized
public-web research in a separate non-sensitive context using only generic
queries. Never disclose private identities, source text, exact private values,
attachments, opaque source identifiers, credentials, or unnecessarily precise
private dates.

Public web tools are read-only; forms, messages, account actions, submissions,
and uploads remain separate authority boundaries. Prefer primary sources and
persist only public citations and facts without private query context. If a
useful query cannot be sanitized without changing its meaning, stop for exact
disclosure authority.

### D-019 — Bounded native-client defaults

Frozen from the canonical tracked Harness defaults, consistent with D-012:
Codex uses
`gpt-5.6-sol`/high, the exact granular approval policy, and
`danger-full-access`. Claude uses Fable/high with Opus/high as its sole ordered
native availability fallback. Bounded noninteractive Claude calls use
`dontAsk` with an explicit narrow tool allowlist and never
`--dangerously-skip-permissions`.

Repository policy and the Harness launcher continue to enforce all
owner-facing domain and external-write gates. Independently validate both
clients' resolved root, model, effort, permission/approval, sandbox, tools,
fallback, retention, and clean exit before live sensitive use.

## Decision audit

The 2026-07-31 audit found no unresolved material owner choice or contradiction
across D-001 through D-019. GitHub protection eligibility, provider account
privacy settings, and Personal-only connector isolation remain execution-time
readback gates. The assistant identity and two-factor-authentication
prerequisite and every research-budget source remain explicitly deferred.

## Checkpoint and validation

This planning checkpoint changes only:

- `TODO.md`;
- `docs/tasks/index.tsv`;
- `docs/tasks/T-355.md`; and
- `docs/plans/t355-personal-project.md`.

Validation completed on 2026-07-31:

- `git diff --check` passed.
- `tests/test-task-ledger-routing.sh` passed with
  `TASK_LEDGER status=pass lines=57 words=270 index_rows=82`.
- The inspected initial planning checkpoint was committed and pushed as
  `babf1b6` on `codex/t355-personal-project`; no pull request was opened.

Record each frozen interview answer in the plan and task record before asking
the next material question.

## Execution checkpoint — 2026-07-31

- The clean planning branch remained synchronized at `d3a35f6` after a fresh
  fetch.
- The canonical Personal path and legacy `codex-workspaces/personal` path were
  both absent. The `projects` parent remained a current-user-owned mode-0700
  directory on the expected NFS mount.
- GitHub authentication exposed repository scope, and
  `GET /repos/rioyokota/personal` returned `404`; this is sufficient for the
  one authorized create attempt, not for retrying an ambiguous write.
- `harness_codex_targets.py validate-all` still reported exactly three managed
  targets. Tmux metadata still showed one three-pane `projects` window and no
  Personal target; no pane or transcript content was inspected.
- The current SSH agent socket is a current-user-owned Unix socket.
- No target or external mutation had occurred at this checkpoint.

### Remote create boundary

- Immutable target: private personal-account repository
  `rioyokota/personal`.
- Pre-write readback: the repository was absent and authenticated API access
  had `repo` scope. The local Personal scaffold was clean on `main` at
  `cafc72b`.
- One create request is authorized. It must not initialize content, invite a
  collaborator, enable Actions, or alter organization settings.
- A wrong or ambiguous create is never deleted automatically. Read back the
  exact owner/name/visibility/access and stop for owner direction if it does
  not match; this is the rollback boundary.

## Next action and retry safety

### Implementation checkpoint

- The exact Personal root is a current-user-owned mode-0700 real Git
  repository. Clean `main` at `d9d64c9` tracks the private
  `git@github.com:rioyokota/personal.git`.
- GitHub repository ID `1318044465` is owner-only. The owner is the sole
  collaborator, pending invitations are empty, Actions is disabled, and the
  remote tree has no workflow.
- Active ruleset `20104679` protects `refs/heads/main` with owner/admin bypass,
  pull requests, exactly zero required approvals, linear history, and deletion
  and non-fast-forward blocks. It has no required status check.
- The Personal repository has compact routed policy, a metadata-safe ledger,
  an empty source register, explicit Codex and Claude non-persistence defaults,
  a strict empty Claude MCP profile, six default-blocked connector rows, and
  17 read-only absolute discovery links to canonical Harness shared skills.
  `./bin/validate` passes with zero sources and zero workflows.
- Harness now implements `harness personal-agent`: one exact Personal root,
  one cross-client runtime lock, prompt-only standard input, scrubbed Harness
  variables, no log, and one mode-0600 value-free receipt. Codex is ephemeral
  Sol/high with the reviewed granular/danger-full-access settings plus
  history, memory, feedback, suggestions, and web search disabled; Claude is
  non-persistent Fable/high with Opus fallback, `dontAsk`, strict empty MCP,
  and a narrow local tool list. The launcher admits only the local domain.
- The user launch sentinels admit only the exact Personal path and explicitly
  classify it non-managed. `config/codex-session-targets.tsv`, tmux, phone,
  monitor, helper, app-server, recovery, and saved-root state are unchanged.
- `tests/test-personal-agent.sh`, `tests/test-agent-config.sh`, and
  `tests/test-codex-targets.sh` pass. The first proves client arguments,
  environment scrubbing, retention controls, connector rejection, lock
  exclusion, metadata-safe receipt, and the unchanged three-target count.
- Integration commit `5688acf` passed `harness validate --base origin/main
  --jobs 6 --no-receipt` at full R3: all focused suites and
  `tests/test-phase1.sh` passed. The native MPI smoke remained the suite's
  expected skip outside a declared MPI environment.
- PR `495` run `30612204934` failed once in portable CI because the new focused
  test tried to validate production target paths that do not exist on the
  GitHub runner. All other suites passed. The failed run was not replayed.
  The changed input now asserts the immutable three-name declaration while
  existing `test-codex-targets.sh` retains isolated path validation; the
  corrected test passes with production Harness variables absent and
  `HARNESS_PORTABLE_CI=1`.
- One real non-sensitive Codex probe and one real non-sensitive Claude probe
  loaded Personal policy at the exact root and exited successfully. No
  connector, live source, web service, or sensitive data was used.

### Deferred connector/privacy gate

Fresh native Codex plugin inventories were identical in Harness, Students,
Swallow, and Personal: enabled `slack` and `github` plugins were advertised in
all four roots. User-level plugin installation is therefore not a
Personal-only connector boundary. The current Codex CLI exposes no proven
per-invocation plugin-disable mechanism that preserves its existing
authentication without touching credentials. Claude can start with a strict
empty MCP profile, but no Personal connector profile has been authorized or
configured.

Provider model-improvement settings are not exposed by either native client
and remain unknown. D-004 and D-015 therefore fail closed: all email,
calendar, and budget rows remain blocked, no connector was authenticated or
used, and live sensitive work is not ready. A later source-specific change
must provide an isolated runtime/profile, prove cross-root absence, and record
provider privacy as off before enabling one read connector. The assistant
invitation and all budget sources remain separately deferred.

### Remaining exact action

No T-355 action remains. A failed mutable-state query remains unknown. Never
retry an ambiguous GitHub write, invitation, bounded process launch, connector
mutation, or sensitive action.

## Publication and final acceptance

- PR `495` merged changed-input head `3f0abf3` as
  `cbc0a017a58b2f4e2d870a7762de3f697dc9e4ac` after required portable run
  `30612387790` passed. Its predecessor run `30612204934` remains the recorded
  one-time portable fixture failure and was not replayed.
- Local Harness `main` fast-forwarded to the merge and matches `origin/main`.
  The three unrelated live-held `.nfs` inodes remain unmodified and untracked.
- The installed Codex and Claude sentinels were already symlinks into the live
  Harness checkout. Their updated tracked targets now admit only the exact
  Personal path and classify it non-managed; no user configuration rewrite was
  needed.
- Personal P-001 is complete at clean synchronized `main`
  `05f16d6916f6a3f64ae4a19f10548d1db4e206ae`.
  `./bin/validate` still reports 17 skills, zero sources, zero workflows, and
  no managed target.
- Final GitHub readback confirms private repository ID `1318044465`, owner
  `rioyokota` as the sole collaborator, zero pending invitations, disabled
  Actions, no workflow, protected branch head `05f16d6`, and active ruleset
  `20104679` with exactly zero required approvals.
- The live bounded launcher reports exact root ready, lock free, connectors
  blocked, and no managed target. The canonical validator reports exactly
  three managed targets.
- Tmux metadata still contains the one three-pane `projects:codex` window in
  order Harness, Students, Swallow plus the separate monitor windows. Personal
  has no pane, session, phone, app-server, monitor, helper, recovery, or saved
  root. No pane or transcript content was inspected.

The initial Personal project is accepted only for metadata-safe local policy
and development. Email, calendar, and budget remain intentionally unusable
until a later connector-isolation/provider-privacy task passes. The assistant
invitation and every budget source remain deferred under their recorded owner
gates.
