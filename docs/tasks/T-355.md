# T-355 — Private Personal operations project

## Status

Phase: `interviewing`.

The owner requested a long Plan–Interview–Execute session for an independent
Personal project rooted at
`/mnt/nfs-03/safe/Users/rioyokota/projects/personal`. It is intended for
sensitive email, calendar, and research-budget work, must retain Harness
skills and commands across Harness cold starts, and must not interfere with
Harness, Students, or Swallow. The owner later froze that Personal will be
developed and operated from Harness without a dedicated tmux or remote-control
session.

No target directory, repository, GitHub resource, collaborator, connector,
Codex root, tmux pane, or phone-visible thread has been created or changed.
The execution design and open decisions are in
`docs/plans/t355-personal-project.md`.

## Reconstructed baseline

- Planning branch: `codex/t355-personal-project`.
- Planning worktree: `/tmp/harness-t355-personal-project`.
- Branch base: pushed T-354 checkpoint
  `b36364af8ac0bde9e4e5097dfca47bb975f35482`; this preserves the still-blocked
  root-relocation handoff.
- The primary Harness checkout was on clean tracked `main` at `a24177e` with
  three unrelated untracked `libexec/.nfs*` inodes. Preserve them.
- The requested Personal path and the older `codex-workspaces/personal` path
  were absent at the 2026-07-31 read-only check. The `projects` parent is a
  current-user-owned mode-0700 NFS directory; the interactive umask was
  `0002`, so creation must explicitly use private permissions.
- `git@github.com:rioyokotalab/personal.git` did not exist at the read-only
  check.
- GitHub reported the `rioyokotalab` organization on a Team plan with base
  repository permission `write`, 67 members, one owner, and 53 outside
  collaborators. Base permission applies to every organization member and a
  lower per-repository role cannot override it. Therefore a private repository
  in that organization cannot currently meet the owner's requirement that
  group members have no access.
- Students and Swallow are private repositories with no direct collaborators
  or teams, but their organization membership still inherits the same base
  permission. Their required pull-request approval count is zero.
- Harness currently models an exact three-target lifecycle:
  `harness`, `students`, and `swallow`. D-010 freezes that Personal is not a
  fourth target, so the target map, sentinels, topology, monitor, helper,
  recovery, remote-agent, phone roots, and app-server roots remain unchanged.
- A project-local `.codex/config.toml` can scope project settings and MCP
  declarations, but installed plugins and connector authorization involve
  user-level state. A Harness-rooted Codex process does not apply Personal's
  project config merely because its tool command addresses the Personal path,
  so D-010a must reconcile the no-session choice with D-004.
- Any bounded Personal execution process will run under the same Unix identity
  selected in D-002. Mode 0700 and separate Git roots isolate group users and
  normal workflows, not another process running as the same user.

## Authority and immutable boundaries

The current authority is planning and durable ledger maintenance only.
Read-only inspection is allowed. Until the interview is frozen and the owner
says `go`, do not:

- create or alter the Personal directory or repository;
- write GitHub organization/repository settings or send an invitation;
- authenticate, enable, disable, or exercise a connector;
- alter Harness's live tmux, monitor, helper, saved-root, app-server, or phone
  state; or
- read panes, transcripts, credentials, email, calendars, or budget data.

GitHub ownership/access architecture is the first blocking decision. Later
execution must preserve approval count zero unless the owner explicitly
chooses otherwise, use no GitHub Actions by default, and never store
credentials or plaintext sensitive records in Git.

## Frozen interview decisions

### D-001 — Repository ownership and group exclusion

Frozen on 2026-07-31 from the owner's exact answer `1`:

- create a private personal-account repository at `rioyokota/personal`;
- prioritize group exclusion over the original `rioyokotalab` ownership
  preference; and
- leave `rioyokotalab` base permissions and existing repositories unchanged.

This changes later assistant-access design: organization roles such as
`Triage` are unavailable on the selected ownership surface, so D-008 must use
the least capability supported by a personal-account repository or select a
metadata-safe collaboration method that requires no repository access.

### D-002 — Security isolation from other same-user agents

Frozen on 2026-07-31 from the owner's exact answer `2`:

- run Personal under the existing `rioyokota` Unix identity;
- use project policy, effective configuration, and cross-root tests as the
  operational boundary; and
- do not claim technical confidentiality from Harness, Students, or Swallow
  processes running as that same user.

The design must therefore keep plaintext sensitive payload out of local
project state, Git history, and logs, while keeping connector credentials out
of all project files. Personal connector capabilities should not be advertised
or enabled in fresh non-Personal roots, but that readback is an operational
guardrail rather than an OS-enforced boundary.

### D-003 — Sensitive-data residency

Frozen on 2026-07-31 from the owner's exact answer `1`:

- keep email, calendar, and research-budget content in their authoritative
  source systems;
- persist only opaque identifiers, status, dates, policy, and redacted
  outcomes needed for durable task recovery; and
- create no local sensitive-data vault or source-content backup.

Message bodies, attachments, attendee-sensitive notes, calendar descriptions,
budget values, exports, generated sensitive drafts, and live-data test
fixtures must not be persisted in the project, Git history, caches, logs, or
validation output. Product-side Codex thread/context retention remains open
under D-015.

### D-004 — Connector and authentication boundary

Frozen on 2026-07-31 from the owner's exact answer `1`:

- use Personal's project-scoped effective configuration first;
- enable only required read connectors initially;
- compare fresh effective tool inventories for Personal, Harness, Students,
  and Swallow before accessing live data; and
- fail closed and return with a dedicated-profile/runtime proposal if the
  selected connectors cannot be limited to Personal's advertised and enabled
  configuration.

Do not enable a connector globally for Personal, silently fall back, or commit
connector state or tokens. Cross-root readback validates an operational
configuration boundary only; it does not supersede D-002.

### D-005 — Email authority

Frozen on 2026-07-31 from the owner's exact answer `1`:

- autonomously search, read, summarize, triage, classify, extract actions, and
  prepare reply or forward text in the active conversation; and
- require one exact owner confirmation for every enumerated batch of sends,
  forwards, Gmail-draft writes, archives, deletions or trash actions, labels
  or moves, unsubscribes, and message-state changes.

A batch confirmation must identify the mailbox, messages or threads,
recipients where applicable, and exact actions. Do not retry an ambiguous
write without state readback, and do not persist fetched bodies or generated
sensitive drafts under D-003.

### D-006 — Calendar authority

Frozen on 2026-07-31 from option `2`, proactive trigger option `2`, and limit
package option `1`:

- autonomous bounded reads, availability/conflict analysis, meeting
  preparation, room search, and exact change drafting are selected;
- qualifying attendee-free transparent holds and personal reminders may be
  written or removed without a second confirmation under a D-006a rule; and
- attendee, resource, busy-time, recurring-event, RSVP, notification,
  cancellation, and non-qualifying deletion changes require exact owner
  confirmation.

D-006a permits proactive qualifying writes. D-006b limits them to at most
three active Personal-created holds, each within 14 days, no longer than two
hours, transparent/private with a generic title, and free of description,
location, conference, guests, resources, recurrence, and notifications.
Remove an unaccepted hold within 48 hours. Reminder changes are not proactive;
they require an explicit owner-requested calendar task but no second
confirmation when all other D-006 constraints hold. Never retry an ambiguous
calendar write without exact state readback.

### D-007 — Research-budget authority

Frozen on 2026-07-31 from the owner's exact answer `1`:

- autonomously read bounded source data, reconcile, detect anomalies, model
  scenarios, forecast, and draft reports in the active conversation;
- require exact confirmation for each enumerated non-binding source edit or
  report submission; and
- never execute approvals, orders, purchases, payments, transfers,
  reimbursements, bank/payee/tax/account changes, attestations, signatures,
  term acceptance, or another financial commitment.

Never retry an ambiguous write or submission without authoritative state
readback. Do not persist budget values, sensitive reports or exports, or
live-data fixtures under D-003.

### D-008 — Administrative-assistant access

Frozen on 2026-07-31 from the owner's exact answer `1`. Read-only evidence
confirmed that a private repository owned by `rioyokota` can grant a
collaborator only write access; personal-account repositories do not offer
read-only or `Triage` roles, and later removal cannot retract a local clone.
After the assistant creates an account, the owner must supply the exact
username and confirm two-factor authentication before one non-retryable
invitation. Protect `main`; grant no connector, credential, `rioyokotalab`, or
Personal runtime access.

### D-009 — Collaboration content

Frozen by D-003 and D-008: GitHub may contain only metadata-safe tasks,
policy/code changes, checklists, opaque identifiers, and redacted decisions.
Never paste, upload, or commit source email/calendar content, attendee details,
attachments, budget values, sensitive reports/exports, credentials, or
connector data. The future assistant invitation is deferred on the missing
account, exact username, and two-factor-authentication confirmation.

### D-010 — No dedicated Personal session

Frozen on 2026-07-31 in the owner's words: “In the same way that
`~/projects/website` does not have a dedicated session in tmux, personal does
not need one. We can continue its development from harness.”

Create no Personal tmux pane/window/session, saved root, phone thread, monitor
or helper target, recovery mapping, managed launch-sentinel target,
remote-agent selector, or app-server root. Keep the existing
Harness/Students/Swallow three-target lifecycle unchanged and operate the
separate Personal repository from Harness. D-018 is consequently frozen as not
applicable. If D-010a selects a bounded process, it may add one exact
non-persistent Personal path admission to the launch sentinels without adding
a managed target.

D-010 conflicts with D-004 if sensitive operations execute directly in the
Harness-rooted process, because Personal project configuration would not
apply. D-010a must select a bounded Personal-rooted process driven by Harness
or explicitly supersede D-004 with Harness-scoped connectors.

## Checkpoint and validation

This planning checkpoint changes only:

- `TODO.md`;
- `docs/tasks/index.tsv`;
- `docs/tasks/T-355.md`; and
- `docs/plans/t355-personal-project.md`.

Validation completed on 2026-07-31:

- `git diff --check` passed.
- `tests/test-task-ledger-routing.sh` passed with
  `TASK_LEDGER status=pass lines=57 words=270 index_rows=82`.
- The inspected initial planning checkpoint was committed and pushed as
  `babf1b6` on `codex/t355-personal-project`; no pull request was opened.

Record each frozen interview answer in the plan and task record before asking
the next material question.

## Next action and retry safety

Ask D-010a from the plan: choose how Harness invokes sensitive Personal
operations without a dedicated session while preserving or superseding
D-004's project-scoped connector boundary. This is an owner choice, not an
executable infrastructure action. A failed mutable-state query remains
unknown. Never retry an ambiguous GitHub write, invitation, bounded process
launch, connector mutation, or sensitive action. Execution starts only after
all material decisions are frozen, the owner approves the complete plan with
`go`, and mutable inputs are revalidated.
