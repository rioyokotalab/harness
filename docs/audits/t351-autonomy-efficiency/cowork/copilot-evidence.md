# Co-pilot evidence

## Sandbox and baseline

- Fact: sealed reciprocal stage `reciprocal-stage-v2/` read completely: `charter.md`, `plan.md`, `benchmark.md`, `state.json` (schema 3, phase `discussing`, driver codex, copilot claude), `stage.json` (mode `reciprocal`, six input digests, destination-before digest equal to the sealed `copilot-evidence.md` digest), `driver-evidence.md`, `copilot-evidence.md`, and `artifacts/copilot-prompt.md`. `candidate-copilot-evidence.md` exists and is empty; the driver's stdout redirection is the sole writer, as required after the first invalid window.
- Fact: this rerun used only Read/Grep/Glob. No Bash, Write, Edit, network, GitHub, tmux, or live-session access occurred; the sandbox tree was not modified. The prior window's failure mode (Bash available, self-write racing the redirection) is structurally excluded here.
- Baseline identity is carried from my sealed independent evidence (exact `f2ce89ac3ea393cb5a7b882bed1b79fe773974fb`, clean detached worktree); no state-changing command has run since, and the repository files re-read this window are byte-consistent with those findings.
- The repository rule to run `harness fleet-health` before yielding is again overridden by this task's closer instruction set (read-only tools, fixed output format); no fleet-health snapshot is possible or included.

## Commands and results

Preserved independent measurements (sealed `copilot-evidence.md`, unchanged): TODO.md 9,199 lines / 70,828 words with 8,919 lines (96.7% of words) in `## Task records`; AGENTS.md 323 lines / 2,717 words; mandatory clean-start context ≥73,545 words; 17 skills totaling 19,208 words with cowork SKILL+protocol+ledger = 6,144; docs-only validation measured at ≤0.1 s and one Mac fixture at 5.82 s; no existing receipt or impact-mapping code anywhere in `bin/`, `libexec/`, `tests/`, `docs/`; only task-scoped `flock` locks; `mktemp` temp roots cleaned via boundary-enforcing `tests/guarded-test-cleanup.sh`.

Re-verified this window with read-only tools:

1. `docs/github-rulesets/harness-main.json`: `required_approving_review_count: 1`, squash/rebase-only merge methods, `strict_required_status_checks_policy: true`, required context `portable-phase1` (integration 15368), linear history, and `bypass_actors: [{actor_id: 5, actor_type: RepositoryRole, bypass_mode: always}]` — a tracked always-bypass admin actor, consistent with the driver's authenticated bypass readback.
2. `tests/test-github-rulesets.sh:46` asserts the stale `required_approving_review_count: 1` — the fixture and test are jointly stale against the live zero readback.
3. `.github/workflows/ci.yml`: triggers on both `push: [main]` and `pull_request: [main]` with the single `portable-phase1` job — the duplicate-main-run pattern is confirmed present in Harness itself.
4. `TODO.md` section boundaries re-confirmed at lines 10/104/139/9058 (`Current state`, `Prioritized next steps`, `Task records`, `Completed anchors`); `tests/focused-suites.tsv` re-confirmed at 82 rows; `tests/guarded-test-cleanup.sh` present.

Unverifiable from this sandbox (unchanged): live GitHub state, private run counts, Students/Swallow rulesets and trees, official documentation URLs. I rely on the driver's exact authenticated readbacks for those, labeled as driver-attested below.

## Critique

Reciprocal dispositions on the five questions:

1. **Approval count.** The driver's authenticated readback of rulesets 19127355, 19716717, 19734040, and 19127356 returning zero does not conflict with my finding; it supersedes it for live state. My finding stands as a confirmed repository bug: the tracked fixture and `tests/test-github-rulesets.sh` assert one. Disposition: accept driver plan change 10 — correct fixture and test to the owner-selected live zero, never write a ruleset (this also satisfies the root-policy rule treating live zero as accepted policy, not a finding). Benchmark case 6 must still be scoped to fixtures that exist per repository so B6 is testable.
2. **Duplicate `main` runs.** The ten-of-ten exact head-tree = merge-tree matches across the five latest merged PRs in each of Students and Swallow, plus strict required checks and squash/rebase-only readback, are precisely the verification my original change 8 demanded. Disposition: accept removal of duplicate `main` runs with receipts bound to tree identity, with two named residuals: (a) ten matches are strong evidence, not a guarantee for future merges (a conflict-resolved squash can yield a novel tree) — the tree-bound receipt fails safe here, since an unmatched tree simply has no receipt and must be validated fresh; (b) the direct-push residual in point 3.
3. **Owner-job skips and the bypass residual.** Accounting for the authenticated bypass readback — Harness and Swallow retain owner/admin always-bypass actors and Students retains its owner pull-request bypass — the **direct-push validation residual** is: a bypass actor can push directly to `main` with no pull request, so after duplicate-`main`-run removal such a push receives no required check, no receipt-bearing PR body, and no hosted run at all in a private repository; its only validation is local discipline plus the weekly unconditional hosted run, up to seven days later. This residual predates T-351 (bypass already permits unchecked pushes) but the change widens its window by removing the `main`-push run that previously caught it within minutes. Local receipts remain auditable self-attestation — same-actor evidence bound to tree/validator/manifest/platform/result, valuable for audit, but not independent attestation; the benchmark invariant for owner/agent-authored code must be stated as a named, accepted reduction with compensating controls, not as "unchanged." With the receipt digest in each owner PR body/trailer, unchanged hosted jobs for all non-owner authors, and the weekly unconditional run (~2 rounded job-minutes/week floor), disposition: accept under the owner's autonomy > efficiency > non-obstructive safety ordering, provided the residual is either recorded as accepted or narrowed per proposed change 2 below.
4. **Tightened thresholds.** The driver adopted my split B1 (≥85% median total, ≥50% per-scenario floor, ≥30% non-ledger reduction), docs-only ≤5 s / skill-only ≤10 s, hard-wired validator-class full escalation, and the post-compaction cold-resume test. Disposition: accept; require matched same-host `/usr/bin/time` before/after samples so the latency claims are comparisons, not single points. My measurements (96.7% of words in task records; ≤0.1 s docs validation) confirm these thresholds are demanding yet reachable.
5. **History, publication, isolation.** Byte-for-byte board archive with a sub-250-line index, branch checkpoints without PRs plus one protected PR per coherent task, project/worktree-scoped receipts and temp roots, and no live lifecycle mutation are all consistent with my topology findings (task-scoped locks only, unique `mktemp` roots, guarded cleanup) and with the driver's stale-ad-hoc-probe observation, which argues against adding repeated live checks. Disposition: accept, retaining two conditions from my sealed evidence: the live task's record stays on the active board until closeout, and cold resume (case 2) executes strictly after compaction through the index pointer alone.

The driver's diagnosis of the first invalid reciprocal window is correct and the retry conditions (fresh stage/seal, no write-capable tools, single stdout writer) were honored this window.

## Proposed plan changes

Retain the driver's ten proposed plan changes, amended as follows:

1. Record the fixture correction (driver change 10) as also amending benchmark case 6 to name per-repository fixtures that actually exist, replacing the nonexistent "approval-count-zero" fixture with the corrected zero-asserting Harness fixture/test.
2. Name the direct-push validation residual in the benchmark and ledger, and either (a) record it as owner-accepted with the weekly run as its bounded detection, or (b) narrow it by retaining the private `main`-push trigger with a job-level condition that skips only when the pushed head tree equals a validated, receipt-carrying merged-PR tree, running otherwise; note that actor/message-pattern conditions are spoofable and must not substitute for tree comparison. Do not silently drop the residual.
3. Require every latency threshold to be evidenced by matched same-host baseline/after `/usr/bin/time` samples recorded in the iteration log.
4. Keep receipts described everywhere — plan, PR bodies, benchmark — as "auditable self-attestation"; prohibit any wording that presents them as independent validation.
5. Unchanged from my sealed evidence and already driver-accepted: safety gates verbatim in always-read entry text with a references-unloaded firing check; unknown/unmapped diffs escalate to the full suite; validator-class files (including the new mapping/receipt/router code and the mapping file itself) hard-wired to complete-suite escalation; Students/Swallow tree-identity readback precedes any workflow edit (now satisfied by the ten-match evidence at current state).

agreement: role=copilot client=claude benchmark-accepted
