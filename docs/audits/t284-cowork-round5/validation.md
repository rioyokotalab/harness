# Validation

## Checks

Focused cowork passed in 16.66 seconds. Python/shell syntax, `git diff --check`,
Claude takeover, source contract, public audit, canonical skill validation,
session executing check, and reciprocal receipt verification pass. Clean full
phase one at committed validating state selected jobs 8 on eight visible CPUs,
passed all 57 focused suites, guarded-delete, and the umbrella gate in 77.18
seconds; native MPI correctly skipped outside a declared environment. Fresh
no-hardlink clone validation passed the session, reciprocal receipt, and focused
cowork suite in 16.45 seconds and remained clean.

## Outcome

The deterministic reproduction now returns timeout/4 for both late snapshot
paths, while the on-time process-loss control remains ready/0. The explicit
timeout outcome is honest at completed-snapshot boundaries without claiming I/O
preemption or changing any public field, flag, or exit code. All acceptance
gates pass.

## Residual risks

Synchronous reads can still block beyond the deadline and only be classified
after completion. This is documented. The fake clocks test decision ordering,
not filesystem latency. The reciprocal native wrapper timed out after producing
a complete candidate; that bounded client failure is retained as evidence and
did not weaken import checks.
