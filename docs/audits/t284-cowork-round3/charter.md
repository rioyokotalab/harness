# Charter

## Task

Round 3 of T-284: Codex drives and Claude is the blinded staged co-pilot. Test
whether clean matched evidence supports raising the focused-test worker default
from four to eight, and whether one bounded read-only wait operation can replace
repeated driver-side status polling without implying import authorization.

## Boundaries

Only Codex may mutate the live task branch, and only after independent evidence,
reciprocal critique, reconciliation, a frozen ready-for-execution plan, and the
owner's standing go for the three-hour refinement scope. Experiments run in the
two disposable clones below. No remotes, credentials, settings, packages,
services, external messages, or destructive cleanup. Claude receives only a
direct-child sealed stage, never the live session or external seal path.

## Baseline and sandboxes

Both sandboxes are no-hardlink local clones of immutable commit
`a9dd994189cfba44777ac3d637d68ae6de72f9e0`:
`/tmp/harness-t284-r3.IqKFA8/codex` for driver experiments and
`/tmp/harness-t284-r3.IqKFA8/claude` for the co-pilot stage parent. Driver-only
prompts, seals, raw timing logs, and protected manifests live under
`/tmp/harness-t284-r3.IqKFA8/driver`, outside the co-pilot sandbox.

## Acceptance

Obtain three strictly sequential alternating passing samples per worker count
with no overlapping benchmark process; record raw elapsed output and environment
limitations. A worker-default change requires every sample to pass and a stable
material improvement. Any wait design must be read-only, bounded by timeout,
emit a final JSON status snapshot, use explicit non-authorizing outcome names,
and preserve `import-copilot` as the only mechanical mutation gate. Claude must
independently criticize both conclusions from sealed bytes. Final changes need
focused cowork/runner, source, public, takeover, diff, receipt, fresh-clone, and
clean full phase-one validation.
