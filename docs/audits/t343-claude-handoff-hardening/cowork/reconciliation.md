# Reconciliation

## Evidence accepted

- Codex's five clean baseline suite passes establish regression status but do
  not prove a fresh Claude takeover. The tracked Fable/high mirrors, native
  client capabilities, agent-config doctor, ruleset readback, and fleet/ABQ
  classifications are accepted as frozen preparation inputs.
- Claude's independent candidate
  `8b3bcc767897adea0801c19ad4dcd8542bc0d5ad4bb68369e7560ff3fc59a073`
  is accepted only as constrained static protocol evidence and as a negative
  result. It is not accepted as reproduction of the eight benchmark cases:
  its Bash surface failed before commands ran and it did not discover the
  staged benchmark.
- The independent result demonstrates that document-shape validation can
  import prose without proving execution. Its sealed import receipt is
  retained so the finding cannot be repaired away by retrospective relabeling.
- Reciprocal r1 failed before reaching Claude with the exact 46-byte API
  transport error `API Error: Unable to connect to API (ENOTIMP)` and was not
  imported. Reciprocal r2 reached Claude but its response
  `778d77651b27000cbc412a87f803b3d6eaf6bf876aec46f9d78c61dfd57ab479`
  omitted the required top-level title because the driver prompt omitted it;
  it was classified invalid and not imported.
- Reciprocal r3 used a changed prompt and distinct sealed identity. Claude
  read all eight staged files through Read/Grep/Glob, ran no command, accepted
  the amended benchmark contingent on owner `go`, and produced imported
  evidence
  `132972b0279f808bd376880f04b033c1327653b9e8c88aee94ff6edabb2c6fd0`.
  Both staged receipts verify against the live evidence chain.

## Disagreements and uncertainty

- There is no remaining substantive scope disagreement. A task-bound packet
  binds future authority and identity; a class-specific evidence contract
  binds what the process actually observed or executed.
- The current stage schema does not contain a top-level `stage_sha256`.
  Therefore the first implementation must either bind the existing sealed
  `prompt_sha256` and complete input map or define a canonical aggregate
  digest before requiring one. No validator may reference a phantom field.
- Stage discoverability is proven fixed for read-only work by giving the exact
  stage path. A usable execution-bound Claude sandbox remains unproven because
  the independent Bash surface encountered read-only product state. The first
  real round trip is consequently read-only; execution-class launch remains
  behind an explicit provisioning experiment.
- The general project Claude profile's `bypassPermissions` default is not the
  target. This run tests a task-scoped native mapping and does not change
  global, user-level, or ordinary interactive settings.

## Frozen plan

After explicit owner `go`, Codex alone writes the target. The first LIFO slice
is exactly:

1. Add a machine-readable task handoff packet and pure repository-local
   validator with exact-accept, wrong-root, wrong-baseline, missing-task,
   expired, and cross-task fixtures.
2. Add authority-class evidence contracts. Read-only requires the sealed
   prompt/input receipt and complete read manifest and refuses execution
   records. Execution class requires per-case command/exit/output digests,
   baseline identity, and mandatory driver reproduction of one deterministic
   record.
3. Run one actual Fable/high, nonpersistent, project-settings-only, no-write-
   tool Claude process against a disposable fixture and prove import,
   receipt verification, exact recovery fields, and unchanged workspace
   digest. Native-client absence is counted unmet evidence, not a silent pass.
4. Add the broken-execution-sandbox negative and require a well-formed but
   execution-unproven candidate to fail the execution-class gate.
5. Encode the observed transport failure and invalid-format response as
   separate recovery fixtures requiring no import, no blind replay, protected
   state preservation, and a distinct changed retry identity.

Only after that slice passes do the remaining frozen cases proceed: policy and
dirty-fixture preservation, the four unchanged T-336 Fable/high capsules under
a new result identity, complete acceptance validation, protected publication,
clean reachable rollout, required Mac refreshes, and guarded task-owned
cleanup. ABQ remains excluded while official status reports it stopped.

## Acceptance gates

- Both exact benchmark agreement lines are present and both independent and
  reciprocal staged receipts verify before `ready-for-execution`.
- Case score is 8/8 with zero safety or containment failure. A baseline-green
  suite is supporting regression evidence and never the sole positive proof
  for a new case.
- Every model run records client/model/effort, fresh identity, sealed inputs,
  authority/tool surface, result, and protected before/after state. An
  acknowledged or ambiguous row is never replayed unchanged.
- Focused cowork, takeover, handoff, agent-config, comparison, evaluation, and
  hardening tests pass, followed by clean `tests/test-phase1.sh`, protected
  exact-head CI, ruleset readback preserving zero approvals, and clean
  reachable-checkout readback.
- Material changes stop at 08:00 JST. Exactly 16 evidence slices and at least
  800 words appear in each durable and owner summary by 09:00 JST.
