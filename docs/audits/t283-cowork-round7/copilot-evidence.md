I've applied the `codex-claude-cowork` skill's reciprocal-critique pass (the task is exactly that stage), keeping all work inside my sandbox and the reciprocal stage; the repo, stage inputs, manifest, and artifacts are untouched (`git status` clean, `git diff --check` clean), and the two out-of-repo scratch trees are labeled for the driver's guarded cleanup (no raw recursive deletion used).

Below is my complete replacement evidence document.

# Co-pilot evidence

## Sandbox and baseline

- **Role/skill.** Invoked as the **reciprocal-critique co-pilot** in a Codex-driven `codex-claude-cowork` release-candidate audit. I named and followed that skill; per its protocol I received only my sandbox (`/tmp/harness-t283-round7-claude`) and the staged reciprocal bundle — never the live exchange `docs/audits/t283-cowork-round7/`.
- **Staged inputs (observation).** `stage-reciprocal/{state.json,stage.json}` record `driver=codex, copilot=claude, mode=reciprocal, phase=discussing, schema_version=2`. `stage.json` pins SHA-256 of all five inputs (`charter.md`, `plan.md`, `state.json`, `driver-evidence.md`, `copilot-evidence.md`) and a `destination_before_sha256=9ce4fa5b…` equal to the imported independent `copilot-evidence.md`. The `candidate-copilot-evidence.md` slot is empty (`e3b0c44…`), i.e. this reciprocal pass produces the replacement.
- **Baseline (observation).** Charter baseline is `4c3602d586fb5d7225f516aa15677a2b6fa1384b`; the checkout HEAD matches it. Python 3.12.3.
- **Boundary honored (observation).** Zero repository/stage/manifest/artifact edits; `git status --short` empty and `git diff --check` clean after all experiments. Every experiment ran in out-of-repo scratch (`/tmp/harness-cowork-recip.*`), left labeled for guarded cleanup. No raw recursive deletion used. Live exchange never accessed.
- **Files inspected (observation).** `SKILL.md`, `references/protocol.md`, `scripts/cowork-session`, `tests/test-codex-claude-cowork-skill.sh`, and the staged `charter.md`, `plan.md`, `driver-evidence.md`, `copilot-evidence.md`, `state.json`, `stage.json`.

## Commands and results

Helper = `shared/skills/codex-claude-cowork/scripts/cowork-session`.

1. **Focused suite (observation).** `sh tests/test-codex-claude-cowork-skill.sh` → `Codex-Claude cowork skill tests passed` (exit 0). Matches both evidence sets.
2. **Parser surface (observation).** `python3 cowork-session --help` lists exactly `{init,check,digests,verify-receipts,stage,import-copilot,advance}`. `stage`/`import-copilot` accept only `--mode`/`--seal`. Passing `--copilot-root` is rejected by argparse: `error: unrecognized arguments: --copilot-root`. **No such option exists in any form.**
3. **Nested-stage residual reproduced (observation, probe 7 confirmed).** In scratch `BOX`, a schema-2 staged session advanced to `discussing`:
   - **(A) Direct-child stage** `BOX/stage` (parent = `BOX`) with seal `BOX/seal-A.json` → **REFUSED**: `seal must be outside the live session and stage-parent sandbox`. Correct.
   - **(B) Nested stage** `BOX/deep/stage` (parent = `BOX/deep`) with seal `BOX/seal-B.json` → **ACCEPTED** (`stage_sha256=… seal_sha256=…`); `seal-B.json` landed mode-0600 **inside the co-pilot-writable `BOX` root**. The seal-location refusal is exactly one directory too narrow when the direct-child precondition is violated.
4. **verify-receipts scope (static + doc, observation).** `verify_receipts` (helper 993–1010) revalidates only the receipt chain and the live-candidate hash and prints `valid {mode} receipt for live co-pilot evidence sha256=…`; it never re-reads the external seal. This non-reopen behavior is already documented twice: `protocol.md:193-195` ("validates the stored seal hash and the receipt/evidence chain, but does not reopen or re-hash the external seal file; comparing retained seal bytes is a separate step") and `SKILL.md:170-171`.
5. **Protocol wording for the residual (observation).** `protocol.md:141-147`: "keep stages direct children (or, if a caller cannot, **extend** the helper with an explicit co-pilot-root argument)." The helper docstring (`cowork-session:548-559`) independently records the same residual as "documented, not assumed safe."

## Critique

I challenge the strongest contrary claim in the independent co-pilot evidence — its **Concrete gap #1** — and I make the two distinctions the pass requires.

- **(a) The protocol does NOT falsely claim `--copilot-root` exists; it recommends a future extension.** The independent evidence asserts the protocol "currently promises an option the helper does not provide." Reading the exact text (probe 5): the verb is "**extend** the helper with an explicit co-pilot-root argument," inside a conditional "if a caller cannot [keep direct children]." That is a prescriptive pointer to *future* work, not a present-tense claim of an available flag — reinforced by the helper's own docstring calling the narrowness a documented residual. So the "false-promise" framing is an **overstatement**; there is no protocol/impl mismatch to reconcile. The residual itself (probe 3/probe 7) is real but only fires when the driver *both* violates the documented direct-child precondition *and* places the seal inside the sandbox root — two choices the protocol already forbids. **Disposition: reject "fix-now."** Do not add `--copilot-root` (speculative hardening the real workflow never needs — every actual stage is a direct child; charter forbids adding features merely because possible). Rewording is not required; at most an optional single-word "future" clarification, which I do not recommend churning the release candidate for.
- **(b) The verify-receipts caveat is output churn that adds no enforcement.** The proposed one-line "external seal bytes not reopened" message changes no behavior — verify-receipts still would not reopen the seal — and the identical caveat is already stated in `protocol.md:193-195` and `SKILL.md:170-171` (probe 4). The green message is already accurately scoped ("…receipt for live co-pilot evidence…") and never claims seal validation. Adding it touches a validated helper's output/test assertions late in the window for zero assurance gain. **Disposition: reject as a code/output change; it is already documented.**
- **Independent evidence I confirm and preserve as valid:**
  - Seal-reopen *feature* → **reject** (probes 3,5): no receipt stores the seal path, so an automatic reopen cannot be path-free/takeover-safe and adds no binding beyond the receipt's `seal_sha256`. I agree with both driver and independent co-pilot.
  - Target-write phase guard → **document** (probe 6): the helper has no authority over arbitrary editor writes; `advance` (helper 1013–1030) only refuses phase skips. Keep phase ordering as an audited process invariant; make no confinement claim. Agree with the driver.
  - Claude-co-pilot behavioral-seal caveat → **document** as the named load-bearing residual: mode-0600 and the path check are not OS confinement (`protocol.md:145-147`), so for a non-OS-confined Claude co-pilot the seal is a location/behavioral control, OS-enforced only under a Codex workspace-write co-pilot. Both parties already agree; keep it named in reconciliation.
  - Probes 1,2,4,5,8,9,10 (symmetry, phase enforcement, path-free 7-key seal, receipt binding, schema-1/2 compat, direct-mode isolation, CLI symmetry, repo integrity) reproduce/hold; preserve.
- **Net:** matched evidence yields a **no-code outcome**. I reach the driver's conclusion, and I additionally trim *both* of the independent co-pilot's "low-risk" proposals (the `--copilot-root` flag/reword and the verify-receipts output line) to documentation-only, on the specific grounds above. The only disagreement between the two independent sets — whether a protocol/impl mismatch exists — resolves in the driver's favor: it does not.

## Proposed plan changes

1. **Accept — reconciliation disposition table (doc-only, plan.md step 5).** Record, with evidence pointers: seal-reopen feature → **reject** (probes 3,5); target-write phase guard → **document** as process invariant + audit deviation (probe 6); nested-stage seal-location residual → **document only** (probes 5,7; already in `protocol.md:141-147` and `cowork-session:548-559`) — **do not** add `--copilot-root` and **do not** reword (no false promise); `verify-receipts` caveat → **reject as output churn**, already documented (probe 4).
2. **Accept — amend evidence-question 2 with the reached answer:** reopening the retained seal is not path-free/takeover-safe and adds no binding beyond the receipt; keep the manual operator comparison. (Documentation, not code.)
3. **Accept — name the Claude-co-pilot behavioral-seal deviation** explicitly in reconciliation, since this session's co-pilot is exactly the un-OS-confined case.
4. **Reject / correct the independent co-pilot's plan-change #4** ("reconcile the protocol/impl mismatch on `--copilot-root`"): there is no mismatch — the protocol recommends a *future* extension and does not promise an existing flag (probes 2,5). No fix now; a future `--copilot-root` with an OS-enforcement design and acceptance test belongs to a separate task, matching the driver's stance on the target-write wrapper.
5. **Accept — keep the conservative default: no live code change is justified.** Advance to `ready-for-execution` only with both valid receipts; if executed, the accepted scope is documentation/reconciliation text only, which must still pass the focused cowork test, source-contract, takeover, public-repo audit, `git diff --check`, and clean full Phase 1. A validated no-code execution is the correct outcome and satisfies the charter's "retain documented residuals when the proposed cure is less reliable or usable."

*(Boundary notes: zero repository/stage/manifest/artifact edits; live exchange never accessed; scratch `/tmp/harness-cowork-recip.*` left out-of-repo and labeled for the driver's guarded cleanup; no raw recursive deletion performed.)*
