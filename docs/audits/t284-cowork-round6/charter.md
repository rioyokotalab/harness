# Charter

## Task

Audit and correct non-finite numeric arguments in `wait-copilot` so its stated
boundedness guarantee cannot be bypassed by Python floating-point spellings.

## Boundaries

Codex drives and owns target changes. Claude receives only a fresh no-hardlink
sandbox and sealed staged inputs. Scope is the smallest helper, protocol, test,
acceptance-index, and ledger change needed for finite timeout/poll validation.
No settings, credentials, services, remotes, publication, or unrelated work.

## Baseline and sandboxes

Baseline is local commit `c78c45a` on `task/t-284-cowork-speed`. Codex inspected
that checkout; Claude receives `/tmp/harness-t284-round6.lSixPU/copilot` with a
no-hardlink clone at the same baseline and a sibling staged exchange.

## Acceptance

Both agents reproduce or trace the edge; reconciliation freezes exact behavior;
the focused cowork test covers non-finite timeout and poll values; focused and
full phase-one checks pass; session receipts validate; cleanup is guarded.
