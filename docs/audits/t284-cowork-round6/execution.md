# Driver execution

## Steps and results

Codex added `math.isfinite` to both existing wait argument guards before the
first snapshot and imported `math`. Two externally bounded focused CLI cases
now require timeout NaN and poll NaN to exit 1 with the existing concise range
message, no traceback, and no JSON observation. The protocol now says both
numeric ranges are finite. Focused cowork passed after the change.

## Deviations

Claude's first independent candidate falsely claimed the exact helper was absent
and confused the staged candidate with driver evidence. Codex rejected it
without import, proved the exact clone/path, and staged a narrower retry. The
accepted independent and reciprocal receipts bind only the corrected passes.
