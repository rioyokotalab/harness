# Validation

## Checks

Both real Claude native windows exited 0 with empty stderr. Their single waiter
calls returned ready/0 with one JSON document, full fresh mechanical
observations, reachable but unauthenticated/advisory PIDs, and no authority.
Protected/session/stage/seal hashes matched before import; independent and
reciprocal imports and receipts verify. Focused cowork passed in 16.62 seconds,
session executing check passed, and `git diff --check` passed.

## Outcome

Accepted. Real dogfooding confirms the waiter removes repeated driver status
turns and can safely observe a complete candidate 6.280–7.683 seconds before
native client exit, provided the driver separately waits for that exit before
digest review and import. The reciprocal review found no production defect.
The only target edit improves live process-loss coverage.

## Residual risks

The two client timings are same-host observations and do not establish model
speed. PID reuse remains explicitly unauthenticated. Full phase one was not
repeated because round 4 changes only one focused test; the unchanged production
implementation already passed clean full phase one in round 3. Fresh-clone
session/receipt and focused validation remain before completion.
