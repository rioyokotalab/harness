Based on tracing `tests/test-phase1.sh`, `tests/focused-suites.tsv`, `.github/workflows/ci.yml`, the `cowork-session` script's `validate_layout`, and the actual `stage-reciprocal` tree, here is the complete replacement evidence document:

# Co-pilot evidence

## Sandbox and baseline

Sandbox: `/tmp/harness-t284-r1-claude`, confirmed at `f7d5bf0d403bdc07079bb4c5e420a2aa9fbb4a02` (unchanged from the independent pass; not re-verified this pass to stay in-window). Reciprocal work was performed only inside the real stage `/tmp/harness-t284-r1-claude/stage-reciprocal`, staged in `mode: reciprocal` per its `stage.json` (`inputs`: `state.json`, `charter.md`, `plan.md`, `driver-evidence.md`, `copilot-evidence.md`; `destination_before_sha256` matches the imported `copilot-evidence.md`). This is a genuine stage tree, not the live session — it lacks a `.git` and has no reachable session path. No target checkout, refs, settings, credentials, or external systems were touched.

## Commands and results

- **Conflict 1 (cowork suite duplication) — my prior claim was reasoned wrong, conclusion still holds.** `grep -n "test-codex-claude-cowork" tests/test-phase1.sh` finds nothing, but I now traced *why*: `test-phase1.sh:236` calls `tools/run-focused-tests.py --manifest tests/focused-suites.tsv`, and that manifest's line 11 lists `tests/test-codex-claude-cowork-skill.sh`. So the suite **does** run once inside the umbrella gate — indirectly through the manifest, not by a literal path string in `test-phase1.sh`. Separately, `.github/workflows/ci.yml` has no standalone `test-codex-claude-cowork-skill.sh` step (only Restic-schedule, mirrored-node onboarding, evaluation, public-repo-audit, guarded-delete, affinity, then `test-phase1.sh`). Net: total executions per CI run = 1 (inside the gate only), so it is *not* duplicated — but my earlier phrasing ("not found duplicated inside test-phase1.sh") wrongly implied it wasn't in the gate at all. Corrected explicitly here.
- **Conflict 2 (check vs. real stage).** Ran `python3 shared/skills/codex-claude-cowork/scripts/cowork-session check stage-reciprocal` directly (not a session-directory proxy, as I used last pass): result was `missing top-level protocol entries: execution.md, reconciliation.md, validation.md`, exit 1. This is a different, more precise rejection than my earlier session-proxy run (which was missing 5 entries). `validate_layout` (script lines 310–318) requires the full, phase-independent `LEGACY_TOP_LEVEL` set regardless of what `REQUIRED_BY_PHASE`/`STAGE_INPUTS` actually need at "discussing". A stage tree only ever contains `STAGE_INPUTS` for its mode plus whatever the co-pilot writes; it structurally cannot satisfy `LEGACY_TOP_LEVEL` until the driver reaches "validating"/"complete". **A new status surface must check against `REQUIRED_BY_PHASE[phase]` (or `STAGE_INPUTS[mode]` for a staged tree), not reuse `validate_layout`/`check` unmodified.**
- **Conflict 3 (prompt seal).** `grep -n "prompt" cowork-session` returns no matches — the script has no `--prompt` concept anywhere, confirming by source (not just the driver's runtime reproducer) that nothing hashes or binds prompt content today. `stage-reciprocal/stage.json`'s `inputs` list has no `artifacts/copilot-prompt.md` entry despite that file existing (1796 bytes) in `artifacts/`. **Accepted**: bind a `stage --prompt FILE` copy's hash into `stage.json.inputs` and revalidate at import; it closes a real integrity gap for near-zero added complexity, is additive (old receipts without a prompt entry stay valid), and matches `write_new_file`'s existing fail-closed/no-symlink pattern.
- **Conflict 4 (addendum vs. full replacement).** Not independently re-tested (out of window); reasoning only. A reciprocal-addendum format saves some output but forces every reader/validator to reconstruct the merged document and adds a submission/final hash distinction to the receipt chain — real recovery cost for a marginal token saving. The smallest safe latency win available (status surface + sealed prompt, conflict 2/3) does not require touching receipt semantics at all. **Rejected** for now, pending a concrete prototype showing bounded recovery cost, per driver's ask.
- **Conflict 5 (CI timing vs. 8-worker sample).** No new run performed (bounded scope). Reconciling the two: the driver's PR #161 run shows the umbrella gate itself costs ~86s of the ~138s total, with ~37s recoverable from the five confirmed CI-duplicated steps (restic-schedule, onboard-mirrored-node, evaluation, public-repo-audit, guarded-delete) — that saving is CI-topology-only and orthogonal to worker count. The 8-worker-vs-4-worker 11.79s (13.4%) local delta is a single paired sample in one sandbox and must not be combined with the CI number as if additive/independent; they are different levers (topology vs. concurrency) measured in different environments. Treat as **uncertain** until a repeated matched-sample 4-vs-8 run exists; the CI-duplication removal is the higher-confidence, already-corroborated saving.

## Critique

- My own conflict-1 reasoning last pass was too literal (string grep) and produced a technically-lucky-but-wrongly-justified conclusion; the driver's push to trace the indirect manifest path was warranted and should be a standing check for any "is suite X duplicated" claim in this workflow.
- The stage-vs-session distinction is the real blocker for a status surface, not a cosmetic implementation detail: `check` enforces a *session-complete* invariant unconditionally, so any status command must key off `phase`/`mode` explicitly rather than trying to make one validator serve both shapes.
- Accepting the prompt-seal binding is low-risk only if the projection stays fail-closed like `project_stage_state` (reject unknown/missing keys) — an additive-but-permissive schema change would reintroduce the same silent-drop risk `project_stage_state`'s docstring warns about.
- The eight-worker number remains the weakest piece of evidence in the whole exchange (n=1, cross-sandbox); it should not gate any default change on its own.

## Proposed plan changes

1. Implement `stage --prompt FILE`: copy to `artifacts/copilot-prompt.md` via `write_new_file` (fail-closed, no symlinks), add its sha256 to `stage.json.inputs`, and revalidate at `import-copilot`. Reject stage creation atomically on copy/hash failure.
2. Add a read-only `status` subcommand keyed on `state.json.phase`/`stage.json.mode`, checking against `REQUIRED_BY_PHASE`/`STAGE_INPUTS` rather than `validate_layout`'s full `LEGACY_TOP_LEVEL`; report candidate byte count and template/invalid/heading-complete state; PID liveness (if surfaced) stays explicitly advisory.
3. Do not adopt the reciprocal-addendum/receipt-split design; keep full-replacement `copilot-evidence.md` as the single record.
4. Do not change the four-worker default from this single 8-worker sample; if repeat measurement is wanted, require ≥2 matched runs per worker count in the same sandbox before reconciliation acts on it.
5. Scope the CI-topology fix to exactly the five confirmed-duplicated steps (restic-schedule, onboard-mirrored-node, evaluation, public-repo-audit, guarded-delete); leave ShellCheck, affinity, and the cowork suite's current single indirect execution untouched, and confirm per-suite failure attribution survives via the focused runner's existing PASS/FAIL log lines before removing any standalone step.
