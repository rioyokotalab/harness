task: Har-381
repository: harness
state: ready
priority: 1
created: 2026-08-03
consumer: codex
record: pending
authority: owner-started-producer-nightly
---
# Objective

Run the producer-only portfolio closeout from 01:00 through 09:00 JST. First
repair the demonstrated lifecycle contradiction in which Har-379 has a merged
complete receipt while the producer queue and index remain ready and its
assignment remained active. Make repository-native validation reject future
queue, receipt, record, and assignment contradictions without rewriting a
published producer packet.

# Scope and benchmark

Independently audit Harness, Personal, Students, Swallow, and Website. Preserve
every existing goal and gate, all unrelated dirty work, and the Har-378 and
Har-379 Aist preservation refs. Metadata-verify consumer idleness without pane
or transcript reads. Measure always-read and routed bytes, command count, wall
time, and validation cost; compact only when equivalent capability checks pass
and a measured cost improves. Public-content checks apply only to Harness and
Website, and no private-project payload may enter a public artifact.

Classify task-owned local branches, worktrees, and temporary residue. Use the
guarded workflow for recursive or multi-path cleanup; remote branches are
report-only. Do not change fleet services, packages, rulesets, approval counts,
hosting settings, credentials, consumer runtime state, or gated task targets.

# Cadence, cutoff, and acceptance

Retain exactly eight hourly evidence slices for 01:00–09:00 JST. Stop material
changes at 08:00 and reserve the final hour for integrated validation,
protected publication, cleanup verification, and handoff. Reuse unchanged
validation evidence and use the minimum protocol-required protected
transitions; never combine producer- and consumer-owned paths merely to reduce
PR count.

Acceptance requires lifecycle contradictions to fail deterministically,
Har-379 to converge without packet-byte mutation, all five repositories to
retain independent clean protected state or an exact value-free blocker,
public audits to pass, eligible local residue to be guarded-cleaned, and final
durable and owner summaries to contain exactly eight evidence-backed slices
and at least 400 words.
