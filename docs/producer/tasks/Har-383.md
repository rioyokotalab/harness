task: Har-383
repository: harness
state: ready
priority: 2
created: 2026-08-03
consumer: any
record: pending
authority: repository-local-synthetic-pilot
---
# Objective

Build a Harness-only synthetic productive-idle selector that prevents
unexpected duration-run queue exhaustion without creating low-value work,
leaking private repository metadata, replaying interrupted actions, or loading
unrelated packets.

# Required route and exclusions

Read the complete compact contract and its two normative tables:

- `docs/audits/har381-producer-lifecycle-closeout/productive-idle-contract.md`
- `docs/audits/har381-producer-lifecycle-closeout/productive-idle-scenarios.tsv`
- `docs/audits/har381-producer-lifecycle-closeout/productive-idle-acceptance.tsv`

The longer `productive-idle-design.md` is lookup-only rationale; read it only
if a contract decision is disputed. Do not modify `docs/producer/` or live
`NIGHTLY.md` behavior, create a real catalog, start a scheduler or wait
controller, touch sibling repositories or consumer runtimes, or allocate a
rollout. Use temporary synthetic fixtures only.

# Benchmark and acceptance

Replay Har-381 from checkpoint `c508483` and require one durable wait, zero
early duplicate reads, unchanged always-read bytes, zero billable Actions
increase, and no extra protected transition. Implement the separate parser,
schema, candidate/start/receipt state machine, hostile-path checks, append-only
event recovery, local admission, privacy-preserving selection, and matched
latency benchmark.

All 29 normative scenarios and all 26 acceptance measures must pass, including
p95 at most 100 ms over all 60 possible synthetic cards, zero private value or
writer-boundary exposure, no target replay, exact idempotent receipts, and a
complete Harness validation. Rollout may be allocated only by later producer
reconciliation after this pilot is complete.
