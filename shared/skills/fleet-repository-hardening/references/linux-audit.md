# Linux and HPC audit

Record each applicable row as `pass`, `finding`, `expected`, `unavailable`, or
`unknown`; classify a failed query as `unknown`.

- Check both approved routes where redundancy is declared.
- Record OS, architecture, capacity, inodes, ownership, and safe modes.
- Read native scheduler, account, and resource declarations; do not infer a
  billing route.
- Check backup repository state, declared schedule, and exact scheduler state.
- Read effective SSH options for representative direct, jump, Git, and default
  hosts.
- Check agent executable cardinality/version and project-scoped configuration.
- Identify managed services and unexpected user-owned listeners or processes
  without reading unrelated process content.
