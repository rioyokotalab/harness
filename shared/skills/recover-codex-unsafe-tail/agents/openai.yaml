interface:
  display_name: "Recover Codex Unsafe Tail"
  short_description: "Safely recover recurring blocked Codex threads"
  default_prompt: "Use $recover-codex-unsafe-tail to diagnose and recover this blocked managed Codex thread without replaying its rejected prompt."
