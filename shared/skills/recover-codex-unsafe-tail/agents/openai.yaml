interface:
  display_name: "Recover or Replace Codex Thread"
  short_description: "Safely recover or bridge managed Codex threads"
  default_prompt: "Use $recover-codex-unsafe-tail to diagnose, recover, or bridge this managed Codex thread without replaying rejected work."
