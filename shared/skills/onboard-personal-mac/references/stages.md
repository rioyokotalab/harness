# Legacy personal-Mac stage index

Do not preload this compatibility index during normal onboarding. The router
in `SKILL.md` and its direct phase/component references are authoritative.

The former aggregate stages map to: planning; private companion; bootstrap and
packages; public control plane; Bash startup; tmux; private SSH; Codex/Claude;
acceptance with rollback/reapply; and post-acceptance orphan cleanup. Select
those direct references from `SKILL.md`, not this index.
