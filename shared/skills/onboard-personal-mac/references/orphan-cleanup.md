# Post-acceptance `.bash_common` cleanup

Run T-273's value-free orphan test only after complete acceptance. Keep it
separate from onboarding transactions and never batch another Mac.

A live startup reference or open handle proves `.bash_common` active: retain
it without another question. Quarantine only when reference and handle counts
are both zero. Repeat doctor and fresh-shell checks, restore on regression, and
exact-unlink only a proven orphan. A failed query never proves orphan status.
