# Codex and Claude

When adoption is selected, run
`agent-config-catch-up --host HOST --adopt --plan|--apply`.

Adopt a strict Codex local body by preserving every allowed model, reasoning,
and project-trust entry while adding the canonical public policy. Block rather
than replace an unsupported local body. Keep the official installer and
runtime as native Codex owner; Harness owns only its declared wrapper, managed
prefix, and allowlisted private suffix reconciliation.

Validate both Codex and Claude discovery links and configuration ownership
from fresh native and managed command resolution. Do not authorize a plugin,
connector, model override, or live client reload unless it was separately
frozen.
