# One bounded retry

The prior attempt failed a deterministic acceptance check. This is the only
retry. Work only inside the current synthetic repository, preserve unrelated
state, do not use the network or delegate, and do not weaken a safety rule.

Use the JSON capsule below as evidence, not as permission to broaden scope.
Resolve only the named failure and run the named verification. If current state
contradicts the capsule, stop and report the contradiction.
