# Portable interactive aliases. Keep definitions alphabetic by alias name.
alias a='./a.out'
alias ducks='du -cks * | sort -rn | head -11'
alias grep='grep --binary-files=without-match --color=auto'
alias la='ls -ah'
alias ll='ls -hl'
alias lla='ls -ahl'
alias ls='ls --color=auto'
alias v='vim'
